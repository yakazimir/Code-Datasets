establece el valor de retorno de una acción especificada
la llamada al método isconnected en un objeto connection verificará si el script php está conectado a un servidor de mensajería
establece el modo de cifrado para el documento
en caso de éxito devuelve el valor de una directiva de configuración
devuelve si habilitar o no la remarcación de consultas range/wildcard/fuzzy/prefix
crea una nueva fecha
obtiene el string a colocar antes del elemento actual
establece el espaciado entre palabras del texto
esta función no debe ser llamada
esta función elimina una base de datos abierta con ibase_connect() o con ibase_pconnect()
ensamblar un url
establece el uso del búfer para objetos grandes dependiendo del valor del parámetro on_off
devuelve un array multidimensional que representa las métricas de la fuente
es usado para enviar encabezados http sin formato
constructor para el objeto solrclient
busca el ámbito del namespace para un prefijo dado
suaviza los contornos de una imagen mientras que se preserva todavía la información del borde
la descripción de la función va aquí
adjunta un splobserver de esta forma puede ser notificado cuando hayan actualizaciones
construye una red neuronal de retropropagación desde un fichero de configuración el cual ha sido guardado mediante fann_save()
añade un nombre de filtro dado por filtername a la lista de filtros adjuntos a stream
la funcionalidad de memcached::getdelayedbykey() es equivalente a la de memcached::getdelayed() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
registra un función de php o una definida por el usuario para usarla como una función escalar de sql para utilizarla dentro de sentencias sql
devuelve la ruta de el fichero omitiendo el nombre del fichero y cualquier barra
la función cubrid_lob2_seek() se usa para mover la posición del cursor de un objeto lob mediante el valor establecido en el argumento offset a la dirección establecida en el argumento origin
re- inicializa eio
comprueba si existe un campo
establece una retrollamada que será invocada durante el procesamiento de la imagen imagick
crea un objeto sdo_dataobject dados un uri de espacio de nombres y un nombre de tipo
devuelve el id de la última fila insertada o el último valor de una secuencia de objetos dependiendo del driver subyacente
añade una operación de escritura al lote
devuelve el punto blanco de la cromaticidad como un array asociativa con las claves x e y
devuelve la posición de el puntero de fichero que representa el índice actual en el flujo del fichero
devuelve información interna de dónde fue lanzada la excepción
devuelve true si filename existe y es escribible
esta función devuelve el tipo del entorno usado para crear una familia de fuentes
devuelve información interna de donde se lanzó la excepción
parchea una cadena str con un patch binario
abre un flujo imap a un buzón dado por mailbox
la fracción de cambio de candidatas en cascada es un número entre 0 y 1 que determina lo grande que debería ser el cambio del valor de una fracción de fann_get_mse() en fann_get_cascade_candidate_stagnation_epochs() durante el entrenamiento de las neuronas candidatas para que el entrenamiento no se estanque
añade un servidor de trabajos a la lista de servidores que pueden usarse para ejecutar una tarea
obtiene la escala de un campo referenciado mediante número del identificador de resultado dado
devuelve una string con todos los caracteres alfabéticos convertidos a minúsculas
mueve un objeto de una colección a otra
comprueba si la variable dada es un objeto
obtiene el índice del color que tiene el tono blancura y negrura más próximos al color dado
obtiene una línea desde el puntero al fichero
decodifica las extensiones de la cabecera del mensaje mime que no son texto ascii véase rfc2047
devuelve un iterador interno para la entrada actual
imprime texto en la posición de texto dada
esta función cierra una sesión curl y libera todos sus recursos
rebobina el iterador a el primer elemento de el almacenamiento
añade un enlace a un recurso web
la descripción de la función va aquí
obtiene información acerca de la tabla especificada
habilita o deshablita el componente de estadísticas
este método devuelve el nombre base del fichero directorio o enlace sin informacion de la ruta de acceso
esta función reemplaza el hijo oldnode con es nuevo nodo newnode proporcionado
imprime o guarda una versión wbmp de la imagen image
establece el uri base
esta función puede modificar y verificar las extensiones de ficheros que estará usando spl_autoload() en respaldo de la función interna __autoload()
define la url de conexión que será usada por las siguientes peticiones soap
este método llama a la llamada de retorno con el valor actual la clave actual y el iterador interno
agregar entradas en el directorio de ldap
devuelve un array de información sobre los límites soft y hard del recurso actual
comprueba si el puntero a un archivo está al final del archivo
esta función se comporta exactamente como fsockopen() con la diferencia de que la conexión no se cierra después de que el script termina
esta función abre un blob en modo lectura y envía su contenido directamente a la salida estándar en la mayoría de los casos será al navegador)
abre una conexión persistente al servidor
devuelve el comentario de una etiqueta id3
elimina uno de los parámetros stats.field
devuelve las interfaces
comprueba si la variable es un recurso
devuelve una matriz con los nombres de campos del documento
devuelve un array con todos los tipos de codificaciones admitidas
rellena un área con un sombreado que ha de ser creado antes con la función ps_shading()
obtiene el nombre del elemento actual directoryiterator
devuelve el número de la línea de inicio de esta función
devuelve true si la key dada existe en el array
esta función devuelve un array con los nombres de las clases padre de la class dada
libera los recursos asociados con un resultado
usada después de cambiar la función de gestión de excepciones al utilizar set_exception_handler() para volver al gestor de excepciones anterior el cuál puede ser la función interna o una definida por el usuario)
emite una llamada a un método rpc remoto
devuelve el número máximo de restricciones que deberían ser devueltas por los campos facet
crea el enlace duro dado por new_path para un fichero especificado por path
pausa un servicio nominado
obtiene una lista de todos modos soportados por el parámetro lib_dir
invoca el método reflejado y pasa sus argumentos como array
indica si namespaceuri es el espacio de nombres especificado
devuelve un array asociativo que contiene la siguiente fila del conjunto de resultados de una consulta
obtener el número de registros en un archivo de base de datos
espara a que los eventos estén activos y ejecuta sus llamadas de retorno
configura un manejador para ser llamado cuando un namespace es declarado
recupera información sobre los tipos de datos soportados por la fuente de datos
busca una fuente y la prepara para un uso posterior con pdf_setfont()
carga un documento xml desde una cadena
devuelve el bloque de asignación de unicode que contiene al carácter
específica un rango de registros a recuperar
el framework de agregación de mongodb proporciona medios para calcular valores acumulados sin tener que usar mapreduce
devuelve una matriz que representa las métricas de la fuente
comprueba si el método es público
este método realiza el trabajo duro de la clase yaf_dispatcher
compara dos números
determina si el elemento actual directoryiterator se puede escribir
encripta y firma el parámetro plaintext con las llaves que se an indicado anteriormente mediante gnupg_addsignkey y gnupg_addencryptkey y retorna el texto encriptado y firmado
aplica una expresión aritmética relacional o lógica a una imagen
establece el carácter delimitador y el circundante para analizar campos csv
con esta función principalmente se pretende obtener el número de filas modificadas en la base de datos
cambia los permisos de un fichero o directorio
crea una nueva imagen desde una parte dada de un fichero gd2 o un url
toma el mayor id de hash disponible
enumera todos los caracteres de unicode asignados entre los puntos de código de inicio y límite inclusive y exclusive respectivamente y se llama a una función para cada uno pasando el valor del punto de código y el nombre del carácter
establece el interletraje («kerning») del texto
añade datos de un objeto grande lob al final de otro
este método se puede usar para preguntar por el ancho alto tamaño y formato de la imagen sin leer toda la imagen de la memoria
advertencia debería utilizarse mcrypt_generic_deinit() en lugar de esta función ya que puede ocasionar colisiones al utilizarla con mcrypt_module_close() debido a múltiples libreaciones de búfer
codifica una cadena usando el algoritmo uuencode
se hace corresponder el carácter dado con su equivalente en minúsculas
igual que datetime::getlasterrors() excepto que trabaja con datetimeimmutable
declara el atributo especificado por attr como de tipo id
comprueba una propiedad de unicode binaria para un punto de código
comprueba la validez de los subiteradores
importa variables desde un array a la tabla de símbolos actual
esta función devuelve el valor de un atributo de la conexión a la base de datos
detiene el bucle principal en el servidor raíz
esta función devuelve los nombres de todas las funciones definidas en el módulo indicado por nombre_modulo
dibuja la cadena string verticalmente en las coordenadas dadas
devuelve un valor de tipo integer que contiene el número del último error
establece ciertos campos de información del documento
la descripción de la función va aquí
crea un array
construye un nuevo objecto soapheader
la descripción de la función va aquí
finaliza la sesión sin guardar datos
advertencia esta función no está documentada actualmente solamente se encuentra disponible la lista de parámetros
es usada para modificar la información almacenada de una etiqueta id3
elimina el documento con el id especificado donde id es el valor del campo uniquekey declarado en el esquema
devuelve el número de filas que hay en un resultado
crea una copia de un objeto gearmanclient
realiza una operación or a nivel de bits
obtiene un array asociativo desde una consulta de odbc
exporta una función reflexionada
establece el parámetro linecap para controlar la forma del final de un trazado con respecto al contorno
obtener el número total de conexiones de la red completa
calcula la tangente para cada valor de real y devuelve el array resultante
finaliza la definición de una fuente type 3
destruye el valor en el índice específicado
esta función consulta los ficheros /etc/hosts.allow y /etc/hosts.deny para comprobar si el acceso al servicio daemon debería permitirse o denegarse a un cliente
obtiene el valor actual del elemento
envía la consulta query a la base de datos por medio de una conexión activa asociada con link_identifier
devuelve la versión del servidor mysql en forma de entero
obtiene el valor quantum value de un color del objeto imagickpixel
lee la información de la última tupla de relation la hace la tupla actual y la devuelve como una matriz asociativa en tuple
abre una conexión persistente a una base de datos de ingres
establece/obtiene la codificación de caracteres interna
consolida un path no versionado dentro de un repositorio en url
advertencia esta función no está documentada actualmente solamente se encuentra disponible la lista de parámetros
crea un objeto char
comprueba si existe o no un índice
cambia los permisos del fichero especificado por el descriptor de fichero fd
una función de tipo callable usada por la opción de contexto log_cmd_delete al borrar un documento
devuelve la representación de string del error
la transacción iniciada por sqlsrv_begin_transaction() incluye todas las sentencias que fueron ejecutadas después de la llamada a sqlsrv_begin_transaction() y antes de llamar a sqlsrv_rollback() o sqlsrv_commit()
el gestor __wakeup()
obtiene el tipo de socket
devuelve el uso de memoria del recurso especificado en megabytes
crea una nueva instancia de haruannotation
añade un filtro de borrosidad a la imagen
define el modo de interpretación del texto para la página
esta función registra una función php como manejadora para los eventos especificados
convierte un bloque iptc en sus etiquetas
encuentra la posición de la última aparición de needle aguja en el string haystack pajar 
devuelve el nodo del atributo en el espacio de nombres namespaceuri con el nombre local localname para el nodo actual
lee información desde un gestor de archivo abierto y crea clases desde el código de bytes
devuelve las propiedades predeterminadas de una clase incluyendo las propiedades heredadas 
limpia todos los parámetros vinculados actuales
se comporta como el carp::cluck de perl
obtiene el elongamiento de la fuente a usar cuando se escribe texto
este método actualiza una entrada un nuevo valor y devuelve si la entrada fue actualizada o no
guarda el documento en el fichero especificado
obtiene nombres similares para el nombre y el país dado
elimina el array de claves dado por keys del servidor
devuelve el id del hilo de la conexión en uso
se llama cuando se llega recursivamente al final de un nivel
recupera un nodo especificado por localname y namespaceuri
cierra fichero abierto o creado y guarda los cambios
este método realmente no sigue las referencias por lo que no sirve para determinar si una referencia está o no rota
obtiene el puerto del servidor soap que se está ejecutando
crea un archivo fifo especial que existe en el sistema de archivos y actúa como un punto de comunicación bi-direccional para los procesos
recupera un nodo especificado por index dentro del objeto domnamednodemap
vacía el contenido de la caché
establece el nombre del fichero de salida para la información generada del propio procesado de la hoja de estilos
obtiene el tipo de imagen potencial
envía la excepción indicada cuando este trabajo está en ejecución
almacena un archivo local en el servidor ftp
igual que datetime::setisodate() excepto que trabaja con datetimeimmutable
configura la función del manejador de declaración de entidades no analizadas para el intérprete xml parser
crea un objeto grande temporal y escribe los datos dados por data en él
devuelve todas las estadísticas de las facetas que fueron establecidas
establece el valor de gris para todas las operaciones de dibujo siguientes
devuelve una matriz que contiene todos los campos del documento
inicia la sesión en la secuencia ftp determinada
obtiene la profundidad de la imagen
igual datetime::__set_state() excepto que trabaja con datetimeimmutable
lee la miniatura embebida de una imagen tiff o jpeg
rebobina el iterador hasta el primer elemento
devuelve un valor entero que que representa la versión del protocolo de mysql utilizada por la conexión representada por el parámetro link
esta función ordenará un array según sus valores usando una función de comparación definida por el usuario
devuelve el tamaño de un archivo resultado que se creó después de aplicar el parche binario desde el archivo file a el archivo original
guarda la red completa a un fichero de configuración
comprueba si el object dado tiene la clase class_name como una de sus madres o si la implementa
toma la raíz enésima dada por nth de a y devuelve el componente entero y el resto del resultado
elimina el número de elementos dado por num del final de la colección
crea un directorio con el acceso especificado por mode
copia los elementos de un iterador en un array
añade una palabra al diccionario dado
la función runkit_lint() lleva a cabo una verifiación de sintaxis lint pelusa en inglés) sobre el código php especificado comprobando errores en la escritura del script
devuelve la subcadena especificada
establece el valor de una propiedad estática
mueve la posición del texto al inicio de la siguiente línea
obtiene múltiples filas de una consulta y las ubica en un array bidimiensional
identifica una imagen y devuelve los atributos
esta función devuelve un string con los espacios en blanco retirados del final de str
este método es llamado inmediatemente después de que la envoltura sea inicializada p.ej
parsea el string de la fuente dada en tokens php usando el escaneador de léxico de zend engine
esta función cierra el recurso abierto con bbcode_create()
escribe datos desde el fichero dado por filename a la posición actual del objeto grande
devuelve el operador de composición asociado a la imagen
obtiene el número de colores únicos de la imagen
establece el valor boost de tiempo del índice de un campo
devuelve las constantes definidas de la clase
este método intenta actualizar una entrada con un nuevo valor
genera un identificador único globalmente guid: globally unique identifier)
devuelve el punto primario azul de la cromaticidad de una imagen
reinicia el string del último error
analiza la clave key y la prepara para usarla con otras funciones
es como imagick::thresholdimage() pero fuerza a todos los píxeles bajo un umbral a ser negros mientras deja todos los píxeles por encima del umbral sin cambios
advertencia esta función no está documentada actualmente solamente se encuentra disponible la lista de parámetros
este método devuelve si un fichero dentro de un archivo phar tiene su crc verificada
redondea las esquinas de una imagen
lee una imagen desde un string binario
anexar una elipse a la ruta actual
afila una imagen
permite al llamador manipular directamente el espacio de disco asignado al fichero especificado por el descriptor de fichero fd para el rango de bytes empezando por offset y continuando length bytes
usualmente es enviado por medio del socket para requerir información acerca del script que esta corriendo
une elementos de un array en un string con glue pegamento 
utilizando sybase_deadlock_retry_count() se puede definir el número de reintentos en caso de bloqueo
genera un número al azar
devuelve un array que contiene todos los elementos de array1 después de haber aplicado la función callback a cada uno de ellos
construye un reflectionobject
esta función intenta asegurarse de que el archivo designado por filename es un archivo subido válido lo que significa que fue subido mediante el mecanismo de subida http post de php 
devuelve un array numérico que corresponde a la fila recuperada y mueve el puntero de datos interno hacia delante
obtiene el espacio de colores de la imagen
rellena el trazaco construido previamente con llamdas a las funciones de dibujo como ps_lineto()
devuelve una copia de un subconjunto del resource de datos de entrenamiento empezando en la posición pos y con length elementos hacia adelante
obtiene el ancho de los caracteres en la fuente
comprime datos con la codificación especificada
devuelve el error sqlstate de la operación anterior de mysql
devuelve la siguiente fila de datos disponible como un array asociativo un array numérico o ambos por defecto 
la llamada al método commit en un objeto connection hace el commit consolida todas las transacciones pendientes de ser consolidadas y que forman parte de la unidad de trabajo actual
obtiene datos para la salida gráfica del archivo desde la base de datos rrd como un array
establece la función de llamada de retorno para aceptar paquetes de datos para una tarea
indica el nodo actual es un comentario
liberación de memoria asignada para la sesión de agente
interpreta una plantilla de tipo string y devuelve el resultado
añade un único componente al formulario dado por form
lee los datos del apuntador de archivo dado y extrae cada archivo codificado mediante uuencode embebido en un archivo temporal
limpia todos los elementos del conjuntos de sondeo
devuelve un array con información acerca de un campo de una consulta que ya se haya ejecutado
la función geoip_country_code_by_name() devolverá la abreviación de dos letras correspondientes al sitio web o a la dirección ip
obtiene la hora de la última modificación del script principal de la ejecución
crea una instancia de gearmanclient que representa un cliente que conecta al servidor de trabajos y envía tareas para que sean completadas
devuelve el nombre del tipo de datos de un campo
obtiene una fila de resultados en forma de un array asociativo o indexado o ambos
devuelve una lista de direcciones ipv4 a la que el host de internet especificado por hostname resuelve
establece el gestor de errores de yaf
leer eventos inotify de una instancia inotify
la función cubrid_set_query_timeout() se usa para establecer el tiempo de espera de la ejecución de consultas
configura la función del manejador de referencia a entidades externas para el intérprete xml parser
lee un fichero que almacena datos de entrenamiento
devuelve el tamaño asociado con el objeto imagick como una matriz con las claves columns columnas y rows filas 
obtiene el hijo de el elemento actual
establece el id de usuario real del proceso actual
establece el nombre de una acción para el rastreo de oracle
asocia una variable de php con una columna para la obtención de consultas usando oci_fetch()
calcula la raíz cuadrada de un número con resto
determina si el punto de código posee la propiedad bidi_mirrored
esta función busca objetos ejecutando una clave o/y una query de texto completo
devuelve un array de los tipos descritos en el wsdl para el servicio web
crea un nuevo número de 64 bits con el valor proporcionado
devuelve el número de parámetros obligatorios definidos en una función
se usa para obtener el tamaño de datos blob/clob
serializa un arrayobject
empties a remote database
devuelve el número de campos columnas de la base de datos abierta filepro
esta función es similar a ingres_fetch_array() con una diferencia: se devuelve un objeto en lugar de un array
la frecuencia en la que las palabras que no ocurran en por lo menos tantos documentos como este serán ignoradas
chequea si todos los caracteres en la string entregada texto son alfanuméricos
vincula una variabel php a una sentencia sql de un recurso devuelto por db2_prepare()
establece el nomb re del archivo o el uri para un esquema relaxng a usar para validación
particiona silábicamente la palabra proporcionada
decrementa un valor numérico de un ítem por el offset especificado
retorna el nombre de la función para este trabajo
obtiene la clase con declaraciones de tipos para el parámetro como un objeto reflectionclass
devuelve el tipo de un objeto variante
intenta cambiar el modo de un fichero especificado por el dado en mode
si no se especifica ninguna base de datos se utiliza la actual
ésta es una implementación de la interfaz arrayaccess que permite la manipulación directa del contenido de un archivo phar utilizando los corchetes de acceso al array
se usa para obtener o establecer el id de sesión para la sesión actual
crea una nueva sesión o reinicializa una sesión existente
realiza una validación relaxng del documento basándose en la fuente rng dada
revisa una petición oauth
devuelve el nombre del host de internet especificado por ip_address
crea una imagen espejo horizontal reflejando los píxeles alrededor del eje y central
crear un nuevo archivo pdf sujeto a varias opciones
obtiene la escena de la imagen
deshacer todos los cambios hechos en el archivo
confirma una transacción en progreso en la conexión especificada e inicia una nueva transacción
cuando de lanza mediante el administrador de control de servicios se requiere un porceso de servicio para registralo con él para establecer la supervisión del servicio y las características de comunicación
extiende o condensa una fuentefont_index, si el valor del parámetro extend es menor que 1 se condensará la fuente
devuelve la ruta del directorio donde php almacena los archivos temporales por defecto
instala una llamada de retorno que decide si una sentencia es almacenada en caché
obtiene el número de instancias de iteradores adjuntos
obtiene el tipo de byte en el texto
selecciona uno o más valores aleatorios de un array y devuelve la clave o claves de dichos valores aleatorios
obtiene el nombre de la clase del objeto dado por object
esta función establecerá una función de retrollamada par ser invocada por eventos en lugar de emitir avisos de php
obtiene la altura de la imagen
hace accesible una propiedad
comprueba si un object existe en el almacenamiento
este método escala las imágenes usando un método de re-escalada líquido
evita la operación de rebobinado en el iterador interno
obtiene el valor de una ruta pcos con el tipo number o boolean
devuelve la dirección ipv4 del host de internet especificado por hostname
retorna el tiempo máximo de inactividad de entrada/salida en milisegundos para el socket.
mueve el iterador a la siguiente posición
llamar una función para cada elemento en un iterador
devuelve un array que contine la información de la versión de todos los servidores de memcache disponibles
comprueba si la lista doblemente enlazada contiene más nodos
devuelve una cadena que contiene el nombre del mes
obtiene el tipo de compresión del objeto
sincroniza la parte visual de postgresql usualmente un proceso web con el servidor postgresql después de hacer una operación de copia realizado por la función pg_put_line()
recupera el campo actual
aplica una corrección gamma a image dada una entrada y una salida gamma
pregunta por el estado actual de un textflow
establece el indicador de posición de fichero para el fichero referenciado por handle
verifica si todos los caracteres en la string entregada text son caracteres en mayúsculas
se usa para obtener información blob/clob de una base de datos cubrid y guarda su contenido a un fichero
devuelve la versión de la api del formato de fichero phar que será usado al crear phars
exporta una extensión reflexionada
cambia el usuario de la conexión de la base de datos de mysqlnd especificada
devuelve el id de persistencia del socket
hace una diferencia binaria de dos archivos y almacena el resultado en un archivo de revisión
retorna el manejador de trabajo para la tarea en ejecución
advertencia esta función no está documentada actualmente solamente se encuentra disponible la lista de parámetros
obtiene el número de línea actual para el intérprete xml dado
lee información blob/clob y la pasa directamente a través del navegador
devuelve información interna de donde se lanzó la excepción
borra todos los recursos asociados con un objeto imagick
destruye un offset
descomprime el string pasado por parámetro que contiene los datos codificados en bzip2
cierra una conexión cliente de db2 creada con db2_pconnect() y devuelve los recursos correspondientes al servidor de la base de datos
anexa una curva bezier para la ruta corriente
la descripción de la función va aquí
ejecuta una única tarea y devuelve una representación de tipo string del resultado
devuelve el nombre de host del sevidor al que se le envió la consulta
establece el desplazamiento de decadencia del peso de sarprop
deserializa las entradas del almacenamiento y los añade al almacenamiento actual
este método actualiza una entrada un nuevo valor y devuelve si la entrada fue actualizada o no
devuelve la cadena artista de una etiqueta id3
avanza la posición actual al siguiente elemento
comprueba si una clase está definida internamente por una extensión o por el núcleo en oposición a estar definida por el usuario
incluir el término de límite superior en el conjunto de resultados
el método subscribe se utiliza para crear una nueva suscripción a un tema específico
puede ser usado para extraer información sobre los campos en un determinado resultado de consulta
está función garantiza la devolución de un índice de color para un color requerido el color exacto o la alternativa más próxima posible
los vínculos añadidos con una de las funciones ps_add_weblink() ps_add_pdflink() etc
obtiene el número de campos columnas de la base de datos especificada
devuelve el número de éxitos de la última búsqueda
antepone el valor dado por value al inicio de la lista doblemente enlazada
retorna el denominador del tanto por ciento completado de la tarea expresado como una fracción
para aumentar el rendimiento newt solamente actualiza la exposición cuando lo necesite no cuando el programa le indique que escriba en el terminal
la función socket_set_block() elimina la bandera o_nonblock del socket especificado por el parámetro socket
devuelve un array que corresponde a la fila recuperada y mueve el puntero de datos interno hacia delante
establece el total del color asociada a este color
simulates a signal receive
obtiene toda la información de la estructura de un mensaje dado
la descripción de la función va aquí
borra un artefacto asociado con la imagen
comprueba si el almacenamiento contiene al object proporcionado
limpieza recursiva del directorio de trabajo workingdir finalizando cualquier operación no completada y eliminando bloqueos que existan en la copia de trabajo
destruye el objeto imagick y libera todos los recursos asociados a él
este método proporciona un valor de intervalo de paso que respetará todos los requisitos de alineamiento del código de renderización de imágenes acelerado dentro de cairo cairo
crea un nuevo buzón de correo especificado por buzon
usado para serialización personalizada
envía una sentencia sql al servidor de bases de datos
indica si un fichero existe y es legible
este método muestra una imagen en un servidor x
fija la base de datos que se encuentra activa actualmente en el seridor asociado con el específico link_identifier
carga los datos desde un apuntador de archivo a un archivo remoto en el servidor ftp
devuelve el punto primario rojo de la cromaticidad como una matriz con las claves x e y
actualiza el archivo de base de datos rrd
se puede usar para implementar un bucle de eventos especial
envía una consulta a la base de datos activa actualmente en el servidor asociada con el identificador de conexión link_identifier
usa spanscorer para remarcar términos de frases sólo cuando aparecen dentro de la frase de consulta del documento
realiza una consulta aql local en bruto usando un intérprete de aql embebido dentro de la biblioteca cliente de db++
devuelve el número de errores tidy encontrados en un documento específico
devuelve estadísticas de cliente por proceso
devuelve true si la imagen tiene un canal mate si no devuelve false
recupera o bien el conjunto óptimo de columnas que identifican de manera única una fila de la tabla o las columnas que son actualizadas automáticamente cuando cualquier valor de la fila es actualizado por una transacción
mueve la posición del texto al desplazamiento especificado
obtiene las instancias registradas del iterador del resultado current()
crea un diccionario usando una etiqueta no vacía que se desea para solicitar un diccionario en_us de_de ...)
devuelve un valor asociado dentro del objeto para una clave especificada
esta función comprueba si el sistema de archivos que contenía el fichero dado soporta los atributos extendidos
leer la estructura de una sección del cuerpo especificado de un mensaje especificado
construye un parentiterator en un iterador
la función mysqli_fetch_lengths() retorna un array que contiene los largos de cada columna de la fila actual dentro del resultset·
comprueba si el fichero es ejecutable
retorna si un iterador puede ser obtenido de la entrada actual
agrega comillas sencillas alrededor de una cadena y rodea de comillas/escapa cualquier comilla sencilla existente permitiéndole pasar la cadena directamente a una función del intérprete de comandos y logrando que sea tratada como un solo argumento seguro
devuelve el tipo de edición del campo correspondiente a field_number
crea una nueva instancia de la clase sin llegar a invocar al constructor
crea un nuevo dispositivo raíz descargando y analizando automáticamente de su ubicación
devuelve el nombre de la propiedad
devuelve el copyright de la api imagemagick como una cadena
especifica una función a llamar cuando un trabajador envía una excepción
la función dio_read() lee y devuelve len bytes de fichero con descriptor fd
establecer el método de compresión de una entrada definida por su índice
ensamblar un url
obtiene el retraso de la imagen
la descripción de la función va aquí
libera recursos de la consulta asociada con result_id
consulta la publicación de un field es decir el valor de la clave /ap) y lo almacena en un fichero
devuelve la fecha juliana para la fecha timestamp unix segundos desde 1.1.1970 o el día actual si timestamp no es dado
realiza lo mismo que fann_cascadetrain_on_data() pero lee los datos de entrenamiento directamente desde un fichero
lee datos blob de la base de datos
prepara la sentencia dada por sql_text usando la conexión connection y devuelve el identificador de sentencia el cual puede usarse con oci_bind_by_name() oci_execute() y demás funciones
devuelve todo de haystack iniciando desde e incluyendo la primera aparición de needle hasta el final
aplica una matriz de convolución a la imagen usando el coeficiente y el índice dados
actualiza un número entero existente si el parámetro old concuerda con el valor guardado actualmente con el valor del parámetro new
exporta una o más funciones para clientes remotos
crea un fichero en disco y escribe el documento postscript en él
un alas de solrparams::setparam
envía datos a todos los servidores de trabajo para comprobar si los retornan
obtiene información sobre los buzones
convierte un dn específico a un formato más amigable al usuario mostrando los tipos de nombre
añade un atributo al elemento simplexml
duplica un descriptor de fichero
crea un nuevo contexto con la ip y puerto específicado
crea un object splfileobject de el fichero
devuelve un índice dentro de la lista de restricciones para ser usado en paginación
obtiene el primer atributo de la entrada proporcionada
retorna el nombre largo para un marco id3v2
rebobina el iterador al inicio del array
declara una preferencia de diccionarios a usar para el lenguaje descrito/referido por tag
comprueba si el contenido de la entrada actual está cifrado
compara las claves de array1 con las claves de array2 y devuelve la diferencia
llamar a fbsql_fetch_assoc() es equivalente a hacerlo a fbsql_fetch_array() con fbsql_assoc como segundo parámetro
la función cubrid_lob2_tell() se usa para indicar la posición del cursor de un objeto lob
rebobina el fichero hasta la primera línea
constructor de solrdocument
establece los factores de muestreo de la imagen
establece una conexión persistente a un servidor mysql
cancels a pending http request
devuelve una cadena que contiene una representación hexadecimal del argumento number dado
devuelve una cadena que contiene la versión del analizador de php en ejecución o extensión
desplaza un mapa de color de una imagen por el número de posiciones dado
una función de tipo callable usada por la opción de contexto log_cmd_update al actualizar un documento
escapes a identifier e.g
abre un archivo de imagen basada en disco o virtual sujeto a varias opciones
obtiene la clave para el elemento actual directoryiterator
lee la información de la primera tupla de relation la hace la actual y la devuelve como una matriz asociativa en tuple
responderá a la truncación p.ej
crea una sesión o reanuda la actual basada en un identificador de sesión pasado mediante una petición get o post o pasado mediante una cookie
crea un recurso tipo correo mime
devuelve el estado de la variable de datos por el nombre variable_name en este servicio
devuelve el estado http de la respuesta
devuelve el valor del atributo
envía el comando site especificado al servidor ftp
la función cubrid_error_code() se usa para obtener el código de error del error que ocurrión durante la ejecución de la api
la descripción de la función va aquí
cierra conexión persistente previamente abierta
escala el sistema de coordenadas
retorna todos los recursos persistentes como una matriz
establece el ancho del contorno usado para dibujar los perfiles del objeto
devuelve la longitud del campo número offset dentro del resultado result
comprueba la validación del elemento actual
reemplaza count caracteres comenzando en la posicion offset con data
habilita las codificaciones chinas tradicionales
es utilizado ara obtener el tamaño en bytes de un segmento de memoria compartida
devuelve un array que describe la última conexión usada desde la agrupación de conexiones del complemento apuntada por el gestro de conexión del usuario
advertencia esta función no está documentada actualmente solamente se encuentra disponible la lista de parámetros
cierra una conexión no persistente a una base de datos postgresql asociada con el recurso de la connection
devuelve el valor de la categoría general de un punto de código
habilita o deshablita termscomponent
devuelve el nombre de la extensión
limpia el bit lo establece a 0 index en a
elimina los extremos que son el color de fondo de la imagen
incorpora la semilla seed al generador de números aleatorios o con un valor aleatorio si no se proporciona seed
establece el orden de detección automática de la codificación de caracteres a encoding_list
el factor por el que el fragmentador de expresiones regulares puede desviarse del tamaño de fragmento ideal especificado por solrquery::sethighlightfragsize) para acomodar la expresión regular
la función cubrid_close() finaliza la transacción que se está procesando actualmente cierra el gestor de conexión y se desconecta del servidor
pasa por alto funciones integradas al reemplazarlas en la tabla de símbolos
devuelve el tamaño comprimido de una especifica entrada de directorio
genera un mensaje log que será distribuido por el logger del sistema
ordenar las entradas de modo que las claves mantengan su correlación con las entradas que están asociadas
fija el nivel mínimo de severidad del cliente para los mensajes
devuelve la información del estado de un fichero en el argumento result de callback
la descripción de la función va aquí
devuelve un array de string siendo cada uno un substring del parámetro string formado por la división realizada por los delimitadores indicados en el parámetro delimiter
establece el tipo de la imagen
esta función devuelve una matriz con la lista de todas las bases de datos cubrid existentes
define la apariencia de la página usando tres parámetros: left top and zoom
la descripción de la función va aquí
cierra un gestor curl compartido y libera todos los recursos
marks event pending
exporta un número gmp a un string binario
escribe los datos del parámetro data en la posición actual del puntero interno del lob
establece la opción de configuración include_path durante la duración del script
sólo es necesario llamar a la función mssql_free_statement() si existe la preocupación de que se está utilizando demasiada memoria durante la ejecución del script
devuelve una lista de los diccionarios disponibles con sus detalles
construye un objeto watcher evperiodic y lo inicia automáticamente
esta función devuelve una matriz multidimensional que contiene una lista de todas las variables definidas ya sean variables de entorno de servidor o definidas por el usuario en el ámbito que get_defined_vars() es llamado
devuelve el número de errores
indica si el nombre de archivo es ejecutable
advertencia esta función no está documentada actualmente solamente se encuentra disponible la lista de parámetros
envía una respuesta al cliente de la petición actual indicando que ocurrió un error
devuelve el nombre de la base de datos que ha dado el recurso de la connection postgresql
devuelve la respueste xml como información de php serializada
establece un atributo con nombre name al valor dado
solrquery::terms_sort_index indica que los términos son devueltos por orden de índice
devuelve la traza de la pila de una excepción como una cadena de caracteres
prepara una sentencia sql para ser ejecutada por el método pdostatement::execute()
añade el recuso ch al grupo de recursos mh
el nombre de archivo asociado a una secuencia de imágenes
compara la variable asociada con la key con old_value y si coincide entonces asigna el new_value a este
funde los datos de data1 y data2 en un nuevo resource de datos de entrenamiento
cambia el valor de transparencia de un píxel que coincide con el objetivo y esté en la zona inmediata
crea un nuevo objeto domxpath
realiza una compararción de una expresión regular multibyte para un string multibyte predefinido
devuelve un string producido según el string de formateo dado por format
se llama cuando la iteración comienza después del primer recursiveiteratoriterator::rewind()
el número de bit fallidos es el número de neuronas de salida que difieren más del límte de fallo de bit véanse fann_get_bit_fail_limit() y fann_set_bit_fail_limit()
esta función permite enviar emails con el manejo correcto de los destinatarios cc y bcc
establece el color para dibujar rellanar o ambos
devuelve el número de elementos de un hash
devuelve el ultimo código de error de la última llamada mysqli_connect()
crea una copia de una estructura fann
este método intenta actualizar una entrada con un nuevo valor
cierra la secuencia de directorio indicada por gestor_dir
devuelve el copyright de la api graphicsmagick como una cadena
la función geoip_continent_code_by_name() devolverá la abreviación de dos letras del continente que corresponde al nombre de sitio web o una dirección ip
la función cubrid_fetch() se usa para obtener una única fila del resultado de la consulta
establece el manejador callback que más tarde será llamado con oauthprovider::callconsumerhandler()
prepara el siguiente resultado de multi_query
devuelve la propiedad tamaño de punto de los objetos
recupera el número de error establecido por la última función posix que falló
establece una instancia de base de datos persistente para el archivo en path con el mode indicado usando handler
devuelve el id autogenerado de la última sentencia insert ejecutada correctamente en la conexión
devuelve el nombre del campo número offset en result
comprueba si una función está definida por un usuario en contraposición a las funciones internas
resta right de left
verifica el texto dado en signed_text y retorna información sobre la firma
inserta el valor dado por newval en el índice especificado por index reorganizando el valor anterior a ese índice y todos los valores subsiguientes a través de la lista
devuelve el coseno hiperbólico de arg definido como exp arg + exp -arg/2
idéntica a mktime() excepto que los parámetros pasados representan una fecha gmt
libera previamente asignado un objeto de sesión snmp
devuelve la distancia hamming entre a y b
construir un nuevo objeto de fichero temporal
parchea una cadena str con un parche unificado en parámetro patch y devuelve el resultado
agrega todos los pares de objetos de datos de un diferente almacenamiento en el almacenamiento actual
se puede establecer un tiempo de espera en cualquier momento por lo que afectará a la recuperación de datos subsiguientes asociados a este cursor incluyento la obtención de más resultados de la base de datos
crea un documento html desde la representación dom
crea un fichero especial u ordinario
construye un nuevo contexto zmq
obtiene un árbol de mensajes hilados
devuelve el valor de una columna especificada en la fila actual del resultado
antepone un contenido a un bloque de contenido existente
esta función se usa para posicionar el cursor asociado al recurso de resultados antes de realizar una obtención
obtiene el ancho del tamaño de la fuente usada en el texto actual el caracter y la letra espaciada
establece el gestor de almacenamiento usado por la caché de consultas
establece el atributo tipo de imagen
separa un observador del sujeto para dejar de notificarlo cuando hayan actualizaciones
lee datos clob de la base de datos
añade ruido aleatorio a la imagen
crea un rectángulo comenzando por las coordenadas especificadas
usar el modo instantánea snapshot para la consulta
obtiene el id del grupo de el fichero
el factor de aumento es un valor mayor que 1 el cual se emplea para aumentar el tamaño del paso durante un entrenamiento rprop
establece el número de grupos de candidatas
añade ruido aleatorio a la imagen
prepara a query para su posterior uso con ifx_do()
se invoca antes de abrir una transacción antes de la primera llamada a ingres_query() o justo después de una llamada a ingres_rollback() o a ingres_commit() para activar o desactivar el modo autoconsigna del servidor cuando se inicia el script el modo autoconsigan está desactivado)
toma el puntero al archivo handle y trunca el archivo a la longitud size
limpia todas las llamadas registradas
esta función comprueba si el hash facilitado implementa el algoritmo y opciones proporcionadas
devuelve un array con información sobre todos los agrupamientos de conexiones
establece el valor gamma de la imagen
dibuja una porción de un círculo con su punto medio enx y
carga un archivo de configuración tidy con la codificación encoding especificada
devuelve el tipo de la variable php var
asocia datos a un object en el almacenamiento
este método añade una nueva entrada al conjunto y devuelve si la entrada fue añadida o no
búsqueda global para el primer índice presente que es igual o mayor que el índice pasado
devuelve la longitud del string dado
devuelve un identificador de imagen que representa la imagen obtenida desde el nombre de fichero dado
esta función hace una copia de variant y realiza una operación de conversión de variante para forzar a la copia a tener el tipo dado por type
comprueba si la función está definida o no en un espacio de nombres
establece el nombre de usuario y la contraseña que deberían utilizarse para la autenticación sasl con los servidores de memcache
traslada escala recorta o rota colores de imagen
obtiene la profundidad de la cuantía de gmagick como una cadena
cuenta los elementos del conjunto de sondeo
véase la sección de consultas de este manual para obtener información sobre lecturas distribuidas en secundarios
establece el valor del canal especificado de este objeto al valor proporcionado que debería estar entre 0 y 1
asigna un valor a la colección desde otra colección previamente creada
determina el orden de las restricciones de campos de faceta
esta función ordena un array de manera que los índices del array mantienen su correlación con los elementos del array asociados
retorna información sobre el estado para una tarea sea o no conocida por el servidor de trabajos
obtiene la posición de el parámetro
la función cubrid_close_prepare() cierra el gestor de solicitud dado por el argumento req_identifier y libera la región de memoria relacionada con el gestor
calcula la base elevada a la potencia exp con modulo mod
activa o desactiva el bit de entrelazamiento
detiene el perfilador y devuelve los datos xhprof de la ejecución
envía la acción con los parámetros in_params a través de un servicio proxy de forma sincronizada y devuelve out_params con valores o false en caso de error
vincula una variable de php a un parámetro de sustitución con nombre o de signo de interrogación correspondiente de la sentencia sql que fue usada para preparar la sentencia
obtiene las interfaces declaradas
intenta iniciar un servicio con nombre
comprueba si el método está protegido
puede utilizarse para obtener información sobre los campos de una determinada consulta realizada
devuelve el número de filas eliminadas insertadas o actualizadas por una sentencia sql
abre un archivo gzip .gz para lectura o escritura
esta función registra un objeto o una jerarquía entera de objetos
comprueba si la función está o no obsoleta
crea una nueva imagen y asocia el valor de imagickpixel al color de fondo
obtiene el tiempo de espera de la suscripción de eventos en segundos o 0 refiriénsose a que no hay tiempo de espera
sincroniza la base de datos
comprueba si el socket es persistente
desncripta la información data la cuál fue previamente encriptada mediante openssl_public_encrypt() y almacena el resultado en decrypted
establece el modo de bloqueo o no-bloqueo en un stream
devuelve null hasta que se llame a mongocursor::next()
obtiene el valor actual
la función mysqli_get_server_version() devuelve la versión del servidor al que está conectado representado por el parámetro link como un valor entero
crea una nueva imagen que es una copia de una existente con los píxeles de la imagen implosionados por el porcentaje especificado
adjunta otro núcleo («kernel») a este para que ambos puedan aplicarse a una única función de morfología o filtro
devuelve si el elemento actual del iterador es aceptable a través de este filtro
coloca un hipervínculo en la posición dada que apunta a una página del mismo documento
esta función devuelve un array con las clases spl actualmente disponibles
incluye información iptc binaria en una imagen jpeg
obtiene los hijos de el elemento actual
obtiene la lista de columnas y permisos asociados a una tabla
devuelve un campo de bits de los modificadores de acceso para este método
en caso de éxito devuelve el tiempo actual como el devuelto por gettimeofday(), mirando el valor en la caché dentro de base si es posible y llamando a gettimeofday() o clock_gettime() si no hay tiempo disponible en la caché
imprime o guarda una versión webp de la imagen dada por image
esta función reinicializa a sus valores por defecto todas las opciones de un gestor curl dado
esta función realiza una búsqueda en el buzón actualmente abierto en el flujo imap dado
devuelve el contenido del objeto blob
esta función crea una nueva instancia de la clase domentityreference
similar a include sin embargo cualquier código que resida fuera de una función o clase es simplemente ignorado
construye un nuevo mapa
la función mysqli_info() devuelve una cadena facilitando información sobre la última consulta ejecutada
obtiene si la variable dada es escalar
devuelve los datos de una localización específica en la base de datos
devuelve el número de filas de la base de datos que fueron cambiadas o insertadas o eliminadas por la sentencia sql más reciente
instala un objeto proxy para enganchar los métodos de los objetos de conexión de mysqlnd
imprime el texto en la posición especificada
devuelve el número de columnas de la consulta más reciente
devuelve la siguiente fila de datos de un conjunto resultado como una instancia de la clase especificada donde las propiedades correspondientes a los nombres de los campos de la fila recuperada y los valores se corresponden con los valores de la fila recuperada
recibe un array con el mensaje multiparte de un socket
conecta al servidor tcp_server especificado p.ej
este método estático determina si ha sido deshabilitado el acceso a escritura en el php.ini del sistema mediante la variable ini phar.readonly
este método de fabricación crea un nuevo hash desde un fichero de definición del disco
la descripción de la función va aquí
la función getimagesize() determinará el tamaño de un fichero de imagen dado y devolverá las dimensiones junto con el tipo de fichero una cadena de texto con el alto/ancho para utilizarla dentro una etiqueta img de html y el tipo de contenido http correspondiente
devuelve el nombre de la función
toma string y pone una barra invertida delante de cada carácter que es parte de la sintaxis de la expresión regular
obtener el tipo original de la excepción lanzada por el servidor
crea un color directo desde el color de relleno actual
cierra la base de datos establecida y libera todos los recursos del gestor de base de datos especificado
establece los factores de muestreo de la imagen
busca el índice ausente anterior no incluido que sea menor que el índice pasado
recorta píxeles de los extremos de la imagen
crea un índice en el/los campo/s especificado/s si aún no existe
abre una base de datos dbase con el modo de acceso dado
esta función es una envoltura para las instrucciones table_get y table_set de apache
devuelve si incluir o no el término de límite superior en el conjunto de resultados
devuelve un array de php desde el yaf_config_simple
el método valid() de emptyiterator
idéntica a fgets() excepto que fgetss() intenta eliminar cualesquiera bytes null etiquetas html y php del texto que lee
crear un nuevo iterador desde una instancia arrayobject
devuelve el mensaje de error del sistema posix asociado con el errno
establece/obtiene la codificación de caracteres de salida http
destruye un objeto judy
devuelve el índice en el que se encuentra el iterador actualmente
almacena un archivo local en el servidor ftp
escalar datos en un vector de entrada antes de alimentarlo a una rna basada en parámetros previamente calculados
compone una imagen en otra en el índice especificado
reemplaza cada píxel con la correspondiente estadística de la proximidad del alto y ancho especficado
el número de épocas de estancamiento de candidatas en cascada determina el número de entrenamiento de épocas permitido para continuar sin cambiar el ecm por una fracción de fann_get_cascade_candidate_change_fraction()
función para extraer una secuencia de un clúster de grafemas predeterminados desde un buffer de texto que puede estar codificado en utf-8
devuelve el color hsl normalizado descrito por el objeto imagickpixel con cada uno de los tres valores como números de punto flotante entre 0.0 y 1.0
guarda la estructura de entrenamiento en un fichero con el formato especificado en fann_read_train_from_file()
devuelve la geometría de la página asociada con el objeto imagick en un array asociativo con las claves width ancho height alto x e y
esta función devuelve un array con toda la información de configuración relacionada con mysqlnd_memcache que ha sido adjuntada a la conexión de mysql
enlaza llamadas de retorno a una conexión cyrus imap
devuelve un identificador de imagen que representa una imagen en negro del tamaño especificado
anexa un círculo a la ruta corriente
llena la ruta actual usando la regla del número de vueltas diferentes a cero
esta función desencripta datos
genera un recurso de certificado x509 desde la csr dada
devuelve el tipo de la columna identificada por column_number
devuelve el número de filas afectadas por la última llamada de select update ó delete asociada con result
devuelve el máximo de memoria en bytes que se ha asignado a su script php
lee los datos de el flujo temporal
la funcionalidad de memcached::getmultibykey() es equivalente a la de memcached::getmulti() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
obtiene un caracter del fichero
inserta el valor dado por value al final de la lista doblemente enlazada
añade una tupla a relation
aplica un filtro de efecto especial que simula una pintura al óleo
devuelve un valor de tipo string que contiene el último error o null si ocurrió ninguno
configura la función del manejador de instrucciones de proceso para el intérprete xml
obtiene la pendiente de activación para la neurona número neuron en la capa número layer contando la capa de entrada como capa 0
establece la edición de la base de datos de objetos que van a se utilizada por conexiones subsiguientes
este método añade una nueva entrada a un hash y devuelve si la entrada fue añadida o no
en general se recomienda utilizar mongodb::lasterror() en lugar de este método
devuelve la representación en forma de texto de esta expresión regular
devuelve el nobre del fichero desde el que se lanzó el objeto
la función cubrid_lob2_size() se usa para obtener el tamaño de un objeto lob
retorna las opciones que han sido establecidas con anterioridad en el trabajador
lanza una excepción indicando que el generador no puede ser presentado
establece la función de retrollamada a emplear durante el entrenamiento
añade una acción javascript al campo solicitado
devuelve la representación en forma de string del objeto reflectionclass
devuelve si un campo será remarcado solamente si la consulta coincide con este campo en particular
rota la imgaen image usando el ángulo angle dado en grados
obtiene una fila de datos del resultado asociado con el identificador de resultado especificado
limpia la caché de entradas de un tipo específico
envía información de estado al servidor de trabajo y a cualquier cliente a la escucha
corrección gamma de una imagen
define el estilo del icono de la anotación
el tamaño de paso inicial es un número positivo que determina el tamaño de paso inicial
copia información entre un descriptor de fichero y otror
busca el desplazamiento de campo especificado
crea una nueva superficie que es tan compatible como se pueda con una superficie existente
devuelve la memoria usada por el array judy
devuelve la fecha de distribución de imagemagick como un string
la función cubrid_set_add() se usa para insertar un único elemento en un atributo de tipo de conjunto conjunto multiconjunto secuencia que se solicitó
borra los archivos dados de la caché del código de operación
obtiene el valor del atributo de nombre name para el nodo actual
establece donde un parámetro oauthdebería ser pasado
lleva a cabo una transacción sin cerrarla
obtiene la posición actual para la ruta de impresión
returns collapse hint
redibuja readline para volver a dibujar la vista
restaura el archivo rrd desde el xml dump
elimina uno de los campos de ordenación
devuelve el valor numérico del mensaje de error de la última operación frontbase
el máximo de épocas de salida determina el número máximo de épocas que podrían ser entrenadas las conexiones de salida después de añadir una nueva neurona candidata
devuelve la cadena invertida
recupera información sobre los datos almacenados en la caché del usuario
dibuja una elipse en la imagen
establece el color de un índice de mapa de color especificado
obtiene el valor de una columna llamada index_or_name si es un string o del número ordinal de columna index_or_name si es un integer de la fila actual del manejador del resultado result de una consulta
finaliza una conexión persistente
envía una consulta u trae todos los resultados
crea un núcleo («kernel») desde uno integrado
devuelve la opacidad usada cuando se dibuja
crea una red neuronal de retropropagación estándar que no está completamente conectada y que tiene conexiones de atajo empleando un array con tamaños de capas
establece el color de fondo de la imagen
esta funcion procesa el html contenido en la cadena source
obtiene la clave para el elemento actual en el iterator interno
códifica una uri a rfc 3986
devuelve el id real numérico de grupo del proceso actual
crea un nuevo documento fdf
guarda una parte del cuerpo completo de un mensaje especificado
crea un alias dado por alias basado en la clase original definida por el usuario
inicia un nuevo object oauthprovider
el constructor de yaf_config_ini
cierra el fichero especificado por fd
esta función necesita un fichero de métrica de fuentes de adobe para conocer los glifos que están disponibles
devuelve el número máximo de documentos del conjunto de resultados completo del cliente para cada consulta
esta función devuelve true si el modo especificado retorna bloques de bytes o false si retorna tan solo bytes
obtiene información de la versión de unicode
devuelve la opacidad de los perfiles del objeto contorneado
devuelve el color del objeto imagickpixel como string
dibuja una curva de bézier desde el punto actual usando 3 puntos de control más
comprueba si están disponibles más filas en el conjunto de resultados dado
desactiva la solicitud de información detallada desactivado por omisión)
devuelve la extensión del fichero
crea un array que contiene un rango de elementos
este método se llama después de iterator::rewind() y iterator::next() para comprobar si la posición actual es válido
prepares and executes a query
fija el modo para firmar
recupera el id generado por la consulta anterior normalmente insert para una columna auto_increment
captura una fila en forma de pseudo-objeto a partir de un identificador de resultado
obtiene el valor actual del carácter espaciado
reinicia el número del último error
parchea un file con un patch binario y almacena el resultado en un archivo dest
devuelve el tamaño del archivo dado en bytes
obtiene toda la información del servicio
asigna la version fdf al documento solicitado
abre una conexión con una base de datos postgresql especificada por el connection_string
esta función ordena un array en orden inverso mayor a menor 
solicita un bloqueo de escritura en la tupla tuple especificada
con bind_textdomain_codeset() se establece la codificación de los mensajes que serán devueltos desde el domain por gettext() y funciones similares
la función fscanf() es similar a sscanf() excepto que toma su entrada desde un archivo asociado con handle e interpreta la entrada de acuerdo al parámetro format especificado que es descrito en la documentación de sprintf()
se comporta de forma idéntica a imagecolorallocate() con la adición del parámetro de transparencia alpha
devuelve la refencia al objeto gmagick acutal con el puntero de imagen apuntando a la secuencia correcta
establece la parte del prefijo usada en el árbol gráfico
escribe datos en un determinado evento buferizado
devuelve la firma de verificación de un archivo phar en una cadena hexadecimal
devuelve las cabeceras de respuesta sin tratar del servidor
recupera los hilos del servidor mysql actuales
esta función limpia el código de error del socket dado o el último error global de socket si no se especifica un socket
devuelve un matriz identidad de tamaño n
guarda la variable var con la clave key solamente si la clave no existe todavía en el servidor
rellena un bloque de imagen con información variable según sus propiedades
una función de tipo callable usada por la opción de contexto log_write_batch al ejecutar una operación por lotes
indica si el puntero interno de objetos grandes está al final del lob
determina si el punto de código especificado es un carácter de puntuación
este método comprime el fichero dentro de un archivo phar usando la compresión bzip2 o zlib
crea una conexión persistente al servidor de oracle y se identifica
igualq que datetime::settimezone() excepto que trabaja con datetimeimmutable
inserta o actualiza variable con la clave cada por variable_key
establece el valor del espacio de color global para el objeto
este método hace que esta conexión sea autenticada
extrae una región de la imagen
la descripción de la función va aquí
devuelve el mensaje de error
ecualiza el histograma de la imagen
aplica un efecto especial a la imagen similar al efecto logrado en un cuarto oscuro fotográfico aplicando un tono sepia
devuelve los detalles del diccionario
establece la geometría de página del objeto imagick
reemplaza el registro dado en la base de datos con la información dada
este método es llamado bajo el cierre del filtro normalmente también durante el cierre del flujo), y se ejecuta después de llamar al método flush
añade un elemento al registro
recupera una ruta por su nombre
la descripción de la función va aquí
después de que se haya creado el socket socket usando socket_create() vinculado a un nombre con socket_bind() e indicado que escuche conexiones con socket_listen() esta función aceptará conexiones entrantes sobre ese socket
este método se puede usar para determinar si un phar tiene un fichero interno eliminado o el contenido de un fichero ha cambiado de alguna forma
se llama cuando comienza la iteración después de llamar a recursivetreeiterator::rewind()
asocia la imagen previa en una lista de imágenes con el objeto imagick
el mínimo de épocas de cadidatas determina el número máximo de épocas que podrían ser entrenadas las conexiones de entrada a las candidatas antes de añadir una nueva neurona candidata
devuelve el nombre de host que usa mongoid para generar identificadores únicos
añade un comentario a la imagen
función callback para la función ob_start() con el fin de reparar el buffer
añade un fichero al archivo zip par la ruta dada
devuelve una cadena que contiene la versión actual de la extensión stomp
calcula el seno para cada valor de real y devuelve el array resultante
guarda un documento fdf
calcula el siguiente entero menor para cada valor de real y devuelve el array resultante
lista las bases de datos disponibles en el link_identifier especificado
el método gearmanclient::do() es obsoleto desde pecl/gearman 1.0.0
devuelve si usar o no spanscorer para remarcar términos de frases sólo cuando aparezcan dentro de la frase de consulta en el documento
aplica iterativamente la función callback a los elementos de array con el propósito de reducir el array a un solo valor
esta función crea un nuevo nodo elemento con el nombre de espacio asociado
añade un script al fdf que una vez que se importe acrobat añade a los scripts del documento
notifica todos los observadores adjuntos
devuelve si la consulta será impulsada boost o no mediante la relevancia del térmido de interés
comprueba si el objeto dado por object es de esta clase o tiene esta clase como una de sus madres
indica cuando el nodo actual es asp
obtiene un objeto de varios elementos de cabecera similar a imap_header()
intenta eliminar drop por completo una base de datos en el servidor asociado al identificador de enlace especificado
obtiene o establece el nombre host de una conexión
devuelve la lista de todos los servidores de la agrupación de servidores
devuelve el tamaño del clob recibido
cambia algunas opciones de cabecera del archivo de base de datos rrd
esta función devuelve un array con los nombres de las interfaces que implementa la class dada y sus padres
crea un rectángulo relleno con color en image comenzando por el punto 1 y finalizando en el punto 2
devuelve el tamaño actual de una entrada de directorio específica
este método anima la imagen en un servidor x local o remoto
devuelve los padres de un objeto
notifica a los clientes en escucha que la propiedad ha cambiado a un valor específico
crea una configuración usada para abrir un diccionario
cierra una conexión abierta y libera los recursos asociados a la conexión
guarda x509 en una cadena nombrada por out en un formato de archivo pkcs#12
construye un infiniteiterator de un iterator
es una versión extendida de msql_fetch_row()
la descripción de la función va aquí
la entrada que se especifica por dn se renombra/mueve
establece el modo de texto predeterminado para todas las consultas select
lee el mensaje s/mime contenido en el archivo dado y examina la firma digital
la descripción de la función va aquí
toma todas las imágenes desde puntero de imágenes actual hasta el final de la lista y las aplasta unas contra otras de arriba a abajo si el parámetro stack es true de lo contrario lo hace de izquierda a derecha
libera los recursos del sistema y de la base de datos que están asociados con un resultado
obtiene la clave actual del iterador
devuelve una de las constantes memcached::res_* la cual es el resultado del último método de memcached ejecutado
devuelve los campos de una tabla y sus metadatos asociados
establece el iterador a la posición en la lista de imágenes definida por el parámtro index
este método añade un campo a la instancia de solrdocument
returns group functions group.func parameter values 
toma un string xml correcto y lo retorna como objeto
libera la memoria usada por una fuente postscript type 1
obtener el género de un nombre en un país concreto
devuelve el ancho del recurso image dado
dibuja la cadena string en las coordenadas dadas
aplica un efecto especial a la imagen similar al efecto conseguido en un cuarto oscuro fotográfico exponiendo selectivamente áreas del papel sensible fotográfico a la luz
cifra los datos y los devuelve
es similar a memcache::connect() con la diferencia que la conexión que establece es persistente
vierte la información de depuración en el registro para el servidor mysql
esta función analiza el documento html del fichero llamado filename
devuelve el valor de la opción especificada para una conexión o sentencia
devuelve el número máximo de términos de consultas que serán incluidos en cualquier consulta generada
devuelve un integer que corresponde al tipo del objeto actual
envía todas las transacciones pendientes en una conexión
devuelve el número de campos del result set dado
obtiene el valor gamma de la imagen
establece un gestor ocupado que estará dormido hasta que la base de datos no esté bloqueada o se alcance el tiempo de espera
despliega los valores de un array como una cadena con formato de acuerdo al formato el cual es descrito en la documentación de sprintf()
abre una conexión a una base de datos
rota el sistema de coordenadas
cambia el tamaño y la dirección de la página para un formato predefinido
esta función ejecuta la prueba automática sobre el algoritmo especificado
devuelve un objeto magickpixeliterator
hace un diff unificado que contiene las diferencias entre old_file y new_file y almacena este en el archivo dest
devuelve la constante definida
establece retval hacia el valor de la opción especificada
devuelve mensajes de: estado de error de sistema y/o mensajes de zip
establece el id real de grupo al proceso actual
devuelve la versión fdf del documento dado o bien el número de versión de la api si no se utilizó ningún parámetro
libera el bloqueo de escritura en tuple previamente obtenido mediante dbplus_getlock()
método la función de distribución acumulativa p se calcula directamente mediante código asociado con la referencia siguiente
devuelve el ancho del píxel de un carácter de una fuente
cierra una conexión al servicio fam
obtiene la resolución x e y de la imagen
esta función sólo la utiliza php internamente no debería ser necesario que sea llamada por el usuario
si un recurso socket es pasado a esta función devuelve el último error que ocurrió en este socket en particular
conectar con un diccionario de nombres externo
obtiene una lista de todos los algoritmos soportados en el parámetro lib_dir
revisa si un número es cuadrado perfecto
leer la lista de buzones
es similar a memcached::set() pero la operación fallará si key ya existe en el servidor
obtiene los nombres de los métodos de una clase
obtiene la propiedad gravedad global para el objeto imagick
igual que datetime::settimestamp() excepto que trabaja con datetimeimmutable
encuentra la longitud del segmento inicial de subject que contiene solamente caracteres de mask
añade un smiley al analizador
establece cómo se unen las líneas
puede utilizarse para obtener información de campos de resultado de una consulta
obtiene la calidad de compresión del objeto
permite representar un modelo sencillo de lectura/escritura que puede emplearse virtualmente en cada plataforma incluidas la mayoría de las derivadas de unix e incluso windows)
devuelve el comentario del archivo zip
devuelve un objeto que contiene la información de los campos
esta función se puede usar para ver qué módulos del controlador están siendo registrados actualmente
reúne las estadísticas de un archivo abierto por el puntero al archivo referenciado por handle
lee la siguiente entrada en un fichero zip
registra un nombre de prefijo de clase local
devuelve la versión de las librerías del cliente svn
extrae una región de la imagen y la devuelve como un nuevo objeto imagick
cuenta el número de apariciones de cada byte-value 0..255 en string y lo devuelve de varias maneras
realiza un ping al flujo para ver si aún está activo
este método obtiene el nombre de la etiqueta xml del elemento actual
envía una consulta única no están soportadas consultas múltiples) a la base de datos activa actual en el servidor que está asociado con el conn_identifier especificado
devuelve la profundidad de la cuantía de imagick como un string
la descripción de la función va aquí
puede crear o abrir un segmento de memoria compartida
el rango válido para el calendario juliano es desde 4713 a.c
posiciona el cursor en el eleménto padre del actual atributo
establece una conexión al servidor memcached
obtiene el nombre de la clase desde la que se llama al método estático
establece el tipo mime de la imagen
es similar a la función mysql_field_name()
cierra la conexión no persistente al servidor de mysql que está asociada con el identificador de enlace especificado
intenta cambiar el propietario del fichero filename por el usuario user
devuelve el número de salidas de cada patrón de entrenamiento del resource de datos de entrenamiento
activa el comportamiento automático de envío
obtiene información de fichero del elemento actual
compara las keys del array1 contra las keys del array2 y retorna la diferencia
establece la resolución de la imagen
ordena las entradas por los valores usando una función definida por el usuario
establece el tiempo máximo de consulta de búsqueda
guarda una interpretación codificada con pem ascii blindada de key en un archivo nombrado por outfilename
elevar left_operand a la potencia right_operand
devuelve un string con barras invertidas delante de los caracteres que necesitan ser escapados
devuelve la configuración actual de session.cache_expire
comprueba si una transacción está actualmente activa dentro del controlador
recupera un campo del documento
este método puede usarse para comprobar una variable antes de pasarla como un argumento a mongoid::__construct()
devuelve la capa del archivo mpeg por lo general 3 para mp3 
devuelve información acerca de la versión de curl
devuelve el índice de la imagen activa actual dentro del objeto imagick
obtener los pulsos transcurridos para todos los intervalos anteriormente cerrados
establece el token y el secreto para las subsecuentes peticiones
comprueba si la clase es o no final
obtener la clave actual
establece la compresión de la imagen
comprueba si un punto de código tiene la propiedad de unicode alphabetic
añade el nuevo nodo atributo attr al elemento
call puts one message on a queue
ejecuta el dispositivo
establece el estilo numérico para el rango especificado de páginas
comprueba si un string está corrompido
establecer la función de error empleada durante un entrenamiento
encuentra la mejor forma de codificar el contenido leído del apuntador de archivo dado
desactiva un gestor load data infile local previamente establecido con mysqli_set_local_infile_handler()
obtiene el ascenso vertical de la fuente
libera un recurso de evento creado previamente
busca una subimagen en la imagen actual y devuelve una imagen de similitud tal que una ubicación que coincida exactamente es completamente blanca mientras que si no coincide ningún píxel negra o de otro modo algún nivel de gris intermedio
establece una función de retorno a usar cuando una tarea no es completada satisfactoriamente
establece/obtiene la codificación de caracteres para una expresión regular multibyte
realiza una conversión del set de caracteres en el string str desde in_charset a out_charset
establece la profundidad de una canal de imagen en particular
elimina uno de los campos de consulta morelikethis
devuelve el ancho del string str
abre una nueva conexión al servidor mysql
elimina el atributo llamado name del elemento
obtiene el valor absoluto de un número
añade o elimina un perfil icc iptc o genérico de una imagen
devuelve una representación en forma de texto de esta fecha similar a la representación que devuelve microtime()
el número de candidatas empleadas durante el entrenamiento calculado multiplicando fann_get_cascade_activation_functions_count() fann_get_cascade_activation_steepnesses_count() y fann_get_cascade_num_candidate_groups()
procesa ciertos elementos de un documento pdf importado
comprueba si las funciones rar emitirán avisos y devolverán valores de error o si ellas lanzarán excepciones en la mayoría de las circunstancias no incluye algunos errores de programación tales como pasar el tipo incorrecto de argumento)
devuelve una referencia al hilo que está siendo ejecutado
puede definir esta función para habilitar la carga de clases
coloca el cursor en el atributo en función de su posición
devuelve una cadena que ha sido debidamente escapada para una inclusión segura en una sentencia sql
lanza una excepción al generador y reanuda su ejecución
devuelve el propietario de un objeto
rebobina el iterador a un índice de inicio específicado en limititerator::__construct()
busca la última ocurrencia de needle en haystack y devuelve la porción de haystack
este constructor crea un nuevo objeto quickhashstringinthash
esta función es idéntica a ereg_replace() excepto que ignora la distinción de mayúsculas-minúsculas cuando se comparan caracteres alfanuméricos
construye un lote de operaciones delete
obtiene todas las cabeceras http de respuesta
la función geoip_record_by_name() devuelve información a partir de un nombre de host o de una dirección ip
rellena y contornea el trazado actual con el color de relleno y de contorno actuales
si se especifica el parámetro opcional base log() devuelve log base arg y en caso contrario log() devuelve el logaritmo natural de arg
apunta al número de campo especificado
esta función prepara para una solicitud de servicio extendido
esta función borra la bandera especificada por flag a las banderas establecidas para los mensajes en la secuencia especificada por sequence
analiza un documento xml
la función runkit_lint_file() lleva a cabo una verifiación de sintaxis lint pelusa en inglés) sobre el nombre de archivo especificado comprobando errores en la escritura del script
crea un grupo de campos de formulario sujeto a varias opciones
restablece la secuencia de directorio indicada por gestor_dir al comienzo del directorio
lee la información de la tupla actual de la relación relation dada
los dispositivos de ømq pueden hacer de intermediarios de direcciones servicios colas o cualquier abstracción que se defina sobre las capas de mensaje y socket. -- zguide
obtiene los iteradores hijo contenidos en un parentiterator
devuelve una matriz como representación de un documento
comprueba si la propiedad existe
aumenta un número almacenado
establece la codificación de caracteres del documento fdf
mongo posee una característica conocia como cursores de seguimiento tailable que son similares al comando de unix tail -f
devuelve el elemento índice de la posición actual del array
especifica un formateador para la salida remarcada
añade un conjunto de imágenes en una imagen más grande
reinicia el puntero interno al principio
devuelve el alto de la imagen
devuelve e elevado a la potencia de arg
obtiene los parámetros de la cookie de sesión
obtiene el rango de uno o más canales de imagen
crea un nuevo número de 32 bits con el valor proporcionado
devuelve la información del estado de un fichero en el argumento result de callback
ejecuta una declaración preparado con odbc_prepare()
imprime o guarda una versión wbmp de image
ordena a com que eventos generados por comobject sean absorbidos por el objeto php sinkobject
comprueba si el terminal tiene capacidades de color y si el programador puede cambiar las definiciones de color usando ncurses_init_color()
obtiene el nombre de un carácter unicode unicode
simula el bosquejo de un lapiz
es similar a memcached::set() pero en lugar de almacenar un único ítem de clave/valor funciona con múltiples ítems especificados en items
entrenar una iteración con un conjunto de entradas y un conjunto de salidas deseadas
devuelve el ancho del texto text en una fuente arbitraria
obtiene el tipo de compresión de la imagen actual
devolverá la identificación del hilo que creó el hilo referenciado
establece la resolución
convierte una cadena del formato utf-8 al formato utf-16
añade una línea recta al trazado actual desde el punto actual hasta las coordenadas dadas
establece los datos contenidos en un esquema relaxng a usar para validación
obtiene un parámetro si ha sido previamente establecido por xsltprocessor::setparameter()
establece el título de proceso del proceso actual
inicia el depurado al archivo pprof_process_id en el directorio de volcado
obtener el tiempo transcurrido para el intervalo anteriormente cerrado
crea un marcador sujeto a varias opciones
devuelve información de errores y alertas de la última operación sqlsrv realizada
obtiene el valor de gravedad actual de la imagen
comprueba si el montón contiene más nodos
la descripción de la función va aquí
obtiene el factor de periodo inestable para una función en particular
devuelve una cadena de un caracter que contiene el carácter especificado por ascii
devuelve estadísticas sobre la conexión cliente
esta función devuelve el tamaño en bytes del iv del algoritmo especificado por el descriptor de cifrado
devuelve estadísticas sobre el servidor de base de datos
esta función cierra un blob previamente abierto para lectura mediante ibase_blob_open() o para escritura mediante ibase_blob_create()
la función tidy_setopt() actualiza la opción option determinada con un nuevo valor value
este método añade una nueva entrada al hash y devuelve si la entrada se añadió o no
revisa el símbolo del número
recuperar los atributos externos de una entrada definida por su índice
devuelve una matriz de parámetros url no codificados
establece un callback a ser llamado cuando una notificación de cambio de variable es recibida
cunstruye el árbol of sdo_dataobjects desde la cadena de instancia xml dada
obtiene información sobre elsistema
devuelve la representación de la excepción en formato string
esta función retorna un nuevo resource de bbcode usado para analizar strings bbcode
esta función obtiene las cabeceras del correo de la secuencia dada por sequence y devuelve una visión general de sus contenidos
es una versión de sybase_fetch_row() que utiliza como índices los nombres de columnas en vez de enteros en el array resultante
este método devuelve un recurso de flujo que puede usarse con todas las funciones de ficheros de php que traten con la lectura de ficheros
comprime el string data dado utilizando codificación lzf
devuelve el último mensaje de error
esta función es un alias de: finfo_set_flags()
escapa un string para un tipo de dato bytea
exporta la clase arrayobject a un array
permite procesar múltiples recursos curl de forma asíncrona
retorna el tiempo actual a esperar en milisegundos para actividad de entrada/salida del socket
obtiene el factor de muestreo horizontal y vertical
suspende la página actual por lo que después se puede reanudar con pdf_resume_page()
esta función descomprime una cadena comprimida
establece el color y el color de espacio actual
establece el valor de la clave /status
analiza str como si fuera un string de consulta pasado por medio de un url y establece variables en el ámbito actual
devuelve el término en el que comenzar
esta función es similar a file() excepto que file_get_contents() devuelve el fichero a un string comenzando por el offset especificado hasta maxlen bytes
devuelve el coseno del parámetro arg
devuelve estadísticas sobre el servidor de caché de documentos
obtiene el número de elementos de un array o el número de propiedades públicas en un objeto
devuelve el resultado de left a la potencia de right
obtiene la profundidad de un canal de imagen en particular
devuelve el nombre de la columna especificada del resultado
usado para buscar sesiones con atributos comunes
hace que un fichero regular referenciado por el descriptor de fichero fd sea truncado a exactamente length bytes
devuelve estadísticas sobre el servidor hyperwave
devuelve el parámetro string con un < br / > o < br > insertado antes de cada nueva línea
esta función imprime la lista de créditos de los desarrolladores de php módulos etc genera los códigos html apropiados para insertar la información en una página
deserializa un objeto arrayobject serializado
establece el color transparente en image
establecer la ruta del directorio para un backend dado
calcula el logaritmo natural para cada valor de real y devuelve el array resultante
devuelve el número de campos en el resultado result
devuelve el nombre abreviado de la clase es decir sin el espacio de nombres
indica si la biblioteca cliente está compilada con segurdid a nivel de hilos
obtener el nombre de clase iterador de array que es utilizado por arrayobject::getiterator()
llamar a este método directamente está obsoleto
este método de fabricación crea un nuevo hash desde un fichero de definición del disco
devuelve el estado acutual del parámetro facet.missing
busca la posición en unidades de grafema de la última coincidencia de una cadena insensible a mayúsculas-minúsculas
limpia el contenido existente
agrega uno o más atributos al dn específico
enumera todos los ajustes del registro
convierte una cadena del formato utf-32 al formato utf-16
busca un carácter unicode por su nombre y devolver su valor de punto de código
recupera una variable asignada
realiza una operación substr() multibyte de forma segura basada en el número de caracteres
intenta borrar un servicio de la base de datos del acs
cancela una sentencia de base de datos
la función long2ip() genera una dirección de internet en formato de puntos por ejemplo.: aaa.bbb.ccc.ddd) a partir de una correcta representación de la dirección
compara array1 con uno o más arrays y devuelve los valores de array1 que no estén presentes en cualesquiera de los otros arrays
trunca un fichero a como mucho un tamaño de offset bytes
avanza al siguiente elemento
lee el contenido del nodo actual como string
devuelve el objeto de destino apuntado por las anclas fuente especificadas
incorpora la semilla seed al generador de números aleatorios o con un valor aleatorio si no se proporciona seed
crea una miniatura de tamaño fijo ampliando o reduciendo de escala la imagen y recortando un área específica desde el centro
crea una imagen compuesta combinando varias imágenes por separado
establece la contraseña para el archivo activo
compone una imagen en otra en el índice especificado
lozaliza el enésimo indice presente en el array judy
este método es un alias de judy::count
esta función implementa un algoritmo de comparación que ordena strings alfanuméricos de la manera en que un humano lo haría
esta función devuelve un array con los nombres de los traits que usa la class dada
igual que datetime::add() excepto que trabaja con datetimeimmutable
generar un string de firma de cabecera oauth basada en el método http final el url y los parámetros de string/array
devuelve la matriz consistente en los elementos de la matriz input que coinciden con pattern
envía el valor dado al generador como resultado de la expresión yield actual y reanuda la ejecución del generador
dibuja el trazado construido previamente con las llamadas a las funciones de dibujo como ps_lineto()
esta función crea una nueva instancia de la clase domprocessinginstruction
crea un nuevo punto de control con un objetivo especificado
busca la posición en unidades de grafema de la primera coincidencia de una cadena insensible a mayúsculas-minúsculas
establece el nombre de archivo antes de que se lea o escriba una imagen
añade un arco circular en el sentido contrario a las agujas del reloj
establece una conexión persistente a un servidor cubrid
mejora la diferencias de intensidad entre elementos claros y oscuros de la imagen
serializa el almacenamiento
este método devuelve un url según el argumento info info y pospone los string de consultas al url según el argumento query
esta función ordena las entradas de forma que las claves mantengan su correlación con la entrada que está asociada con una función de comparación definida por el usuario
abre una conexión al servidor hyperwave en el host hostname
ejecuta una sentencia preparada con sqlsrv_prepare()
recupera el nombre de tabla desde un resultado dado por result
separa un canal de la imagen y devuelve una imagen en escala de grises
excepto por la dirección de dibujo esta función se comporta exactamente como pdf_arc()
comprueba si un flujo o una url es o no local
a diferencia de yaf_view_simple::assign() este método asigna un valor de referencia al motor
devuelve información acerca de la biblioteca subyacente rrdtool
devuelve el entero del valor de la propiedad para un nombre de valor dado tal como se especifica en el fichero de base de datos de unicode propertyvaluealiases.txt
obtiene la «edad» del punto de código
devuelve el valor del parámetro facet.date.gap
indica si un nodo representa sólo texto sin nada de html 
guarda el objeto the sdo_das_xml_document en una cadena
lucene soporta el escape de caracteres especiales que son parte de la sintaxis de la consulta
devuelve el número de veces en que el substring needle aparece en el string haystack
establece el contenido de una respuesta
se utiliza para establecer el valor de las diferentes opciones de la función assert() o consultar su valor actual
mueve todas las instancias de iterador adjuntadas hacía la siguiente
devuelve la frecuencia de muestreo o tasa de bits de un archivo mpeg
obtiene la lista de módulos definidos en config si no se han definido siempre habrá un módulo llamado index
traducir caracteres iso-8859 a caracteres t61
la llamada al método connect en un objeto samconnection conecta el script php a un servidor de mensajería
devuelve el número de imágenes asociadas con el objeto imagick
esta función es una versión extendida de ingres_fetch_row()
devuelve el xml enviado en la última petición soap
selecciona un umbral individual para cada píxel basado en un rango de valores de intensidad en su zona local
envía datos al servidor de trabajos y a cualquier cliente a la escucha para este trabajo
libevent proporciona una capa de abstracción por encima del api de eventos
escala una imagen a las dimensiones deseadas con un muestreo de píxeles
fusiona la fuente con el objeto solrdocument actual
esta función le permite sobrescribir el dominio actual para una búsqueda unica del mensaje
el tiempo permitido para que la búsqueda finalice
cambia cualquier píxel que coincida con el color definido para el relleno
establece el término en el que parar
este es el primer enchanche del sistema de enganches de complementos de yaf
inicia el bucle de eventos para la base de eventos especificada
devuelve el color descrito por el objeto imagickpixel como un array
obtnener la cuota de ajustes por usuario
dibuja una línea en la imagen usando el color del contorno la opacidad del contorno y el ancho del contorno acutales
crea un documento xml desde la representación dom
esta función establece el valor de un atributo extendido del archivo
esta función esta obsoleta
añade un perfil nominado al objeto imagick
calcula el arcocoseno para cada valor de real y devuelve el array resultante
envía la excepción indicada cuando este trabajo está siendo ejecutado
lee y verifica el mapa del fichero guardando el número de campos e información
disminuye un valor integer almacenado
obtiene el contenido de una ruta pcos con el tipo stream fstream o string
lee las cabeceras exif desde un archivo de imagen jpeg o tiff
obtiene la entrada actual del array
devuelve el tipo del campo número offset dentro del resultado result
devuelve el tamaño de un blob y otra información útil
devuelve el prefijo de faceta
devuelve el ítem que fue previamente guardado bajo la clave dada por key
activa el colector de referencia circular estableciendo zend.enable_gc a 1
esta función es un alias de: soapheader::soapheader()
permanece bloqueado hasta que haya algún tipo de actividad en conexiones curl_multi
localiza una entrada utilizando su nombre
compone y retorna un string que representa un campo de cabecera mime válido que tiene una forma similar a: subject: =?iso-8859-1?q?pr=fcfung_f=fcr?= entwerfen von einer mime kopfzeile en el anterior ejemplo subject es el nombre del campo y la parte que empieza con =?iso-8859-1?... es el valor del campo
devuelve el valor ascii del primer carácter de la string
devuelve una única columna de la siguiente fila de un conjunto de resultados o false si no existen más filas
comprueba si el parámetro espera un array
obtiene los encabezados de la última respuesta
establece el factor de umbral del error de paso de sarprop
establece una función callback para una señal y acción
procesa datos incrementalmente al interior del recurso de correo mime entregado
deserializa un packet wddx
comprueba si la clase es o no abstracta
asigna un identificador al trabajador
la descripción de la función va aquí
enumera los proveedores de enchant e indica alguna información rudimentaria sobre ellos
devuelve la frecuencia de documento mínima a devolver para ser incluido
elimina los extremos que son el color de fondo de la imagen
especifica una decoración que va a ser aplicada cuando se anota texto
finaliza y devuelve el paquete wddx
obtiene la cantidad de memoria usada por la caché de la ruta real
anota una imagen con texto
obtiene un carácter desde un puntero a un archivo dado
envía la acción con parámetros al servicio a través de un proxy y establece el valor
si solrquery::sethighlightalternatefield() se le pasó el valor true este parámetro especifica el número máximo de caracteres del campo a devolver
la descripción de la función va aquí
libera todo el array judy
ésta función configura la consulta analizadora ccl para un servidor con definiciones de puntos de acceso calificadores ccl y su asignación al rpn
devuelve las variables estáticas
devuelve los formatos soportados por imagick
devuelve el número de filas afectadas por la última sentencia delete insert o update ejecutada por el correspondiente objeto pdostatement
compara cada imagen con las formas de disposición del formato gif de la imagen previa en la secuencia
separa una clave representación tipo cadena) en una representación tipo array
mueve el apuntador interno a la posición dada en el elemento directoryiterator
la descripción de la función va aquí
borra un nombre del sistema de ficheros
esta función devuelve la porción de haystack la cual comienza en la última aparición de needle y que va hasta el final de haystack
establece la bandera de mezcla alfa para usar los efectos de capa de la biblioteca gd incluida
añade ficheros de un directorio que coinciden con la expresión regular pattern
returns min parameter
devuelve la traza de la pila
permite implementar sus propios gestores de protocolo y flujos para usarlos con todas las demás funciones de sisitema de archivos tales como fopen() fread() etc.)
convierte un archivo jpeg en un archivo wbmp
exporta una reflexión
configura una o más opciones de la conexión dada
este método intenta actualizar una entrada con un nuevo valor
método rápido para crear nuevos sockets desde un contexto
vincula temporalmente la clausura a newthis y la llama con cualquier parámetro dado
ordenar las entradas por clave manteniendo la correlación de claves para la entrada
devuelve todos los campos que solr debería generar para remarcación de trozos
el máximo de épocas de cadidatas determina el número máximo de épocas que podrían ser entrenadas las conexiones de entrada a las candidatas antes de añadir una nueva neurona candidata
es una función específica de apache que es similar a < !--#include virtual...-- > en mod_include
esteblece el ancho de línea para todas las operaciones de dibujo siguientes
la descripción de la función va aquí
avanza el proceso de ejecución actual a otra acción
elimina una o más opciones
desenfoca una imagen con un umbral de contraste
crea una red neuronal de retropropagación estándar que no está conectada completamente
la función sscanf() es la entrada análoga de printf()
establece la temperatura de sarprop
la función cubrid_current_oid() se usa para obtener el oid de la posición del cursor actual desde el resultado de la consulta
ejecuta el lote de operaciones de escritura
obtiene el número de filas del result set dado
recogida de las fuerzas de los ciclos de basura existentes
establece el buffer para escribir operaciones en el stream dado a buffer bytes
añade un elemento hijo al nodo y retorna un simplexmlelement del hijo
imprime o guarda una versión xbm de la imagen image
actualiza la copia de trabajo en path a la revisión revno
devolverá una lista de encabezados a ser enviados al navegador / cliente
creará un nuevo índice secundario para relation que consiste en los dominios especificados por domlist y es del tipo type
la función ip2long() genera una dirección de red internet ipv4 desde su representación en formato estándar de internet cadena con puntos 
desplaza el apuntador de filas interno del resultado sybase asociado al identificador result_identifier hasta la línea row_number
determina si el elemento actual directoryiterator es un enlace simbólico
establece la ruta de petición del token
devuelve el comentario de una entrada usando la entrada índice
recupera variables de configuración interna de la extensión iconv
recupera el documento resultante de errores completo
inicializar los pesos empleando el algoritmo de widrow + nguyen
establece la escena de la imagen
la función cubrid_lob2_size64() se usa para obtener el tamaño de un objeto lob
devuelve el siguiente lote de registros
obtiene un número que garantiza ser único para relation y se devolverá en la variable dada como uniqueid
el método mongoclient::close() cierra forzosamente una conexión a la base de datos incluso si se están usando conexiones persistentes
esta función es una abreviación de hwapi_insert()
obtiene información sobre la última transferencia
esta función mueve el cursor del conjunto de resultados al índece del campo especificado
devuelve una descripción del último error
cancela la inscripción del buzón especificado por mailbox
la función cubrid_seq_put() se usa para actualizar el contenido del elemento solicitado en un atributo de tipo secuencia usando oid
escapa la cadena dada para que pueda ser usada de forma segura en una declaración sql
comprueba si el método es un constructor
devuelve el arco coseno hiperbólico de arg es decir el valor cuyo coseno hiperbólico es arg
limpia los parámetros de escala
establece dónde registrar los errores
un cursor normalmente obtiene un lote de objetos de resultados y los almacena localmente
compara uno o más canales de imagen de una imagen con una imagen reconstruida y devuelve las métricas de la distorsión especificada este método está disponible si imagick ha sido compilado con la versión 6.4.4 o superior de imagemagick
intenta cambiar el propietario del enlace simbólico filename al usuario user
crea una nueva relación
envía la consulta sql query a mysql sin recuperar ni almacenar automáticamente en búfer las filas de resultados como mysql_query() lo hace
cierra las conexiones abiertas a los servidores de memcache
ajusta la cantidad de tiempo que se muestra un marco de una imagen animada
envía un comando de cierre a mysql
crea una copia del nodo
cierra la conexión no-persistente al servidor msql que está asociada con el especificado identificador de enlace
permite la compartición de datos entre gestores curl
obtener los atributos externos de una entrada definida por su nombre
devuelve el nombre del código de error icu
determina si el carácter especificado debería ser considerado como carácter ignorable en un identificador
crea un archivo jpeg desde image
crea un contexto de prefijo/ns para la siguiente petición xpath
obtiene varios valores que se establecieron con la función ps_set_value()
obtiene todos los errores de imap si los hay que han ocurrido durante esta petición de página o desde que se reinició la pila de errores
comprueba si el objeto pasado está referenciado en el mapa
obtiene el tipo del decodificador
devuelve el valor integer de una var con la especificada base para la conversión por defecto es base 10 
establece el índice del mapa de color de la varita de píxeles
obtiene el número de filas afectadas por la última consulta
esta función devuelve una nueva instancia de la clase domnodelist con todos los elementos descendientes con un nombre de etiqueta dado por name en el orden en que fueron encontrados en un recorrido preorden de este elemento árbol
crea y devuelve un contexto de flujo con cualquier opción proporcionada en options
convierte un string en un array
devuelve todas las consultas de facetas
comprueba si un punto de código tiene la propiedad de unicode uppercase
toma el trazado actual y lo usa para definir el borde de un área de recorte
devuelve un string que describe el código resultante del último método de memcached ejecutado
esta función comprueba si la propiedad dada por property existe en la clase especificada
la función pg_copy_from() inserta registros dentro de una tabla desde el parámetro rows
hace borrosa una imagen
devuelve el nombre de la sesión actual
define el modo de compresión para el documento
devuelve el nombre del campo dado por field
mueve el iterador al siguiente nodo
obtiene el ansho del cairoimagesurface
añade un object dentro del almacenamiento y opcionalmente se asocian a algunos datos
devuelve el color de fondo de la imagen
por defecto queda en espera si es necesario hasta que el semáforo pueda ser adquirido
devuelve el punto primario verde de la cromaticidad
consigna la transacción pendiente de la conexión de oracle dada por connection
es un método conveniente que escala una imagen proporcionalmente al doble de su tamaño original
envía una respuesta
la descripción de la función va aquí
esta función puede ser usada manualmente para buscar una clase o interface usando las funciones registradas __autoload
añade rutas definidas mediante configuraciones en la pila de rutas de yaf_router
permite que se cambien los valores de los campos msg_perm.uid msg_perm.gid msg_perm.mode y msg_qbytes fields de la estructura de datos de la cola de mensajes subyacente
define la planicie para la página
establece el límite de inglete.devuelve true en caso de éxito o false en caso de error
obtiene el ancho de la imagen
este método permite registrar su propia clase dom extendida para usarla después por la extensión dom de php
devuelve las claves numéricas y de tipo string del array
parchea un file con un patch y almacena el resultado en un archivo
está función garantiza la devolución de un índice de color para un color requerido el color exacto o la alternativa más próxima posible
crea un enlace simbólico al objetivo target existente con el nombre especificado en link
destruye un objeto gmagick y libera todos los recursos asociados con él
comprueba si la variable dada es null
calcula g s y t tal que a*s + b*t = g = gcd a b donde gcd es el máximo común divisor
formatea la sigiente parte de un textflow en un área rectangular
reúne las estadísticas del archivo o enlace simbólico nombrado por filename
constructor privado para no permitir la creación de instancias con el operador new
elimina un parámetro si ya existía
realiza una conversión han-kaku - zen-kaku en el string str
el flujo de 2-piernas o la suscripción de petición
devuelve todas las entradas del mapa
muestra un rastreo php
mueve los mensajes de correo especificados por msglist al buzón especificado por mailbox
rodea la imagen con un borde del color definido por el objeto imagickpixel de color de borde
este método convierte una cadena de respuesta xml del servidor apache solr a un objeto solrobject
comprueba que el hash proporcionado coincida con la contraseña facilitada
devuelve un parámetro dado con el nombre param_name
cierra un grupo de recursos curl
el tamaño de paso máximo es un número positivo que determina lo grande que podría ser el tamaño de paso máximo
decodifica cualquier cifrado tipo % ## en la cadena dada
devuelve las dependencias listando tanto aquéllas que sean obligatorias como las conflictivas
determina si el punto de código especificado es un código de control de iso
devuelve un objeto que proporciona acceso a las propiedades de audio de el fichero mpeg
devuelve una cadena que representa la versión del servidor mysql al que está conectada la extensión mysqli
devuelve los tipos de imagen soportados por la instalación actual de php
devuelve la versión de la api imagemagick como un string y como un número
valida un documento basado en el fichero de esquema dado
obtiene el texto completo del últime mensaje de error de imap que ocurrión de la página actual
devuelve el tamaño del campo dado por field
es similar a fbsql_fetch_array() con una diferencia se devuelve un objeto en lugar de un array
establece el método de interpolación
libera los recursos asociados con el objeto colección
note que esta comparación es sensible a mayúsculas y minúsculas y a diferencia de strcmp() esta función no es segura con material binario
imprime el string de error
recupera el formato de imagen como uno de los definidos en cairoformat
establece el nombre de fichero de una imagen en particular en una secuencia
descomprime la cadena data dada que contiene los datos codificados en lzf
retrasa la ejecución del programa por el nùmero de segundos y nanosegundos dados respectivamente por seconds y nanoseconds
desencripta data y devuelve los datos sin cifrar
la función cubrid_get_autocommit() se usa para obtener el estado del modo auto-commit de la conexión de dase de batos cubird
lee una imagen desde un nombre de archivo
comprueba si una función está desactivada mediante la directiva disable_functions
ésta es una implementación de la interfaz arrayaccess que permite la manipulación directa del contenido de un archivo tar/zip utilizando los corchetes de acceso al array
añade un parámetro al objeto
esta función restaura la configuración de tidy a sus valores por omisión
devuelve la longitud de un campo
ésta función decodifica los data asumidos a ser codificados por utf-8 a iso-8859-1
se hace corresponder el carácter dado con su equivalente en mayúsculas
abre una conexión al demonio del servicio fam
cuenta el número de palabras dentro de string
esta función crea una nueva instancia de la clase domattr
define la apariencia de la página para ajustarse al rectángulo especificado por los parámetros
agrega un caracter de escape barra invertida antes de cada caracter especial de la cadena enviada como parámetro
devuelve la precisión de un campo
ajusta un string hasta un número dado de caracteres utilizando un caracter de salto de string
activa el bloqueo de eventbuffer de tal forma que puede utilizarse de forma segura por múltiples líneas de ejecución a la vez
devuelve la versión del cliente como un entero
cuenta el número de elementos en el array judy
devuelve un tipo de dato entero correspondiente al tipo de dato de judy de su array específico
sincroniza el estado de un fichero en memoria con un dispositivo de almacenamiento
devuelve el arco tangente hiperbólico de arg es decir el valor cuya tangente hiperbólica es arg
convierte una fecha juliana al calendario judío
establece una cuota de límite superior sobre una base por buzón
obtiene el destino de un enlace del sistema de ficheros
multiplica left por right
convierte un archivo png a un archivo wbmp
devuelve un array con las estadísticas recopiladas por el complemento de replicación y equilibrado de carga
la distancia levenshtein se define como el número mínimo de caracteres que se tienen que sustituir insertar o borrar para transformar str1 en str2
finaliza la transaccion actual escribiendo todos los inserts updates y deletes en el disco y desbloqueando todos los bloqueos de fila y tabla mantenidos por la transacción
añade un marcapáginas a la página actual
escalar datos de entrada y salida basados en parámetros previamente calculados
invalida inmediatamente todos los valores existentes
verifica si todos los caracteres en la string entregada text generan una salida visible
vuelca la información contenida en una sentencia preparada directamente en la salida
coloca un hipervínculo en la posición dada que apunta a una página web
este método calcula un identificador para los objetos añadidos a un objeto splobjectstorage
inicia un nuevo patrón
crea un nuevo archivo pdf usando el nombre de archivo proporcionado
devuelve la versión actual de solr
remueve un parámetro requerido
búsqueda global del índice anterior presente que es menor que el índice pasado
la descripción de la función va aquí
este método se puede usar para determinar si un phar guardará los cambios al disco inmediatamente o si se necesita una llamada phar::stopbuffering() para habilitar el guardado de los cambios
establece el modo predeterminado de blob en todas las consultas de selección
encuentra la posición numérica de la primera ocurrencia del needle aguja en el string haystack pajar 
devuelve un identificador de transacciones global que pertenece a una operación de escritura no más antigua que la última escritura realizada por el cliente
agrega un frame a la etiqueta id3v2
devuelve un número de error del servidor la última solicitud) identificado por id
reanua un servicio pausado nominado
la función geoip_setup_custom_directory() cambiará el directorio personalizado de la base de datos de geoip
hace brillar una luz distante sobre una imagen para crear un efecto tridimensional
obtiene el número de bits usados para describir cada componente de color de la imagen
para archivos phar basados en tar este método lanza una excepción de tipo badmethodcallexception ya que la compresión de ficheros individuales dentro de un archivo tar no está soportada por el formato de fichero
devuelve el índice de la imagen activa actual dentro del objeto imagick
devuelve el objeot que sirve como clave del mapa en la posición actual de la iteración
escala convenientemente una imagen proporcionalmente al doble de su tamaño original
elimina un elemento del registro
devuelve el estilo de fuente usado cuando se anota texto
la descripción de la función va aquí
esta función devuelve el nombre del índice del campo especificado en caso de éxito o false en caso de fallo
busca la posición offset de campo espcificada
elimina la entrada especificada de la base de datos
devuelve una desviación aleatoria desde una distribución beta con los parámetros a y b
devuelve el último mensaje de error global rrd
realiza una negación lógica de variant
devuelve la precisión de la columna especificada en el resultado
esta función ordena un array
devuelve la información de las estadísticas del sistema de ficheros en el argumento result de callback
lee nbytes del objeto slob
establece un propiedad nominada de la imagen
añade datos al final de un buffer de eventos
devuelve todos los mensajes de alerta de imap generados desde la última llamada a imap_alerts() o el inicio de la página
prueba si un bit es establecido
elimina un campo de la lista de campos
agrega el archivo directorio o enlace simbólico en path al directorio de trabajo
establece la clave con nombre de la entrada del registro de imagemagick al valor dado por value
establece la consulta de búsqueda
guarda el estado actual de los gráficos
asocia la siguiente imagen de la lista de ímágenes con un objeto imagick
establece el color de relleno a usar cuando se dibujan objetos rellenos
configura o cambia llamadas existentes para el evento buferizado event
comprueba si un array contiene más entradas
obtiene el grupo del archivo
crea un objeto domdocument del tipo especificado con su elemento documento
reposiciona el índice del fichero abierto asociado con el flujo el recurso socket o el descriptor de fichero especificado por fd al argumento offset según la directiva whence como sigue: eio_seek_set - establece la posición igual a offset bytes
devuelve el id efectivo numérico de usuario del proceso actual
recorre cada valor de array pasándolos a la función callback
devuelve un solrinputdocument equivalente al objeto
envía datos arbitrarios a todos los servidores de trabajo para ver si los repiten
devuelve el contenido de la entrada usando su índice
un error no se pueden clonar por lo que este método resulta en un error fatal
esta función implementa un algoritmo de ordenación que ordena las cadenas alfanuméricas en la manera en que lo haría un humano mientras mantiene las asociaciones de clave/valor
crea una nueva instancia de haruannotation
esta función permite cambiar las bases de datos dentro de una sesión por una o más bases de datos especificadas a ser usadas en la búsqueda recuperación etc
cuenta todos los elementos en un array o algo de un objeto
define opciones de comportamiento
devuelve las fuentes configuradas
esta función ejecuta el formulario proporcionado
oculta una marca de agua digital dentro de la imagen
esta función devuelve una nueva instancia de la clase domnodelist que contiene los elementos con el nombre de etiqueta local buscado
remuestrea la imagen a la resolución deseada
consigna la transacción en uso
obtener el número de neuronas de cada capa de la red neuronal
comprueba si un campo puede contener valores null
determina si el elemento actual tiene un hijo
finaliza una plantilla que fue creada con la función ps_begin_template()
finaliza una definición de plantilla
la funcionalidad de memcached::prependbykey() es equivalente a la de memcached::prepend() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
codifica el string str dado mediante el esquema de codificación de cabecera mime
esta función hará una copia física incluyendo el contenido si existe y devolverá el nuevo objeto o un objeto de error
envía los datos que han resultado y la actualización a estado completado para este trabajo
establece el color descrito por el objeto imagickpixel usando valores normalizados para el tono saturación y luminosidad
cierra un descriptor de lob o file
contar los elementos de un iterador
elimina una o más opciones del trabajador
libera un conjunto de resultados
define los encabezados a enviar en las peticiones soap
este método es llamado para establecer las opciones del flujo
carga el vector de codificación de caracteres desde un fichero y cambia el vector de codificación de tipos de letra a éste
establece el texto guía línea de espacio) para la página
esta función devuelve los detalles de la clave bits key type 
asigna recursos para contener un descriptor o un localizador lob
define el de estilo de transición de la página
retrasa la ejecución del programa durante el número de microsegundos dados
la función error_reporting() establece la directiva error_reporting en tiempo de ejecución
determina si el punto de código especificado es un carácter «en blanco» o de «espacio horizontal» que separa visiblemente palabras en una línea
devuelve el nombre del limitador de caché actual
obtiene las unidades de resolución de la imagen
devuelve el número de alertas tidy encontradas en un documento específicado
planifica la ejecución de event cuando el evento especificado en event_set() ocurra en como mínimo el tiempo especificado por el parámetro timeout
este método crea un nuevo hash desde una definición de una cadena de caracteres
crea un array que contiene variables y sus valores
obtiene el valor de una variable de entorno
establece el factor de aumento empleado durante un entrenamiento rprop
devuelve el nombre con la ruta de el archivo entrada
este método crea un nuevo conjunto desde un fichero de definición del disco
la descripción de la función va aquí
enumera los campos de una tabla de mysql
establecerlo a true habilita los trozos remarcados para ser generados en la respuesta de consulta
devuelve el equivalente a exp arg - 1 calculado de tal forma que no pierde precisión incluso cuando el valor de arg se aproxima a cero un caso donde exp arg - 1 puede ser inexacto debido a la resta de dos numeros que son casi iguales
obtiene si la variable dada es de tipo string
esta función es un alias de: finfo_file()
define si éste nodo es abierto o no cuando el esquema esta mostrándose por primera vez
esta función devolverá la lista de dsn disponibles después de llamarla varias veces)
añade el nuevo nodo de atributo dado por attr al elemento
si colapsar o no fragmentos contiguos en un único fragmento
crea una imagen espejo vertical reflejando los píxeles alrededor del eje x central
establece la longitud de palabra mínima bajo la cual las palabras serán ignoradas
ensamblar un url
este método de fabricación cres un nuevo hash desde un fichero de definición de una cadena de caracteres
crea una instancia de imagick para una imagen o conjunto de imágenes especificos
abre una tubería hacia un proceso ejecutado bifurcando el comando dado por command
devuelve la longitud máxima de palabra bajo la cual las palabras serán ignoradas
establece el contenido del xml a analizar
asocia un recurso con nombre especificado por filename a un flujo
toma el contenido del archivo nombrado por infilename y lo firma usando el certificado y su clave privada coincidente especificados por los parámetros signcert y privkey
devuelve la función de error empleada durante un entrenamiento
comprueba si existe un elemento
dada una cadena que contiene un directorio esta función devolverá el número total de bytes del sistema de archivos o partición de disco correspondiente
establece una opción del cliente
la descripción de la función va aquí
obtener las cabeceras mime de una sección en particular del cuerpo de los mensajes especificados
este método comprueba si una entrada con la clave proporcionada existe en el hash
remover todos los objetos excepto los contenidos en otro almacenamiento del almacenamiento actual
devuelve un array multi-dimensional con información detallada sobre todos los tipos de bases de datos geoip
elimina la bandera de borrado de un mensaje especificado que está establecida mediante imap_delete() o imap_mail_move()
crea un comando sql preparado que puede incluir 0 o más marcadores de parámetro con el carácter ? los cuales representan parámetros de entrada salida o entrada/salida
postgresql soporta la conversión automática de caracteres entre servidor y el cliente para conjuntos de caracteres determinados
retira espacios en blanco u otros caracteres del inicio de un string
esta función se usa para recuperar el valor de retorno que sigue a la ejecución de un procedimiento de una base de datos de ingres procedimiento al macenado 
devuelve una referencia del objeto imagick actual con el puntero de imagen en la secuencia correcta
no opera nada a hacer
este método elimina una entrada del hash y devuelve si la entrada fue eliminada o no.las estructuras de memoria asociadas no serán liberadas inmediatamente sino cuando el propio hash sea liberado
la función cubrid_lock_read() se usa para poner el bloqueo de lectura en la instancia apuntada por el oid dado
retorna un array que contiene todos los valores de array1 que están presentes en todos los arguementos
devuelve el identificador del grupo de procesos del proceso pid o false en caso de error
devuelve la entrada actual de almacenamiento
sets user fields parameter uf 
deserializa el almacenamiento desde spldoublylinkedlist::serialize()
establece el identificador del cliente usado por varios componentes de la base de datos para identificar usuarios de aplicaciones ligeras que se identificaron con el mismo usuario de base de datos
intenta clonar la excepción lo que resultará en un error fatal
obre un flujo para acceder a los datos a los que hace referencia oid
aplica un vector de color a cada píxel en la imagen
realiza un copia del fichero source a dest
crea un paquete wddx con una estructura que contiene la representación serializada de las variables pasadas
es notoriamente lenta
devuelve una cadena con las barras invertidas eliminadas reconoce las marcas tipo c \n \r ..., y la representación octal y hexadecimal
asocia la siguiente imagen de la lista de ímágenes con un objeto gmagick
asocia la base event_base con el evento event
esta función cierra una conexión cliente db2 creada con db2_connect() y devuelve el recurso correspondiente al servidor de base de datos
abre o reutiliza una conexión a un servidor mysql
la función dio_close() cierra el descriptor de fichero fd
devuelve una versión de number con formato
destruye una sesión
esta función permite usar parser dentro de object
devuelve el número de filas en el resultado result
comprueba si la función es interna en contraposición a las funciones definidas por el usuario
es similar a sybase_fetch_assoc() con la diferencia de que devuelve un objeto en vez de un array
establece la pendiente de activación para todas las neuronas de la capa número layer contando la capa de entrada como capa 0
esta función se puede usar para conocer qué niveles de registro están habilitados actualmente
mejora los bordes de la imagen con un filtro de convolución del radio dado
establece el número de filas que las bibliotecas de oracle client almacenará en búfer después de una llamada exitosa a oci_execute() y por cada petición de obtención interna subsigiente a la base de datos
obtiene el contenido de la caché de la ruta real
cierra el conjunto de resultados
recupera información sobre el uso de memoria caché de la sesión
marca el registro dado para ser borrado de la base de datos
construye un objeto de tipo reflectionfunction
devuelve la escala de la columna especificada en el resultado
envía un mensaje mediante el socket
activa la paginación ldap enviando el control de paginación tamaño de página cookie...)
crea un mosaico de 9 miniaturas de la imagen especificada con la operación de proceso de imágenes aplicada en varias intensidades
divide un array en arrays con elementos del tamaño definido en size
rellena un archivo tar o zip desde un iterador
hace que las nuevas notificaciones sean puestas en cola hasta que gupnp_service_thaw_notify() sea llamado
esta función mezcla un array crea un orden aleatorio de sus elementos 
devuelve la primera clave de la base de datos y reestablece el apuntador de clave interno
computa el símbolo jacobi de a y p
revierte una unidad de trabajo pendiente de ser consolidada
averigua si una variable global está registrada en una sesión
lista todos los archivos de bases de datos abiertos
obtiene el tamaño descomprimido del archivo entrada
especifica el número máximo de caracteres a analizar de un campo cuando se usa el fragmentador de expresiones regulares
devuelve true si el archivo está marcado como el archivo original
establece los atributos terminales y la tasa de baudios del fichero fd
esta función es idéntica que llamar a fopen() fwrite() y fclose() sucesivamente para escribir información en un fichero
devuelve las flags de expresión regular véase regexiterator::__construct() para una lista de todas las flags
devuelve el número de patrones de entrenamiento del resource de datos de entrenamiento
establece la función de activación para la neurona número neuron en la capa número layer contando al capa de entrada como capa 0
esta función se prepara para la recuperación de archivos después de una búsqueda exitosa
añade información xml sin formato a un domdocumentfragment
sitúa el iterador al comienzo del mapa
añade una anotación de un enlace web a la url objetivo en la web
devuelve el arco seno hiperbólico de arg es decir el valor cuyo seno hiperbólico es arg
crea un nuevo hash de contraseña usando un algoritmo de hash fuerte de único sentido
traer una variable almacenada desde la caché
ajusta los niveles de una imagen escalando la caída de los colores entre los puntos blanco y negro especificados al rango completo de cuantía disponible
executes query on a server
devuelve true si la distancia entre dos colores es menor que la distancia especificada
ajusta controles específicos de ssl para las solicitudes
devuelve el prototipo del método
calcular los parámetros de escala de entrada para un uso futuro basados en datos de entrenamiento
establece una o más opciones para el valor dado
este método puede ser usado para determinar si el fichero es un directorio
obtiene la ubicación relativa del dispositivo raíz
carga una biblioteca de tipos y registra sus constantes en la máquina como si fueran definidas usando define()
limpia todos los recursos asociados al objeto gmagick
esta función permite cambiar el estado de persistencia de un objeto soapserver entre peticiones
reviste repetidamente la textura de la imagen a través y hacia abajo del lienzo de la imagen
el carácter dado se hace corresponder con un equivalente «plegado» case folding ; si el carácter no tiene dicho equivalente se devuelve el carácter mismo
inserta un value en el montón
obtiene el color de relleno actual
borra una tabla y todas las estructuras de información asociadas
rrd puede aceptar la entrada desde varias fuentes de datos ds por ejemplo tráfico entrante y saliente
rebobina al primer elemento del primer iterador interno
es una función de ayuda para dbx_sort() para facilitar la construcción y el uso de funciones de ordenación personalizadas
realiza una exclución lógica
la descripción de la función va aquí
calcula los valores propios de una matriz cuadrada y opcionalmente calcula los vectores propios derechos e izquierdos
establece el índice de aprendizaje
rebobina el puntero interno de un array al primer elemento y devuelve el valor del primer elemento del array
busca la dirección del secundario que está siendo usado para lecturas
recupera la extensión de un fichero
desplaza el apuntador interno de fila dentro del resultado de ms sql result_identifier) hasta apuntar al número de fila row_number siendo la primera fila la número 0
obtiene la media y la desviación estándar de uno o más canales de imagen
crea un socket de flujo o de datagrama en local_socket
desactiva las redirecciones automáticas permitiendo que la petición sea manualmente redirigida
esta función recupera la información de los atributos de un objeto de cualquier versión
obtiene la parte entera de una variante
devuelve los encabezados soap de la última respuesta
cierra el identificador de enlace especificado y libera el resource
genera una clave de acuerdo al algoritmo hash dado utilizando el password provisto por el usuario
esta función desecha el contenido del búfer de salida en cola y lo desactiva
obtiene el dueño de el fichero
clasificar el resultado de una búsqueda ldap devuelta por ldap_search()
se usa para recuperar un campo cuando el objeto es tratado como una matriz
obtiene información del dispositivo raíz
esta función es conveniente cuando se codifica una cadena a ser usada como la parte de consulta de una url como método práctico para pasar variables a la siguiente página
obtiene las clases declaradas
este método estático solamente se puede usar dentro de una rutina de interoperabilidad cargadora de un archivo phar para inicializar el phar cuando sea directamente ejecutado o cuando se incluya en otro scrip
la descripción de la función va aquí
obtiene el tamaño del fichero de el elemento actual directoryiterator
modifica el nivel actual de profiling de la base de datos
establece la versión oauth para las subsecuentes peticiones
devuelve el número de versión menor de la versión de pdflib
devuelve true si el elemento actual tiene hijos o false en caso contrario
habilita o deshabilita morelikethis
llamado cuando recursivamente se llega al final de un nivel
establece la longitud de las porciones negras y blancas de una línea discontinua
con este método se crea un directorio vacío con la ruta dada por dirname
prepara una búsqueda en la conexión dada
obtener la cuota de ajustes por usuario y las estadísticas de uso por buzón
devuelve el nombre de una entrada de directorio específica
computa el inverso del modulo de a b
devuelve el número de advertencias de la última consulta para el enlace dado
usado para indicar que además de las restricciones basdas en términos de un campo de faceta debería ser computada una cuenta de todos los resultados coincidentes que no tienen valor para el campo
obtiene la última modificación para el elemento actual directoryiterator en formato unix timestamp
devuelve el nombre del campo que corresponde a field_number
crea una vista 3d
incluir el término de límite inferior en el conjunto de resultados
obtiene el número del espacio de columna que se corresponde con el campo nominado del identificador de resultado dado
usado para controlar cómo deberían ordenarse los resultados
comprueba si la función devuelve o no una referencia
establece parámetros en el contexto especificado
establece el modo de análisis
traslada el origen del sistema de coordenadas
esta función ordena un array manteniendo la correlación de los índices del array con los elementos con los que están asociados
devuelve el bitrate del archivo mpeg
dibuja una curva bézier cuadrática usando coordenadas absolutas desde el punto actual a x y 
computa la diferencia entre arrays con una verificación de índices adicional compara la información y los índices mediante una función de llamada de retorno
hace corresponder el carácter especificado con un carácter de «imagen reflejada»
calcula la suma de vectores de real0 a real1 y devuelve el vector resultante
establecer el nombre de clase iterador que es usado por arrayobject::getiterator()
la descripción de la función va aquí
devuelve el número de secuencia de mensaje para el uid dado
crea una nueva instancia de documento
destruye la cola de mensajes especificada por queue
marca los mensajes listados en msg_number para su eliminación
dice si el algoritmo del modo abierto es un algoritmo por bloques
recupera un array de errores
la función image_type_to_mime_type() determinará el tipo mime de una constante imagetype
destruye la red por completo y libera adecuadamente toda la memoria asociada
devuelve la representación de un objeto en formato string
ejecuta una función personalizada especificada por execute procesándola igual que cualquier otra llamada eio_*
lee el error cuadrático medio de la red
retorna el último código de retorno dado por el servidor de trabajos
establece la posición del iterador en la lista de imágenes especificada con el parámetro index
se puede invocar a esta función para añadir un recipiente al principio de una cadena de recipientes
obtiene el siguiente resultado de la última petición
devuelve la fecha de distribución de graphicsmagick como una cadena
obtiene el nombre base del elemento actual directoryiterator
devuelve un valor sqlstate que representa el motivo por el que el último intento de conexión a la base de datos falló
construye un objet de la clase reflectionextension
le indica al evento base que pare el envío de los eventos opcionamente después de un número de segundos dado
establece el tipo de imagen
recupera una lista de claves extranjeras de la tabla especificada o una lista de claves extranjeras de otras tablas que hacen referencia a la clave primaria de la tabla especificada
activa compresión automática de valores largos
sets socket options
reemplaza la secuencia de imágenes actual por la imagen del objeto sustituto
construye un nuevo objecto soapvar
añade una corrección a mis usando cor
descodifica una cadena codificada mediante uuencode
lee una imagen desde un gestor de fichero abierto
indica si el contexto es persistente
devuelve el nombre de archivo de una imagen en particular de una secuencia
devuelve un array con los alias de un tipo de codificación conocido dado por encoding type
devuelve el número de campos que contiene un resultado
los campos se cambian de lugar según el criterio y la dirección de ordenación especificados los campos pueden ser ordenados por valor boost nombre de campo y número de valores
sets tie breaker parameter tie parameter 
sella encripta la información dada por data mediante el método dado por method con una clave secreta generada aleatoriamente
extrase el pid del id de mongo
importa un array php en una nueva instancia splfixedarray
verifica si todos los caracteres en la string entregada texto son alfabéticos
este método cuenta el número de hijos de un elemento
devuelve la traza de la pila como un array
configura el número de niveles de prioridad de los eventos
devuelve una representación de un objeto datetime de esta fecha
establece el nombre del archivo desde donde los blobs se van a leer o escribir
mover a la siguiente línea en el fichero
revierte la transacción actual que fue iniciada por pdo::begintransaction()
la función geoip_db_avail() devuelve si la base de datos geoip está disponible y puede ser abierta
reinicia el objeto actual
la descripción de la función va aquí
esta función devuelve una cadena con las banderas del índice del campo dado separadas mediante espacios
recupera la variable env
registra una función php en lua con el nombre de función que se indique en $name
elimina todas las funciones de retorno de llamada establecidas anteriormente
devuelve el estado actual del autocommit
este método devuelve el valor de la opción de memcached dada por option
esta función devuelve un objeto con los nombres de las columnas del conjunto de resultados como propiedades
habilita/deshabilita la capacidad de cargar entidades externas
es una función apropiada para establecer cabeceras http para salidas de fdf
crea un clob a partir de los datos recibidos
este método devuelve el iterador interno actual
tunes database connection options
cierra la conexión dada por un parámetro id
establece la escala de la salida swf
esta función es útil para recuperar muchos valores sin llamar repetidamente a filter_var()
elimina una variable almacenada de la caché
devuelve un mensaje de error que describe el código de error curlm dado
es utilizado para borrar un segmento de memoria compartida
establece un atributo en el manejador de la base de datos
genera una representación yaml de los datos en data proporcionados
devuelve una cadena que contiene el número dado por number representado en base tobase
anexa un arco a la ruta corriente
esta función establece el dominio en el que se realizarán las búsquedas llamadas por gettext() normalemente por el nombre de la aplicación
obtiene el texto actual de modo de interpretación
este método se usa para crear colecciones capped de tamaño fijo) y otras colecciones que requieren opciones especiales
este método mueve el simplexmliterator a el siguiente elemento
obtiene la actual fuente usada
enruta una petición entrante
libera la memoria ubicada internamente para almacenar los resultados
decodifica datos binarios pasados como parámetros a una fdu
importa la llave keydata y retorna un array con la información sobre el proceso de importación
usa el trazado actual como un trazado de recorte y pone fin al trazado
devuelve el color de relleno actual
devuelve la información de la imagen de esta superficie o null si la superficie no es una superficie de imagen o si cairocontext::finish() procedural : cairo_surface_finish() ha sido llamado
se usa para obtener meta-información blob/clob desde la base de datos de cubrid cubrid obtiene blob/clob ejecutando la sentencia sql y devuelve todos los lobs como un array de recursos
establece el número de documentos similares a devolver en cada resultado
obtiene el valor del espacio de color global
devuelve el valor del parámetro facet.date.other
especifica la expresión regular para la fragmentación
recupera el objeto respuesta actual
reinicia el error cuadrático medio de la red
devuelve el formateador de la salida remarcada
define una constante con nombre en tiempo de ejecución
comprueba si la característica solicitada dada por feature está soportada para la versión especificada dada por version
crea un enlace duro
elimina un campo del documento
convierte una fecha y hora tal como fué almenacenada en el archivo paradox en un formato legible por humanos
cambia la contraseña del usuario dado por username
método de efecto especial que desplaza aleatoriamente cada píxel en un bloque definido por el parámetro radius radio 
borra un archivo virtual con nombre y libera su información de estructura pero no el contenido 
crea un nuevo evento búfer ssl búfer para enviar sus datos a otro evento búfer
desenlace de directorio ldap
establece el tamaño en caracteres de los fragmentos a considerara para remarcación
construye un nuevo objeto de tipo reflectionmethod
llena un array con num entradas del valor del parámetro value las keys inician en el parámetro start_index
obtiene el valor de una ruta pcos con el tipo name string o boolean
normalmente cuanquier cosa que realice una salida como echo o print será sacada como si fuera impresa desde el ámbito padre
obtiene el nombre de la función api que lanzó la última excepción o falló
envía un correo electrónico
devuelve el número de atributos
finalizar la ruta actual del objeto sin realizar operaciones de llenado y pintado
devuelve el estado del colector de referencia circular
recupera la instancia de la clase yaf_application
envía una consulta a mysql
verifica si val es un número finito legal en esta plataforma
establece la calidad de compresión de una imagen
establece la fuente especificada completamente a usar cuando se anota texto
especifica una consulta de filtro
decodifica una cadena binaria codificada hexadecimalmente
devuelve un string con barras invertidas antes de los caracteres que aparecen en el parámetro charlist
devuelve entrada última fecha modificación
convierte una cadena del formato utf-16 al formato utf-8
elimina uno de los parámetros facet.query
esta función devuelve una copia del nodo a importar y le asocia con el documento actual
devuelve la propiedad fuente de los objetos
actualiza el archivo rrd definido a través de rrdupdater::__construct()
devuelve el numero de filas afectadas por la última consulta insert update replace or delete
el primer prototipo es cuando se enlazó con libmcrypt 2.2.x el segundo cuando se enlazó con libmcrypt 2.4.x o superiores
el número de pendientes de activación del array fann_get_cascade_activation_functions()
añade un nombre de filtro dado por filtername a la lista de filtros adjuntos a stream
obtiene la profundidad de la imagen
almacena un archivo en la caché de código de byte evitando todos los filtros
la descripción de la función va aquí
mueve el puntero interno al siguiente campo
construye un lote de operaciones insert
elimina un campo del documento cuando al campo se accede como una propiedad del objeto
elimina todos los documentos que coincidan con la consulta dada
establece el peso de la conexión
devuelve el nombre local o remoto de una conexión de socket dada
devuelve el identificador de sesión del proceso pid
ordena las entradas por clave usando una función definida por el usuario
aplica un filtro digital que mejora la calidad de una imagen con ruido
construir un nuevo iterador filesystem a partir del path
quita el primer valor del array y lo devuelve acortando el array un elemento y corriendo el array hacia abajo
esta función devuelve el array de extensiones javascript registradas mediante v8js::registerextension()
cancela una petición especificada por req
devuelve el número máximo de caracteres del campo a devolver
establece algún parámetro pdflib con el tipo string
inicia perfiles xhprof
devuelve información sobre el cliente y la conexión especificada
devuelve el nombre de la organización a la que una ip está asignada
la función cubrid_lob2_seek64() se usa para mover la posición del cursor de un objeto lob mediante el valor establecido en el argumento offset a la dirección establecida en el argumento origin
borra el valor del índice específicado
establece el número máximo de trozos remarcados para generar por campo
inicia la base de datos en un servidor local o remoto
establece una opción de socket de zmq
para archivos phar basados en tar este método descomprime el archivo entero
señala un process creado usando proc_open() que debe terminar
determina si pspell_save_wordlist() guardará los pares de sustitución junto con la lista de palabras
devuelve el peso de la fuente usada cuando se anota texto
extrae/decodifica una sección de mensaje del archivo indicado
arremolina los píxeles desde el centro de la imagen donde los grados indican el alcance del arco a través del cuál cada píxel es movido
con este método se pueden ejecutar tuberías del marco de acumulación y recuperar los resultados mediante un cursor en lugar de obtener solamente un documento tal como se haría con mongocollection::aggregate()
este enganche será provocado después de que finalice el proceso de enrutamiento
destruye la base de eventos event_base y libera todos los recursos asociados
evalua y convierte los valores de un assoc_array en valores adecuados para su uso en una sentencia sql
recolecta basura de transacciones xa no finalizadas
recupera información sobre un proceso abierto mediante el uso de proc_open()
elimina un mensaje de una cola
establece opciones sobre el campo solicitado
la función cubrid_num_rows() se usa para obtener el número de filas del resultado de la consulta
añade una acción al campo de formulario solicitado que se ejecutará al enviarse
cargar la copia binaria dada en la caché de archivo/usuario de apc
escalar datos en un vector de entrada después de obtenerlo de una rna basada en parámetros previamente calculados
comprueba si el método es estático
retorna el nombre corto para un marco id3v2
construye un lote de operaciones update
este método almacena un conjunto existente en un fichero del disco en el mismo formato que puede leer loadfromfile()
devuelve un objeto solrobject que representa la respuesta xml del servidor
escala el tamaño de una imagen a las dimensiones dadas
esta función es útil para comprobar si la llamada soap ha fallado pero sin usar excepciones
crea y devuelve una nueva función anónima con el cuerpo y variables vinculadas como ésta pero posiblemente con un objeto vinculado diferente y un nuevo ámbito de clase
especifica la opacidad del los perfiles del objeto contorneado
establece el formato de una imagen en particular en una secuencia
instanciar la clase runkit_sandbox_parent desde dentro de un entorno de aislamiento de procesos creado desde la clase runkit_sandbox proporciona algunos medios controlados para que un aislamiento de procesos hijo acceda a su padre
exporta una clase reflejada
elimina los elementos designados por offset y length del array input y los reemplaza con los elementos del array replacement si se proporcionan
si un trozo no se puede generar debido a que no hay términos coincidentes se puede especificar un campo para usarlo como copia de seguridad del sumario predeterminado
establece la máscara de recorte de una imagen desde otro objeto imagick
crea una nueva instancia de haruoutline
leer un directorio al completo mediante las llamadas al sistema de opendir readdir y closedir y devuelve o los nombres o un array en el argumento result de la función callback dependiendo del argumento flags
codifica data en base64
abre un archivo zip nuevo para lectura escritura o modificación
obtiene una instancia de haruencoder para el codificador especificado
importar un número gmp de un string binario
pregunta a la parte remota del socket dado que puede resultar en un host/puerto o en una ruta de sistema de archivos unix dependiendo de su tipo
devuelve el contenido de un objeto grande
devuelve el valor del parámetro facet.date.end
obtiene la orientación de la imagen
devuelve un array de frames id3v2 asociados con la etiqueta id3v2
lee datos del buffer de entrada del evento buferizado
este método normalmente se usa para ejecutar yaf_application en un trabajo de crontab
proporciona una representación de cadena para este objeto rararchive
elimina el registro del nombre de una función en los servidores de trabajo asegurando que no se envían para esa función y ese trabajador) más trabajos
limpia las sesiones expiradas
deforma una imagen utilizando varios métodos de distorsión mapeando la paleta de colores de la imagen de origen a una nueva imagen destino normalmente del mismo tamaño que la imagen de origen a menos que bestfit esté establecido a true
envía un mensaje de error al registro de errores del servidor web o a un fichero
devuelve un arreglo indexado por la posición de las columnas que representa una fila en un bloque de resultados
establece el tamaño de punto de la fuente a usar cuando se anota texto
añade un círculo
llama a la llamada de retorno dada por el primer parámetro callback con los parámetros de param_arr
calcula la media de un conjunto de imágenes
establece el directorio base de plantillas
lee la información de un archivo exe de bcompiler y crea clases desde el código de bytes
define en qué página se debe mostrar cuando se abre el documento
serialización personalizada de objetos solrdocument
adquiere un flujo que representa una variable usada en comunicaciones internas con libeio
mejora el contraste de una imagen a color ajustando los colores de los píxeles para abarcar el rango completo de colores disponibles
devuelve el tamaño del array
sets ranking mode
obtiene un array de todos lo buzones que tiene suscritos
libera los recursos asociados con el descriptor previamente asignados con oci_new_descriptor()
devuelve una conexión persistente a una base de datos ibm db2 universal database ibm cloudscape o apache derby
devuelve el contenido del fichero de un repositorio indicado en repos_url de forma opcional se puede indicar opcionalmente el número de revisión en revision_no
elimina un objeto desde un padre especificado
asigna una prioridad al evento bevent
envía un comando com_* básico a mysql
esta función crea una nueva instancia de la clase domcdatasection
esta función crea una nueva instancia de la clase domdocumentfragment
habilitará o deshabilitará el volcado implícito
este método obtiene un objeto splfileinfo para el fichero de referencia
se encarga de tener una variable en particular vinculada a una columna dada en el conjunto de resultados desde una consulta
devuelve el tipo de datos sqlt sin tratar de oracle del campo dado por field
desactiva todas las capas activas
devuelve el índice del color en la paleta de la imagen que está más próximo al valor rgb especificado y al nivel alpha especificado
abre un documento pdf virtual o basado en disco y lo prepara para su uso posterior
guarda una variable en caché en el almacén de datos
el comando findandmodify modifica atómicamente y devuelve un único documento
devuelve el último mensaje reportado por el servidor
la función cubrid_send_glo() se usa para leer información de una instancia glo y enviarla a la salida estándar de php
cambia el propietario del fichero especificado por el descriptor de fichero fd
reanuda una página para añadir más contenido en ella
devuelve un string que representa la versión de la biblioteca cliente
establece los atributos externos de una entrada definida por su nombre
hace que los colores de la versión de la paleta de una imagen coincidan más estrechamente con la versión de color verdadero
establecer el tiempo de conexión en segundos para la conexión searchd
borra un textflow y las estructuras de información asociadas
lee el código de bytes de una clase existente de php y lo escribe en el gestor de archivo abierto
establece el color de relleno actual a un valor de gris entre 0 y 1 inclusive
obtener la línea de el fichero
intentará resolver relation_name y encontrar el id interno del servidor el nombre real del host y la ruta de la base de datos en este host
la función cubrid_col_size() se usa para obtener el número de elementos de un atributo de tipo de colección conjunto multiconjunto secuencia 
devuelve una cadena que contiene el código de error sqlstate del último error
devuelve un id que se puede usar para acceder a la cola de mensajes de system v con la clave dada por key
devuelve la frecuencia de documento máxima
crea una función anónima desde los parámetros pasados y devuelve un nombre único para ella
devuelve el color del píxel especificado
dada la matriz de argumentos que contiene valores númericos este método interpola los colores encontrados en esas coordenadas a través de la imagen completa usando sparse_method
la función cubrid_connect() se usa para establecer el entorno para la conexión al servidor usando la dirección del servidor número de puerto nombre de la base de datos nombre de usuario y contraseña
obtiene el nombre del espacio del color
establece el nombre del campo del que obtener los términos
comprueba si la implementación de dom implementa una característica específica dada por feature
recupera un archivo remoto desde el servidor ftp y lo guarda en un archivo local
consigna una transacción global entre servidores de mysql iniciada por mysqlnd_ms_xa_begin()
este método recupera un valor de un hash mediante su clave
obtiene información sobre el último error que ocurrió
este rebobina el iterador al comienzo
llama al callback del manejador del consumidor registrado la cual es establecida con oauthprovider::consumerhandler()
obtener el nombre del fichero del el elemento actual directoryiterator
devuelve el valor decimal equivalente al número octal representado por el argumento cadena_octal
devuelve la versión del protocolo de mysql usado
número límite de peticiones en un grupo de peticiones
una compararción de expresiones regulares para strings multibyte
obtiene la transformación actual de la matriz de la página
convierte una imagen basada en paleta creada por funciones como imagecreate() a una imagen de color verdadero como imagecreatetruecolor()
anota una imagen con texto
cierra el enlace a una base de datos informix que está asociado al identificador de enlace especificado
mapea a mlt.fl
invoca una función reflexionada
devuelve un identificador de imagen que representa la imagen obtenida desde el nombre de fichero dado
busca todas las funciones declaradas en el archivo dado y escribe sus códigos de bytes correspondientes a un gestor de archivo abierto
cierra todos los gestores de página abiertos y cierra el documento pdf de entrada
cierra parcialmente o no una conexión full-duplex
ordena a solr qué tipos de parámetros de solicitud deberían ser incluidos en la respuesta para propósitos de depuración valores legales incluidos:
de manera predeterminada cada documento tiene un objeto de páginas raíz para todas las páginas
añade una tarea de alta prioridad en segundo plano para ejecutar en paralelo junto con otras tareas
devuelve un array que se corresponde con la fila obtenida o false si no existen más filas
devuelve la escala de la columna con índice field
devuelve una matriz con la fecha date analizada o false si se produjo un error
toma una cadena codificada en bruto o mediante base64 y la desencripta usando un método y una clave
establece el espacio de color de una imagen
devuelve el contenido de una celda de un set de result frontbase
obtiene el valor actual
establece las flags
establece el máximo de épocas de candidatas
establece la fracción de cambio de candidatas en cascada
lee múltiples entradas de los resultados porporcionados luego lee los atributos y valores múltiples
esta función devuelve un objeto con propiedades de sólo lectura con información sobre el cliente de base de datos db2
restringe las coincidencias de términos que comienzan con el prefijo
devuelve el pid del servidor
usado para obtener conjuntos consecutivos de resultados de consultas destpués de la ejecución de un bloque almacenado o anónimo de pl/sql de oracle cuando dicho bloque devuelve resultados de consultas con la función de pl/sql dbms_sql.return_result de oracle
devuelve los datos o información asociada al objeto señalado por la posición actual de el iterador
devuelve la excepción anterior el tercer parámetro de exception::__construct()
comprueba si el elemento actual es válido
devuelve una cadena que especifica la fuente usada cuando se anota texto
devuelve los id de los objetos que poseen errores en el último sondeo
devuelve la posición del cursor de campos utilizado en la última llamada de mysqli_fetch_field()
establece el id de grupo efectivo del proceso actual
crea un nuevo objeto pdflib con la configuración predeterminada
retorna el tipo de codificación utilizada para hacer la compresión
esta función permite añadir un nuevo ítem al árbol checkbox
devuelve si está habilitado o no termscomponent
defragmenta el índice para un rendimiento de búsquda más rápido
extrae en forma de array asociativo una fila a partir de una consulta
compara cada imagen con la siguiente en una secuencia y devuelve la región circundante máxima de cualesquiera deferencias de píxeles que se descubra
obtiene todos los resultados restantes de la última petición
instala un proxy para las sentencias de mysqlnd
este método se usa para especificar un conjunto de campos a devolver restingiendo así la cantidad de información en la respuesta
interpreta una plantilla y devuelve el resultado
esta función crea una nueva instancia de la clase domcomment
devuelve la longitud del campo especificado
obtiene un recurso
obtiene el estilo de la línea de unión
esta función añade otro par nombre/valor al mecanismo de reescritura de urls
determina si el punto de código especificado es un carácter «graphic» imprimible excluyendo los espacios 
ejecutará la entrada a través de la red neuronal devolviendo un array de salidas el número de las que son iguales al número de neuronas de la capa de salida
obtiene un token de petición secreto y cualquier parámetro adicional de respuesta del proveedor de servicio
rebobina la secuencia temporal del documento
recibe todas las cabeceras de petición http en la solicitud actual
establece la posición para la impresión del texto en la página
comprueba si key es la clave privada que se corresponde con cert
establece el nonce para todas las solicitudes posteriores
establece una conexión a un servidor ms sql
ejecuta una sentencia preparada y devuelve un objeto de conjunto de resultados
elimina las sesiones expiradas
desecha el contenido del búfer de entrada del terminal sin esperar a entradas adicionales 
determina si es punto de código especificado es un carácter de espacio según java
calcula el logaritmo en base 10 para cada valor de real y devuelve el array resultante
instancia un objeto de la clase yaf_application
genera y devuelve un nuevo id de sesión
deshace cualquier cambio local de la ruta en la copia de trabajo
devolver el identificador de proceso del proceso actual
al igual que array() no es realmente una función es un constructor del lenguaje
compara una o más imágenes y devuelve la imagen de diferencia
devuelve el tamaño de el fichero en bytes para el fichero referenciado
devuelve el operador de composición asociado a la imagen
comprueba si la instancia de memcached se creó recientemente
obtiene una lista de todas las colecciones de la base de datos y las devuelve como un array de documentos los cuales contienen sus nombres y opcioines
obtiene la parte entera de una variante
la función dgettext() le permite sobrescribir el domain actual en una búsqueda en un mensaje único
convierte data a texto utf-7 modificado
ejecuta una sentencia previamente preparada o abre un puntero hacia una
esta es una interfaz a gettimeofday 2 
mover el iterador a la siguiente entrada en el array
se desplaza al siguiente elemento
establece el límite de candidatas
devuelve la cuenta de color asociada a este color
este método se llama durante la instanciación del objeto de la clase del filtro
esta función permite establecer el valor actual del recurso checkbox
establece el número de veces que una imagen animada se repite
recupera la variable character_set de mysql
rebobinar el iterador
obtiene el nombre del codificador
encuentra la posición numérica de la primera aparición de needle aguja en el string haystack pajar 
libera cualquier memoria asociada con la imagen image
se usa para iterar a través de una columna específica dentro de un resultado y extraer un segmento de datos lob de tamaño definido por el usuario
establece el grosor de las líneas cuando se dibujan rectágulos polígonos elipses etc..., a thickness píxeles
método mágico para acceder al campo como si fuera una propiedad
rebobina el iterador hasta el inicio
devuelve la longitud de la imagen en bytes
define la relación grupal y jerárquica entre capas
detecta la codificación de caracteres del string str
esta función pretende usarse como una implementación predeterminada para __autoload()
recupera las rutas registradas
escanea a empezando con el bit de start hacia los bits mas significantes hasta que el primero bit establecido es encontrado
lee un archivo lo descomprime y lo escribe en la salida estándar
retorna el nombre de la función que esta tarea tiene asociada con por ejemplo la función que el trabajador de gearman llama
la función conviete el nombre de ruta dado por pathname de un archivo existente accesible y un identificador de proyecto a un valor de tipo integer para usarlo por ejemplo con shmop_open() y otras claves ipc de system v
autentica contra un servidor ldap tomando un rdn y contraseña especificados
añade una tarea para ser ejecutada en paralelo con otras tareas
esta función implementa un algoritmo de comparación que ordena strings alfanuméricos de la manera en que un humano lo haría lo cual se describe como orden natural
devuelve el tamaño de punto de la fuente usado cuando se anota texto
si el primer y único parámetro es un array min() devuelve el valor más bajo de ese array
lee una imagen desde un nombre de archivo
renombra una entrada definida por su índice
establece el punto actual a unas coordenadas nuevas
realiza una operación and a nivel de bits
llamado cuando se abre la envoltura de flujo justo antes de streamwrapper::stream_open()
obtiene el número de columna actual para el intérprete xml dado
remover objetos contenidos en otro almacenamiento de el almacenamiento actual
devuelve el equivalente decimal del número binario representado por el argumento binary_string
la descripción de la función va aquí
crea un nuevo objeto domentityreference
desconecta el hilo referenciado por el contexto de la llamada ¡de manera peligrosa!
la función dio_fcntl() realiza la operación especificada por cmd sobre el descriptor de fichero fd
la decadencia es un número pequeño negativo que es un factor por el que los pesos deberían decrementar en cada iteración durante un entrenamiento quickprop
si está establecido a true solr coloca los nombres de los gestores usados en la respuesta al cliente para propósitos de depuración
elimina un filtro de flujo previamente añadido al flujo con stream_filter_prepend() o stream_filter_append()
crea un yar_client hacia un yar_server
la función cubrid_lob2_export() se usa para guardar el contenido de datos blob/clob en un fichero
factor de periodos inestables de influencia para funciones las cuales son sensibles a él
devuelve una conexión a una base de datos ibm db2 universal database ibm cloudscape o apache derby
prepares a query for execution
aplica un efecto especial a la imagen similar al efecto conseguido en un cuarto oscuro fotográfico exponiendo selectivamente áreas del papel sensible fotográfico a la luz
reinicializa el array después de ser deserializado
devuelve el año de una etiqueta id3
establece la información del localismo
trae una fila de la información que fue devuelta por odbc_do() o odbc_exec()
la función cubrid_move_cursor() se usa para mover la posición del cursor actual de req_handle por el valor establecido en el argumento offset a la dirección establecida en el argumento origin
devuelve una copia binaria de los ficheros y variables de usuario dados desde la caché de apc
carga una copia binaria desde un fichero a la caché de fichero/usuario de apc
procesa una respuesta para un comando com_* básico enviado al cliente
revierte la transacción en uso
obtiene la altura de la página
comprueba si existe un fichero o directorio
disables an event connect listener object
obtiene el id generado por la anterior operación insert que haya creado un valor default unique
muestra el parámetro arg
establece la codificación para los datos recuperados desde un campo de caracteres
truncates a large object resource
establece el parámetro facet.date.other
retorna el numerador del tanto por ciento completado de la tarea expresado como una fracción
devuelve el momento actual medido como el número de segundos desde la época unix (1 de enero de 1970 00:00:00 gmt)
establece el operador de composción de una imagen
retorna el tamaño de los datos que serán devueltos por una tarea
crea una nueva instancia de la clase
devuelve los índice del array actual
envía un command de solicitud site exec al servidor ftp
devuelve el mensaje de error de la última operación frontbase
retorna el último código de retorno de gearman
retorna el manejador del trabajo para esta tarea
obtiene la posición del apuntador al archivo dado por ejemplo su desplazamiento en el flujo del archivo sin comprimir
mezcla el color de relleno de cada píxel con la imagen
esta función devuelve el uid del número de secuencia del mensaje dado
obtiene la propuesta de renderización de la imagen
devuelve el alto del píxel de un carácter de la fuente especificada
la función geoip_domain_by_name() devolverá los nombres de dominios de segundo nivel asociados con un nombre de host o una dirección ip
copia un objeto grande o una parte del mismo a otro objeto grande
esta función se usa para obtener la información del esquema solicitada de una base de datos
al enviar más de una instrucción sql a el servidor o ejecutar un procedimiento almacenado con varios resultados hará que el servidor retorne varios conjuntos de resultados
la función cubrid_lob2_new() se usa para crear un objeto lob blob y clob 
incrementa un valor numérico de un ítem por el offset especificado
dibuja una línea del punto actual al especificado
abre el archivo de relación dado
establece el punto blanco de cromaticidad de la imagen
calcula el nivel de bit exclusivo or xor de dos números gmp
esta función elimina un registro de la base de datos
se comporta de forma parecida a sybase_connect() con dos diferencias principales
fija la base de datos actualmente activa en el servidor que está asociada con el identificador de conexión link_identifier especificado
la descripción de la función va aquí
extrae un fichero subido por medio del campo selección de fichero fieldname y lo almacena en savepath
devuelve el contenido del objeto char
convierte una fecha juliana a una cadena que contiene la fecha del calendario juliano con el formato mes/día/año
devuelve un campo de bits de los modificadores de acceso para esta clase
establece una función de llamada de retorno que se invoca cuando el usuario presiona la tecla de suspensión normalmente ^z)
rompe este nodo en dos nodos en el índice especificado por offset manteniéndolos en el árbol como hermanos
asocia mysql_connection con memcache_connection empleando pattern como una expresión regular pcre y callback como una retrollamada de notificación o para desestablecer la asociación de mysql_connection
devuelve true si los mecanismos de protección como el drm están habilitados para este archivo
dibuja el carácter c verticalmente en las coordenadas especificadas en image
vincula el valor de un parámetro a una variable de sentencia
está función es identica a readgzfile() excepto que retorna el archivo en una matriz
restaura una opción de configuración dado su valor original
habilita la incorporación de fuentes coreanas
convierte left y right a valores de tipo integer y después realiza la división entera
retorna todos los recursos regulares actuales como una matriz
devuelve todos los campos a usar para similitud
lee el código de bytes de una constante existente de php y lo escribe al gestor de archivo abierto
construye un nuevo objeto svm listo para aceptar datos de entrenamiento
define el punto de inicio para la nueva ruta de dibujo
destruir el valor para el índice especificado
establece el algoritmo de entrenamiento
añade una consulta de faceta
indica si el nodo es parte de un documento html
obtiene el nombre del módulo de sesión actual
comprueba si la variable es un array
devuelve un identificador de color representando el color compuesto por los componentes rgb dados
obtiene el modo de compatibilidad que afecta a la forma de realizar los cálculos de todas las funciones de la extensión
realiza una negación not a nivel de bits en variant y devuelve el resultado
lee una cabecera del conjunto de resultados
obtener las conexiones de la red
devuelve el tipo de dato de la columna especificada en el resultado
es similar a la versión c de la función de mismo nombre dado que ejecuta el command dado y muestra el resultado
elimina el perfil nominado de la imagen y lo devuelve
imprime o devuelve una versión con la sintaxis remarcada del código contenido en el fichero dado por filename usando los colores definidos en el remarcador de sintaxis interno de php
exporta la clave key como una cadena pem codificada y la almacena en out que es pasado por referencia
devuelve un string serializado del contenido de los datos de la sesión actual almacenados en el array superglobal $_session
establece el valor del índice dado por index al valor especificado por newval
describe el tipo de conexión en uso incluyendo el nombre del servidor anfitrión
devuelve el código de error
devuelve la longitud de la imagen en bytes
devuelve el método de interpolación de la imagen especificada
la definición de página como un string
usa el método de exponenciación rápida para aumentar el left_operand a la potencia right_operand con respecto al módulo modulus
éste metodo copia el nodo actual y devuelve el apropiado objeto dom
este método no afecta al estado del cursor: si todavía no se le ha consultado se podrán aplicar límites saltos e tc
cambia el valor de la variable de configuración interna especificada por type a charset
borra una región de una imagen y colapsa la imagen para ocupar la porción borrada
calcula el coseno hiperbólico para cada valor de real y devuelve el array resultante
la función cubrid_insert_id() recupera el id generado para la columna auto_increment que fue actualizada por la consulta insert previa
crear una imagen nueva desde un fichero gd o una url
dibuja aun rectángulo redondeado dadas dos coordenadas los radios de las esquinas x e y y usando el contorno el ancho del contorno y la configuración de relleno actuales
añade una petición al grupo de peticiones
devuelve el perfil nominado de la imagen
aunque es técnicamente innecesario el método phar::startbuffering() puede proporcionar un aumento significativo de rendimiento al crear o modificar un archivo phar con un gran número de ficheros
devuelve la longitud del campo especificado
inicia una nueva definición de patrón
crea una copia de un objeto solrdocument
analiza un certificado proporcionado por x509certdata y devuelve un identificador de recurso para él
cierra la sesión actual
el método gearmanclient::echo() es obsoleto desde pecl/gearman 1.0.0
solicita un bloqueo exclusivo de relation previniendo incluso de acceso a lectura de otros clientes
establece el número de épocas de estancamiento de salida en cascada
esto rebobina el iterador hasta el inicio
la función geoip_netspeedcell_by_name() devolverá el tipo y la velocidad de conexión awill internet correspondiente a un nombre de host o a una dirección ip
define el estilo de la línea de unión para la página
devuelve el código de una excepción
genera una cadena de longitud arbitraria de bytes aleatorios criptográficos aptos para usos criptográficos como la generación de sales claves o inicialización de vectores
borra el archivo especificado por path desde el servidor ftp
obtiene un atributo de una sentencia
permite deshabilitar una envoltura de flujo ya definida
establece un fichero que contiene pares de sustitución
devuelve la función de parada empleada durante el entrenamiento
devuelve los hijos de una colección o los atributos de un documento
crea un iterador de cualquier cosa que se pueda recorrer
obtiene una lista de los métodos de resumen disponibles
devuelve un puntero a resultado conteniendo las bases de datos disponibles en el daemon fbsql
establece el mínimo de épocas de candidatas
obtiene el constructor de la clase reflejada
cambia parámetros del servidor en tiempo de ejecucción
devuelve el valor del punto de código de unicode del carácter dado
obtiene el próximo evento fam en espera
cierra la secuencia imap
devuelve los formatos soportados por gmagick
devuelve un id que se puede usar para acceder al semáforo de system v con la clave dada por key
retorna el último error devuelto por el servidor msql
devuelve una cadena que describiendo la conexión representada por el parámetro link incluyendo el nombre del servidor anfitrión)
devuelve un entero que describe el back-end utilizado por libev
esta función devuelve el id que puede ser usado para desplegar el logo de zend usando la imagen incorporada
una función útil para mejorar consultas durante el desarrollo de código y pruebas
get document by id
devuelve los datos asociados con un object en el almacenamiento
devuelve el número de campos en el conjunto de resultados
esta función analiza el string to_parse siguiente las reglas en bbcode_container creado por bbcode_create()
obtiene el resultado de una consulta asicrónica
genera un nuevo par clave privada y clave pública
retorna el número de campos del resultset especificado
cierra el objecto slob asociado con el identificador slob bid
obtiene la versión del servidor apache
devuelve un array dado la vuelta es decir las claves de array se convierten en valores y los valores de array se convierten en claves
devuelve la versión de la biblioteca sqlite vinculada
envía un mensaje multiparte mediante el socket
esta función convierte a numero desde radianes a grados
establece explícitamente la matriz de transformación actual
devuelve el número total de parámetros definidos en una función ya sean opcionales u obligatorios
véase yaf_route_simple::__construct()
coloca una imagen y la escala
la descripción de la función va aquí
elimina una consulta de filtro
obtiene el factor de muestreo horizontal y vertical
después de que el socket socket haya sido creado usando socket_create() y vinculado a un nombre con socket_bind() se le puede indicar que escuche conexiones entrantes sobre socket
este método está destinado a la creación de rutinas de interoperabilidad stubs específicas del formato de fichero phar y no está destinado a ustilizarlo con archivo phar basados en tar o zip
devuelve una imagen en escala de grises con un efecto tridimensional
implementa directo a la memoria formatos de imagen
establece el valor de tiempo de espera de la retrollamada del temporizador
obtener el número de tendencias de cada capa de una red
hace del proceso actual un líder de sesión
determina si el elemento actual directoryiterator es un directorio
realiza una equivalencia a nivel de bits en dos variantes
devuelve una lista con el nombre de las tablas de una base de datos mysql
obtiene un array de la marca de tiempo unix y de los valores almacenados para cada fecha en la actualización más reciente del archivo de base de datos rrd
establece el tamaño y el índice del objeto imagick
devuelve el sdo_dataobject raíz
esta función devuelve la versión escapada de la cadena dada
calcula el nivel de bit or de dos números gmp
lee una imagen desde un nombre de fichero
resuelve el problema de mínimos cuadrados lineal encontrado min x in || b - ax || devuelve un array que representa x
construye un object apciterator
elimina el recurso del archivo paradox y libera toda la memoria
mapea a hl.fl
obtiene el número de línea donde ocurrió el error
obtiener el tamaño de el array
es llamado durante la unserialización del objeto
asocia datos o información con el objeto actualmente señalado por el iterador
establece el método de píxel virtual de la imagen
convierta codificaciones de caracteresa de vars variables de la codificación from_encoding a la codificación to_encoding
transforma una imagen a un nuevo espacio de color
establece el color actual del contorno a los valores rgb proporcionados
devuelve el número del último error
la descripción de la función va aquí
obtiene la siguiente fila y la devuelve como un objeto
esta función determina si las palabras unidas serán tratadas como compuestos legales
la descripción de la función va aquí
obtiene el número de inodo para el elemento actual directoryiterator
rellena las referencias pasadas con el tamaño actual del terminal
la descripción de la función va aquí
idéntica a file_get_contents() excepto que stream_get_contents() opera sobre un recurso de flujo ya abierto y devuelve el resto del contenido en una cadena hasta maxlength bytes y comenzando por el offset especificado
devuelve más detalles del estado http
crea un vector de inicialización iv desde una fuente aleatoria
decodifica el string codificado por palabras str en cabecera mime
si el primer y único parámetro es un array max() devuelve el valor más alto de ese array
esta función es un alias de: soapserver::soapserver()
devuelve el código de error de la operación más reciente
establece el valor del tiempo de espera en stream expresado como la suma de seconds y microseconds
devuelve un array con índices numéricos que contiene la siguiente fila del conjunto de resultados de una consulta
el usuario debería definir siempre este método para un acción este es el punto de entrada de la misma
obtiene el contenido de un parámetro de un documento pdi de tipo numérico
devuelve la fecha unix correspondiente a la medianoche de pascua del año dado
devuelve la respuesta sin tratar del servidor
optimiza la base de datos interna
cuando usa stat() lstat() o cualquier otra función enumerada en la lista de funciones afectadas abajo php examina la información que devuelven esas funciones para proporcionar un rendimiento más rápido
añade datos a un blob creado con ibase_blob_create()
indica si este nodo de texto contiene únicamente espacios en blanco o si está vacío
la función socket_read() lee desde un recurso socket socket creado por las funciones socket_create() o socket_accept()
obtiene el contenido de un parámetro de cadena de un documento pdi de tipo string
devuelve el tipo de una razón
devuelve una lista separada por comas de los ficheros de configuración procesados por php.ini
obtiene las flags establecidas en una instancia de splfileobject como un integer
obtiene el argumento especificado de una lista de argumentos de una función definida por el usuario
devuelve la zona horaria correspondiente a una combinación de códigos de país y de región
extrae una región de la imagen
activa o desactiva el canal alfa de la imagen
devuelve el índice del color especificado en la paleta de la imagen
generar una firma basada en el método http final url y un string/array de parámetros
dibuja una porción de un círculo con su punto medio enx y
al igual que closedir() solo que el valor predeterminado de dir_handle es $this
devuelve una cadena que contiene el carácter especificado por el valor del pundo de código de unicode
construye el objeto eventbuffer
devuelve el nombre del índice del campo especificado
devuelve el número de filas afectadas por una consulta insert update o delete
esta función retorna el objeto al que pertenece el ancla especificada
comprueba si el flujo de bytes especificado es válido para a la codificación especificada
elimina el objeto char para el id del objeto char dado
devuelve true si el archivo mpeg tiene derechos de autor
devuelve las propiedades reflexionadas
hace commit a los cambios hechos en local enumerados en el targets array en el repositorio con el mensaje de log log
si las funciones radio fallan entonces guardan un mensaje de error
mueve la posición del apuntador interno en el iterador
ordena un array por clave en orden inverso manteniendo la correlación entre la clave y los datos
comprueba si el método es abstracto
habilita la incorporación de fuentes chinas tradicionales
crea una instancia gearmanworker que representa a un trabajador que se conecta a un servidor de trabajo y acepta tareas a ejecutar
devuelve un string que representa el almacenamiento
elimina count caracteres comenzando desde la posición offset
reduce el ruido granular de una imagen mientras preserva los bordes de la imagen original
cambia el valor del color de cualquier píxel que coincida con el objetivo y esté en el área inmediata
devuelve un string con la representación json de value
genera una cadena de consulta codificada estilo url a partir del array asociativo o indexado dado
devuelve un número pseudo-aleatorio en el rango 0 1
cuando se le pasa un hash válido creado con un algoritmo soportado por password_hash() esta función devolverá un array con información sobre dicho hash
cambia los momentos de último acceso y modificación de un fichero
obtiene los nombres de los modificadores
vincula la variable de php variable al parámetro de sustitución de variable vinculada de oracle bv_name
devuelve el tipo de la imagen
lee el código de bytes de una clase y realiza una llamada de retorno a una función de usuario
cambia el cargador externo por defecto de entidades
crea un nuevo objeto domprocessinginstruction
establece el parámetro de consulta común explainother
obtiene si está disponible o no root_device anunciando su presencia 
el número de funciones de activación del array fann_get_cascade_activation_functions()
obtiene el espaciado entre palabras del texto
establece el tipo del xmlrpc base64 o fecha-hora para un valor de cadena php
construye un objeto gmagickpixel
acepta una conexión sobre un socket creado previamente por stream_socket_server()
establece el punto primario azul de la cromaticidad de la imagen
devuelve un entero solrquery::facet_sort_index o solrquery::facet_sort_count)
dibuja un rectángulo
descifra una imagen que ha sido cifrada anteriormente
reemplazará el id de sesión actual por uno nuevo y mantendrá la información de la sesión actual
lee atributos y valores desde una entrada en la búsqueda de resultados
este método es llamado siempre que los datos son leídos desde o escritos en el flujo adjunto como con fread() o fwrite()
rellena un bloque pdf con información variable según sus propiedades
devuelve los parámetros en forma de array de reflectionparameter
devuelve el objeto throwable anterior el tercer parámetro de error::__construct()
elimina una imagen de la lista de imágenes
realiza un entramado ordenado basado en varios mapas de umbral de entramado predefinidos pero sobre múltiples niveles de intensidad lo que puede ser diferente para distintos canales según los argumentos de entrada
lista las tablas en el rango solicitado y los privilegios asociados con cada tabla
rellena la ruta actual utilizando la regla de zigzagueo de número distinto de cero luego colorea la ruta
este método se usa con cursores de seguimiento
devuelve la severidad de la excepción
indica si la tarea está actualmente siendo ejecutada
actualiza el mapa con un nuevo par clave-valor
elimina nodos de texto vacíos y une nodos de texto adyacentes en este nodo y todos sus hijos
tomará un string str y devolverá un número hash 32-bit único de este
calcula el seno hiperbólico para cada valor de real y devuelve el array resultante
devuelve str con todos los caracteres alfabéticos convertidos a mayúsculas
recupera el contenido de una celda de un conjunto de resultados de mysql
crea un directorio en la copia de trabajo o repositorio
mueve el puntero del iterador al siguiente elemento
obtinene un objeto imagickpixeliterator de una sección de imagen
elimina el valor según la clave key
establece el manejador de excepciones predeterminado si una excepción no es capturada dentro de un bloque try/catch
devuelve el nombre de archivo asociado a una secuencia de imágenes
formatea el intervalo
esta función realiza una consulta sin esperar a que todos los resultados de consulta hayan sido completados
éste metodo previene que se pueda clonar un objeto
obtiene una variable almacenada en la caché del usuario
obtiene el tamaño del fichero dado
busca el siguiente índice ausente no incluido que sea mayor que el índice pasado
la descripción de la función va aquí
la descripción de la función va aquí
exporta el contenido de una copia de trabajo o un repositorio en un directorio limpio
determina si el punto de código especificado es un dígito hexadecimal
devuelve la marca de tiempo unix unix timestamp de la actualización más reciente de la base de datos rrd
elimina el buzón espeicificado por mailbox
devuelve una máscara de bits de funcionalidades soportadas
busca en subject una coincidencia con la expresión regular dada en pattern
cambia el tamaño de una imagen a las dimensiones dadas y elimina cualquier perfil asociado
cierra una sentencia preparada
obtiene el sub iterador interno actual
elimina el objeto blob para el id del objeto blob dado
devuelve un manejador de archivo que soporta operaciones de lectura
guarda los datos dados por data en un objeto grande
esta función es la opuesta a htmlspecialchars()
genera un string que es el nombre de la ruta del terminal controlador actual para el proceso
devuelve un array que contiene todos los valores de array1 que tienen claves que están presentes en todos los argumentos
entrena un conjunto de datos completo leído desde un fichero por un período de tiempo
el nombre del campo
restablece una envoltura incluida que se dejó de registrar previamente con stream_wrapper_unregister()
ésta es una implementación de la interfaz arrayaccess que permite la manipulación directa del contenido de un archivo phar utilizando los corchetes de acceso al array
devuelve un array con todas las conexiones abiertas e información sobra cada uno de los servidores
obtener las flags de comportamiento de la clase arrayobject
devuelve el número de filas afectadas por una consulta asociada a result_id
devuelve el número de columnas de una consulta ya preparada o ejecutada
crea la imagen de un conjunto de datos particulares desde el archivo de rrd
obtiene el índice del mapa de color de la varita de píxeles
recupera un archivo remoto desde el servidor ftp y lo guarda en un archivo local
ajusta el contraste de una imagen con un algoritmo de contraste sigmoide no lineal
mapea a facet.limit
se puede llamar en pool::collect() para determinar si este objeto es basura
esta función convierte un dominio unicode al formato idna ascii-compatible
establece el valor del tiempo de espera de la retrollamada de inactividad
si mostrar o no información de depuración
obtiene la parte de el árbol de el elemento actual
cierra la conexión al servidor ingres que está asociada con el enlace especificado
obtiene todas las cabeceras de petición http de la llamada actual
suscribirse a un buzón nuevo
detiene la medición del tiempo para el intervalo anteriormente iniciado
configura las marcas para eventos de lectura y escritura
la versión plural de gettext()
carga un segundo o subsiguiente fichero de esquema en un das xml que ya ha sido creado con el método estático create()
abre un fichero bzip2 .bz2 para lectura o escritura
devuelve la consulta de búsqueda principal
devuelve el estado de la copia de trabajo de los ficheros y directorios dando las modificaciones adiciones borrados y otros cambios a los elementos de la copia de trabajo
creará una copia vacía de la relación dada por relation bajo un nuevo nombre dado por name pero con los índices predeterminados
cierra un gestor de página y libera todos los recursos de página relacionados
crea una instancia gearmantask que representa una tarea para enviar al servidor de trabajos
este método almacena un hash existente en un fichero del disco en el mismo formato que loadfromfile() puede leer
inicia la búsqueda y ejecución de los callback definidos por el usuario
busca la aguja needle en el pajar haystack usando una comparación flexible a menos que esté establecido strict
establece el valor quantum de un elemento de color del objeto imagickpixel
devuelve el histograma de la imagen como un array de objetos imagickpixel
redimensiona una imagen adaptativamente con información dependiente de la triangulación
php 7
devuelve parte de la cadena pajar comenzando desde e incluyendo la primera coincidencia de la cadena aguja insensible a mayúsculas-minúsculas hasta el final de pajar
libera un recurso de agente con todos sus diccionarios
fusiona una secuencia de imágenes
un servidor db++ preparará un intérprete de tcl para una conexión cliente
establece la imagen de tesela para usarse con todas las funciones de relleno de regiones como imagefill() y imagefilledpolygon() cuando se rellena con el color especial img_color_tiled
envia una consulta a la base de datos activa en ese momento en el servidor y que está asociada con el identificador de conexión link_identifier especificado
lleva a cabo una transacción
comprueba si el iterador es válido
guarda el objeto sdo_das_xml_document en un fichero
imprime el texto text con la fuente y el tamaño actuales en la posción actual
inserta la entrada descrita con clave y valor en la base de datos
devuelve información sobre un stream flujo existente
comprueba si la posición actual del puntero interno es aún válida
esta función es similar a strcasecmp() con la diferencia de que puede especificar el límite superior de) número de caracteres de cada cadena a ser usado en la comparación
añade un nuevo elemento al conjunto de sondeo y devuelve el id interno de dicho elemento
divide un string str en strings más pequeños tokens con cada token delimitado por cualquier caracter de token
genera una cadena de bytes pseudo-aleatoria con el número de bytes determinado por el parámetro length
ejecuta comandos por la shell bourne y abre el flujo pty al proceso
escribe un string producido de acuerdo con el format en el recurso del flujo especificado mediante handle
establece el indicador de posición de archivo de handle al principio del flujo del archivo
recupera un nodo especificado por index dentro del objeto domnodelist
devuelve el número de entradas almacenadas en el resultado de previas operaciones de búsqueda
devuelve estadísitcas sobre el servidor fulltext
recuperar la instancia de la clase yaf_application
devuelve el ancho de la imagen
recuperar la información de paginación enviada por el servidor
construye un nuevo recursivetreeiterator desde un iterador recursivo suministrado
este método obtiene el arrayiterator que es usado para almacenar los iteradores añadidos con appenditerator::append()
retorna el nombre de un género correspondiente a un id dado
hace una diferencia binaria de dos archivos y almacena el resultado en un archivo de revisión
elimina uno de los parámetros facet.date
obtiene el iterador interno
devuelve el equivalente a log 1 + number calculado de tal forma que no pierde precisión incluso cuando el valor de number se aproxima a cero
añade una lista de servidores de trabajos que puede usarse para ejecutar una tarea
determinar si el carácter especficiado es permisible en un identificador
establecer una conexión de la red
los archivos tar/zip no ejecutables no pueden tener una rutina de interoperabilidad por lo que este método simplemente lanza una excepción
toma la raíz enésima dada por nth de a y devuelve el componente entero del resultado
obtiene el tamaño empaquetado del archivo entrada
devuelve el valor de la opción de un socket
esto puede ser usado para detener el corrido del script y esperar respuestas en el socket conectado
con este método cuanquierl cadena se puede añadir al archivo tar/zip
avanzar al siguiente nodo
devuelve una cadena formateada según el formato dado usando el parámetro de tipo integer timestamp dado o el momento actual si no se da una marca de tiempo
devuelve el último error de la sesión ortográfica actual
esta función prepara para una petición de tipo extended services utilizando el profile para use of z39.50 item order extended service to transport ill profile/1)
devuelve el ancho de un campo correspondiente a field_number
realiza una operación de implicación a nivel de bits
hace que esté disponible para ser leída la siguiente fila del conjunto de resultado
registra una función con la cola de __autoload proporcionada por spl
detiene la búsqueda y ejecución de los callback definidos por el usuario
establece el método de disposición de una imagen
hace que el script duerma hasta la marca de tiempo especificada dada por timestamp
obtiene los comentarios de documentación de una clase
devuelve el código del sistema operativo anfitrión del archivo de entrada
esta función devuelve los parámetros de la base de datos cubrid o false en caso de error
devuelve el número de alertas de accesibilidad encontradas en un documento específico
analiza datos get/post/cookie y establece variables globales
hace borrosa de forma radial una imagen
devuelve la clase en la que se ha declarado el método
obtiene el método de disposición de la imagen
comprueba si la propiedad especificada está o no definida
cerrará y eliminará la relación dada por relation
entrecomilla el string de entrada si fuera necesario y escapa los caracteres especiales contenidos en dicho string usando un estilo de entrecomillado apropiado para el controlador subyacente
añade una tarea de prioridad baja en segundo plano para ser ejecutada en paralelo junto a otras tareas
configura el contexto de flujo para el próximo documento libxml cargado o escrito
activa el debug output si el parámetro on_off es igual a true y lo desactiva si es igual a false
la función cubrid_get_class_name() se usa para obtener el nombre de la clase del oid
establece el valor de la variable de entorno de apache especificado por variable
constructs event object
recodifica el fichero señalado por el gestor de ficheros input al fichero señalado por el gestor de ficheros output según la petición de recodificación dada por request
abre una ventana centrada de un tamaño específico.
selecciona la base de datos activa actualmente en el servidor que está asociada con el identificador link-identifier
esta función escapará unescaped_string para que sea segura ponerla en una mysql_query()
establece los permisos en el archivo remoto especificado en mode
redimensiona una imagen empleando el algoritmo de interpolación dado
devuelve true si el objeto tiene más imágenes cuando se atraviesa la lista en dirección hacia delante
busca la posición de la primera ocurrencia de un string en otro string
esta función devuelve los nombres de todos los módulos compilados y cargados en el intérprete de php
busca dns de registros de tipo type que corresponde a un host
devuelve true si val es infinito positivo o negativo como el resultado de log 0 o cualquier valor demasiado grande para caber en un flotante en esta plataforma
la descripción de la función va aquí
devuelve una representación de tipo string del tipo de conexión usada
ejecuta una consulta preparada anterioremente con ingres_prepare()
este método sólo existe para anular la instanciación de la clase closure
obtiene un código de error dado en un intérprete xml
lee el contenido del actual nodo incluyendo el nodo mismo
devuelve la imagen actual como un string
configurar un servidor yar http rpc
crea un polígono relleno en image
define la forma a ser usada en el final de las líneas
traer una sección en particular del cuerpo de los mensajes especificados
quitar un object de el almacenamiento
inicia una conexión de flujo o datagrama al destino especificado por remote_socket
este método establece el valor de una opción de memcached dada por option
la función socket_recv() recibe len bytes de información en buf desde socket
establece una nueva conexión a un servidor de oracle y se identifica
secuencia datos del apuntador de archivo fuente aplica la codificacion y escribe al apuntador de archivo de destino
devuelve el producto de valores de un array
establece el ancho de la página
coloca la siguiente fila de una consulta en los búferes internos accesibles mediante oci_result() o usando varibles previamente definidas con oci_define_by_name()
devuelve la primera muestra de datos desde la especificada rra del archivo rrd
inicia una conexión hacia address usando el recurso socket socket el cual debe ser un recurso socket válido creado con socket_create()
obtiene los componentes rojo verde y azul de la definición de color dada
este método comprueba si una entrada con la clave proporcionada existe en el hash
crea una nueva marca temporal
realiza una validación relaxng del documento basándose en el esquema rng dado
antepone el string dado por value al valor del ítem existente
recupera la información de los parámetros y de las opciones de un flujo o contexto
esta función suspende la ejecución del script hasta que la base de datos emita uno de los eventos especificados
establece el peso de la fuente a usar cuando se anota texto
devuelve el color de relleno usado cuando se dibujan objetos rellenos
devuelve el tipo de el fichero referenciado
registra session_write_close() como una función de cierre
obtiene el número de argumentos pasados a la función
recupera la clave actual
crea una estructura de datos de entrenamiento desde una función proporcionada por el usuario
covierte los píxeles planos en píxeles cifrados
obtiene el color actual del trazo
obtiene el nombre de la tabla actual para el result set especificado
esta función devuelve el tipo de columna que se corresponde con el índice del campo dado
ejecuta una consulta preparada
aborta inmediatamente el bucle de eventos activo
escala las salidas de los datos de entrenamiento al rango especificado
este método encuentra el hijo de un elemento
rellena un archivo phar con el contenido de un directorio
devuelve el nombre de la clase
devuelve el mensaje de error relacionado con el código de error determinado
esta función devuelve el conjunto de caracteres de la conexión cubrid actual y es similar a la función de cubrid compatible con mysql cubrid_client_encoding()
añade el valor cómo el último elemento
esta función establece un nombre a un juego de resultados
esta función calcula y devuelve la caja circundante en píxeles de un texto freetype
establecer el modo de operación
returns max parameter
devuelve una lsita de los nombres de las clases que pertenecen a esta extensión
obtiene la distancia de la línea de base de las letras en minúsculas
este método es la versión insensible de arrayobject::natsort
obtener el número de neuronas de salida
la función cubrid_schema() se usa para obtener la información del esquema solicitado de la base de datos
obtiene el número de mensajes recientes en el buzón actual
asocia una o más opciones con la varita
añade varios componentes al formulario dado por form
abre un archivo de imagen
establece un callback de servicio proxy por señal
devuelve todas las propiedades asociadas que coniciden con el patrón
solicita monitorizar una colección de archivos dentro de un directorio
establece la zona horaria predetermiada usada por todas las funciones de fecha/hora
devuelve un string con el nombre del cursor activo
libera un recurso mime
obtiene la longitud de un campo referenciado por número del indentificador de resultado dado
compara el left_operand al right_operand y devuelve como resultado un entero
devuelve el límite inferior del primer rango de datos para todas las facetas de fecha de este campo
para archivos tar este método comprime el archivo entero usando la compresión gzip o bzip2
obtiene la cadena asociada a un error code dado en un intérprete xml
la descripción de la función va aquí
comprueba si el fichero se puede leer
establece el tamaño del imagick
este método devuelve el alto del cairoimagesurface
el comando distinct devuelve una lista de valores distintos para la clave dada a través de una colección
determina si el punto de código especificado es una letra titular
suaviza los extremos de la imagen al estilo viñeta
recupera la versión del servidor mysql
inicia una nueva sesión y devuelve el manipulador curl para el uso de las funciones curl_setopt() curl_exec() y curl_close()
indica si el archivo dado es un enlace simbólico
generalmente no se debería tener que construir un mongocommandcursor manualmente ya que existen funciones ayudantes como mongocollection::aggregatecursor() y mongocollection::parallelcollectionscan()
establece la cadena dada como versión de xml
esta función devuelve una cadena que representa la versión de la biblioteca cliente
devuelve un informe de rastreo para cada consulta inspeccionada por la caché de consultas
lee hasta el final en el puntero de el fichero dado de la posición actual y escribe el resultado a el búfer de salida
crea un nuevo objeto paradox
crea un nuevo objeto cairoimagesuface del tipo format
devuelve el rango de cuantía para la instancia de imagick
reanudación usando la interfaz de newt después de llamar a newt_suspend()
compara dos string de números de versiones estandarizados de php
este método importa la hoja de estilos a xsltprocessor para realizar transformaciones
cambia el directorio virtual actual donde se buscarán los archivos de relaciones mediante dbplus_open()
cierra la entrada del directorio especificado
devuelve el valor actual del parámetro de configuración magic_quotes_gpc
este método no debería llamarse directamente
lee la información de sesión desde el almacenamiento de sesiones y devuelve el resultado
el primer prototipo es cuando se enlazó con libmcrypt 2.2.x el segundo cuando se enlazó con libmcrypt 2.4.x o superiores
establece la función de retrollamada del temporizador
establece el texto que aparece antes de un término remarcado
calcula la fecha juliana para una fecha en el calendario calendar especificado
la descripción de la función va aquí
devuelve una matriz como representación del documento de entrada
esta función devuelve un recurso de conexión en caso de éxito cero en caso de fallo
devuelve el número de error de la última operación curl
define una cookie a ser enviada conjunto con la peticiones soap
establece el color de borde de la imagen
closes a large object
esto puede ser usado para incrementar o disminuir el depurado en un área diferente de la aplicación
comprueba una conexión al servidor o intenta reconectar si la conexión ha caído
crea un nuevo recurso socket de tipo af_inet que escucha en todas las interfaces locales del puerto dado esperando para nuevas conexiones
crea un nuevo analizador xml con soporte para espacios de nombres xml y devuelve un gestor de recurso que hace referencia al mismo para poder utilizarlo con las demás funciones de xml
envía un command arbitrario al servidor ftp
devuelve la hora de la salida del sol para un día especificado como timestamp y ubicación dados
añade la información dad a la base de datos
deshacer todos los cambios hechos a una entrada
establecer el valor en el índice especificado por newval
este método devuelve las banderas establecidas en el manifiesto de un phar
esta función verifica un objeto o una jerarquía entera de objetos
ensamblar un url
establece el token del callback que será llamado después con oauthprovider::calltokenhandler()
escriba hasta len bytes de data al fichero fd
establece el uri que contiene el xml a ser analizado
registra una función php o función definida por el usuario para ser utilizada como una función de ordenación específica dentro de sentencias sql
importa un flujo que encapsula un socket en un recurso de extensión de socket
devuelve la posición de búsqueda actual de archivo de un objeto slob abierto
devuelve la descripción de una razón
establece el tipo de la variable var a tipo
crea una nueva expresión regular
devuelve la extensión de la constante imagetype_xxx dada
ccontinúa recuperando/enviando un archivo en modo de no-bloqueo
lee una imagen desde una cadena binaria
bloquea un objeto para ser editado en modo exclusivo por el usuario que llama a esta función
crea un fichero ordinario o especial a menudo
se comporta como strftime() excepto que el instante devuelto es tiempo medio de greenwich gmt 
obtiene el valor de una propiedad enumerada o entera de unicode para un punto de código
devuelve el valor del parámetro facet.date.hardend
devuelve el últmo mensaje de error para la llamada más reciente a una función de mysqli que puede haberse ejecutado correctamente o haber fallado
crea un objeto documento xml
elimina un campo del documento
esta función devuelve true si el algoritmo especificado es un algoritmo por bloques o false si es uno de flujo
escribe los datos del documento dentro de una salida estándar
calcula la intersección de arrays con una comprobación de índices adicional compara la información mediante una función de retrollamada
establece el número de épocas de estancamiento de candidatas en cascada
establece el factor mu de quickprop
retorna un mensaje textual en inglés correspondiente al último error numérico como devuelto por yaz_errno()
verifica el estado de los hilos
únicamente se necesita llamar a la función mssql_free_result() si existe la preocupación de que se esté utilizando demasiada memoria durante la ejecución del script
lista todos los procedimientos en el rango solicitado
obtiene las iteraciones de la imagen
resuelve filename en la ruta incluida según las mismas reglas que en fopen() / include
se usa para cambiar la posición del fichero del descriptor de fichero proporcionado
devuelve el desplazamiento del error de paso de sarprop
intenta eliminar el directorio nombrado por dirname
devuelve el número de documentos en la base de datos
devuelve si el iterador aún está sobre un elemento válido de un mapa
obtiene la clave con prefiro y postfijo
devuelve el número máximo de términos que debería devolver solr
esta función acepta uno dos o cuatro parámetros no tres :
comprueba si se puede escribir en el fichero actual
establece el factor de escala horizontal y vertical del sistema de coordenadas
devuelve la longitud del campo especificado
reemplaza los colores de una imagen con los definidos por el parámetro replacement
esto es un alias de solrparams::addparam
valor de errno en el caso de retornar un valor gearman_errno
establece la calidad de servicio necesaria de un clúster
devuelve un string de la ruta absulota del dispositivo terminal actual que está abierto en el descriptor de ficheros fd
conecta el último punto con el primer punto de un trazado y dibuja la línea cerrada resultante
obtiene el número de filas afectadas en una operación de mysql anterior
devuelve el número de campos de este documento
este método añade una nueva entrada a un hash y devuelve si la entrada fue añadida o no
toma una captura de pantalla de la pantalla completa
devuelve el prefijo por el que los términos coincedentes deben ser restringidos
obtiene el valor del límite del inglete
cierra el trazado actual
obtiene el directorio actual
usa el esquema w3c xsd para validar el documento como es procesado
crea un archivo virtual con nombre de sólo lectura desde la información proporcionada en la memoria
construye un nuevo objecto soapparam
obtiene el tiempo de la última modificación de un archivo remoto
devuelve el método de compresión de una entrada de directorio especificada por zip_entry
cambia el valor de píxeles individuales basados en la inatensidad de cada píxel comparado con el umbral
toma la petición de firma de certificado csr en inglés) represenatada por csr y la almacena como texto ascii blindado en out que es pasado por referencia
especifica el número de filas que se van a saltar
devuelve el tamaño de el frame en bytes
devuelve un array con los tipos de firmas soportados
libera un descriptor asignado por oci_new_descriptor()
escribe el contenido de cadena al archivo gz dado
devuelve el número de filas extraidas por una consulta a través del identificador result_id después de haber llamado a ifx_query() o ifx_do()
es similar a popen() pero provee un grado de control mucho más grande sobre la ejecución del programa
devuelve con éxito
esta función es un alias de: soapvar::soapvar()
se puede establecer un tiempo de espera en cualquier momento y afectará a consultas subsiguientes del cursor incluyendo la obtención de más resultados desde la base de datos
añade un nuevo seguimiento o modifica un seguimiento existente para el fichero o directorio especificado en pathname
establece la versión de swf a usar en la película
colocar al corriente los valores de los atributos en documentos especificados
establece la frecuencia bajo la cual los términos serán ignorados en los documentos fuente
desvincula el socket de un extremo
compone dos imágenes y produce una sóla imagen que es la composición de una imagen izquierda y derecha de una pareja estéreo
establece una opción de contexto de zmq
obtiene el actual patrón guía
la fracción de cambio de salida en cascada es un número entre 0 y 1 que determina lo grande que debería ser el cambio del valor de una fracción de fann_get_mse() en fann_get_cascade_output_stagnation_epochs() durante el entrenamiento de las conexiones de salida para que el entrenamiento no se estanque
toma su parámetro errno como un código de error de socket devuelto por socket_last_error() y devuelve el correspondiente texto explicativo
la descripción de la función va aquí
esta función comprime la cadena dada usando el formato de datos zlib
este método se usa para convertir un archivo phar ejecutable en un fichero tar o zip
cierra la sesión actual
obtiene el valor de una opción de configuración de php
escala las entradas de los datos de entrenamiento al rango especificado
obtiene el interletraje («kerning») del texto
cierra una conexión a un servidor de bases de datos
devuelve el servidor que será seleccionado por una clave dada por server_key en particular para todas las operaciones memcached::*bykey()
reemplaza o inserta la entrada descrita con key y value en la base de datos especificada por handle
la descripción de la función va aquí
guarda el valor dado por value en un servidor de memcached bajo la clave especificada por key
reúne las estadísticas del fichero nombrado por filename
algunas bases de datos admiten procedimientos almacenados que devuelve más de un conjunto de filas también conocido como conjunto de resultados)
esta función permite saber si inotify_read() bloqueará o no
obtiene el número de registros filas en la base de datos especificada
establece la calidad de compresión predeterminada del objeto
finaliza la ejecución del script
establece o cambia las opciones del analizador
cerrará todas las conexión del/de los servidor/es de bases de datos
ejecuta una sentencia sql en una única llamada a la función devolviendo el número de filas afectadas por la sentencia
anexa la cadena de caracteres data al final de la data caracter del nodo
la función cubrid_get_query_timeout() se usa para obtener el tiempo de espera de consulta de la petición
devuelve el conjunto de caracters predeterminado de la conexión de la base de datos
devuelve un mensaje de texto describiendo el código de error dado
se necesita invocar esta función antes de cada llamada a mcrypt_generic() o mdecrypt_generic()
anexar una curva bezier a la ruta actual el punto actual y el punto x2 y2 son usados como putos de control para la curva bezier y el punto actuales movido a el punto x3 y3 
devuelve el generador de trozos de texto para el texto remarcado
la descripción de la función va aquí
puede ser utilizado para dividir una cadena en trozos más pequeños lo que es útil por ejemplo para convertir la salida de la función base64_encode() y hacer que coincida con la semántica del rfc 2045
construye un objeto observador de evprepare e inicia el observador autmáticamente
un archivo exe o auto-ejecutable consiste en 3 partes: el stub código ejecutable p.ej
finaliza la definición de un patrón
añade servers a la agrupación de servidores
calcula la lista de acceso al grupo para el usuario especificado en el parámetro name
agregar parámetros requeridos por el proveedor oauth
obtener el iterator interno
dibuja un píxel en las coordenadas especificadas
devuelve un identificador de imagen que representa una imagen en blanco del tamaño especificado
guarda la imagen adjunta al archivo especificado
esta es una función de bajo nivel del api que se utiliza para hacer llamadas soap
desactiva la verificación del certificado y servidor ssl esto no se recomiendo para entornos en producción
revise la sección de consultas de este manual para más información sobre distribución de lecturas a secundarios
establece las flags a ser usadas por el splfileobject
devuelve una cadena hexadecimal representación de el crc del archivo de entrada
devuelve el valor de la opción especificada con name
coloca un hipervínculo en la posición dada que apunta a un fichero de un programa el cual se iniciará al hacer clic sobre él
establecer si la consulta será impulsada boost por la relevancia del término de interés
solicita el subsistema sftp desde un servidor ssh2 ya conectado
genera una nueva csr certificate signing request - petición de firma de certificado) basada en la información proporcionada por dn el cuál representa el nombre distinguido que se va a usar en el certificado
este método devuelve la suma de verificación crc32() del fichero dentro de un archivo phar
cierra el dado puntero del fichero bzip2
rebobina de nuevo al primer elemento de la iterator
devuelve todas las opciones de configuración registradas
cierra un puntero a un archvio hacia una tubería abierta por popen()
intenta determinar las capacidades del navegador de usuario buscando la información del navegador en el archivo browscap.ini
configura el testigo
elimina el atributo con el nombre dado
obtener el número de neuronas de entrada
devuelve los datos previamente almacenados si el valor con la key existe en el servidor en este momento
coloca una página pdf importada en la página sujeta a varias opciones
obtiene un objeto splfileinfo para el padre de el fichero actual
carga el fichero ttf dado y opcionalmente incrusta sus datos dentro del documento
devuelve el número de entradas de cada patrón de entrenamiento del resource de datos de entrenamiento
inicia la creación de perfiles en modo de muestra que es una versión más ligera de peso de xhprof_enable()
establece el espaciado interlínea del texto
procesa cada uno de los recursos del grupo
recorta a lo largo de trazados nominados desde un perfil 8bim si está presente
devuelve la versión de la extensión
devuelve la hora de la puesta de sol para un día especificado como timestamp y ubicación dados
devuelve el número de columnas de una base de datos abierta filepro
recupera la información de una tabla relacionada con la instancia de la tabla colocada más recientemente
añade un fichero al archivo zip usando su contenido
elimina uno de los campos morelikethis
construye un objeto eventbase
prepara una frase desde una cadena lucene sin escapar
obtiene una fila de un conjunto de resultados asociado al objeto pdostatement
devuelve la tangente hiperbólica de arg definida como sinh arg /cosh arg 
devuelve si la entrada actual es un array o un object para que un iterador puede ser obtenido a través de recursivearrayiterator::getchildren()
ejecuta un stored procedure en una base de datos del servidor ms sql
devuelve el número máximo de trozos remarcados a generar por campo
devuelve un string con el último mensaje de error
obtiene el valor de la propiedad
la función cubrid_commit() se usa para ejecutar el envío de la transacción a la que apunta conn_handle actualmente en progreso
crea un nuevo archivo de base de datos paradox
obtiene información sobre un grupo porporcionando su id
establece la codificación de entrada/salida del documento
calcula el producto escalar de real0 con real1 y devuelve el vector resultante
use este método para comprobar si el fichero referenciado por el objeto splfileinfo es un link
devuelve el tamaño de la colección
establece el intervalo de tiempo a esperar actividad de entrada/salida del socket
este constructor crea un nuevo objeto quickhashintstringhash
dibuja un rectángulo con su esquina inferior izquierda enx y
establece las flags de expresión regular
retorna un array usando los valores de la matriz array como keys y su frecuencia en array como valores
devuelve los nombres de las interfaces
establece la frecuencia de documento mínima usada para determinar la cuenta de términos
la descripción de la función va aquí
aplica el filtro dado por filtertype sobre la imagen image
devuelve texto en inglés describiendo la petición sqlite que falló más recientemente
elimina la ventana superior de la exposición y redibuja las áreas de exposición que sobrescribió dicha ventana.
el comportamiento de esta función es casi idéntico al de preg_replace() excepto por el hecho de que en vez del parámetro replacement se debería especificar callback
examina string para buscar coincidencias con pattern luego reemplaza el texto coincidente con la salida de la función callback
espera a que se asigne un trabajo y llama a la función de retorno apropiada
obtener el nombre de el parámetro
esta función se puede usar para controlar la verbosidad del registro y los tipos de actividades que deberían registrarse
convierte una fecha ya almacenada en el archivo paradox en formato legible por humanos
reduce el ruido granular de una imagen mientras preserva los bordes de la imagen original
establece una fuente la cual ha de ser cargada antes con la función ps_findfont()
la función bindtextdomain() establece la ruta para el dominio
toma un valor array y devuelve un nuevo array con el orden de los elementos invertido
ordena un resultado de una llamada a dbx_query() con una función de ordenación personalizada
recupera un parámetro de autenticación donde la clave es key
el tamaño de paso mínimo es un número positivo pequeño que determina lo pequeño que podría ser el tamaño de paso mínimo
restaura un conexto de gráficos previamente guardado
establece el punto primario rojo de la cromaticidad de la imagen
escribe todos los fotogramas en un gestor de archivo abierto
establece la longitud de palabra máxima sobre la que las palabras serán ignoradas
crea un destino con nombre sobre una página arbitraria en el documento actual
especifica un generador de trozos de código para texto remarcado
permite al proceso pid unirse al grupo de procesos pgid
imprime una imagen gd2 en filename
obtiene el descenso vertical de la fuente
rellena la caché de página con información de un fichero por lo que lecturas posteriores de ese fichero no bloquearán la e/s del disco
añade una anotación al objetivo dentro del archivo pdf actual
une tres cadenas en una y devuelve el resultado
establece el nivel de comprensión de salida de swf
elije un secundario aleatorio para una conexión desde la cual realizar las lecturas
establece el comentario de una entrada definido por su índice
devuelve el código de error de la última ejecución de expresión regular de pcre
obtiene el elemento acutal de la pila de apciterator
compara dos strings empleando el mismo tiempo sin importar si son iguales o no
recupera el nombre de la base de datos desde una llamada a cubrid_list_dbs()
la función cubrid_lob2_close() se usa para cerra un objeto lob devuelveto por cubrid_lob2_new() u obtenido de un conjunto de resultados
solicita a event_base que detenga la emisión de eventos
establece las opciones del analizador
esta función toma el nodo node de la clase simplexml y lo convierte en un nodod domelement
fija el nivel de severidad mínima del servidor
establece la fuente actual con el tamaño fontsize especificado empleando el manejador de fuentes font devuelto por pdf_load_font()
establece el título del hilo
devuelve el primer motivo de error
escribe una cadena en un objeto slob
cada servidor virtual de iis está asociado con número de instancia
cierra la base de datos paradox
elimina un campo del documento
cairosurface es un tipo abstracto y como tal no debería ser instanciado en los scripts de php
el comportamiento de esta función es similar a la de preg_replace_callback() excepto que las retrollamadas se ejecutan en función de cada patrón
muestra una imagen o una secuencia de imágenes en un servidor x
establece las flags
devuelve la ruta de el fichero
la descripción de la función va aquí
concatena una matriz a la matriz de transformación actual ctm en inglés)
establece el valor actual del aumento del texto
desecha todos los colores de los píxeles excepto uno
este método finaliza todas las añadiduras/eliminaciones hechas al índice
rebobina el iterador hasta el primer elemento de el iterador interno superior
devuelve una cadena que contiene el código de error sqlstate de la función más recientemente invocada de sentencia preparada que tuvo éxito o falló
la función socket_sendto() envía len bytes desde buf a través del socket socket al puerto port en la dirección addr
este método actúa como si guardara y después cargara el documento poniendo el el documento en una forma normal
inicia una nueva plantilla
devuelve un array con información sobre la ubicaciones de certificados disponibles donde buscar certificados ssl
comprueba si el valor por omisión para el parámetrp está disponible
lee todos los valores del atributo en la entrada en el resultado
devuelve la traza de pila de una excepción
extrae las palabras clave de query con los ajustes para el tokenizer dado index de forma opcional con las estadísticas de ocurrencia por palabra clave
esta función devuelve un identificador único del objeto
devuelve una matriz que contiene una imagen reconstruida y las diferencias entre las imágenes
devuelve el color del índice del mapa de color especificado
obtiene el nodo actual
valida un documento basado en un esquema definido en la cadena dada
devuelve información sobre la configuración del servidor
devuelve el identificador de grupo de proceso del proceso actual
esta función devuelve un array asociativo que se corresponde con la fila obtenida y después mueve el puntero interno de datos hacia adelante o devuelve false cuando se ha alcanzado el final
renombra o mueve un fichero a una nueva ubicación
devuelve el valor de la categoría bidireccional del punto de código que se emplea en el algoritmo bidireccional de unicode uax #9)
obtener los pulsos transcurridos para el intervalo anteriormente cerrado
establece el punto de control de la función callback por cada señal
mueve el cursor al primer atributo
aumenta la base a la potencia exp
crea una imagen espejo vertical reflejando los píxeles alrededor del eje x central mientras los rota 90 grados
el número de épocas de estacamiento de salida en cascada determina el número de épocas que se le permite al entrenamiento continuar sin cambiar el ecm por una fracción de fann_get_cascade_output_change_fraction()
loads ispell data
la función socket_shutdown() permite detener la entrada y salida de toda la información lo predeterminado que está siendo enviada a través del socket
obtiene el nombre del cursor para el párametro result_id dado
devuelve un array asociativo de dos dimensiones con estadísticas del servidor es
esta función invoca un analizador ccl
fires/sets a syncevent object
determina si el punto de código especificado tiene la categoría general ll letra minúscula)
este es un alias de solrdocument::clear()
añade los elementos pasados al inicio de array
cierra una relación abierta previamente con dbplus_open()
obtiene el valor del atributo en el espacio de nombres namespaceuri con el nombre local localname para el nodo actual
crea una nueva imagen con el color de fondo especificado
envía información de estado al servidor de trabajo y a cualquier cliente a la escucha
volcar a la salida una cadena con la representación de un valor interno de zend
si la opción curlopt_returntransfer está activada para uno de los recursos entonces esta función devolverá el contenido del recurso curl en formato string
devuelve el umbral de frecuencia en el que las palabras que no ocurran en por lo menos tantos documentos como este serán ignoradas
devuelve el número de elementos de un hash
verifica el permiso del usuario sobre un archivo
devuelve una cadena que contiene la versión del motor zend ejecutándose actualmente
se usa cuando el objeto es tratado como una matriz para añadir un campo al documento
devuelve si colapsar o no fragmentos contiguos en un único fragmento
la descripción de la función va aquí
esta función devuelve el nombre del modo
devuelve un string de representación de el frame
obtiene o especifica la base de datos que usa la conexión
implementa directo a la memoria formatos de imagen
imprime o guarda una imagen png desde image
la descripción de la función va aquí
simula borrosidad en movimiento
determina si el elemento actual directoryiterator es un fichero normal
establece una función a ser llamada cuando un trabajador necesita enviar datos antes de completar el trabajo
crea una anotación rectangular en la página actual
rellena y dibuja el trazado construido previamente con llamadas a funciones de dibujo como ps_lineto()
extrae todos los ficheros de un archivo tar/zip al disco
quita una imagen de la lista de imágenes
set query alternate q.alt parameter 
convierte variant desde un valor vt_date o similar a una fecha unix
asigna una variable al motor de vistas
empaqueta los argumentos dados a una cadena binaria según el formato dado por format
crea una nueva página y la inserta antes de la página especificada
devuelve el cuerpo del mensaje numerado por msg_number en el buzón actual
remueve uno o más atributos de un dn específico
convierte una fecha juliana a una cadena que contiene la fecha gregoriana con el formato mes/día/año
la funcionalidad de memcached::setbykey() es equivalente a la de memcached::set() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
cierra el trazado y lo contornea
reemplaza los valores de array1 con valores que tienen las mismas claves en cada uno de los siguientes arrays
devuelve el índice de la fila actual del conjunto de resultados almacenado en búfer dado por result
mapea a facet.date.start
establece un parámetro
decodifica data codificada en base64
encripta data con la clave pública key y almacena el resultado en crypted
obtiene el inode del script actual
devuelve una matriz asociativa con información detallada acerca de la fecha dada
devuelve el siguiente par clave-valor en el map indicado despúes de la key específicada
obtiene las propiedades predeterminadas de la clase dada
genera una string de bytes pseudo-aleatorios
este método se ejecuta cuando se utiliza la función count() sobre un objeto que implemente la interfaz countable
este método devuelve un objeto simplexmliterator que contiene sub elementos de el elemento simplexmliterator actual
libera los recursos del sistema y de la base de datos que están asociados con un determinado recurso de sentencias 
esta función termina el cifrado especificado a través del descriptor de cifrado el parámetro td
establece una o más opciones del cliente
constructor privado para evitar la inicialización directa
envía una petición a un servidor imap cyrus
establece el valor gamma de la imagen
activar un evento específico almacenado en el buffer
lleva a cabo el formateo textline y pregunta por las métricas resultantes
devuelve un iterador para la entrada del iterador actual
libera un bloqueo exclusivo que se obtuvo en una clave dada mediante wincache_lock()
comprueba si el objeto referenciado aún existe
devuelve el mime content type para un fichero determinado usando información desde el fichero magic.mime
la descripción de la función va aquí
devuelve los valores de una sola columna de input identificado por la clave de columna column_key
obtener el número de directorios y ficheros encontrados por la expresión glob
abre el archivo especificado por path en el modo de acceso mode con
rellenar la ruta actual usadando la regla even-odd
ejecuta una consulta que no devuelve resultados en una base de datos dada
formatea e imprime todas las filas de la consulta result_id en una tabla html
obtiene el nombre del campo siguiente al campo dado
calcula el factoriala! de a
la descripción de la función va aquí
retorna el nombre de la función con la que esta tarea está asociada por ejemplo la función que llama el trabajador gearman
esta función es similar a variant_cast() excepto que la variante es modificada in situ no se crea una nueva variante
establece la resolución de la imagen
verifica si todos los caracteres en la string entregada text son imprimibles
establece las iteraciones de la imagen
ejecuta una query aql sobre el server y dbpath dados
indica si una palabra ya existe en la sesión actual
la funcionalidad de memcached::touchbykey() es equivalente a la de memcached::touch() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
configura las funciones de los manejadores de elementos
mapea al parámetro stats.field
crea un diccionario usando un fichero pwl
devuelve un resultado con los parámetros de uno o más procedimientos almacenados
devuelve un mesaje claro del error en formato de texto de la última operación curl
obtener el número de línea
devuelve el identificador del tipo de sistema del servidor ftp remoto
establece el punto primario verde de la cromaticidad de la imagen
registra namespaceuri y prefix con el objeto domxpath
rellena un bloque de texto con infomación variable según sus propiedades
devuelve todos los valores del array array e indexa numéricamente el array
devuelve el mensaje de excepción
verifica si se ha indicado al bucle del evento de salir con eventbase::stop()
devuelve una cadena descripción de el último error al conectar
establece el factor de decadencia de quickprop
devuelve el factor por el que el fragmentador de expresiones regulares puede desviar desde el tamaño de fragmeto ideal para acomodar la expresión regular
indica cómo se deben mostrar las páginas
devuelve el color del píxel de un array como valores quantum
comprobará si el fichero rpm es un fichero válido rpm
adquiere una referencia fuerte de un objeto convirtiendo virtualmente la referencia débil en una fuerte
recupera el protocolo mysql
este método devuelve el índice del nodo actual
comprobar si el elemento actual es válido
cierra el apuntador del archivo gz dado
esta función se puede usar para establecer qué módulos deberían registrarse
establece el valor que será devuelto predeterminadamente para las columnas con valor null al extraer una fila
obtiene una lista de los módulos cargados en el servidor apache
devuelve el valor absoluto de una variante
ejecuta una tarea en segundo plano retornando el manejador del trabajo que puede ser usado para obtener el estado de la tarea en ejecución
devuelve str con todos los caracteres alfabéticos convertidos a minúscula
obtiene el tamaño total de la caché
obtiene las instancias registradas del iterador del resultado key()
imprime un texto una línea por debajo de la última línea
inclina el sistema de coordenadas en la dirección x e y por alpha y beta grados respectivamente
devuelve el string de último error
la función cubrid_version() se usa para obtener la versión del módulo de php de cubrid
generalmente este método no se utiliza
añadir un value al final de la cola
devuelve o establece el comportamiento de autocommit del recurso de conexión especificado
la función obtiene información acerca de la entrada definida por su nombre
este método debería usarse para comprobar si es posible una compresicón antes de cargar un archivo phar que contiene ficheros comprimidos
realiza una comprobación de la dificultad de crackear la contraseña proporcionada empleando el diccionario especificado
la función mysqli_thread_id() devuelve el id del hilo de la conexión actual el cual puede ser destruido usando la función mysqli_kill()
coloca el indicador de posición del apuntador al archivo gz dado al comienzo del flujo del archivo
añade definición rra por descripción de archivo
recupera una devolución de subresultados para valores dentro de la faceta dada
advertencia esta función no está documentada actualmente solamente se encuentra disponible la lista de parámetros
devuelve el nombre de ruta absoluto canonizado en el argumento result de la función callback
decodifica el text cifrado en base-64 dado
la función cubrid_affected_rows() se usa para obtener el número de filas afectadas por la última sentencia sql insert delete update 
calcula el número de caracteres los cuales puede ser incluidos dentro de un ancho especificado
mapea a facet.date.end
encuentra si la extensión está cargada
comprueba si un archivo php.ini se ha cargado y recupera su ruta de acceso
la funcionalidad de memcached::getbykey() es equivalente a la de memcached::get() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
establece el index del bit en a
esta función indica si dos nodos son el mismo nodo
consolida una transacción que se inició con sqlsrv_begin_transaction()
define la apariencia de la página para ajustarse a la ventana
finaliza la página
usado para pedir información de estado del servidor gearman que llamará a la función de retorno especificada usando el método gearmanclient::setstatuscallback()
búsqueda inclusive para el primer índice ausente que es igual o mayor que el índice pasado
un cursor típicamente obtiene un lote de objetos de resultados y los almacena localmente
probar con un conjunto de entradas y un conjunto de salidas deseadas
empieza un objeto de texto y establece la posición corriente del texto a 0 0 
invoca la función y pasa sus argumentos como array
esta función envía un sonido bip a la terminal
devuelve la posición del puntero al fichero referido por handle
crea y devuelve un nuevo recurso de evento
recupera un remote_file desde el servidor ftp y lo escribe en el apuntador del archivo especificado
devuelve un array que contiene la siguiente fila del conjunto de resultados de una consulta
elimina el semáforo dado
establece la imagen de pincel para usarla con todas las funciones de dibujo de líneas como imageline() y imagepolygon() cuando se dibuja con los colores especiales img_color_brushed o img_color_styledbrushed
divide el string dado mediante una expresión regular
vincula el nombre dado en address al socket descrito por socket
escribe una cadena en el fichero gzip2 dado
devuelve las estadísticas de cada canal de la imagen
para múltiples campos si se especifica un valor boost válido el valor especificado será multiplicado por el valor boost actual para este campo
devuelve un mensaje de error y el valor sqlcode que representa el motivo por el que el último intento de conexión a la base de datos falló
cierra el documento postscript
esta función verifica si el atributo es un id definido
hace una copia exacta de un objeto imagick
mueve el elemento del iterator interno al siguiente elemento si hay alguno en caso contrario rebobina el iterator interno hasta el inicio
abre una ventana con el tamaño y posición especificados
establece el mínimo de campos de faceta que deberían ser incluidos en la respuesta
esta función realiza una petición parcial por una uri
cancela una transacción en progreso en el recurso de conexión especificado y comienza una nueva transacción
establece la función de parada empleada durante el entrenamiento
establece la longitud máxima de una línea a ser leída
obtiene la categoría de la página
establece el mínimo de épocas de salida
encuentra el siguiente número primo
aplica un filtro digital que mejora la calidad de una imagen con ruido
se invoca para determinar si el enlace de conexión actual tiene habilitada la autoconsigna
establece el método de disposición de una imagen
recupera un nodo especificado por su nodename
exporta el contenido de un lob a un fichero
obtiene el número de pedido de un mapa
debería invocarse una vez por objeto cuando este ha finalizado su ejecución o ya no existen referncia al mismo
guarda x509 en un archivo nombrado por outfilename en un formato pem codificado
los campos se cambian de lugar según el criterio y la dirección de ordenación especificados los campos pueden ser ordenados por valor boost nombre de campo y número de valores
rodea la imagen con un borde del color definido por el objeto gmagickpixel de color de borde o por una cadena de color
este método devuelve si un fichero está comprimido dentro de un archivo phar con la compresión gzip o bzip2
iniciliza una sentencia y devuelve un recurso para usarlo con mysqli_statement::prepare
la descripción de la función va aquí
abre un nuevo fichero zip para su lectura
cierra la imagen image recuperada con la función pdf_open_image()
permite dos modos diferentes de dibujar imágenes de color verdadero
recupera la lista de los flujos registrados disponibles en el sistema en ejecución
la descripción de la función va aquí
crea una nueva instancia rrdcreator
afila una imagen
devuelve el elemento actual
obtiene el nombre del fichero sin ningún tipo de información de la ruta de acceso
devuelve una palabra clave que identifica el tipo de la sentencia de oci8 dada por statement
establece el espacio de color de una imagen
devuelve el identificador de entrada para la primera entrada en el resultado
la función usa la prueba probabilística de miller-rabin para revisar si un número es primo
abre filename y lo procesa línea por línea en busca de etiquetas < meta > en el archivo
bloquea un buffer
reemplaza los atributos y el contenido de un objeto
modifica la prioridad del proceso actual por la cantidad especificada en increment
comprueba si el campo especificado por field de de la fila actual de la sentencia dada por statement es null
establece el punto primario azul de la cromaticidad de la imagen
este método comprueba si el nodo tiene atributos
mueve el puntero de filas interno del resultado de mysql asociado con el identificador de resultado especificado para apuntar al número de fila especificada
devuelve el id real numérico de usuario del proceso actual
busca la primera ocurrencia de needle en haystack y devuelve la porción de haystack
detiene un servicio con nombre
captura una ventana o su área de cliente usando un gestor de ventanas la propiedad hwnd en instancia com 
devuelve una tabla con los términos y la información asociada tal y como fue recibida del servidor en la última función yaz_scan() realizada
la función cubrid_fetch_array() se utiliza para consultar una única fila del resultado de la consulta y lo devuelve como array
devuelve un registro con la estructura sqlca.sqlerrd[0 ..
obtiene el valor actual del texto saliente
establece el nombre de archivo de una imagen en particular en una secuencia
establece el nivel de opacidad de la imagen
recupera una variable de post
reinicializa el evento base
establecer los colores de primer plano y fondo activos
elimina el directory especificado en el servidor ftp
retorna un array conteniendo todos los posibles géneros que pueden ser almacenados en la etiqueta id3 correspondiente
establece/obtiene el idioma actual
devuelve el mensaje de error informix asociado con el error informix más reciente
esta función está obsoleta desde la versión 5 de pdflib use pdf_get_value() con el parámetro major en su lugar
declara el atributo name como de tipo id
determina si el elemento actual directoryiterator es ejecutable
libera todos los bloqueos de tupla mantenidos por este cliente
provoca que el trabajador espere a cualquier actividad desde uno de los servidores de trabajo gearman cuando trabaja en modo entrada/salida no bloqueante
devuelve la respuesta del servidor
añade una nueva página al documento
devuelve la máscara de recorte de la imagen
esta función analiza una url y devuelve un array asociativo que contiene aquellos componentes presentes en la url
establece el valor para uno o más parámetros a ser usado en las siguientes transformaciones con xsltprocessor
cierra la conexión con el servidor frontbase asociado al link_identifier especificado
mueve el iterador al nodo anterior
imprime el glifo de la posición ord del vector de codificación de fuentes de la fuente actual
dada una cadena que contiene una ruta a un archivo o directorio esta función devolverá el último componente de nombre
devuelve si están habilitadas o no las estadísticas
este método devuelve una versión serializada del conjunto en el mismo formato que que puede leer loadfromstring()
calcula el coseno para cada valor de real y devuelve el array resultante
voltea la imagen dada por image usando el modo dado por mode
ordena un array por clave manteniendo la correlación entre la clave y los datos
devuelve el color del borde de la imagen
devuelve un array a partir de array con todas las claves convertidas a minúsculas o mayúsculas
devuelve el nombre del atributo
compara dos rutas de acceso recursivamente path1 y path2
con este método cualquier fichero o url se puede añadir al arcivo tar/zip
este método sustituye xincludes en un objeto domdocument
escribe hasta length bytes desde str en el índice offset desde el principo del fichero
toma el contenido del archivo nombrado por infile y lo encripta usando un cifrado rc2 de 40 bits por lo que sólo lo podrá leer los destinatarios previstos especificados por recipcerts
devuelve el tiempo cuando el contenido de el fichero ha sido cambiado
reemplaza los colores de la imagen usando una paleta hald
avanza el puntero interno del array hasta su último elemento y devuelve su valor
mueve la posición actual a el inicio de la línea siguiente y imprime el texto
libera la conexión al servidor por lo que otras sentencias sql podrían ejecutarse pero deja la sentencia en un estado que la habilita para ser ejecutada otra vez
establece una condición para el almacenamiento en caché automático de sentencias que no contienen las sugerencias sql necesarias para habilitar su almacenamiento en dicha caché
coloca una tabla en la página total o parcialmente
una función de tipo callable usada por la opción de contexto log_getmore al ejecutar una operación get_more
obtiene el valor por omisión del parámetro de una función definida por el usuario o en el método
los cambpo se especifican mediante nombre_campo : bool
recupera la línea actual del fichero
devuelve el dominio predeterminado del nodo
devuelve si el nombre de fichero dado es un nombre de fichero phar válido que será reconocido como un archivo phar por la extensión phar
termina de monitorizar un recurso
devuelve el número de errores encontrados en la configuración del objeto object tidy especificado
establece la función de llamada de retorno que acepta paquetes de datos para una tarea
devuelve un string con el primer caracter de str en minúscula si ese caracter es alfabético
devuelve el nombre del espacio de nombres en el que está definida la clase
obtener el elemento actual del iterator interno
enables an event connect listener object
a diferencia de mongocollection::find() este método solamente devolverá el primer resultado del conjunto de resultados y no una instancia de mongocursor que se pueda recorrer
el destructor
comprueba si la función es varíadica
borra esta colección y elimina sus índices
renombra una entrada definida por su nobre
revise la sección de consultas de este manual para más información sobre distribución de lecturas a secundarios
devuelve el valor negativo de un número
establece el punto primario rojo de la cromaticidad de la imagen
obtiene una copia de un array
es una variación de memcached::setoption() que toma un array de opciones a establecer
recibe un mensaje de un socket
recupera información sobre la utilización de memoria global
divide el left_operand por el right_operand
devuelve las propiedades estáticas
trae el buffer que contiene la información pdf generada
escribe la parte de la cabecera de una archivo bcompiler
devuelve la ruta del directorio actual usado para guardar la información de sesión
use este método si tiene un resultado de comando sin tratar con información de cursor en él
recupera información que hay en caché y meta-datos del almacén de datos de apc
obtener información de resultado
establece el valor para el campo dado
esta función no debe ser llamada
devuelve el numero de columnas para la consulta más reciente en la conexión representada por el parámetro link
obtiene las propiedades no estáticas accesibles del objeto dado por object según el ámbito
devuelve el código de control que fue el último enviado a este proceso de servicio
es idéntica a preg_replace() excepto que sólo devuelve los sujetos posiblemente tranformados donde hubo una coincidencia
devuelve la propiedad nominada de la imagen
ordena un array por sus claves
los gestores de almacenamiento que están disponibles depende de la configuración en tiempo de compilación del complemento de caché de consultas
establece una opción en la sesión del recurso curl
configura un manejador para ser llamado cuando se salga del alcance de una declaración de namespace
transforma el nodo origen en un domdocument aplicando las hojas de estilo dadas por el método xsltprocessor::importstylesheet()
recupera un elemento del registro
desestablece connection
fija el nivel mínimo de severidad de los errores
devuelve el tipo de cursor utilizado en una consulta
comprueba si la propiedad es o no predeterminada
cancela el evento event
envía un comando allo al servidor ftp remoto para reservar espacio para la carga de un archivo
la descripción de la función va aquí
cambia la configuración de los drivers del gestionador de errores y de log
esta función devuelve el índice del nodo actual
obtiene un registro de una base de datos como una matriz indexada
obtiene la combinación del entrelazado de un objeto
la función dngettext() le permite sobrescribir el domain actual en una búsqueda en un mensaje único en plural
descarga una copia de trabajo desde el repositorio en repos a targetpath con la revisión revision
devuelve la metainformación de una fichero dentro de un archivo phar
lee una linea de datos del resultado
obtiene el módulo del left_operand usando modulus
reemplaza la línea actual de ayuda con la procedente de la pila
esta función devuelve una cadena que representa la versión del servidor cubrid
agrega una nueva página al documento
obtiene el número de i-nodo de el objeto filesystem
esta función devuelve el contenido de un documento como un objeto del tipo hw_api_content
devuelve el número de línea en la que el objeto fue instanciado
obtiene la respuesta original de la más reciente petición
esta función comprime la cadena dada utilizando el formato de datos deflate
rellena un área con un sombreado basado en un objeto de sombreado
pone una variable en caché en el almacén de datos sólo si no está ya almacenada
carga un documento xml de un archivo
este método devuelve el elemento seleccionado del árbol de casillas de verificación
establece el multiplicador de peso
es usada para eliminar información almacenada de una etiqueta id3
comprueba si el tipo de variable dado es integer
intenta cambiar el grupo del enlace simbólico filename a group
establece la pendiente de la activación para todas las neuronas de todas las capas ocultas
avanza al siguiente elemento del mapa
abre un fichero y devuelve su descriptor de fichero
devuelve la versión del servidor mysql
define si la anotación se muestra inicialmente abierta
renderiza el objeto gmagickdraw en la imagen actual
devuelve la cadena de título de una etiqueta id3
obtiene una ruta de ubicación xpath del nodo
usada para obtener información acerca de los campos en un resultado de consulta determinado
devuelve un objeto id3v2 para el fichero mpeg
la funcionalidad de memcached::replacebykey() es equivalente a la de memcached::replace() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
quita un object de el almacenamiento
libera un certificado asociado con el recurso x509cert especificado de la memoria
establece el patrón de discontinuidad actual a unidades de b negro y w blanco
comprueba si la posición actual del iterador es válida
devuelve la temperatura de sarprop
devuelve la lista de campos que serán devueltos en la respuesta
devuelve una descripción textual del código de error fdf de error_code
crea un fichero con un nombre de fichero único cuyo permiso de acceso está establecido a 0600 en el directorio especificado
establece el espacio de la palabra para la página
crea una nueva copia de todos los datos de la base de datos
construir un nuevo filteriterator que consiste en pasar un iterator y aplicar varios filtros a este
obtiene el valor actual del espaciado de la palabra
registra el nombre de una función en el servidor de trabajos y especifica la llamada de retorno quer corresponde a esa función
devuelve las flags véase regexiterator::setflags() para una lista de todas las flags disponibles
obtiene el color actual del espacio del trazado
una forma pseudo orientada a objetos de leer un directorio
escapa value para su uso en el contexto implicado por flags
se comporta de manera similar a ifx_connect() pero con dos diferencias considerables
muestra el texto de cadena en la posición indicada.
abre una imagen ccitt en bruto
establece un fichero que contiene una lista personal de palabras
obtiene el número de filas de un resultado
devuelve un código de error devuelto por la última operación de db++
devuelve la lista de todos los usuarios conectados
añade una etiqueta al set de bbcode_container usando tag_rules
después de permanecer inactivo en el servidor por algún tiempo los cursores por omisión mueren
establece el valor actual del inglete limite de la página
determina si el punto de código especificado es un carácter de dígito
devuelve un array asociativo con las estadísticas del servidor
extrae el archivo completo o los ficheros dados en la ruta que se especifique
los archivos phar contienen una rutina de interoperabilidad cargadora en inglés stub escrita en php que se ejecuta cuando el archivo es ejecutado en el propio php mediante inclusión: < ?php include&nbsp 'myphar.phar' ; ? > o por simple ejecución: php myphar.phar
obtiene la combinación de entrelazado de la imagen
busca el último índice ausente incluido que sea igual o menor al índice pasado
ésta es la forma más fácil de obtener uan colección a partir de un objeto de base de datos
dada una cadena que contiene la ruta a un fichero o directorio esta función devolverá la ruta del directorio padre que está a levels niveles del directorio actual
comprueba si un iterador está adjunto o no
aplica un filtro de onda a la imagen
devuelve el factor de umbral del error de paso de sarprop
inicia la medición del tiempo
elimina un directorio
incrusta una secuencia de imágenes para formar una sóla imagen coherente
crea un objeto domdocumenttype vacío
selecciona la base de datos predeterminada para su consulta
la función cubrid_next_result() se utiliza para obtener resultados de la consulta siguiente si varias instrucciones sql se ejecutan y cubrid_exec_query_all y la bandera cubrid_execute() esta establecida como activa
compara uno o más canales de imagen de una imagen con una imagen reconstruida y devuelve la métrica de distorsión especificada
posiciona el cursor en el atributo dado en el espacio de nombres especificado
cierra la conexión a la base ms sql server que está asociada con el identificador de conexión especificado
toma la petición de frima de certificado csr en inglés) representada por csr y la almacena como texto ascii blindado en el archivo nombrado por outfilename
selecciona una base de datos y ejecuta una consulta sobre ella
este método devuelve true si phar.readonly es 0 y el archivo phar real del disco no es de sólo lectura
desliza un extremo de una imagen a lo largo del eje x o y creando un paralelogramo
devuelve un resultado con los procedimientos almacenados registrados en una base de datos
envía una solicitud para ejecutar una sentencia preparada con parámetros dados y espera el resultado
descomprime datos codificados en raw/gzip/zlib
si y sólo si el argumento es true entonces en lugar de emitir advertencias y devolver un valor especial indicando error cuando la biblioteca unrar encuentre un error una excepción de tipo rarexception será lanzada
esta función es similar a la función ps_stringwidth() excepto que devuelve un array de dimensiones que contiene el ancho el ascendente y el descendente del texto
este método se puede usar para preguntar por el ancho alto tamaño y formato de la imagen sin leer toda la imagen de la memoria
devuelve una cadena que representa la versión de la biblioteca cliente mysql
convierte el carácter especificado a unicode
fusiona un documento con otro
devuelve el valor de un atributo por su nombre y uri del namespace o una cadena vacia si el atributo no existe o no está posicionado en el eleménto
lee un fichero y lo escribe en el búfer de salida
recupera la información de la asignación de memoria compartida de apc
comprueba si un objeto es una instancia de la clase
devuelve el valor del elemento cuyo índice es index basado en 0 
devuelve la información sobre el certificado x509cert proporcionado incluyendo los campos tales como subject name issuer name purposes fechas valid from y valid to etc
inserta la cadena data en la posición offset
esta función es muy similar a la función px_get_record() pero utiliza internamente un enfoque diferente para recuperar los datos
selecciona un documento pdf diferente para desplegar los resultados de formulario desde el formulario en donde se originaron
devuelve el tamaño asociado con el objeto gmagick como una matriz con las claves columns columnas y rows filas 
devuelve una descripción del sistema operativo en que se está ejecutando php
devuelve las opciones de cliente establecidas internamente
recupera un campo según su nombre
forma concisa de obtener una colección con un nombre separado por el carácter punto
devuelve el nombre del paquete de imagemagick
obtiene el texto de la última excepción lanzada o la razón del fallo de la llamada a la función
crea una nueva instancia de la clase hw_api_object
elimina una región de una imagen y colapsa la imagen para ocupar la porción eliminada
recupera el canal de color especificado como un número de coma flotante entre 0 y 1
no hace nada sólo recorre el ciclo de peticiones entero
devuelve información sobre la creación e iteración del cursor
imprime el texto text con la fuente actual
igual que datetime::settime() excepto que trabaja con datetimeimmutable
define la apariencia de la página para ajustarse al ancho de la delimitación de la caja y establece la posición izquierda de la página para el valor de left
desasigna un color previamente asignado con imagecolorallocate() o imagecolorallocatealpha()
decodifica la información de sesión serializada proporcianda en $data y rellena la variable superglobal $_session con el resultado
crea una nueva instancia de harudestination
interpreta una plantilla y muestra el resultado inmediatamente
devuelve un identificador de imagen que representa la imagen obtenida desde el nombre de fichero dado
inclina una fuente dada
configura una opción en un intérprete xml
establece el operador de composción de una imagen útil para especificar cómo componer la miniatura de la imagen cuando se usa el método imagick::montageimage()
obtiene la cadena de error de cualquier error bzip2 devuelto por el puntero dado
ordena las entradas por valores usando un algoritmo de ordenado natural
inserta un objeto nuevo
esta función trae todos los elementos descendientes con unos localname y namespaceuri dados
esta función es parecida a hebrev() con la diferencia que esta convierte líneas nuevas (\n) a < br > \n
revierte todos los añadidos/eliminados hechos en el índice desde el último envío
la descripción de la función va aquí
es idéntico a la función:
esta funcion devolverá una fecha unix correspondiente a la fecha juliana dada en jday o false si jday no está dentro de la época unix años gregorianos entre 1970 y 2037 o 2440588 < = jday < = 2465342
ajusta el factor de escala a aplicar en las direcciones horizontal y vertical al espacio de coordenadas actual
devuelve un array vacío
añade una etiqueta a una imagen
devuelve un objeto con propiedades que corresponden a la fila recuperada y mueve el puntero interno hacia delante
en ciertas situaciones podría ser necesario eliminar un cursor del servidor
compara value1 con value2
devuelve una representación textual de esta entrada
obtiene el conteo de la población
devuelve el color de fondo de la imagen
prepara una declaración para su ejecución
la función cubrid_col_get() se usa para obtener el contenido de los elementos del atributo del tipo de colección conjunto multiconjunto secuencia que se solicitó como array
devuelve el límite de fallo de bit empleado durante un entrenamiento
transforma el nodo origen a una uri aplicando la hoja de estilos dada por el metodo xsltprocessor::importstylesheet()
este método es una interfaz flexible que permite crear referencias a bases de datos vea mongodbref 
obtiene el nombre del fichero desde donde fue creada la excepción
devuelve el nombre de la columna especificada por el parámetro column_number
solicita monitorizar un simple archivo
exporta el desafío desde una clave pública y desafío firmados y codificados
devuelve el número de campos del documento
devuelve el número de filas en el conjunto de resultados
borra un fichero dentro de un archivo
exporta una extensión reflexionada devolvíendola en forma de string
busca la última ocurrencia de needle en haystack y devuelve la porción de haystack
escala la lista de núcleos («kernels») dada por la cantidad proporcionada con o sin normalización de la suma de los valores de los núcleos según las banderas dadas)
cierra el gestor de cifrado especificado
establece el conjunto de caracteres predeterminado para la conexión actual
obetiene el retraso de la imagen
crea una imagen espejo horizontal reflejando los píxeles alrededor del eje y central
obtiene el número de días despúes del 21 de marzo en el cuál cae pascua para un año dado
mientras que application.dispatcher.throwexception esté activado también se puede llamar al método yaf_dispatcher::throwexception true () para habilitarlo), yaf lanzará una excepción en lugar de emitir un error cuando ocurren errores
establece el array de pendientes de activación de candidatas en cascada
la función cubrid_execute() se usa para ejecutar la sentencia sql dada
obtiene una linea desde el gestor dado
carga una imagen raw
obtiene la uri del espacio de nombres del nodo basado en prefix
devuelve un string quoted printable creado de acuerdo con su definición en rfc2045 sección 6.7
cancela una consulta asíncrona enviada con pg_send_query() pg_send_query_params() o pg_send_execute()
habilita las codificaciones coreanas
esta función restablece el mecanismo de reescritura de urls y elimina todas las variables de reescritura establecidas previamente por la función output_add_rewrite_var() o por el mecanismo de sesiones si session.use_trans_sid se estableció en session_start()
la función geoip_country_name_by_name() devolverá el nombre del país correspondiente a un equipo o dirección ip
crea un efecto de botón tridimensional simulado aclarando y oscureciendo los bordes de la imagen
mapea a facet.date.gap
devuelve el contenido de una celda de un conjunto de resultados msql
crea un nuevo objeto datetimeimmutable con la marca de tiempo modificada
recupera los argumentos de la acción especificada
ecualiza el histograma de la imagen
la función cubrid_drop() se usa para borrar una instancia de la base de datos usando el oid de la instancia
idéntica a la función date() excepto que la fecha/hora devuelta es en greenwich mean time gmt 
recupera los datos especificados por key desde la base de datos indicada con handle
lee una línea de el fichero e interpreta este de acuerdo a el format especificado como es descrito en la documentación de sprintf()
carga una imagen png
la descripción de la función va aquí
devuelve el mensaje de la última comprobación realizada
lee la información de la tupla previa de la relación dada por relation la hace la tupla actual y la devuelve como una matriz asociativa en tuple
retorna la carga de trabajo
comprueba la validez de una fecha formada por los argumentos
lista las columnas y los privilegios asociados para la tabla dada
busca un elemento en el árbol de casillas de verificación mediante los datos de elemento
escapará correctamente la cadena especificada mediante item para ser usada en una sentencia sql sqlite
computa la diferencia entre arrays con una comprobación de indices adicional compara la información mediante una función de llamada de retorno
usada después de modificar la función de gestión de errores usando set_error_handler() para revertir al gestor de errores previo el cual puede ser el incorporado o una función definida por el usuario)
obtiene la función de activación para la neurona número the neuron en la capa número layer contando la capa de entrada como capa 0
la funcionalidad de memcached::deletemultibykey() es equivalente a la de memcached::deletemulti() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
verifica que la firma signature es correcta para la información data especificada usando la clave pública asociada con pub_key_id
lee los bytes hasta el parámetro length desde el apuntador al archivo gz dado
devuelve si la entrada actual del iterador es válida
devuelve un identificador de imagen que representa la imagen obtenida desde el nombre de fichero dado
copia un recurso de curl manteniendo las mismas preferencias
decodifica un campo de la cabecera mime
al igual que con el concepto del sistema de ficheros de unix de montar dispositivos externos en rutas dentro del árbol de directorios phar::mount() siempre se refiere a ficheros y directorios externos como si estuvieran dentro de un archivo phar
si el objeto fuera de la base de datos se actualizará
los archivos tar/zip no ejecutables no pueden tener un alias por lo que este método simplemente lanza una excepción
analiza los colores dentro de una secuencia de imágenes y elige un número fijo de colores que representan la imagen
añade una tarea de alta prioridad para ser ejecutada en paralelo junto con otras tareas
la funcionalidad de memcached::casbykey() es equivalente a la de memcached::cas() con la excepción de que server_key se puede utilizar para que la clave dada por key corresponda a un servidor específico
por defecto yaf_router usa un yaf_route_static como su ruta predeterminada
carga un archivo chdb mapeándolo en memoria
este método devuelve una versión serializada del hash en el mismo formato que loadfromstring() puede leer
comprueba la información sobre el buzón actual
devuelve un array con los tamaños de clave soportados por el algoritmo especificado si devuelve un array vacío es porque todos los tamaños de clave entre 1 y mcrypt_module_get_algo_key_size() son soportados por el algoritmo
conecta el último punto con el primer punto de un trazado
obtiene una matriz de todas las funciones definidas
esta función crea un blob escribe un fichero al completo en él lo cierra y devuelve el id del blob asignado
obtiene la transformación actual de la matriz de la página
el primer prototipo es cuando se enlazó con libmcrypt 2.2.x el segundo cuando se enlazó con libmcrypt 2.4.x o superiores
define el alto de la página
analiza la cadena de dirección como está definido en rfc2822 y para cada dirección
cambia los permisos de un fichero o directorio
devuelve el punto primario rojo de la cromaticidad como un array con las claves x e y
comprueba si el parámetro permite null
trunca un lob
clona una función
permite controlar el brillo la saturación y el tono de una imagen
recuperar información sobre los parámetros a procedimientos
establece la calidad de compresión predeterminda del objeto
comprueba si el método especificado está o no definido en la clase
se comporta como current() con una diferencia
define el contenedor en el que mostrar el pdf resultante definido en fdf_save_file()
devuelve un objeto que contiene la siguiente fila del conjunto de resultados de una consulta
devuelve el código de error de la función de la sentencia invocada más recientemente que tuvo éxito o falló
devuelve las posiciones de la neurona inicial
determina si el punto de código especificado es un carácter de control
devuelve el día de la semana
calcula la suma de vectores de real1 desde real0 y devuelve el vector resultante
devuelve las funciones definidas en una extensión
crea una nueva instancia rrdupdater
establece una conexión con un servidor ldap en un determinado hostname and port
recupera el valor del canal de color especificado como un número de punto flotante entre 0 y 1
recupera información sobre el uso de memoria caché del usuario
establece el máximo de profundidad permitida
calcula el resto de la división entera de n por d
dibuja una elipse con centro en las coordenadas especificadas en image
devuelve el color del borde de la imagen
envoltura para comandos de listas desordenadas
establece el nombre de la tabla de una base de datos paradox que se creó con la función px_create_fp()
pregunta sobre la información de un matchbox en la página actual
véase la sección de consultas de este manual para obtener información sobre lecturas distribuidas en secundarios
cambiará el nombre de relation al dado por name
define o redefine la pareja de colores dada para obtener los colores de primer plano y fondo dados
gobtiene el acl para un buzón dado
convierte un número gmp a la representación de cadena en base base
intenta renombrar oldname a newname moviéndolo a otro directorio si fuera necesario
escribirá una cadena en un segmento de memoria compartida
añade una celda a una tabla nueva o ya existente
crea una red neuronal de retropropagación estándar que no está completamente conectada empleando un array con tamaños de capas
esta función devuelve el número de filas que se vieron afectadas por la última consulta insert update o delete que se ejecutó en el contexto de la transacción especificada
el método xpath busca el nodo simplexml para los hijos que cumplan la path xpath
simula un dibujo a carboncillo
obtiene el estado de un trabajo en segundo plano para un manejador de trabajo dado
permite deshabilitar errores libxml estándares y habilitar el manejo de errores del usuario
encripta data con la clave privada key y almacena el resultado en crypted
reemplaza uno o más atributos de un dn específico
mueve el puntero de fila interno del resultado msql asociado con el identificador de consulta especificado para indicar el número de fila especificado
dibuja texto en la imagen
devuelve el índice de tamaño asociado con el objeto imagick
desliza un extremo de una imagen a lo largo del eje x o y creando un paralelogramo
convierte el documento correcto xml del fichero dado en un objeto
todas los string que se envíen a la base de datos deben estar en utf-8
determina si el punto de código especificado es un carácter imprimible
esta función encripta datos
convertir una cadena 8bit a una cadena base64 de acuerdo al documento rfc2045 sección 6.8
cierra el archivo pdf generado y libera todos los recursos del documento relacionados
crea una estructura de datos de entrenamiento vacía
elimina cualquier función que estuviese registrada con anterioridad asegurando que que no se envían más trabajos a ese trabajador
crea una imagen espejo vertical reflejando los píxeles alrededor del eje x central
establece la pendiente de activación en la neurona número neuron de la capa número layer contando la capa de entrada como capa 0
la función cubrid_set_drop() se usa para borrar un elemento que se solicitó desde el atributo del tipo de conjunto dado conjunto multiconjunto de la base de datos
retorna un string con el último error encontrado
guardar los datos del modelo en un archivo para su uso posterior
la descripción de la función va aquí
devuelve un booleano que indica si habilitar o no la generación de trozos remarcados en la respuesta de la consulta
elimina el fichero directorio o enlace simbólico en el path del directorio de trabajo
calcula el tamaño del búfer que debería ser asignado para recibir datos subordinados
devuelve el nombre de la tabla donde se encuentra el campo especificado
mueve el puntero interno a fila del resultado frontbase asociado con el identificador de resultado indicado para que apunte el número de fila especificado
la función session_unset() libera todas las variables de sesión actualmente registradas
establece una llamada de retorno para la división de lectura/escritura definida por el usuario
almacena en un array las longitudes de cada columna de resultado de la última fila devuelta por fbsql_fetch_row() fbsql_fetch_array() y fbsql_fetch_object()
establece una función de llamada de retorno usada para obtener el estado actualizado de un trabajador.
envía una petición de actualización xml sin formato al servidor
este método se emplea para establecer una clase propia que será utilizada cuando se invoque a splfileinfo::getfileinfo() y splfileinfo::getpathinfo()
determina la representación de un carácter para un dígito específico en la base especificada
vincula un parámetro a un stored procedure mssql local o remoto 
define o redefine el color dado
devuelve el código de error asociado al objeto alnzado
añade un atributo a el objeto
sets maximum request body size
devuelve los encabezados soap de la última petición
recupera un contenido existente
esta función devuelve hasta len bytes de un blob que se haya abierto para lectura mediante ibase_blob_open()
ejecuta la expresión xpath dada por expression
devuelve las cabeceras de todos los mensajes de un buzón
obtiene el nodo actual de la lista doblemente enlazada
la descripción de la función va aquí
devuelve los nombres y valores de todas las constantes definidas actualmente
devolverá el hash de un string utilizando el algoritmo estándar basado en des de unix o algoritmos alternativos que puedan estar disponibles en el sistema
toma el tamaño de bloque del algoritmo especificado
determina si el punto de código especificado es un carácter base
parchea un file con un patch binario y almacena el resultado en un archivo dest
revela si la medición comenzó
obtener el manejo de flags como está establecido en filesystemiterator::__construct() o filesystemiterator::setflags()
obtiene el ancho actual de la línea
el array de funciones de activación en cascada es un array con las diferentes funciones de activación empleadas por las cadidatas
obtiene el nombre de host estándar para el equipo local
cambia el usuario logueado en la conexión especificada
devuelve el número de registros afectado por la última sentencia sql de escritura
llama a la función callback del token registrado que fue establecido con oauthprovider::tokenhandler()
prácticamente todo lo que no son operaciones crud se puede realizar con una orden de base de datos
emplea el analizador de fechas normal y establece un objeto dateinterval desde las partes relativas de la cadena analizada
devuelve parte de la cadena pajar desde la primera coincidencia de la cadena aguja hasta el final de pajar incluyendo la aguja 
devuelve la parte coincidente de una expresión regular multibyte
devuelve un arreglo asociativo con los nombres de las campos como llave y sus tipos de dato como valores para la consulta asociada con result_id
prepara una sentencia sql para su ejecución
la descripción de la función va aquí
obtiene un objeto reflectionproperty para una propiedad de una clase
vuelve al directorio padre
establece la función de activación para la capa de salida
comprueba si existe o no un índice
puede usarse para ordenar varios arrays al mismo tiempo o un array multidimensional por una o más dimensiones
devuelve un array de php desde el yaf_config_ini
busca en subject coincidencias de pattern y las reemplaza con replacement
obtiene los permisos del fichero
establece el parámetro linejoin para especificar la forma de las esquinas del trazado que está contorneado
usado para serialización personalizada
crea una nueva conexión e inicializa sus parámetros
establece el iterador imagick para la primera imagen
elimina una variable con la clave dada por variable_key y libera la memoria ocupada
inicializa la funcionalidad de color en ncurses
esta función es un alias de: finfo_buffer()
encripta la información dada con el método y la clave dados y devuelve una cadena codificada en bruto o mediante base64
establece cambia el valor de la propiedad
devuelve un código de error correspondiente a la última llamada a una función interbase
retorna el tamaño de la carga de trabajo datos que el trabajador tiene que procesar en bytes
convierte una cadena de texto lógica a una visual
devuelve un array de errores de la función de la sentencia invocada más receientemente que tuvo éxito o falló
devuelve las cabeceras de respuesta sin tratar enviadas al servidor solr 
destruye un evento específico almacenado en el buffer y libera todos los recursos asociados
devuelve el campo desde el cuál los términos son recuperados
recodifica el string string según la petción de recodificación request
escanea a empezando con el bit de start hacia los bits mas significantes hasta que el primero bit establecido es encontrado
añade un comentario a una imagen
obtiene una línea del fichero la cual está en formato csv y devuelve un array que contiene los campos leídos
establece la apariencia de un campo es decir el valor de la clave /ap
si se dan tres argumentos esta función devuelve una copia de str donde todas las apariciones de cada carácter byte simple en from han sido convertidas al carácter correspondiente en to es decir todas las apariciones de $from[$n] han sido reemplazadas con $to[$n] donde $n es un índice válido en ambos argumentos
encuentra la última aparición de un needle dentro de un haystack
comprueba si las conexiones a los servidores de memcache son conexiones persistentes
analiza los colores dentro de una imagen de referencia y elige un número fijo de colores que representan la imagen
mostrar una imagen en la página
obtiene el conjunto de grupos del proceso actual
escribe una imagen al nombre de archivo especificado
imprime el texto en la posición actual de la página
calcula el ancho de un glifo en puntos si fue impreso con la fuente y el tamaño de fuente dados
este método se puede usar para preguntar por el ancho alto tamaño y formato de la imagen sin leer toda la imagen de la memoria
obtiene la indexación del byte actual en un intérprete xml dado
esta es una manera alternativa y más sencilla de usar yaf_view_simple::assign()
devuelve parte del string haystack iniciando desde e incluyendo la primera aparición de needle aguja hasta el final del haystack pajar 
devuelve una cadena que contiene una representación binaria del argumento number
verifica si el cliente se ha desconectado
set event listeners error callback
escribe el contenido de un string a el fichero
copia una tabla a un array
imprime texto en la posición actual
al enviar más de una instrucción sql en el servidor o ejecutar un procedimiento almacenado con varios resultados causará que el servidor devuelva varios conjuntos de resultados
devuelve la resolución como una matriz
recupera todas las anclas de destino de un objeto
este constructor permite la creación de objectos soapserver en modo wsdl o modo no-wsdl
obtiene la ruta y nombre de el fichero actual
calcula el resumen criptográfico md5 del archivo especificado por el parámetro filename usando el algoritmo de resumen de mensajes md5 de rsa data security inc
devuelve el último código de error de la llamada más reciente a una función mysqli que podrá haberse ejecutado con éxito o haber fallado
establece una función a ser llamada cuando un trabajador envía un warning
evita que las llamadas puedan modificar un buffer de eventos
crea un documento xml desde la representación dom
añade un mensaje de cadena dado por message al buzón especificado por mailbox
une tres archivos en uno y almacena el resultado en un archivo dest
suma left a right usando las siguientes reglas tomadas de la biblioteca msdn que se corresponden a aquellas de visual basic: reglas de adición de variantes si entonces ambas expresiones son de tipo cadena concatenación una expresión es de tipo cadena y la otra un carácter adición una expresión es numérica y la otra es una cadena adición ambas expresiones son numéricas adición alguna expresión es null se devuelve null ambas expresiones están vacías se devuelve un subtipo integer
devuelve el número máximo de bytes requeridos para mostrar una columna de un bloque de resultados
esta función es similar a domdocument::getelementsbytagname pero busca un elemento con el id dado
establece el motor de peticiones que estará enviando las peticiones http
este método devuelve true si el archivo tar/zip archive del disco no es de sólo lectura
calcula la tangente hiperbólica para cada valor de real y devuelve el array resultante
establece el error de umbral para dibujar curvas cúbicas bezier
esta función es idéntica al operador de comillas invertidas
retorna el id correspondiente a un género
envía la consulta dada por query al servidor ingres
asigna valores al motor de vistas después se pueden acceder a los valores directamente mediante su nombre en la plantilla
comprueba si el índice existe
canoniza nodos a un fichero
añade una cantidad dada de núcleo de convolución unidad al núcleo («kernel») preescalado y normalizado dado
devuelve el servicio de internet asociado con port para el protocol especificado de acuerdo a /etc/services
obtiene el método de disposición de la imagen
registra un complemento véase yaf_plugin_abstract
obtiene el mensaje de error mnogosearch
obtiene el nombre del módulo del controlador
obtiene el nombre simbólico para un valor de token php
pregunta información detallada acerca de una fuente cargada
pone una restricción a la relación relation dada
recupera una varible de cookie
unserializes a bson document i.e
elimina todos los documentos que coincidan con cualquiera de las consultas
establece la orientación de la imagen
cierra la conexión a la base de datos
asigna el evento bevent a la base event_base
obtiene la distancia de la línea de base de las letras en mayúsculas
recupera el último error de libxml
este método comprueba si una entrada con la clave proporcionada existe en el
la descripción de la función va aquí
obtiene la información del certificado de autoridad el cual incluye los ca_path y ca_info establecidos por oauth::setcapath()
devuelve la secuencia de elementos del array array tal y como se especifica en los parámetros offset y length
ejecuta una tarea de baja prioridad y retorna un string con el resultado
esta función devuelve el string input rellenado por la izquierda la derecha o en ambos lados hasta la longitud especificada
finaliza una página y aplica varias opciones
establece el búfer de lectura
devuelve la cantidad de canales de un archivo mpeg
aplica la rotación especificada al espacio de coordenadas actual
define la rotación del ángulo de la página
devuelve el logaritmo en base 10 de arg
ejecuta una consulta y devuelve un único resultado
es utilizada para tomar el nombre del cifrador especificado
construye un objeto arrayiterator
se invoca para determinar el conjunto de caracteres que está usando el cliente ingres desde ii_charsetxx donce xx es el código de instalación)
obtiene y ordena números de mensajes según los parámetros dados
devuelve todos los campos facet
búsqueda global para el último índice presente que es igual o menor que el índice pasado
vincular el array de php var_array al parámetro de sustitución de oracle name el cual apunta a un array de oracle pl/sql
posiciona el cursor en el siguiente atributo si se encuentra en un atributo o lo mueve al primer atributo si se encuentrae un eleménto
devuelve true si el archivo nombrado por filename fue subido mediante http post
el iterador a la siguiente entrada
devuelve la representación en forma de texto de este objeto
vincula un parámetro a una variable de sentencia
calcula e elevado a la potencia de cada valor de real
especifica el número máximo de filas a devolver en el resultado
libera el semáforo si actualmente está adquirido por el proceso llamador de otro modo se generará una advertencia
borra un objeto pdflib y libera todos los recursos internos
instancia un objeto de la clase sqlite3 y abre una conexión a una base de datos de sqlite
lee length bytes desde la posición actual del puntero interno del lob
esta opción permite que mongos envíe resultados de consultas parciales si un trozo está inaccesible
comprueba si el parámetro fue declarado como un parámetro variádico
lee len bytes del contenido dentro del buffer especificado
especifica un carácter de sustitución cuando la codificación de caracteres de entrada es inválida o el código del carácter no existe en la codificación de caracteres de salida
devuelve un array que contiene el estado de todos los servidores de memcache disponibles
esta función almacena los datos obtenidos desde un consulta ejecutada usando ingres_query() en un array asociativo usando los nombres de los campos como claves
empiza una nueva página
obtiene la longitud máxima de línea establecida por splfileobject::setmaxlinelen()
devuelve la raíz cuadrada de arg
este es un alias de solrinputdocument::clear
devuelve el número de campos de los resultados devueltos por el servidor ingres después de una llamada a ingres_query()
devuelve el número de filas de un conjunto de resultados
devuelve la decoración aplicada cuando se anota texto
cierra la conexión a una base de datos sybase que se ha asociado al identificador de conexión link_identifier
añade una palabra a una lista personal de palabras del diccionario dado
cambia el servlet especificado a un nuevo valor
crea un objeto textflow o añade texto y opciones explícitas al textflow existente
verifica si val no es un numero como por ejemplo el resultado de acos 1.01 
obtiene la información http sobre la última respuesta
devuelve el modo de operación véase regexiterator::setmode() para una lista de todos los modos de operación
crea una nueva instancia de la clase pasando los argumentos al constructor de la clase
devuelve el número máximo de tokens a analizar en cada campo del documento que no esté almacenado con soporte termvector
ejecuta una consulta preparada por ibase_prepare()
devuelve las banderas del campo especificado
devuelve el índice de la imagen activa actual dentro del objeto gmagick
obtiene el carácter delimitador y el circundante usados para analizar campos csv
la descripción de la función va aquí
devuelve una cadena de error limpia del código de error dado
libera la memoria asociada a query_identifier
es como imagick::thresholdimage() excepto que fuerza a todos los píxeles por encima del umbral a ser blancos dejando todos los píxeles por debajo del umbral sin cambios
bloquea o desbloquea el fichero de la misma manera portable que flock()
registra el nombre de una función en el servidor de trabajo con un tiempo máximo de espera opcional
obtener todas las categorías del mismo nivel que la actual
obtiene información sobre los buzones suscritos
habilita la incorporación de fuentes chinas simplificadas
llena un array con el valor del parámetro value usando los valores del array keys como claves
comprueba si la variable dada es numérica
lee todos los valores del atributo en la entrada en el resultado
devuelve una lista de las funciones definidas en el objecto soapserver
este es el único método estático de la clase sdo_das_xml
devuelve la posición numérica de la primera ocurrencia de needle en el string haystack
genera un número aleatorio
activa o desactiva la autoconsigna de las modificaciones de la base de datos
combina una o más imágenes en una sóla imagen
comprueba si name0 es un alias de name1
comprueba si el método es privado
solamente debería usarse para almacenar información personalizada en un fichero que con pueda ser representado con la información almacenada existente con un fichero
devuelve la información del estado de un fichero en el argumento result de callback
obtiene la información de inclued
anexa un rectángulo a la ruta actual
lee el código de bytes de una función existente de php y lo escribe al gestor de archivo abierto
crea un objeto solr
aplica un filtro de efecto especial que simula una pintura al óleo
establecer las flags que cambian el comportamiento de la clase arrayobject
dibuja un polígono usando el contorno el ancho del contorno y el color o textura de relleno actuales usando la matriz de coordenadas especificadas
obtiene el momento del último cambio del i-nodo de un archivo
genera una representación yaml de data proporcionada en el filename
abre una conexión con la base de datos de ingres dada por database
lista las tablas en la database especificada
crea un patrón basado en un sombreado el cual ha de ser creado antes con la función ps_shading()
verifica si una clave específica existe dentro de un segmento de memoria compartida
la función cubrid_set_autocommit() es usada para establecer el modo de confirmación automática de la conexión de base de datos actual
la función yaz_record() inspecciona un registro del conjunto resultado actual que está en la posición especificada por el parámetro pos
marca un método como accesible
especifica el término de donde empezar
solicita el monitoreo a un directorio y a sus archivos
devuelve los comentarios de documentación de una función
calcula el ancho de una cadena en puntos si fue impresa con la fuente y tamaño de fuente dados
se comporta como el carp::croak de perl
extrae el nodo superior de la cola
obtiene el valor gamma de la imagen
inicia la definición de un glifo para una fuente type 3
devuelve un objeto en el que cada atributo representa una columna devuelta de una fila extraida del bloque de resultados
cancela un grupo de peticiones especificadas por el recurso de grupo de peticiones grp
la descripción de la función va aquí
funciona de forma similar a msql_connect() con dos claras diferencias
comparar value1 con value2
devuelve el último mensaje de error del servidor ms-sql
yaf_route_supervar es similar a yaf_route_static con la diferencia de que yaf_route_supervar buscará información de ruta en la cadena de consulta y el parámetro supervar_name es la clave
es final lo que significa que no puede ser sobrescrito
divide un conjunto enlazado de núcleos («kernels») y devuelve un array de imagickkernels
establece el desplazamiento del error de paso de sarprop
este método devuelve el elemento actual como un objeto simplexmliterator o null
convierte el parámetro dado por mime_encoded_text en utf-8
define una cookie para ser enviada junto con el resto de las cabeceras de http
sigue y firma las re-direcciones automáticamente el cual está en activado por omisión
cambia el directorio actual de php a directorio
a diferencia de strlen() iconv_strlen() cuenta el número de veces que aparece cierta secuencia de bytes str de acuerdo a la codificación de caraceters especificada resultado que no es necesariamente igual a la longitud del string en bytes
crea una imagen espejo horizontal reflejando los píxeles sobre el eje y central mientras que los rota 270 grados
permite implementar un filtro propio sobre cualquier flujo registrado usado con las demás funciones de sistema de archivos como fopen() fread() etc.)
crea una nueva instancia rrdgraph
ejecuta una sentencia sql en una única llamada a función devolviendo el conjunto de resultados si los hay que devuelve la sentencia como un objeto pdostatement
obtiene el tamaño de la imagen
aunque esta función puede manejar fechas que se remontan hasta el año 1 3761 a.c. tal uso puede no ser significativo
determina si el punto de código especificado es un carácter alfanumérico letra o dígito)
devuelve el último error de la biblioteca openssl
añade un borde tridimensional simulado alrededor de la imagen
devuelve lo mismo que cuando se ejecuta time() al crear el id
se hace corresponder el carácter dado con su equivalente de letra titular
no opera nada a hacer
calcular los parámetros de escala de entradas y salidas para un uso futuro basados en datos de entrenamiento
elimina el perfil con nombre de la imagen y lo devuelve
carga un modelo 3d desde un archivo basado en disco o desde un archivo virtual
escribe una imagen al nombre de fichero especificado
devuelve el índice del conjunto de resultados completo para las consultas donde el conjunto de documentos devueltos debería comenzar
obtiene el valor de la clave /status
establece el color actual del relleno y del contorno a los valores rgb proporcionados
esta función activará el almacenamiento en búfer de la salida
devuelve el valor en el índice index específicado
expande todos los enlaces simbólicos y resuelve las referencias de caracteres /./, /../ y / extra en la ruta de entrada dada por path y devuelve el nombre de la ruta absoluta canonizado
calcula el siguiente entero mayor para cada valor de real y devuelve el array resultante
establece la función de activación para todas las capas ocultas
guarda la consulta de bases de datos rrd en el archivo de imagen definido por el método rrdgraph::__construct() y devuelve la información detallada acerca de gráfico generado si - se utiliza como nombre de archivo de imagen los datos de la imagen también se devolverán en el array de resultados
obtiene el prefijo del espacio de nombres del nodo basándoes en la uri del espacio de nombres
especifica la sintaxis de registro preferida para la recuperación
establece el autor de la respuesta usado para preparar la respuesta de solr
devuelve el par clave/valor actual de un array y avanza el cursor del array
crea un nuevo objeto desde el string content
llamado para cada elemento para comprobar si tiene hijos o no
elimina el atributo oldnode del elemento
carga una fuente para su uso posterior
devuelve un identificador de conexión necesario para la mayoría de las operaciones de oci8
establece el color de fondo por omisión del objeto
trata array como si fuera una pila y coloca la variable que se le proporciona al final del array
devuelve un resultado con las llaves primarias de una tabla
esta función ordena las claves de las entradas usando una función de compración suministrada por el usuario
envía una advertencia para este trabajo mientras está en ejecución
construye una clase reflectionparameter
devuelve un array asociativo con las propiedades de los campos sql de informix de cada columna de la consulta
devuelve la versión actual de timezonedb
devuelve el tiempo en milisegundos permitido para que la consulta finalice
escribe información de sesión al almacenamiento de sesiones
dibuja un punto usando el color de contorno y el grosor de contorno actuales en las coordenadas especificadas
obtener el índice de conexión empleado al crear la red
obtiene información acerca de la versión y capacidades de la biblioteca gd instalada
devuelve el último error que ocurrión en este agente
ésta función codifica el string data a utf-8 y devuelve una versión codificada
esta función devuelve true si el modo especificado es para ser utilizado con algoritmos por bloques o de lo contrario devuelve false
es similar a msql_fetch_array() con una diferencia - un objeto es devuelto en vez de un array
borra los registros de una tabla especificada por las claves y los valores en assoc_array
establecer las conexiones de la red
este método es una versión estática de closure::bindto()
este constructor crea un nuevo objetos quickhashintset
reemplaza la tupla old con la información de la dada por new sólo si old coincide completamente con una tupla dentro de relation
devuelve el valor redondeado de val con la precision especificada número de dígitos después del punto decimal)
toma el tamaño del iv perteneciente a una combinación específica de los parámetros cipher / mode
arremolina los píxeles desde el centro de la imagen donde los grados indican el alcance del arco a través del cuál cada píxel es movido
la función cubrid_new_glo() se usa para crear una instancia glo en la clase solicitada clase glo 
establece el comentario de una entrada definido por su nombre
establece el punto actual para la impresión de gráficos
envía un message del tipo msgtype debe ser mayor que 0 a la cola de mensajes especificada por queue
establece el parámetro flatness
envía la señal sig al proceso con el identificador de proceso pid
devuelve información de la extensión a la que pertenece la función
mueve el apuntador interno a la línea específicada en el fichero
retorna el último código de retorno de gearman
obtiene el servicio con el tipo o false si no condigue el dispositivo
se llama cuando el siguiente elemento está disponible
actualiza el contenido de un objeto char asociado al identificador del objeto char bid
coloca una simple línea de texto en la página sujeta a varias opciones
imprime una ruta actual
genera desviaciones aleatorias desde una distribución gamma cuya densidad es a**r)/gamma r * x** r-1 * exp(-a*x)
devuelve la longitud del segmento inicial de subject el cual no contiene ningún caracter de mask
corta una parte de str especificada por los parámetros offset y length
copia los datos de table a un array empleando delimiter como delimitador de campos y la lista de campos fields
especifica el conjunto de codificación usado para anotaciones el texto
funciona del mismo modo que eventbuffer::read() excepto que no copia los datos: únicamente los elimina del principio del buffer
devuelve la versión de la biblioteca sqlite3 como una constante de cadena y como un número
afila la imagen adaptativamente afilando con más intensidad cerca de los bordes de la imagen y con menos intensidad lejos de los bordes
realizar un único barrido sin bloqueos sobre el bucle embebido
devuelve la versión que corresponda en la base de datos de geoip tal y como se define en el fichero binario
crea un nuevo objeto domelement
esta función libera los recursos de memoria usados por el formulario y todos los componentes que han sido añadidos al mismo incluyendo aquellos componentes que están en subformularios)
crea dos sockets conectados e indistinguibles y los almacena en fd
concatena left con right y devuelve el resultado
crea una nueva instancia de hw_api_attribute con el nombre y valor especificados
devolverá la versión actual de la extensión de rpmreader
se usa para provocar una condición de error de usuario se puede utilizar junto con el gestor de errores interno o con una función definida por el usuario que ha sido establecida como el nuevo gestor de erroresset_error_handler()
importa las variables get/post/cookie en el ámbito global
ejecuta la expresión xpath dada por expression y devuelve un resultado escrito si es posible
reabre el flujo espeicificado a un nuevo buzón dado por mailbox sobre un servidor imap o nntp
es similar a memcached::get() pero en lugar de un único ítem de clave obtiene varios ítems cuyas claves se especifican en el array keys
retorna los namespaces usados en el documento
liberación de memoria asignada para obtener resultados
establece string y pattern para una expresión regular multibyte
devuelve los nombres de todas las variables definidas en el ámbito activo no sus valores 
esta función enviará el contenido del búfer de salida si existe 
lista todos los gestores soportados por esta extensión
devuelve todos los campos de facet.date
dice si el modo abierto retorna bloques por ej., true para cbc y ecb y false para cfb y stream)
lee hasta eof en el puntero al archivo dado desde la posición actual y escribe los resultados en el buffer de salida
es similar a crear una nueva instancia de un objeto com excepto que sólo devolverá un objeto a su script si el objeto ya está en ejecución
esta función permite definir varias opciones en el recurso checkbox
obtiene la escena de la imagen
se comporta como mssql_connect() pero con dos diferencias:
comprueba si la constante especificada está o no definida en la clase
devuelve el límite del recurso especificado
rebobinar el directorio hasta volver al inicio
construye un nuevo limititerator de un iterator con un determinado inicio offset y un máximo count
activa/desactiva el lanzamiento de excepciones mientras ocurran errores inesperados
devuelve el objeto throwable previo por ejemplo uno proporcionado como tercer parámetro de exception::__construct()
crea una copia del trabajador
obtiene la metadata de los campos de una consulta preparada por sqlsrv_prepare() o sqlsrv_query()
esta función es idéntica a ereg() excepto que ignora la distinción de mayúsculas-minúsculas cuando se comparan caracteres alfanuméricos
elimina tuple de relation si coincide exactamente con una tupla dentro de la relación
hace pings a una conexión de base de datos y trata de conectarla de nuevo si está rota
devuelve información de la tabla dada
cierra la conexión al servidor memcached
intenta eliminar remover una base de datos entera del servidor asociado con el identificador de enlace especificado
se le llama cuando recursiva está en un nivel inferior
comprueba si la clase es o no iterable
devuelve una descripción de un error_code devuelto desde sqlite_last_error()
cierra el diccionario indicado mediante el identificador dictionary
la descripción de la función va aquí
libera un bloqueo exclusivo previamente obtenido por dbplus_xlockrel()
obtiene el nombre de la ruta efectiva del proceso de enrutamiento
metamorfosea un conjunto de imágenes
este método es llamado para establecer metadatos en el flujo
devuelve el número de error y la cadena de error de cualquier error bzip2 devuelto por el puntero del fichero dado
consulta el valor de la clave /encoding
añade otro campo a la faceta
aplica un filtro digital que mejora la calidad de una imagen con ruido
convierte la codificación de caracteres del string str a la especificada por to_encoding indicando opcionalmente la codificación de caracteres inicial en from_encoding
devuelve un número formateado según la cadena de formato dada usando la marca de tiempo timestamp de tipo entero dado o el momento local actual si no se da la marca de tiempo
cambia la contraseña para el user especificado
devuelve el xml recibido en la última respuesta soap
ubica el puntero interno de un objeto grande
si dos líneas se unen en un ángulo pequeño y la unión de líneas está establecida a ps_linejoin_miter el pico resultante será muy largo
contornea el trazado con el color y el ancho de línea actuales y lo borra
obtiene el número de propiedades públicas en la clase arrayobject
acepta un número variable de argumentos cualquiera que pueda ser o una cadena que contenga el nombre de una variable o una matriz que consista en nombres de variables u otras matrices
libera toda la memoria asociada al identificador de result recibido como parámetro
define el color de trazo de la página
ejecuta una consulta sql devolviendo un objeto de la clase sqlite3result si la consulta devuelve resultados
se podría llamar en un bucle personalizado al llamar a eio_poll()
crea una red neuronal de retropropagación estándar completamente conectada
comprueba si una clase está definida por un usuario en oposición a una clase interna
devuelve el valor de una opción de contexto
la descripción de la función va aquí
eliminia un recurso ch dado de un recurso mh dado
establece los límites blando y duro para un recurso de sistema dado
devuelve una cadena con el número de la versión del servidor
el primer prototipo es cuando se enlaza con libmcrypt 2.2.x el segundo cuando se enlaza con libmcrypt 2.4.x o 2.5.x
configura la función del manejador de declaraciones de notación notation para el intérprete xml parser
escala el tamaño de una imagen a las dimensiones dadas
envía datos al servidor de trabajos y a cualquier cliente a la escucha para este trabajo
establece una conexión persistente a un servidor cubrid
enumera de manera eficiente todos los puntos de código con sus categorías generales de unicode
devuelve el valor actual del mapa que está siendo iterado
crea un efecto de botón tridimensional simulado aclarando y oscureciendo los bordes de la imagen
calcula la clave soundex de str
configura una opción en el gestor curl compartido dado
devuelve un objeto que representa una etiqueta id3v1 la cual puede ser usada para obtener información sobre la etiqueta id3v1
devuelve el nombre de un campo de un resultado de una consulta
esta función devuelve un objeto con ciertas propiedades de la columna especificada
devuelve todos los parámetros de los pares nombre-valor del objeto
ejecuta directamente una sentencia sql
comprueba si el tipo de variable dado es float
envía los datos que han resultado del proceso y actualiza el estado a completo para este trabajo
obtiene el color actual del trazo
recupera la función de retrollamada
devuelve un array asociativo que corresponde a la fila recuperada y mueve el puntero de datos interno hacia adelante
obtiene un artefacto asociado con la imagen
este método comprueba si el cursor mongocommandcursor ha expirado y la base de datos no tiene más resultados que enviar al cliente
aplica una expresión aritmética relacional o lógica a una imagen
añade otro analizador a bbcode_container
indica a newt que devuelva el terminal a su estado inicial
establecer el valor para el índice dado
devuelve el punto primario verde de la cromaticidad
obtiene el modo de escritura del codificador
devuelve el valor de un atributo
devuelve el tamaño del fichero dentro del archivo phar
indica si la libreria cliente está compilado como thread-safe
devuelve el número de marcadores de parámetros presentes en la sentencia preparada
devuelve el perfil nominado de la imagen
define el codificador utilizado actualmente en el documento
esta función devuelve el valor de un campo específico de una fila específica de un conjunto de resultados
añade una colección de documentos al índice
la función cubrid_put() se usa para actualizar un atributo de la instancia del oid dado
obtiene la página corriente del documento
obtiene el valor actual de la escala horizontal
devuelve el seno del parámetro arg
solamente debería ser llamado dentro de la rutina de interoperabilidad de un archivo phar
devuelve el último error encontrado
inicia el depurado al archivo apd_dump_process_id en el directorio de volcado
define el modo destacado de la anotación cuando es clickeada
devuelve el número de filas casos/registros/filas) afectadas por consultas de tipo insert update y delete
la función mysqli_insert_id() devuelve el id generado por una consulta en una tabla con una columna que tenga el atributo auto_increment
invoca al método reflexionado
controla si una declaración xml será generada al inicio del documento xml
los archivos tar/zip no ejecutables no pueden tener una rutina de interoperabilidad por lo que este método simplemente lanza una excepción
esta función provee de los atributos y valores definidos dentro de una etiqueta xml
obtiene el color actual de relleno
crea y abre un objeto slob
devuelve el nombre unicode de una propiedad dada tal como se da en el fichero de base de datos de unicode propertyaliases.txt
inicia una transacción xa entre servidores de mysql
asigna un valor a un offset determinado
devuelve el objeto apuntado por una referencia débil
computa una intersección de arrays compara la información mediante una función de llamada de retorno
devuelve un conjunto de resultados que representa una notificación asíncrona pendiente
este método se usa para modificar los límites de los recursos de la biblioteca imagemagick subyacente
la clase mongocursor posee varios métodos para establecer banderas en el objeto de consulta
crea un nuevo objeto para el splfileinfo nombre_archivo especificado
convierte un guid binario de 16 bytes en un string
finaliza un página que ha sido empezada con la función ps_begin_page()
escapa cualquier caracter en una cadena que pudiera ser usada para engañar a un comando shell para que ejecute comandos arbitrarios
habilita el manejo de columnas de tipo long y longvarbinary
crea una nueva definición de capa
la descripción de la función va aquí
renombra original_name como new_name en la tabla global de funciones
devuelve el campo sec del timestamp
devuelve el nombre de la tabla en la que está el campo especificado
transfiere un conjunto de resultados de la última consulta
devuelve las entradas almacenadas en el resultado
escalar datos en un vector de entrada después de obtenerlo de una rna basada en parámetros previamente calculados
devuelve una dirección de correo correctamente formateada como está definido en rfc2822 dada la información necesaria
la función glob() busca todos los nombres de ruta que coinciden con pattern según las reglas usadas por la función glob() de la biblioteca estándar de c las cuales son similares a las reglas usadas por intérpretes de comandos comunes
elimina el identificador de memoria compartida shm_identifier
devuelve el número de filas afectadas por la última consulta insert update o delete asociada al link_identifier
asigna o recupera el database password de la base de datos usado por la conexión
parchea una cadena str con un patch binario
establece la imagen para poner la imagen
especifica el esquema para la recuperación
obtiene los extremos mínimo y máximo) de la imagen
devuelve el equivalente decimal del número hexadecimal representado por el argumento cadena_hex
establece la longitud de las porciones negras y blancas de una línea discontinua
la descripción de la función va aquí
establece si usar o no spanscorer para remarcar o no términos de frases sólo cuando aparecen en la frase de consulta del documento
obtiene el nombre de la extensión que definió una clase
define el ancho de la línea para la página
calcula el máximo común divisor de a y b
da formato csv a una línea pasada como el array fields y la escribe terminada con una nueva línea) en el manejador dado por handle del fichero especificado
reinicializa una sesión con los valores originales guardados en el almaceén de sesiones
solicita un bloqueo de escritura en la relación relation dada
este método comprueba si una entrada con la clave proporcionada existe en el hash
finaliza la transacción actual retrocediendo todas las sentencias emitidas desde el último commit
obtiene los extremos mínimo y máximo) de uno o más canales de imagen
sincroniza el estado de ficheros que están en memoria con un dispositivo de almacenamiento
esta función no devuelve hasata que se pulse una tecla
prepara el evento que se va a utilizar en la función event_add()
devuelve un resultado que contiene las llaves foraneas de una tabla
pre-procesa texto para después formatear y crear un objeto textflow
establece la compresión de la imagen
esta función devuelve el id que puede ser usado para desplegar el logo de php usando la imagen integrada
elimina una función de la cola de autocarga
obtener información del servidor solr
calcula la raíz cuadrada de a
devuelve el puerto de internet que corresponde a service para el protocol especificado de acuerdo a /etc/services
define un objeto de destino el cual se convierte en un blanco para saltar cuando el esquema es clickeado
la función cubrid_save_to_glo() se usa para guardar el fichero solicitado en una instancia glo
vacía los búferes de escritura del sistema de php y de cualquiera que sea el backend en el que se esté usando php cgi un servidor web etc. 
este método devuelve una verisón serializada del hash en el mismo formato que puede leer loadfromstring()
finaliza un patrón que fue creado con la función ps_begin_pattern()
actualiza al instante valores de los atributos en documentos dados
cambia los permisos de acceso de la manera especificada por mask user y group
devuelve información sobre el calendario calendar especificado
espara a que los eventos estén activos y ejecuta sus llamadas de retorno
