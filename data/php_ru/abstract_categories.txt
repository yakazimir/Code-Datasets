xml	simplexmlelement simplexmliterator xmlreader
variable_names	$values $max_depth $additional_headers $priority $hostname $pad_type $micro_seconds $systemid $array_result $restrict $callbacks $errstr $filtername $val1 $result_entry_identifier $train_data $output $object $signal $height $dither $docs $def_value $length $frompath $read_write $image2 $outputgamma $image1 $option $agent $column_name $addtl $attrlat $statement $enclosure $content $buf $value2 $value1 $sigma $replace_by $y1 $y2 $use_keys $cache_cb $item $onoff $on_off $classname $dbhandle $linefeed $needle $quality $signature $path_to $autoload $handle $ascii $source $val2 $node_namespace $luminosity $storage $src_y $src_x $week $bson $longitude $baseclass $src_w $fieldcode $src_h $input $retval $matches $ranker $revision $newscope $entry $column_number $charlist $bordercolor $fontfile $second $full_status $include_dot $identifier $srcy $srcx $mva $dest_width $bytes_max $sorting_order $interval $database $channel $bz $ini $table $arg4 $version1 $tv_usec $div $session_id $pipes $hebrew_text $client_identifier $definition $encoding_mode $tdo $id $flags $servername $config $pieces $metric $linebreak $tobase $disable $mustunderstand $use_errors $substrchar $arr $rows $start_revision $authns $errmsg $severity $brigade $draw $arg $lob1 $ftp_stream $level $importednode $descriptor $cipher $use_include_path $name $row_number $scanner_mode $index $var_array $line_pos $streams_context $space $targetpath $args $filtertype $array_or_countable $text_domain $part $start $directory $tile $timelimit $matrix $open_mode $ctor_args $newvalue $resumepos $new_setting $convmap $encodingfile $return_float $param_arr $nonrecursive $imageinfo $to_parse $encoded_header $arg1 $arg3 $arg2 $return $character_mask $userdata $escape_char $array1_sort_order $sortby $namespaceuri $pid $param $max $cit_flags $addr $new_location $msgs_in_queue $max_table_length $large_object $pad_length $what $message $read $nbytes $iptcblock $year $pri $username $jd $seed $version2 $code $color $type $remote_socket $out_charset $query $internal_date $action_name $token $newthis $tag $y3 $filters $result_type $node_name $contextnode $stylesheet $proper_address $reflector $brush $more_entropy $message_type $faultcode $field_value $search_value $newparent $machine $write $bbcode_container $class $str $return_var $action $base $field_name_or_number $error_types $hebrew $step $len $record $number $with_attrib $connect_type $max_chars_per_line $except $table_name $cas_token $tag_name $jargon $ip_address $microseconds $uid $path1 $numeric_prefix $grp $iterator_class $cut $buffer $sizelimit $fd2 $syntax $path2 $dest_height $longopts $precision $other_options $remote_file $suffix $pad_string $inside $str2 $extended $result_identifier $thickness $fdf_document $bind_rdn $pasv $wbmpname $input_headers $raw $max_memory $revno $ch $stmtname $haystack $headerfault $attr $gmpnumber $cx $cy $handler $unixtimestamp $driver_options $local_file $error_msg $record_number $preferences $priority2 $signed_text $antialias_steps $zp $array2 $p $groupsort $array1 $green $parser $blocksize $image $varname $iterator $frontend $serialized $main_str $server_socket $effect $day $version $matcheddn $is_hex $files $value $noise_type $entries $servicename $params $edition $request $src_im $arrays $note_name $language $maxlen $body $sort_flags $n $assoc_array $extended_data $timezone_identifier $filter $i $d $b $c $a $radius $mailbox $x $binary_string $initial_value $tightness $r $delimiter $bbcode_arg_parser $priority1 $user_agent $http_response_code $null_as $contents $status $mh $extrainfo $start_index $frombase $jpeg_file_name $preserve_keys $dbase_identifier $imagedata $cast_as $arg_num $opacity $server_key $host $log $categorize $dst_image $case_insensitivity $exception_handler $pdfdoc $in_fd $rev1 $rev2 $get_as_float $separator $deref $newname $function $points $char_list $julianday $value_compare_func $start_element_handler $backend $dn $mtime $draw_settings $sig $plaintext $target $deep $keys $req $res $vars $operation $new_include_path $qtime $blendmode $small $session_mode $referrals $red $colorize $assoc $is_dst $new_size $frameid $maxrows $cursor_orientation $wsdl $extension $process $from_encoding $stream $listener $workload $armor $who $decimals $additional_parameter $client_area $dev $var1 $lob_type $preg_flags $interface_name $output_callback $dst_im $country_code $note_value $process_sections $warning $slant $comment $param_name $driverdata $key_compare_func $path_from $basenode $details $property $max_matches $session_data $angle $log_message $command $bucket $utf32string $bytes_transferred $library $from $limiter $criteria $wouldblock $new_password $field_name $flag $string $from_root $depth $denominator $object_id $ending $pct $field_offset $bgd_color $options $shmid $result $in $cursor_offset $case $max_item_length $filesize $return_array $msgid2 $msgid1 $it $types $iv $dest $calendar $sections $servers $sortfilter $startpos $initial $modify $dictionary $service $y $server $modifiers $major $message_code $interface $httponly $database_name $faultstring $hits $unescaped_string $dir_handle $percent $old_password $allow_string $opts $cutoff $encoding $oldnode $attribute $protocol $backlog $fingerprint $link_identifier $want_peer $num_points $verbosity $val $feature $data_type $consumed $setting $notification_code $utf16string $isid $peername $var $link $newrdn $function_name $exception $font $is_final $bind_password $query_data $threshold $round $beta $thumbnail $bestfit $mod $field $search $nseconds $func $lob_to $delay $num $reference $zip_entry $minute $ident $enabled $pngname $dictionary_link $finfo $array1_sort_flags $topath $maxlength $x_server $src_image $before_needle $client_info $right_operand $seconds $cwd $local_socket $schema $decode_binary $descriptorspec $optname $class_name $font_index $row $dst_h $spool $argument $dst_w $component $regs $salt $lob2 $observer $dst_x $dst_y $location $miter $password $encoded_string $column $execute $format $glue $count $socket $oid_format $octal_string $refnode $dir $gearman_return_t $columns $mask $new_link $add_empty $fetch_style $type_namespace $psdoc $is_associative $targets $value_cb $encoded_headers $optlist $left_operand $utf8string $prepend $save_indexes $text $deleteoldrdn $tv_sec $repos $original $to_encoding $how $whence $timetype $modulus $method $hex_string $phonemes $qualifiedname $secure $limit $encoding_list $port $one_way $response_code $timestamp $unique $mxhosts $expiry $optval $recurse $working_copy $anything $tag_rules $throw $magic_file $syntax_only $input_parameters $position $expire $border $faultname $type_name $max_decoded_len $elementid $offset $strict $array $smiley $pgid $ns $alpha $filepath $weights $chunklen $newval $sandbox $saveflag $address $extend $x3 $x2 $x1 $connection $index_or_name $detail $multiplier $timezone $maxlifetime $lookup_table $condition $ignore_transparent $increment $expiration $minor $enc_type $parameter $month $ann $stmt $variable $destination $data $date $output_headers $jday $doc $url $out $uri $age $datetype $chunk_size $transfer_encoding $dirname $stream_or_context $ury $urx $faultactor $module $exp $alias $inputgamma $workingdir $attrlong $key $connection_string $closing $real_usage $optionне $debug_level $base_group_id $oid $replace $clear_realpath_cache $resource $time $error_handler $soapheaders $size $operand $subject $raw_output $str1 $width $charset $expression $ncolors $base_dn $interlace $imap_stream $character_set $extendedclass $env $case_insensitive $peg $facility $atime $doctype $pathname $normalized $parameter_type $errcode $end $mode $accessible $in_charset $file_name $json $out_fd $fields $blue $spelling $newnode $extra_headers $headers $break $timeout $varname1 $zurl $workfactor $domain $imagetype $jpegname $attributes $registernodens $gid $node $revision_no $escape $trimmarker $ordering $error_type $cas_tokens $namespace $latitude $is_prefix $errormode $skip $split_length $nanoseconds $iptcdata $opt $codeset $line $additional_parameters $to $locale $stack $bit_on $zip $td $callable_name $field_number $method_name $bv_name $llx $lly $saturation $filename $replacement $items $indent $file_extensions $first $recursive $scale $window_handle $repos_url $style $force $stream_or_url $sx $sy $tidy $closure $genre_id $juliandaycount $pattern $background $errno $clause $repository_url $prefix $new_path $max_len $is_xhtml $localname $oid_only $exclude $context $user $reps $arguments $group $min $autoload_function $client_flags $job_handle $allowable_tags $with_cas $genre $actor $hour $cmp_function $numerator $path $obj $still_running $module_name $weight $callback $oldname $walk_to_top $arg_separator $end_revision $double_encode $publicid $hue $gmt_offset $error_code $sql_text $error_message $infos $region_code $phi $end_element_handler $lob_from $cmd $file $category $signmode $fp $zend_extensions $stream_filter $attrsonly $fl $words $foreground $fd $zenith $operator
magic_methods	get clone destruct call construct wakeup
exception	Exception SolrClientException ErrorException StompException SolrIllegalArgumentException SolrIllegalOperationException SolrException
types	simplexmlelement reflector iterator int float tidy number imagick splobjectstorage mysqli array dateinterval splsubject zmqsocket bool gmp recursiveiterator|iteratoraggregate domnode domdocument string oci-lob void object callable domattr domdocumenttype imagickdraw recursiveiterator closure mysqli_stmt resource splobserver mysqli_result oci-collection datetimezone traversable mixed
