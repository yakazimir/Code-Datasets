retourne une chaîne de caractères décrivant la connexion représentée par le paramètre link incluant le nom d'hôte du serveur)
cherche la position numérique de la dernière occurrence de needle dans la chaîne de caractères haystack
configure la correction gamma
cette fonction crée une copie de variant et effectue alors un transtypage pour forcer la copie à avoir le type donné par type
récupère la méthode de disposition de l'image
analyse un document xml
résout le nom de fichier filename en utilisant le chemin d'inclusion en utilisant les mêmes règles que les fonctions fopen() / include
retourne le nombre de lignes du résultat result
importe les variables get/post/cookie dans l'environnement global
est final ce qui signifie qu'il ne peut pas être surchargé
récupère un objet snmp spécifié par l'identifiant object_id en utilisant une requête get
change la sortie blindée
ferme une connexion persistante
place une page pdf importée sur la page suivant diverses options
récupère le nombre de lignes d'un jeu de résultats
bascule un évènement en attente
construit un nouvel objet gmagickpixel
retourne la chaîne d'erreur associée au code erreur errorcode
récupère un champ par son nom
c'est une interface à la fonction système getrusage 2 
récupère la portion entière d'un variant
clone une fonction
cette méthode réalise les lourds travaux de la classe yaf_dispatcher
crée une nouvelle instance rrdcreator
modifie quelques options de l'en-tête d'un fichier de base de données rrd comme le fait de renommer la source des données
retourne la valeur arrondie de val à la précision precision nombre de chiffres après la virgule)
vérifie le statut des threads
retourne true si le processus fils est stoppé ; cela n'est possible que si l'appel à pcntl_waitpid() a été fait avec l'option wuntraced
compresse les données data en utilisant l'encodage lzf
retourne true si le mot est correctement orthographié, false si la variable des suggestions est fournie et la remplit des suggestions possibles
écrit une image dans un fichier spécifié
retourne les données à partir de l'endroit spécifié dans la base de données
récupère le nom de base de données à l'appel de mysql_list_dbs()
arrête le script jusqu'à l'instant indiqué par le paramètre timestamp
un objet splfileobject ne peut pas avoir de fils aussi cette méthode retournera toujours null
retourne le nombre de messages récents dans la boîte aux lettres courante
crée une nouvelle forme
cette fonction retourne un objet avec des propriétés en lecture seule qui retournent des informations à propos du client de base de données db2
convertit tout ou partie d'un document yaml provenant d'une chaîne vers une variable php
décrémente le compteur interne d'un objet syncmutex
indique si un fichier est exécutable
trie les éléments par clé, en conservant la relation avec les éléments
modifie le dossier courant en directory
modifie le curseur de position dans le fichier handle
crée une copie de l'objet solrdocument
la fonction cubrid_lob2_read() lit len octets depuis les données lob et retourne les octets lus
définit des vérifications spécifiques à ssl pour les requêtes
indique si le worker exécute des stackables ou non
tente de retourner la chaîne str après avoir supprimé tous les octets nuls toutes les balises php et html du code
toutes les chaînes envoyées à la base de données doivent être en utf-8
notifie tous les observateurs attachés
crée une base de données dbase dans le fichier filename et avec les champs fields
réduit le bruit speckle d'une image en préservant les bords de l'image original
retourne une ligne entrée par l'utilisateur
retourne l'id de session du processus pid
cette fonction retourne le nombre de colonnes dans le jeu de résultats en cas de succès ou false si une erreur survient
définit la valeur retval à l'option spécifié
annule une notification de modification de variable
retourne le copyright de l'api imagemagick api
bloque le processus pendant un nombre de secondes fourni
utilisé pour établir des connexions sécurisées utilisant ssl
envoie un statut d'échec pour ce travail indiquant que le travail a échoué de façon inattendu (à contrario d'un échec avec une émission d'une exception donnée)
encodes une uri conformément à rfc 3986
cette option permet à mongos d'envoyer des résultats partiels de requête si un partage est inaccessible
retourne une chaîne représentant les balises telles qu'analysées par tidy
envoie une instruction via le socket pour redémarrer l'interpréteur
installe un proxy pour les requêtes mysqlnd
définit la façon dont la page doit être affichée
récupère un objet snmp qui suit l'objet spécifié par le paramètre object_id
supprime un large objet de la base de données identifié par le oid
est la version php de la fonction c qui exécute la commande command et retourne le résultat
définit l'adresse ip depuis laquelle les connexions http sont effectuées
ajoute un attribut avec comme espace de noms namespaceuri et comme nom name avec comme valeur value
un curseur typiquement récupère un lot d'objets résultants et les stocke localement
récupère la largeur courante de la ligne
améliore le contraste de la couleur de l'image en ajustant la couleur des pixels pour correspondre à l'intervalle de couleurs disponibles
crée un objet slob et l'ouvre
si le paramètre optionnel base est spécifié, log() retourne alors le logarithme en base base sinon log() retourne le logarithme naturel ou népérien) de arg
ajouter les données de value au début d'un élément existant
active la compression automatique des valeurs larges
est utilisé pour remplacer la valeur d'un élément identifié par la clé key
compare left avec right
envoie des informations de statut au serveur de travaux ainsi qu'à tous les clients à l'écoute
lit la structure du message msg_number
vérifie si la fonction réfléchie est une closure
calcule la signature des données data en générant une signature digitale cryptographique en utilisant la clé privée associée avec le paramètre priv_key_id
ajoute une fonction de rappel sur une connexion cyrus imap
sert à créer un paquet wddx avec une structure qui contient la représentation des variables passées en arguments
retourne les options du champ field_offset
si un extrait ne peut être généré en raison de termes ne correspondant pas la méthode peut spécifier un champ à utiliser comme sauvegarde ou comme résumé par défaut
supprime toutes les directives de regroupement et désactive les regroupements
retourne le nom de login de l'utilisateur qui possède le processus courant
lit tous les en-têtes de réponse http sur netscape serveur
cette méthode ne suit actuellement pas les références aussi elle ne peut déterminer si les références sont cassées ou non
utilisé lorsque l'objet est traité comme un tableau pour ajouter un champ au document
efface une fonction de la pile autoload
définit le nombre maximal d'extraits à coloriser par champ
crée un objet domdocumenttype vide
récupère la valeur du chemin pcos
efface un message depuis la file d'attente
définit le masque du clip de l'image depuis un autre objet imagick
stoppe le profilage et retourne les données xhprof correspondant à son exécution
exécute l'expression rationnelle posix pour les chaînes multi-octets
cette méthode peut être utilisée pour obtenir les dimensions de l'image sans lire toute l'image en mémoire
retourne le copyright de l'api graphicsmagick
récupère la montée verticale de la police
retourne un entier décrivant le backend utilisé par libev
définit le type de l'image
lorsque vous envoyez plus d'une commande sql au serveur ou que vous exécutez une procédure stockée avec de multiples résultats cela va conduire le serveur à retourner plusieurs jeux de lignes
réinitialise une session avec les valeurs originales sauvegardées dans le stockage de session
récupère la représentation textuelle d'un pays depuis la constante de la classe gender
limite le nombre de requêtes dans un groupe de requêtes
retourne un identifiant d'image représentant une image obtenue à partir du fichier filename
vérifie si l'élément courant est valide
avertissement cette fonction n'est pas documentée et seule la liste des arguments est disponible
il n'est pas nécessaire d'appeler cette fonction car php libère toutes les ressources à la fin de chaque demande
rassemble les éléments d'un tableau en une chaîne
restaure un contexte graphique précédemment sauvegardé
copie une ressource curl retournant une nouvelle ressource curl avec les mêmes préférences
récupère les informations d'une boîte de la page courante
récupère le champ courant
récupère toutes les fonctions __autoload() enregistrées
ferme le blob blob_handle ouvert en lecture avec ibase_open_blob() ou en écriture avec ibase_blob_create()
retourne une partie d'une chaîne
configure la couleur mate
retourne le numéro de la colonne dont le nom est field_name dans le résultat result
retourne le nombre d'éléments dans la table de hachage
retourne la valeur de la colonne à la ligne donnée dans le résultat ms sql
lit le nombre de couleurs uniques de l'image
génère un nombre aléatoire
retourne un objet datetime représentant cette date
retourne une chaîne contenant le code d'erreur sqlstate de la commande la plus récente qui a été préparée
définit le gestionnaire d'attente qui patientera tant que la base de données n'est pas verrouillée ou que le délai d'attente ne soit atteint
les archives tar/zip non-exécutables ne peuvent pas avoir de conteneur de chargement donc cette méthode ne fait que soulever une exception
retourne un tableau d'aliases pour un type connu d'encodage
si un attribut contenant un tag a été retourné depuis la fonction radius_get_attr() radius_get_tagged_attr_data() va retourner le tag depuis l'attribut
lit le numéro d'inode du fichier
ecrit les données de session dans le support de stockage
retourne un entier correspondant au type judy de l'objet
calcule le symbole de legendre de a et p
retourne la dernière note du serveur postgresql sur la connexion connection spécifiée
définit le gamma de l'image
remplit un espace avec un dégradé, basé sur un objet de dégradé
retourne au premier élément
retourne le message d'erreur du fichier bz2 représenté par le pointeur bz
prend le contenu du fichier infilename et le signe en utilisant le certificat et la clé privée contenus dans les arguments signcert et privkey
les archives phar contiennent un chargeur ou conteneur stub écrit en php qui est exécuté quand l'archive elle-même est exécutée soit par inclusion : < ?php include&nbsp 'monphar.phar' ; ? > soit par simple exécution : php monphar.phar
définit le gestionnaire à utiliser pour préparer la réponse depuis solr
envoi une sortie binaire des fichiers et des variables utilisateur spécifiés depuis le cache apc vers un fichier donné
retourne si l'on doit utiliser spanscorer pour mettre en évidence les termes d'une phrase uniquement lorsqu'ils apparaissent dans la phrase de requête du document
attache une adresse ip spécifique au vendeur pour la requête radius courante
retourne la précision de la colonne indiquée du jeu de résultats
modifie une option d'un champ
implémente l'algorithme de comparaison qui ordonne les chaînes tel qu'un homme le ferait
crée un sdo_dataobject pou un espace de nom uri et un nom de type donnés
cette fonction retourne un tableau avec les noms des interfaces que la classe class ainsi que ses parents implémentent
corrige le gamma d'une image
active/désactive l'envoi implicite
cette méthode ajoute une nouvelle entrée dans la table de hachage et retourne le fait que l'entrée ait bien été ajouté
attache une adresse ip à la requête radius courante
importe les variables dans la table des symboles
retourne des statistiques sur le serveur de cache de document
calcule l'arc tangente de chaque valeur de real et retourne le tableau résultant
retourne le nombre maximal de caractères depuis un champ lors de l'utilisation d'une regex
ouvre une connexion avec la bibliothèque kadm5 en utilisant le principal et le mot de passe password pour obtenir les créances initiales depuis le serveur d'administration admin_server
crée un fichier spécial ou ordinaire
répondra à une opération de tronquature effectuée au moyen de ftruncate()
récupère divers paramètres
configure le délai d'expiration de connexion en secondes pour une connexion à searchd
annule les ajouts/effacements effectués sur l'index depuis le dernier commit
vérifie si un fichier php.ini est chargé, et récupère son chemin
retourne le nom du champ de l'index spécifié
crée un objet de document xml
dessine une ligne entre deux points fournis
lit l'index de la couleur spécifiée avec sa teinte blanc et noir
cette fonction ré-initialise le cache opcode dans sa globalité
retourne la chaîne str dont les séquences de caractères %xy avec xy deux valeurs hexadécimales auront été remplacées par le caractère ascii correspondant
configure la chromacité du point rouge
créer une nouvelle instance de la classe en utilisant les arguments fournis qui seront distribués à son constructeur
indique si la fin d'un fichier compressé est atteinte c'est à dire si le pointeur est à la position eof
envoi l'objet gmagickdraw dans l'image courante
cette méthode est appelée en réponse à opendir()
récupère une liste de toutes les collections de cette base de données et la retourne sous la forme d'un tableau de documents contenant leurs noms et leurs options
ajoute une tâche de forte priorité à effectuer en parallèle d'autres tâches
liste toutes les boîtes aux lettres souscrites
lit toutes les valeurs de configuration
cette fonction est une abstraction des commandes table_get et table_set d'apache
configure la position à plat à une valeur comprise entre 0 et 100 inclus
décode toutes les séquences % ## et les remplace par leur valeur
rend la prochaine ligne d'un jeu de résultats disponible pour lecture
retourne un timestamp unix pour pâques à minuit pour une année donnée
compte les éléments du poll
crée un diff binaire de 2 chaînes et retourne le résultat
dessine un arc de cercle
recherche des objets grâce à leur clé d'exécution et/ou grâce à une requête en texte plein
cette fonction remplace le fils oldnode par le nouveau noeud spécifié
calcule les entiers g s et t tels que a*s + b*t = g = gcd a b où gcd est le pgcd de a et b
retourne la valeur d'errno dans le vas d'une valeur de retour gearman_errno
récupère le nombre minimal de facettes des champs qui doivent être incluses dans la réponse
exécute une fonction de rappel dans le scope global
lit des données dans le socket socket qui doit être une ressource créée par la fonction socket_create() ou la fonction socket_accept()
est fonctionnellement identique à la méthode memcached::deletemulti() excepté le fait que le paramètre server_key peut être utilisé pour lier les clés keys avec un serveur spécifique
l'appel à la méthode isconnected sur un objet de connexion vérifie si le script php est connecté à un serveur de messagerie
remet à sa position initiale le temporisateur d'apache qui vaut par défaut 300 secondes
lit le fichier remote_file présent sur le serveur ftp ftp_stream
dessine un polygone dans l'image image
cette fonction vide le tampon de sortie sans l'envoyer au navigateur
supprime les noeuds de texte vides et fusionne les noeuds de texte adjacants dans ce noeud ainsi que tous ces enfants
retourne le temps absolu auquel ce watcher sera appelé la prochaine fois
convertit une chaîne utf-16 en utf-8
spécifie le terme de départ
exporte une extension reflétée et retourne le résultat sous forme de string
réduit une image de moitié en gardant les proportions
retourne une référence vers l'objet gmagick courant avec le pointeur de l'image à la bonne séquence
configure la durée maximale de recherche
écrit le contenu de la chaîne string dans le fichier compressé zp
la fonction snmp2_real_walk() est utilisée pour parcourir un nombre d'objets snmp en commençant par l'objet identifié par object_id et retourne non seulement leurs valeurs mais aussi les identifiants de leurs objets
vérifie si la mesure est en cours
modifie le mode par défaut des objets text
retourne le nombre d'enregistrements lignes dans la base dbase_identifier
complète un bloc de réponse en libérant la requête
replace l'itérateur à la position précisée dans limititerator::__construct()
cette méthode retourne les drapeaux fixés dans le manifeste pour un phar
retourne la taille d'un fichie résultat créé après l'application d'un patch binaure sur un fichier original
le secret partagé est nécessaire pour le rétablissement des données comme les mots de passe et les clés de chiffrement
cette méthode retourne l'élément sélectionné de l'arbre des boîtes à cocher
récupère le numéro de ligne de départ
retourne une chaîne contenant le numéro de version du moteur d'analyse zend actuellement en cours d'utilisation
libère la mémoire allouée pour ispell par mnogosearch
ajuste les niveaux d'une image en modifiant les couleurs qui sont entre deux couleurs données
crée un objet recursiveiteratoriterator depuis un objet recursiveiterator
améliore les bords intérieurs de l'image avec un filter de convolution utilisant le rayon fourni
prend le contenu du fichier infilename et le chiffre en utilisant un chiffrement rc2 à 40-bit de manière à ce que le message ne puisse être lu que par le possesseur de recipcerts
définit la couleur de la bordure de l'image
définit la taille de l'objet gmagick
lit ou définit le nom de login à utiliser avec la connexion
ouvre une connexion au serveur hyperwave sur l'hôte hostname
lit une ligne dans la ressource handle
convertit la requête de signature de certificat représentée par csr au format ascii et la stocke dans la variable out qui est passée par référence
passe à l'élément suivant
construit un nouvel objet evio et démarre l'observateur automatiquement
récupère les lignes depuis le jeu de résultats dont le drapeau dbx_result_unbuffered est défini
récupère la valeur d'une option spécifiée pour une ressource de requête ou une ressource de connexion
retourne le nom d'un dossier d'archives donné
met à l'échelle les données dans le vecteur de sortie avant de les passer à ann en se basant sur les paramètres précédemment calculés
lit le contenu du noeud courant incluant les enfants et le balisage
vérifie si le paramètre a été déclaré comme un paramètre variadique
définit la couleur de remplissage de la page
configure la fonction handler pour qu'il soit le gestionnaire des messages générés par le serveur sybase
liste toutes les catégories de même niveau
prépare la requête query pour une utilisation ultérieure avec la fonction ifx_do()
récupère l'état courant du paramètre facet.missing
retourne une chaîne de caractères représentant la version de la bibliothèque du client mysql
lit une ligne dans le résultat identifié par result_id et retourné par odbc_do() ou odbc_exec()
libère les ressources associées à key_identifier
récupère plusieurs paramètres qui ont été fixés directement par ps_set_parameter() ou indirectement par une ou d'autres fonctions
place le pointeur de colonne à la colonne spécifiée
lit une image depuis un fichier
n'est vraiment utile que si vous risquez d'utiliser trop de mémoire durant votre script
applique une expression arithmétique relationnelle ou logique à une image
change la racine du script en cours et la remplace par directory puis change le dossier courant de travail en /
récupère tous les champs à utiliser pour la similarité
formate et affiche toutes les lignes issues de la requête result_id dans un tableau html
retourne le sinus hyperbolique de arg défini comme exp arg - exp -arg/2
chaque tableau entretient un pointeur interne qui est initialisé lorsque le premier élément est inséré dans le tableau
fonctionne exactement comme la méthode eventbuffer::addbuffer() excepté le fait qu'elle déplace les données au début du buffer
exécute une implication sur les bits de deux variants
identique à la méthode evfork::__construct() mais ne démarre pas automatiquement l'observateur
retourne le prochain lot de lignes ms sql server
incrémente la valeur d'un élément identifié par la clé key par la valeur value
recherche un profile icc et le prépare pour une utilisation ultérieure
vérifie si la valeur null est autorisée comme valeur du paramètre
demande à event_base de stopper la distribution d'événements
retourne le nombre de requêtes terminées
réinitialise une requête préparée de tel sorte à retrouver son état initial avant exécution
efface un tableau ainsi que toutes les données structurées associées
applique le codage aux données binaires data pour qu'elles puissent être correctement retournées depuis la requête car la bibliothèque libsqlite n'est pas compatible avec les données binaires)
lit la réponse brute de la dernière requête
ajoute une condition à la requête
exécute une requête préparée
vérifie si tous les caractères de la chaîne text sont des chiffres hexadécimaux
définit l'opérateur de composition de l'image
enregistre la fonction session_write_close() comme fonction de fermeture de session
récupère des informations internes lorsqu'une exception a été lancée
retourne la partie d'une chaîne à partir de la première occurrence de needle sensible à la casse), et jusqu'à la fin de la chaîne needle inclus 
retourne la chaîne de caractères str après l'avoir convertie du format quoted printable binaire 8 bits en accord avec la rfc2045 section 6.7 et non pas la rfc2821 section 4.5.2 pour que les virgules additionnelles ne soient pas effacée du début de la ligne)
récupère la hauteur d'un cairoimagesurface
ferme un fichier spécifié par le paramètre fd
retourne le contenu d'une cellule depuis un jeu de résultat frontbase
change un descripteur de fichier sur lequel des opérations d'évènement agissent
sert à trier simultanément plusieurs tableaux ou bien à trier un tableau multidimensionnel suivant l'une ou l'autre de ses dimensions
retourne la valeur courante durant l'itération
fixe la largeur d'une ligne pour toutes les opérations de dessin suivantes
supprime la valeur de la propriété
affiche une image dans la page
retourne le nom de la colonne identifiée par le paramètre column_number
défini la fonction d'erreur utilisée pendant l'entrainement
repositionne le curseur du fichier ouvert spécifié par l'argument fd qui peut être un flux une ressource de socket ou un descripteur de fichier) à la position offset suivant la directive whence comme suit : eio_seek_set - définit la position à offset octets
retourne le format de l'image imagick
retourne une chaîne représentant un stockage
ferme le pointeur qui sert à écrire dans l'historique système
assigne le nombre de niveaux de priorités d'évènement
aide à générer un squelette de classe pour l'utiliser comme évier d'événement
la fonction geoip_db_filename() retourne le nom du fichier contenant la base de données geoip spécifiée
importe la clé keydata et retourne un tableau avec les informations à propos du processus d'importation
dessine un polygone en utilisant les paramètres courants du contour la couleur ou la texture de remplissage et en utilisant le tableau de coordonnées spécifié
modifie le quota de la boîte aux lettres quota_root
déverrouille un objet verrouillé
cette méthode est appelée en réponse à la fonction closedir()
définit le nom de la table d'une base de données paradox qui a été créé avec la fonction px_create_fp()
récupère le statut courant d'un flux de texte
génère une déviation aléatoire d'une distribution gamma dont la densité est a**r)/gamma r * x** r-1 * exp(-a*x)
renomme une entrée définie par son nom
effectue une rotation de l'image d'un degré spécifié
retourne le dernier message d'erreur généré par le serveur frontbase
récupère les informations d'allocation mémoire partagée d'apc
lit le propriétaire du fichier
retourne un entier contenant le oid du type de base du champ field_number donné dans la ressource postgresql result
retourne le nombre de résultats de la dernière recherche
redimensionne une image aux dimensions voulues avec un filtre
lit le type de compression
retourne un tableau associatif qui contient la ligne en cours dans le résultat result
crée une nouvelle instance haruannotation
lit les options activées telles qu'elles ont été configurées avec filesystemiterator::__construct() ou filesystemiterator::setflags()
cette méthode cherche les fils d'un élément
le développeur doit toujours implément la méthode run pour les objets qui doivent être exécuté
fera attendre le contexte appelant une notification depuis l'objet référencé
définit la couleur de remplissage à utiliser pour dessiner des objets pleins
récupère une ligne depuis un jeu de résultats associé à l'objet pdostatement
vérifie si une interface a été définie
colorise les pixels correspondant à la couleur transparente
calcule les paramètres d'échelle d'entrée pour une utilisation future en se basant sur les données d'entrainement
crée un dossier avec les permissions spécifiées par l'argument mode
indique si la bibliothèque cliente est compilée avec la sécurité thread
cette fonction analyse un document html contenu dans la chaîne source
vous devez appeler mcrypt_generic_init() avant chaque appel à mcrypt_generic() ou mdecrypt_generic()
retourne la chaîne str après avoir mis en majuscule la première lettre de tous les mots si ce caractère est alphabétique
ajoute un nom de glyphe et/ou une valeur unicode à un encodage personnalisé
crée le dossier nommé directory sur le serveur ftp
retourne la taille d'une valeur
cette fonction est un alias de : finfo_file()
contrôle le format du jeu de résultats : si les résultats doivent être retournés sous forme de tableau numérique ou associatif
retourne une chaîne représentant le type de connexion utilisé
chiffre le paramètre plaintext avec les clés qui ont été fixées avec gnupg_addencryptkey auparavant et retourne le texte chiffré
affiche le texte à la position spécifiée
lit la longueur du champ identifié par son numéro
détermine si la connexion est occupée
est utilisé, après le changement de la fonction de gestion d'exceptions avec la fonction set_exception_handler() pour revenir à l'ancien gestionnaire d'exceptions qui peut être la fonction interne ou une fonction définie par l'utilisateur)
vérifie si l'élément courant est valide
trouve la position en graphème de la première occurrence dans une chaîne en effectuant une recherche insensible à la casse
crée un objet solr
fait une copie physique incluant le contenu éventuel et retourne un nouvel objet ou un objet d'erreur
lit le numéro de la colonne nommée field_name
sauvegarde le document dans le fichier spécifié
retourne des informations sur la création du curseur et son itération
crée un analyseur xml et retourne une référence sur cet analyseur pour qu'il puisse être utilisé ultérieurement par d'autres fonctions xml
linéarisation personnalisée d'objets solrdocument
prend en paramètre path le chemin d'un fichier ou d'un répertoire et en fournit le nom de la dernière composante
une syntaxe simple pour récupérer une collection avec des noms séparés par des points . 
récupère les informations concernant la dernière requête exécutée
détermine si la variable donnée est de type nombre décimal
analyse subject pour trouver l'expression qui correspond à pattern
définit/récupère l'encodage des caractères pour les expressions rationnelles multi-octets
alloue un nouveau curseur oracle sur la connexion spécifiée
retourne une chaîne de caractéres contenant la version de php judy
spécifie la qualité de compression de l'image
utilisé pour indiquer que en plus des contraintes relatives aux termes d'un champ facet un comptage de tous les résultats correspondants qui n'ont pas de valeur pour le champ doit être calculé
compte le nombre d'occurrences de tous les octets présents dans la chaîne string et retourne différentes statistiques
retourne le nombre d'avertissements tidy rencontrés pour le document spécifié
vérifie s'il y a encore des résultats depuis une requête multiple
cette méthode peut être utilisée pour obtenir les dimensions de l'image sans lire toute l'image en mémoire
calcule la différence de deux tableaux associatifs compare les données et les index avec une fonction de rappel
ajoute une variable au cache utilisateur
modifie la taille d'une image aux dimensions données et supprime les profiles associés
crée un objet evperiodic watcher associé avec l'instance de la boucle d'événement courant
remplit le chemin courant en utilisant la règle even-odd puis peint le chemin
retourne le nombre de lignes affectées par la dernière requête en écriture
pour des champs à valeur multiple si une valeur de boost valide est spécifiée la valeur spécifiée sera multipliée par la valeur de boost courant pour ce champ
récupère la taille associée avec l'objet gmagick
convertit une image basée sur une palette créée par une fonction comme imagecreate() en une image en couleur vraie comme imagecreatetruecolor()
retourne le titre d'un tag id3
lit le bytecode depuis php pour une constante existante constantname et écrit le bytecode dans le fichier ouvert désigné par filehandle
récupère un sous résultat pour y trouver des valeurs d'une facette donnée
si l'objet est déjà dans la base modifie la base et sinon insère l'objet
change le dossier courant de php en directory
crée une image par miroir vertical à l'aide d'une symétrie autour de l'axe des abscisses
détruit l'agent de recherche créé par udm_alloc_agent()
lève le verrou en écriture obtenu par dbplus_lockrel()
cette fonction est un raccourci pratique pour mettre à l'échelle une image proportionnellement pour la réduire de moitié de taille
récupère l'instance haruencoder actuellement utilisée dans le document
lit le statut courant de la base de données fournie
retourne la prochaine ligne de données disponible sous la forme d'un tableau associatif d'un tableau numérique ou les deux par défaut)
retourne la couleur d'arrière plan de l'image
récupère un objet de requête à exécuter sur la base de données
libère la mémoire des ressources du système et de base de données associée du jeu de résultats
récupère l'heure de modification du fichier courant de l'objet directoryiterator
construit un objet d'événement nommé ou non
construit un nouvel objet solrdocument
lit le gamma de l'image
crée une nouvelle instance gearmanjob représentant un travail assigné à un agent
configure la propriété d'image name à la valeur value
définit une fonction de rappel à utiliser lorsqu'une tâche échoue
retourne le message lisible expliquant l'erreur dont le numéro est errno
retourne le chemin d'un fichier
retourne la configuration courante du limiteur de cache
identique à la fonction readdir() seul le paramètre dir_handle vaut par défaut $this
récupère le nom du fichier dans lequel l'exception a été créée
définit le commentaire d'une entrée définie par son index
détache un itérateur
compare une image à une image reconstituée et retourne une métrique
applique la fonction de hash hash aux données data
remplace les couleurs de l'image en utilisant la table de correspondance hald
convertit le texte mime_encoded_text en utf-8
défini le changement décroissant du poids de sarprop
retourne le timestamp unix avec les microsecondes
ajoute des données à la fin du tampon d'événement
formate la portion suivante d'un flux de texte dans un espace rectangulaire
la fonction cubrid_new_glo() est utilisée pour créer une instance glo dans la classe demandée classe glo 
ajoute une tâche de fond de faible priorité à effectuer en parallèle d'autres tâches
défini une option de socket zmq
initialise le générateur de nombres aléatoires avec seed ou avec une valeur aléatoire si aucun paramètre seed n'est fourni
ouvre une nouvelle archive zip pour lecture écriture et modification
cette fonction crée une nouvelle instance de la classe domprocessinginstruction
récupère la valeur de l'attribut étendu nommé name du fichier path
cette fonction retourne un objet avec certaines propriétés d'une colonne spécifique
place une page pdi dans la page courante aux coordonnéesx y, et la met à l'échelle
définit un index sur une colonne spécifique
annule une transaction globale sur les serveurs mysql commencée par la fonction mysqlnd_ms_xa_begin()
définit l'espace de couleurs de l'image
détermine si la variable donnée est un booléen
effectue une validation relaxng sur le document selon le schéma rng défini par filename
calcule une hash digest pour les données d'entrée en utilisant la méthode fournie
lit ou définit le nom de l'hôte à utiliser avec une connexion
lit le nombre de lignes dans le jeu de résultats result
charge des données ispell
cette fonction est utilisée pour charger des mibs additionnelles ie spécifiques aux fabricants ainsi les oids humainement lisibles comme vendor-mib::foo.1 au lieu des oids numériques peuvent être utilisés
retourne l'opérateur de composition associé à une image
lit toutes les lignes d'un résultat oracle dans un tableau à 2 dimensions
cette méthode vous permet de spécifier un champ qui doit être traité comme une facette
retourne la valeur de l'élément à l'index index en commençant à 0)
retourne la taille de l'image en octets
trie le résultat d'une recherche ldap retournée par la fonction ldap_search()
récupère la valeur pour une position donnée
établit un accès à un objet
remplace les couleurs de l'image avec celles définies par le paramètre replacement
détermine si l'élément courant a des fils
revient au début du flux temporaire du document
retourne le statut des fichiers et des dossiers de la copie de travail en fournissant les modifications les ajouts les suppressions ainsi que les autres modifications des éléments de la copie de travail
récupère le numéro de la dernière ligne
lit les données brutes sqlt du type du champ field
défini le nombre de groupes candidats
liste les gestionnaires d'affichage utilisés
sert à connaître la taille en octets d'un bloc de mémoire partagée
constructeur privé pour empêcher l'initialisation directe
définit la taille maximale des en-têtes pour la connexion
récupère la position courante pour l'affichage
lit les données depuis le flux temporaire
retourne la taille du champ d'index field_number
la fonction cubrid_set_autocommit() est utilisée pour définir le mode auto-commit pour la connexion à la base de données cubrid
libère le segment de mémoire partagée identifié par shm_identifier et créé par shm_attach()
retourne le nombre de messages dans la boîte aux lettres courante
envoi l'action avec les paramètres au service via le proxy de façon synchrone et récupère la valeur
suspend l'exécution du script jusqu'à ce que l'un des événements spécifiés soit posté par la base de données
construit un objet watcher evperiodic et le démarre automatiquement
décompresse une chaîne
convertit tout ou partie d'un document yaml provenant d'une url vers une variable php
après qu'une demande radius ait été construite elle est envoyé grâce à la fonction radius_send_request()
configure le nom du fichier d'image avant d'y accéder
crée une copie de l'objet gearmanclient
liste toutes les classes définies
la fonction utilise le test de probabilité miller-rabin
protège une requête sql littérale pour le requêtage à la base de données postgresql
lit la méthode de récupération
la méthode clone empêche un objet d'être cloné
la fonction socket_recvfrom() reçoit len octets de données du buffer buf depuis name sur le port port si le socket n'est pas du type af_unix, en utilisant socket
ouvre une nouvelle connexion vers le serveur mysql
fixe la rotation d'un système de coordonnées
s'identifie auprès du serveur hyperwave
extrait des informations sur le contenu du cache opcode et son utilisation
récupère le nom de la classe
retourne l'identité du thread qui a créé le thread référencé
alloue une session mnogosearch
vérifie si le fichier a les autorisations en écriture
applique un effet spécial qui simule une peinture à l'huile
reportez-vous à la section sur les requêtes de ce manuel pour plus d'informations sur la distribution de la lecture aux secondaires
récupère le nom d'une fonction de l'api qui a lancée la dernière exception ou qui a échouée
écrit les données du fichier filename dans le lob à partir de la position courante
récupère les préférences de sécurité
fonctionne de la même façon que la méthode eventbuffer::read() mais ne vide pas les données du tampon
attache un attribut sous forme de chaîne à la requête radius courante
retourne la fréquence minimale des documents à retourner aux fins d'inclusion
annule un groupe de requêtes spécifié par la ressource de groupe grp
lit le type de données sql d'un champ identifié par son numéro
lit le bytecode d'une fonction existante depuis php et l'écrit dans le fichier désigné par la ressource de fichier filehandle
positionne l'itérateur imagick à la dernière image
importe des pixels depuis un tableau dans une image
se déplace vers la prochaine entrée
égalise l'histogramme de l'image
la fonction geoip_record_by_name() retourne les informations enregistrées correspondant au nom de l'hôte ou à l'adresse ip
éclate une chaîne par expression rationnelle
définit si ce noeud est ouvert ou non lorsque le contour est affiché pour la première fois
détruit le réseau en entier et libère proprement toute la mémoire associée
ferme un dossier d'archive donné
retourne un tableau contenant la prochaine ligne d'une requête
si la boucle d'événements par défaut n'est pas créée la méthode evloop::defaultloop() la crée avec les paramètres spécifiés
cette fonction retourne la liste de tous les pilotes pdo disponibles qui peuvent être utilisés avec le paramètre dsn de la fonction pdo::__construct()
libère la mémoire et les données associées avec le jeu de résultats postgresql associé
retourne la valeur de l'option option de configuration php
remplace count caractères en partant de la position offset par les données data
affiche le message d'erreur
modifie l'action javascript d'un champ donné
définit les facteurs d'échantillonnage de l'image
ajoute un flou de déplacement
tente de verrouiller le mutex de l'appelant
détermine si une variable est définie et est différente de null
récupère les données de l'application précédemment définies avec la méthode gearmanclient::setdata()
cette méthode est appelée en réponse à fwrite()
définit le commentaire pour une entrée définie par son nom
un mécanisme pseudo-objet permet la lecture d'un dossier
la méthode yaf_controller_abstract::__construct() est finale ce qui signifie qu'elle ne peut être surchargée
récupère la valeur par défaut du paramètre d'une fonction ou d'une méthode définie dans l'espace utilisateur
retourne le nom de la table à laquelle le champ appartient ou l'oid de la table si le paramètre oid_only vaut true
est une combinaison de fbsql_fetch_row() et fbsql_fetch_assoc()
active un élément de structure précédemment créé ou un autre élément de contenu
retourne la valeur d'un attribut basé sur sa position ou d'une chaîne de caractères vide si l'attribut n'existe pas ou n'est pas positionné sur le noeud
retourne la couleur de l'indice de la carte des couleurs spécifiée
change la base de données courante et active sur le serveur associé avec l'identifiant de connexion link_identifier
ouvre déchiffre) les données sealed_data en utilisant la clé privée priv_key_id et la clé d'enveloppe env_key et remplit open_data avec les données déchiffrées
ferme le lien au serveur de données informix associé à l'identifiant de connexion link_identifier
récupère le nombre total de cache hits
la méthode sqliteresult::key() retourne l'index de la ligne courante d'un jeu de résultats bufferisé result
cette méthode est appelée en réponse à readdir()
récupère le code erreur de l'analyseur xml
définit modifie la valeur de la propriété
définit la largeur des contours de l'objet
récupère la limite inférieure du premier intervalle de la date pour toutes les facettes de date pour ce champ
lit une ligne de résultat dans un tableau associatif depuis une requête odbc
cette fonction retourne le jeu de caractères sur la connexion cubrid courante et est similaire à la fonction cubrid compatible mysql cubrid_client_encoding()
retourne un index de couleur à tous les coups
arrête l'interface newt
retourne le nombre de niveaux d'imbrications du système de temporisation de sortie
retourne le corps du message numéro msg_number de la boîte aux lettres courante
retourne la hauteur de la police font en pixels
équivalent de la fonction imagick::thresholdimage() mais force tous les pixels au-delà du seuil threshold en noir et laisse les autres pixels inchangés
quelques bases de données supportent les procédures stockées qui retournent plus d'une ligne de résultats aussi connu comme des jeux de résultats)
copie les messages email spécifiés par msglist dans la boîte aux lettres nommée mbox
définit la hauteur et la largeur d'une vidéo
définit le nom de fichier avant la lecture ou l'écriture d'une image
définit les options du socket
effectue un test du module ouvert et désigné par td
définit le contenu de la réponse
retourne la limite de la ressource
recoit un message du socket
calcule la taille du buffer qui doit être allouée pour recevoir les données auxiliaires
vérifie si un champ existe
lit la valeur courante
retourne la première raison d'erreur
dénombre la population de a
retourne le contenu d'une cellule de la ligne et à la position spécifiées depuis le jeu de résultat sybase
ouvre ou réutilise une connexion à un serveur mysql
attache un attribut entier à la requête radius courante
récupère une variable stokée dans le cache utilisateur
décode un message mime qui contient des données non ascii voir rfc2047 
implémente un formatage directe en mémoire
cette fonction analyse une url et retourne un tableau associatif contenant tous les éléments qui y sont présents
décompresse la chaîne source contenant des données compressées bzip2
retourne le succès de l'action
se connecte au serveur searchd exécute une requête de recherche puis lit les résultats obtenus
libère une ressource de dictionnaire
lit la cible d'un lien symbolique
indique si l'internaute a abandonné la connexion http
retourne le facteur utilisé par l'expression rationnelle de fragmentation pour la déviation depuis la taille idéal du fragment afin d'accommoder l'expression rationnelle
efface tous les codes d'erreur qui ont été enregistrés pour le socket socket ou bien pour le socket général
retourne true si le tag existe mais qu'il est vide
jette le contenu de l'entrée du tampon du terminal sans attendre d'autre entrée additionnelle
crée un objet gender en se connectant optionnellement à un dictionnaire externe
retourne un tableau avec des informations à propos d'un paramètre après qu'une requête n'ait été préparée
sérialise la liste
retourne le gestionnaire de fichier qui supporte les opérations de lecture
vérifie si val est une valeur légale finie sur la plate-forme
récupère le nombre de neurones sortants
ajoute la chaîne data à la fin des données dans le noeud
cette fonction ferme la ressource ouverte par finfo_open()
mongo a une fonction permettant de voir le contenu du curseur en temps réel similaire à la commande unix tail -f
calcule et retourne le rectangle entourant le texte text écrit avec une police truetype
définit un masque d'ombrage en utilisant un objet d'ombrage
retourne la version du protocole mysql utilisé
modifie la valeur d'un champ fdf
cette méthode retourne une version linéarisée de la table de hachage dans un format que la fonction loadfromstring() peut lire
récupère une constante définie
positionne le curseur sur le nom de l'attribut dans l'espace de nom spécifié
cette méthode est la méthode la plus simple pour récupérer une base de données
envoie toutes les notifications en attente et arrête la mise en file d'attente de nouvelles
retourne un tableau d'erreurs depuis l'appel le plus récent à une fonction mysqli qu'elle est réussi ou non
construit un objet recursivedirectoryiterator() concernant le dossier souhaité
retourne un tableau d'informations sur toutes les files d'attente de connexion
récupère toutes les facettes des champs
bien qu'il soit possible de manipuler des dates à partir de l'an 1 3761 avant j.c. une telle utilisation a peu de sens
retourne la version de l'apiimagemagick api sous forme de chaîne et de nombre
cette fonction crée une nouvelle instance de la classe domattr
récupère les informations d'une extension
la fonction mysqli_get_server_version() retourne un entier représentant la version du serveur sur lequel on est connecté représenté par le paramètre link
récupère la structure des données du noeud courant
récupère le fils de l'élément courant
récupère la valeur du paramètre facet.method
retourne les données de l'enregistrement record dans un tableau
ajoute une ou plusieurs options à celles déjà présentes
associe une variable php avec une colonne pour une requête de récupération de données utilisant la fonction oci_fetch()
retourne le prototype de la méthode
retourne la liste de tous les serveurs qui sont dans sa liste
multiplie l'opérande left_operand par l'opérande right_operand
calcul le reste de la division entière de n par d
retourne le code d'erreur de la dernière opération sur la connexion frontbase fournie
crée une nouvelle session ou réinitialise une session existante
construit un nouvel objet recursivecachingiterator qui pourra ensuite être passé comme itérateur
si aucune base de données n'est fournie la base de données courante sera utilisée
récupère la variable depuis le client ; cette méthode va chercher le paramètre name dans les paramètres de la requête ; si le nom n'est pas trouvé, alors la recherche se poursuivra dans les variables post get cookie server
récupère l'acl pour la boîte aux lettres donnée
la fonction cubrid_lob_close() est utilisée pour fermer tous les blob/clob retournés depuis la fonction cubrid_lob_get()
retourne une connexion persistante à une base de données ibm db2 universal database ibm cloudscape ou apache derby
récupère le numéro d'erreur mnogosearch
place des guillemets simples autour d'une chaîne d'entrée si nécessaire et protège les caractères spéciaux présents dans la chaîne d'entrée en utilisant le style de protection approprié au pilote courant
calcule le hachage de la chaîne to_hash en utilisant le contexte déclenché par morceaux de hachages et retourne le hachage correspondant
exécute une requête soap
vérifie si la méthode est protégée
crée une instance stoppée du watcher evprepare
définit le nom de la classe de l'itérateur du tableau utilisé par arrayobject::getiterator()
stocke un certificat x509 dans le fichier output au format prm
convertit une chaîne de caractères en tableau
adoucit les contours d'une image tout en préservant les informations de bords
démarre la boucle principale du serveur racine
lit le nombre de dossiers et fichiers trouvés par l'expression glob
retourne une chaîne avec le numéro de version du serveur
supprime un élément à une position donnée
annule une transaction interbase
libère une clé privée créée par la fonction openssl_pkey_new()
compare le tableau array1 avec un ou plusieurs tableaux et retourne les valeurs du tableau array1 qui ne sont pas présentes dans les autres tableaux
soumet la tâche au prochain worker du pool
retourne la liste de toutes les fonctions ajoutées à l'objet serveur soapserver
récupère plusieurs valeurs qui ont été fixées par ps_set_value()
vérifie si la souscription à un service est valide
connexion à un dictionnaire externe
calcule les paramètres d'échelles d'entrée et de sortie utilisés en se basant sur les données d'entrainement
récupère la propriété globale de la gravité pour l'objet imagick
lit les options d'un analyseur xml
ouvre le blob blob_id en lecture et envoie son contenu directement vers la sortie standard le navigateur dans la plupart des cas
une fonction de rappel de type callable utilisée par le paramètre de contexte notification appelé lors d'un événement
retourne le code d'erreur du fichier bz2 représenté par le pointeur bz
compte tous les éléments d'un tableau ou quelque chose d'un objet
la fonction cubrid_set_drop() est utilisée pour supprimer un élément depuis un attribut donné de la base de données
le facteur par lequel le fragmenteur regex peut dévier de la taille idéale du fragmentspécifié par solrquery::sethighlightfragsize pour arranger l'expression rationnelle
définit le poids de la police de caractères à utiliser lors d'une annotation
retourne l'objet servant comme clé dans le map à la position courante de l'itérateur
récupère les en-têtes de la dernière réponse
lit les options de comportement de l'objet arrayobject
définit le format d'une image particulière d'une séquence
lance/défini un objet syncevent
retourne le nom de domaine nis par défaut
ajoute une frame à un tag id3v2
retourne la version de l'api d'une archive phar ouverte
crée une nouvelle relation
est une version évoluée de msql_fetch_row()
récupère la fonction déclarante
les périphériques ømq peuvent effectuer une médiation entre les adresses les services les files d'attente ou toute autre abstraction pour définir le message et l'interface des sockets. -- zguide
lie un facet.date.start
est identique à la fonction gzgets() excepté que gzgetss() tente de supprimer toutes les balises html et php du texte lu
récupère le nombre de lignes affectées par la dernière requête
stoppe la boucle d'événement
construit un nouvel objet tokyotyrant et optionnellement se connecte à la base de données
récupère des statistiques de la base de données distante
vérifie si l'itérateur est toujours sur un élément map valide
les méthodes evloop::suspend() et evloop::resume() suspendent et reprennent une boucle
crée un miroir horizontal de l'image en reflétant les pixels autour d'un axe y central et en effectuant une rotation de 270 degrés
retourne la taille d'un champ
retourne la largeur de l'image référencée par image
active les polices japonaises internes
cette fonction ne continue pas tant qu'une touche n'est pas appuyée
retourne un tableau avec toutes les interfaces déclarées
retourne le masque du clip de l'image
effectue un entrainement sur un jeu complet de données qui peut être lu depuis un fichier pour une période de temps
définit le texte qui doit apparaître avant un terme colorisé
assigne une valeur au moteur de rendu la rendant ainsi disponible directement par son nom au sein du template
assemble left avec right et retourne le résultat
déchiffre le message s/mime contenu dans le fichier infilename en utilisant le certificat et la clé privée spécifiés par recipcert et recipkey
définit une fonction de rappel à utiliser pour récupérer les informations de statut mis à jour depuis l'agent
effectue la césure d'une chaîne
divise n par d
retourne les espaces de noms déclarés dans un document
cette fonction déplace le pointeur de lignes interne du résultat cubrid associé avec l'identifiant de résultat spécifié) pour pointer sur un numéro de ligne spécifique
la fonction geoip_db_get_all_info() retourne des informations détaillées sous la forme d'un tableau multidimensionnel sur tous les types de bases de données geoip
crée une nouvelle instance haruannotation
crée un diff binaire de 2 fichiers et enregistre le résultat dans un fichier patch
surveille une collection de fichiers dans le dossier spécifié
lit les informations sur le fichier courant
ré-initialise le tableau après sa dé-linéarisation
remplace la clé primaire courante de la relation relation avec la combinaison de domaines spécifiée par domlist
vérifie si un itérateur peut être obtenu depuis l'entrée courante
efface un gestionnaire de rappel installé précédemment et restaure les paramètres du terminal
exécute une négation sur les bits sur le paramètre variant et retourne le résultat
supprime tous les suffixes courants de recherche depuis la base dns ; la méthode eventdnsbase::addsearch() ajoute un suffixe
méthode de convenance pour définir la taille de rognage et la géométrie de l'image depuis des chaînes de caractères
efface un fichier sur le système de fichiers distant
re-défini la constante constant fournie en value
tente de charger une extension de la bibliothèque sqlite
définit l'option pour essayer de sauvegarder toutes les informations du canal alpha en opposition à la transparence à couleur unique) lors de la sauvegarde d'images png
supprime le verrou en écriture sur la ligne tuple précédemment obtenu par dbplus_getlock()
retourne une propriété d'une image
récupère le paramètre d'identification dont la clé est key
définie le nombre maximal d'enregistrements que la requête doit retourner
affecte la valeur newval à l'index index
surcharge les fonctions intégrées les remplace dans la table des symboles
synchronise le client postgresql normalement un processus serveur web avec le serveur postgresql après une opération de copie faite par pg_put_line()
ceci retourne le nom d'hôte que mongoid utilise pour générer des identifiants uniques
permet de spécifier l'en-tête http string lors de l'envoi des fichiers html
enlève le premier élément depuis la table de propriétés de l'objet
retourne une chaîne qui représente le type de connexion utilisé avec la connexion link_identifier y compris le nom du serveur hôte
récupère l'instance yaf_application
retourne la taille du champ field oracle
récupère le code d'erreur de la dernière opération fdf
fixe plusieurs valeurs qui sont utilisées par beaucoup de fonctions
modifie les permissions d'un fichier ou d'un dossier
définit la profondeur maximale autorisée
définit la taille en caractères du fragments à considérer pour la coloration
appelé lors de la fermeture du gestionnaire de flux tout de suite après l'appel à la méthode streamwrapper::stream_flush()
ajoute une nouvelle entrée à l'ensemble et retourne des informations sur l'ajout
supprime l'objet référencé ou tous les objets si les paramètres vallent void) depuis la file d'attente du worker référencé
soustrait une taille de length octets du tampon en commençant à la position start
implémente l'algorithme de comparaison qui ordonne les chaînes tel qu'un homme le ferait
ferme une archive ouverte ou nouvellement créée et sauvegarde les modifications
récupère le contenu du chemin pcos
lie un facet.limit
convertit un nombre gmp en chaîne de caractères dans la base base
retourne les propriétés informix sql pour tous les champs d'une requête sous la forme d'un tableau associatif
retourne la configuration dans une chaîne de type ini dans un tableau associatif
choisit la couleur utilisée pour le dessin et le remplissage
récupère le nombre total de neurones dans la totalité du réseau
recherche dans a en commençant à la position start vers les bits de poids lourd jusqu'à ce qu'elle rencontre un bit à 0
récupère le nom d'une fonction
détermine le mode d'optimisation de l'extension lzf
annule toutes les modifications non validées pour une connexion connection oracle et met fin à la transaction courante
vous pouvez définir cette fonction pour activer le chargement automatique des classes
retourne la clé correspondante précédente de la base de données
récupère le mode courant de graphique
met à l'échelle la taille de l'image aux dimensions voulues
génère une chaîne de caractères qui représente le nom du chemin du terminal courant du processus
la fonction pcntl_signal_dispatch() appelle les gestionnaires de signaux installés par pcntl_signal() pour chaque signal en attente
crée un signet avec diverses options
crée un tableau
initialise les poids en utilisant les algorythme widrow et nguyen
le flux 2-legged ou la demande de signature
récupère les informations d'un drapeau
retourne le code erreur de la dernière regex pcre exécutée
prend la chaîne str et retourne un nombre presque unique sur 32 octets
définit temporairement pour une requête) la valeur d'attribut d'un document
la fonction cubrid_version() est utilisée pour récupérer la version du module php cubrid
lit un document fdf à partir d'une chaîne de caractères
ajoute une ellipse au chemin courant
retourne le dossier de travail courant
retourne le event loop time courant qui est la durée pendant laquelle la boucle d'événement reçoit des événements et démarre leurs anaylyses
récupère les variables statiques
récupère le nombre maximal de contraintes qui peut être retourné pour les facettes des champs
retourne la taille d'un fichier donné en octets
récupère l'indice de l'image active courante dans l'objet gmagick
etablit le temps courant en demandant au kernel et mise à jour du temps retourné par evloop::now() pendant l'exécution
retourne le type d'un fichier donné
vérifie la liste des fonctions définies par l'utilisateur ainsi que celles internes à php afin d'y trouver function_name
fusionne les fichiers file1 file2 et file3 en un seul et le stocke dans dest
crée une ressource email mime
ferme la connexion ftp_stream et libère les ressources
cette fonction retourne une chaîne de caractères avec les drapeaux de la position du champ fourni séparés par un espace
la fonction snmprealwalk() est utilisée pour parcourir des objets snmp en commençant à l'objet identifié par object_id et retourne non seulement les valeurs mais aussi les identifiants des objets
ajout un contenu à un bloc de contenu existant
définit le style de police de caractères à utiliser lors d'une annotation
déplace le curseur sur le prochain attribut si positionné sur un attribut ou déplace sur le premier attribut si positionné sur un élément
retourne le type d'un champ dans le résultat d'une requête
effectue un entrainement sur une itération avec un jeu d'entrées et un jeu de sorties désirées
configure le nom de fichier d'une image dans une séquence
définit les paramètres pour le contexte spécifié
retourne le message d'erreur fdf
retourne le numéro du signal qui a provoqué la fin du processus fils
sauvegarde l'objet sdo_das_xml_document vers un fichier
crée une nouvelle instance de la classe hw_api_object
récupère la version des bibliothèques clientes svn
ajoute une section d'une courbe bézier cubique décrit par les trois points de contrôle donnés au chemin courant
établit une connexion persistante vers un serveur cubrid
si le curseur a commencé son itération la commande sera ré-exécutée
est équivalente à la méthode memcached::touch() excepté le fait que le paramètre server_key peut être utilisé pour lier la clé key avec un serveur spécifique
construit l'objet de boucle d'événements
crée un nouveau fichier de base de données paradox
charge un jeu de constantes depuis le cache
compare la valeur value1 avec la valeur value2
configure les paramètres de sécurité des sessions du protocole snmp v3
lie un serveur http sur une adresse et un port spécifiés
retourne le jeu de caractères utilisé par le client ingres parmi les jeux ii_charsetxx où xx est le code d'installation)
cette méthode met à jour une entrée avec une nouvelle valeur et retourne le fait que l'entrée a bien été mise à jour ou non
retourne le code xml de la dernière réponse soap
configure le mode de classement
récupère l'inode d'un fichier
retourne la clé courante dans le tableau array
récupère la taille totale du cache
exécute une commande sur un serveur distant
définit des drapeaux
configure le mode bloquant du flux stream
reportez-vous à la section sur les requêtes de ce manuel pour plus d'informations sur la distribution de lecture aux secondaires
demande le nombre qui sera garantit comme unique pour la relation relation et le retourne dans le paramètre uniqueid
retourne le dernier code retourné par le serveur de travaux
définit l'image de rendu
récupère les quotas de chaque utilisateur
ferme la session courante
spécifie un formateur pour la sortie colorisée
ecrit des données vers l'évènement bufferisé indiqué
ferme toutes les connexions vers le daemon de mise en cache rrd
crée une nouvelle instance de l'objet depuis la chaîne content
retourne le code xml de la dernière requête soap émise
retourne le nombre de paramètres d'une requête préparée
retourne la position courante (à partir du début) du pointeur de lecture sur l'objet de grande taille large_object
ouvre un fichier image local ou un fichier image virtuel suivant diverses options
fait une copie du fichier source vers le fichier dest
dessine une ellipse sur l'image
retourne un tableau contenant les informations d'un champ après qu'une requête de type sélection ait été exécutée
lit la liste des gestionnaires de flux disponibles sur le système courant
efface un objet avec un parent spécifique
configure la compression par défaut de l'objet
lit le contenu du cache
définit le mode de l'opération
retourne le nombre d'enregistrements contenus dans la base de données
supprime un clé publique autorisée
ajoute un élément au registre
crée un nouveau bouton
supprime un en-tête http depuis les en-têtes de la requête
ajoute une nouvelle page au document
active les vérifications de certificats et d'hôtes sll activé par défaut)
ajoute un libellé à une image
charge un typelib et enregistre son contenu dans le moteur comme s'ils avaient été définis en utilisant define()
déplace le nombre d'octets fourni depuis un tampon source vers la fin du tampon courant
retourne les clés numériques et littérales du tableau array
retourne le sinus de arg arg in radians
route une requête entrante
appelle une fonction ou une méthode utilisateur nommée function avec les arguments qui suivent
exécute une requête sql préparée par odbc_prepare()
retourne si le champ ne doit être mis en évidence que si la requête correspond à un champ en particulier
crée un nouvel analyseur xml avec le support des espaces de noms et retourne une ressource qui pourra être utilisée avec les autres fonctions xml
retourne le nombre de lignes qui ont été affectées par la dernière requête insert update ou delete qui a été exécuté dans le contexte de transaction spécifié par link_identifier
effectue une recherche
exporte la clé fingerprint
retourne le code erreur résultant de l'appel le plus récent à une fonction interbase
crée un processus de pointeur de fichier exécuté en effectuant un fork de la commande fournie par le paramètre command
récupère la requête brute envpyée au serveur solr
retourne une chaîne contenant la version du serveur oracle ainsi que les options disponibles
récupère la hauteur de l'image
indique si la variable donnée vaut null
retourne les données de débogage de la dernière tentative de connexion
construit un nouvel objet représentant un fichier temporaire
ouvre un document fdf
retourne le message du statut de l'erreur du système ou de zip
détermine si la variable donnée est de type chaîne de caractères
crée un nouvel objet domimplementation
la fonction geoip_id_by_name() retourne le type de connexion internet correspondant au nom de l'hôte ou à l'adresse ip
retournes des statistiques sur le client par processus
invoque une fonction reflétée
ajoute un commentaire à une image
calcule la tangente hyperbolique pour chaque valeur du paramètre real et retourne le tableau résultant
définit la taille en points de la police de caractère utilisée lors d'une annotation
c'est le premier hook dans le plugin système des hooks yaf ; si un plugin personnalisé implémente cette méthode alors elle sera appelée avant de router la requête
ouvre une fenêtre centrée de la taille spécifiée.
modifie le propriétaire du fichier spécifié par le descripteur de fichier fd
identique à imagick::thresholdimage() mais force tous les pixels en dessous du seuil en blanc laissant tous les autres pixels inchangés
ajoute le mot word au dictionnaire fourni
retourne le dernier code gearman retourné
définit l'encodeur actuellement utilisé pour le document
associe des données à un objet du stockage
décode un champ d’en‐tête mime
retourne la valeur d'un champ fdf
liste les champs de la table mysql spécifiée
retourne le nombre de lignes affectées lors de la dernière requête insert update replace ou delete avec link_identifier
retourne le style de la police de caractères utilisée lors d'une annotation
retourne l'index de la couleur du pixel situé aux coordonnées spécifiées dans l'image image
soustrait l'opérande right_operand de l'opérande left_operand
récupère le nom de la classe parente pour un objet ou une classe
retourne la chaîne str après avoir ajouté des antislashs devant tous les caractères qui sont présents dans la liste charlist
remplace l'image de pavement courante par l'image tile à utiliser dans tous les remplissages comme avec les fonctions imagefill() et imagefilledpolygon() lors des remplissages avec l'option img_color_tiled
la fonction geoip_domain_by_name() va retourner le second niveau du domaine associé avec un nom d'hôte ou l'adresse ip
défini le nombre de propriétés par événement de base
fusionne les calques de l'image en un seul
crée un diff binaire de 2 fichiers et enregistre le résultat dans un fichier patch
retourne les espaces de noms utilisés dans un document
récupère les itérations de l'image
exporte une classe reflétée
récupère le code erreur de la dernière opération
cette fonction permet d'ajout un nouvel élément l'arbre des boîtes à cocher
crée un nouveau domdocument basé sur une différence
cherche la position numérique de la première occurrence de needle dans la chaîne de caractères haystack
lit la valeur de l'élément courant
ajoute un nouvel élément dans le poll et retourne l'id interne de l'élément ajouté
retourne le dernier code retourné par gearman pour une tâche
exporte une réflexion
cette fonction retourne une nouvelle ressource bbcode utilisée pour analyser les chaînes bbcode
retourne la somme des valeurs du tableau array
retourne le segment de chaîne trouvé par une expression rationnelle multi-octets
indique si le document est un commentaire
trie un résultat depuis un appel à la fonction dbx_query() avec une fonction de trie personnalisée
retourne la chaîne str après avoir converti tous les caractères alphabétiques en majuscules
lit l'index de l'image active courante dans l'objet imagick
les archives tar/zip non exécutables ne peuvent pas avoir d'alias donc cette méthode ne fait que soulever une exception
trie les entrées par les clés en utilisant une fonction définie par l'utilisateur
ajoute une nouvelle entrée au hash et retourne des informations sur l'ajout
ouvre un dossier dans un fichier zip pour lecture
charge les données issues du fichier identifié par handle jusqu'à la fin du fichier
génère un message qui sera inscrit dans l'historique par le système
définit un objet de destination qui devient une cible à atteindre lorsque le contour est cliqué
déplace le pointeur interne de ligne dans le résultat de requête result_identifier jusqu'à la ligne row_number
cette méthode vous permet d'enregistrer votre propre classe étendue dom à utiliser ensuite dans l'extension dom de php
sert à lire une valeur d'un objet snmp représenté par object_id
convertit le paramètre pathname d'un fichier existant et de l'identifiant de projet proj en un entier integer à utiliser avec la fonction shmop_open() et d'autres fonctions system v ipc
vérifie si un attribut dans l'espace de noms namespaceuri nommé localname existe en tant que membre de l'élément
termine une page et y applique diverses options
retourne les valeurs d'une colonne du tableau d'entrée array identifiée par la clé column_key
retourne le délai d'exécution en millisecondes autorisé pour une requête
libère toutes les ressources associées à un objet imagick
ajoute le serveur indiqué au pool de serveurs
ouvre explicitement une connexion ftp sécurisée avec ssl sur l'hôte host spécifié
active les méthodes de dessin rapide antialias concernant les lignes et les polygones
cette fonction crée une nouvelle instance de la classe domtext
récupère la valeur courante d'un attribut de document spécifié
