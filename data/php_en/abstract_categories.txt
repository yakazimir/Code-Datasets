xml	simplexmlelement sdo_das_xml simplexmliterator xmldiff\memory sdo_das_xml_document xmldiff\file xmldiff\dom xmlreader xmldiff\base
thread	threaded thread
variable_names	$condition_type $error_handler $cascade_candidate_limit $nsname $output $identifier $height $dither $def_value $io_threads $option $agent $hint $attrlat $array2 $buf $isname $xml_file $replace_by $use_keys $item $exit_struct $parameters_array $blob $path_to $ascii $table-name $sql_param $intattrcount $imgtype $untrustedfile $new_min $maxlifetime $appname $revision $country_code $charlist $new_output_max $bordercolor $errornum $full_status $jday $srcy $srcx $domlist $wantsink $disposetype $dest_width $use&quot; $channel $cache_limiter $using_exceptions $div $sec_level $dir $msgtype $icon $client_identifier $wtimeout $configuration_file $new_data $watch_descriptor $imageformat $buttons $use_errors $dirent $dbaddr $field_index $rows $object_name $max_bytes $ca_path $descriptor $c2 $left $row_number $method_name $report_uncaught_exceptions $assertion $old_mbox $array_or_countable $text_domain $timelimit $selectors $newvalue $desired_output $opcode $bid $initialize $imageinfo $rights $savepath $neuron $userdata $queryfield $progid $linecap $metrictype $namespaceuri $rownumber $param $msgs_in_queue $imagick $stylesheet $condition_option $afmfile $step $nbytes $year $tail $iter $passwd_len $type $wildcard $blocking $action_name $clientoptions $qualifiedname $batch_size $result_type $node_name $new_data1 $purpose $new_data2 $message_type $rprop_decrease_factor $field_value $max_epochs $wait $tile_geometry $compare $fieldboostvalue $key_len $priv_passphrase $error_types $hebrew $stroke_width $userid $crop $number_of_folds $pixels $pharpath $max_chars_per_line $except $table_name $new_input_min $jargon $with_comments $nowait $numeric_prefix $add_query_to_cache_if_not_exists $buffer $fd2 $syntax $age $event_handler $service_level $blob_handle $result_identifier $pass $hash_algorithm $pasv $variable-name $max_memory $textflow $broker $minwordlength $callback_function $attr $out_params $typelib_name $bind_index $unixtimestamp $driver_options $section $artifact $antialias_steps $find_query_in_cache $morphologymethod $linefeed $dump_directory $frontend $measureerror $nth_index $mustunderstand $timeperiod $sourcedoc $cascade_max_out_epochs $servicename $num_data $language $body $days $soap_request $words $files $sarprop_step_error_shift $binary_string $dbpath $contents $src_im $position $certs $dns_base $arg_num $sid $sig $mimemail $limited $sourcefp $nullpolicy $match $limiter $rev1 $rev2 $store_result $separator $cursor_offset $cainfo $off_hour $keydata $opts $size_correction $writecb $backend $radix $bestfit $lenght $seconds $req $res $query_identifier $ord $cacert $small $session_mode $red $ref $colorize $new_width $raw_request $tagsets $from_encoding $workload $ranker $timeout_write $pathto $pipeline $pathname $note_value $fieldlength $lib_dir $slant $param_name $driverdata $session_data $angle $row_a $page_number $contrast $bytes_transferred $library $part_number $infile $clear_cache $schema_type $max_neurons $from_neuron $string $from_root $color_space $pct $ndocs $shmid $preview $bmax $in $case $max_item_length $id $max_size $hconn $part $it $iv $ip $x509 $calendar $sections $cascade_activation_functions $access_token_url $startpos $dictionary $set_element $modifiers $major $hosts $learning_rate $preserve $rrd_file $database_name $unescaped_string $eol_style $par3 $par1 $dir_handle $resolution $oldnode $attribute $word_wrap $val $feature $data_type $protocoloptions $consumed $base_dir $var $quickprop_mu $font $fam_monitor $token_secret $queries $brightness $auth_passphrase $frame $is_output $mysql_connection $lob_to $factors $reference $zip_entry $challenge $num_neurons2 $priv_key_id $num_neurons1 $sandbox $batchsize $form $client_info $white_point $descriptorspec $column_key $server_hash $component $regs $imageid $functions $request_token_url $top $inc_by $miter $cmp_function $pxdoc $pk_qualifier $check_consistency $gearman_return_t $columns $is_associative $value_cb $encoded_headers $brigade $env_keys $text $original $req_identifier $to_encoding $how $max_table_length $parse_mode $method $embedtext $close_path $encoding_list $max_acquire $cut $response_code $timestamp $parent_outline $timeoutms $argument_count $spacing $magic_file $config $at_front $border $faultname $max_decoded_len $offset $local_path $accuracy $fk_qualifier $x3 $x2 $x1 $x0 $seed $cookie $exp $complement $parser $source_string $driveroptions $utf8string $url $out $uri $datetype $cascade_min_out_epochs $stream_or_context $ury $urx $module $msgdesc $connection_rate $full_info $key $connection_string $classes $attrlong $low $oid $replace $newnode $resource $rpmr $log $xsd_file $connoptions $bbcode_initial_tags $quota_limit $defaulthost $headerfault $principal $extendedclass $layers $local_username $consumer_secret $layer $auth_protocol $normalized $parameter_type $errcode $database_password $auth_session_handle $request_parameters $json $field_number $spelling $filenames $dec_by $max_repetitions $zurl $workfactor $jd $gid $fk_owner $neurons_between_reports $is_prefix $iptcdata $painttype $locale $kernelstring $persistent_id $to_hash $pubkeyfile $indexfile $appendrawoutput $namespace_uri $order $saturation $indent $newdir $conn $get_scripts $window_handle $gstate $tidy $noise $background $route $usec $is_xhtml $num_layers $user $reps $group $client_flags $num_output $new_value $opsys $with_error_message $numerator $cipherid $tablename $ignore $facetquery $bottom $error_message $nanoseconds $interlace $host_name $texture_wand $nodns $blackls1 $values $cfg $ms_timeout $pad_type $expect $column-name $array_result $callbacks $train_data $callback_url $signal $database $docs $frompath $prompt $draw $streams_context $statement $value1 $webindexfile $y0 $y1 $y2 $y3 $mask_image $rprop_increase_factor $mincount $compcode $signature $timeout_read $event $autoload $handle $query_string $ctx $tagnum $luminosity $signature_alg $pmo $caches $x_resolution $bson $longitude $baseclass $personal $isolation $param_arr $sparse_method $input $newscope $entry $from $off $checkbox $functionname $mva $new_include_path $plugin $interval $methods $default $table $trans_args $tv_usec $introspection $rprop_delta_zero $optlist $encoding_mode $mysqli_connection $checkboxtree $flags $service_level_option $tobase $disable $capath $hobj $zoom $other $doctype $level $boxname $index $param_value $sigma $args $success $directory $certificate $matrix $col_name $ctor_args $charactername $resumepos $cache_cb $reason $return_float $nonrecursive $proxy $arg4 $arg1 $arg3 $arg2 $ang1 $link_or_trans_identifier $character_mask $right $min_priority $escape_char $privkey $sortby $original_name $tpl_content $facetsort $cit_flags $large_object $error_function $version1 $read $relation $real $isid $fieldvalue $version2 $x509cert $remote_socket $server_socket $fragmenter $x509certdata $token $newthis $tag $xresolution $more_entropy $output_vector $softcommit $search_value $newparent $old_file $sec_name $field_name $bbcode_container $journal $str $extract_type $what $record $auth_type $cwd $fetch_type $namespace $optval $archive $iterator_class $bind_value_type $script_code $lowerlefty $lowerleftx $data-type $dest_height $remote_file $pad_string $weights $extended $sortdirection $stroke_opacity $diff $revno $step_callback $fstype $index_start $error_msg $hmode $lock $preferences $zp $column_name $array1 $blocksize $verbosity $frameid $main_str $variables $cap $event_name2 $log_dir $event_name1 $value $noise_type $ok_packet $params $geometry $request $gmax $arrays $document_or_id $n_retries $maxlen $trimmarker $constants $timezone_identifier $patch $initial_value $delimiter $string_literal $http_response_code $dataoffset $utf16string $priv_protocol $new_name $dbase_identifier $n_priorities $numcolors $host $outer_bevel $multiline $exception_handler $pdfdoc $estimated $interlace_scheme $envelope $value_compare_func $memcache_connection $dn $mtime $db $first_page $deep $req_params $connect_type $operation $description $save_indexes $adjoin $new_size $increment $process $reverse $retphar $on_new_socket $base_group_id $tpl $liveforever $units $cols $index_key $bottom_left_x $bottom_left_y $page_per_pages $only_names $gamma $key_compare_func $max_matches $character $unserialize $cascade_candidate_stagnation_epochs $xstep $wouldblock $view $messageheaders $flag $case_sensitive $set_leading $sec $set $y_resolution $result $filesize $events $return_array $bytes $dest $mime_encoded_text $initial $cache_type $fieldname $targettopic $readable $message_code $xmlversion $http_headers $responsewriter $encoding $variant $char_list $link_identifier $isglobal $suffix_as_key $to_neuron $details $setting $write_timeout $unique $peername $elementset $fetch_flag $exception $is_final $bind_password $state $threshold $achtung_wild $exclusive $yray $delay $ident $ignore_transparent $targets $local_socket $schema $ex $treedepth $optname $constantname $font_index $row $rot $oid_only $formatter $ids $trace $compose $lob1 $lob2 $observer $ed $exclude $priv_key $encoded_string $block $configargs $count $field $subjectlength $mask $new_link $help $country $activation_steepness $idna_info $session_id $slop $retries $slot $which $deleteoldrdn $imagecontents $search_criteria $intattrs $externalpath $limit $port $shm_identifier $return_to_cache $working_copy $anything $edition $fam $gravity $siginfo $type_name $code_page $term_type $work $community $next $target_frame $connection $index_or_name $kerning $update_query_run_time_stats $factor $pos $minor $pk_owner $parameter $digit $month $writeconcern $variable $data $parameter-number $date $doc $chunk_size $perm $embed $bias $dirname $compression $faultactor $stop_function $word_space $query_is_select $updateoptions $user_data $debug_level $new_height $maxwordlength $offset_wand $lob_identifier_array $dash_on $problem $subject $raw_output $callbackfunc $user_string $publicid $ncolors $new_output_min $new_max $dispose $atime $blobidarray $blur $ey $quota_root $in_charset $sinkinterface $file_name $clut $blue $new_location $property_name $pad_length $jpegname $activation_function $bcc $escape_string $len $errormode $waitsearcher $skip $input_headers $database_options $fontsize $to $contextname $stack $result_id $bit_on $learning_momentum $td $ty $tx $bv_name $qualifier $par2 $recursive $typemap $soapheaders $conn_url $sealed_data $force $expr $cascade_num_candidate_groups $signmode $prefix $sense $new_path $max_len $mis $column $handler $components $min $algoname $reschedule_cb $cascade_activation_steepnesses_count $xpath $hour $bevent $still_running $stmt $oldname $end_revision $min_weight $ignore_upsert_status $owner_password $filter $gmo $error_code $stringparam $maxsegments $region_code $phi $maxanalyzedchars $dash_off $cmd $rprop_delta_max $fq $fp $fl $blob_data $foreground $fd $fg $codepoint $owner $hostname $micro_seconds $restrict $types $filtername $result_entry_identifier $spotname $pty $object $colnum $length $selectorcount $addtl $index_end $new_input_max $content $regex $iterations $keyword $on_off $dbhandle $green $needle $path_name $opened_path $signo $high $ndots $storage $src_y $src_x $securityprefs $levels $val1 $src_w $src_h $run $script $matches $imagickkernel $webindex $okay $fontfile $function_args $bytes_max $modifier $kerneltype $minute $callable $datefield $trigger $send_headers $hebrew_text $tdo $name1 $name0 $pieces $admin_server $infilename $operationheaders $day $substrchar $arr $oldset $version $errmsg $severity $key_identifier $arg $ftp_stream $session $auto_release $sql $scanner_mode $var_array $server_path $beginoptions $start $frame_name $msg_number $notext $orientation $displace $streamid $convmap $encodingfile $patterns_and_callbacks $decoration $data2 $gc $is_persistent $data1 $error_callback $line_pos $signature_method $fontid $pid $label $max $update $create $non_repeaters $modulus $username $condition $permissions $out_charset $query $gmin $getastype $fontname $contextnode $user_vars $aliases $uv $softlimit $oauthexception $class $fdf_data $fromlength $passwd $base $field_name_or_number $connection_id $sync $number $with_attrib $dict_type $type_namespace $tag_name $option_value $black_point $ip_address $microseconds $mutex $uid $params_array $old $uir $detail $a $inner_bevel $precision $radius $str2 $timeout_type $str1 $fdf_document $bind_rdn $wbmpname $yresolution $prepare_stmt $gmpnumber $rectangle $new_file $old_data $signed_text $scaling $extension_name $bbcode_arg_parser $tv_sec $after $user_agent $subscribed $effect $max_callbacks $up_right_x $up_right_y $authns $point_size $channel_type $desiredmsgtype $layout $cursor_type $bind_arg $sort_flags $n $m $extended_data $k $i $f $g $d $e $b $c $desired_error $sp_name $rpath $x $y $w $r $image_type $p $q $status $simplepre $write_options $csr $frombase $newflags $composite_object $gray $preserve_keys $imagedata $opacity $server_key $collection $blob_id $cascade_min_cand_epochs $newname $points $dsn $comobject $connections $hardlimit $threshold_map $servername $maxlength $buffers $origin $vendor $shading $revents $array $blendmode $xdoc $queue $assoc $rmax $address $cursor_orientation $wsdl $watermark_wand $extension $max_tries $stream $listener $write $who $client_area $alpha $lob_type $interface_name $pagesize $flatness $comp_flags $word $element $permission $min_savings $property $properties $bucket $registernodens $sorttype $timeout_interval $lowmark $new_password $extraattribs $depth $scope $denominator $object_id $destroy $direction $sarprop_temperature $bgd_color $socket_type $auto_enable $saveflag $msgid2 $msgid1 $open_mode $overwrite $epochs_between_reports $procedure $servers $sortfilter $modify $cert $reflector $encoder $interface $cor $npriorities $function_code $max_interval $blockname $dispinterface $amplitude $fingerprint $author $ca_info $error $algorithm_directory $httponly $newrdn $allow_string $judy_type $training_algorithm $round $x_shear $parameters $ttl $actor $thumbnail $mod $search $whence $passphrase $enabled $plaintext $finfo $enc_type $topath $compatid $paramters $verbose $src_image $persistent $before_needle $foundonly $manual $newfile $oldfile $read_timeout $dst_h $spool $argument $fielddesc $salt $dst_x $dst_y $close $daemon $algorithm $num_input $format $y_shear $oid_format $refnode $bpc $psdoc $sftp $delimiters $url_encode $surface $prepend $repos $raise $param_key $signature1 $signature2 $hex_string $filters $pk_table $template_dir $serialize $supervar_name $expiry $whitepoint $memsize $dependencies $extrainfo $mimetype $expire $bind_value $highmark $user_password $sortorderby $newval $enum_print $extend $cascade_output_change_fraction $max_weight $cascade_output_stagnation_epochs $host_ip $param_type $lookup_table $bmin $dbmode $deferred $expiration $iscritical $failure_callback $ann $magnitude $destination $parser_mode $nullable $gtrid $transfer_encoding $crypto_strong $alias $inputgamma $workingdir $wx $restart_syscalls $disp_time $closing $real_usage $as_array $props $max_depth $clear_realpath_cache $time $close_type $intparam $generator $genre_id $operand $mysql_flags $expression $addresses $base_dn $variable_key $upperrightx $upperrighty $write_ver $additional_headers $case_insensitive $peg $facility $data_size $radius_handle $mode $ind $inc $ini $outfile $extra_headers $preserve_cloned_files $backlog $varname1 $document_element_name $domain $attributes $bufferlength $node $revision_no $ordering $error_type $phase $family $param_number $latitude $charattr $link $split_length $pagenumber $record_number $codeset $line $additional_parameters $root_device $delta_x $callable_name $packet_id $cluster_threshold $replacement $items $scale $decrypted $f404 $style $unit $stream_or_url $sharpen $glyphname $juliandaycount $errno $extra_parameters $repository_url $searchresult $available $compression_type $secure $ystep $tiebreaker $headers $new_mbox $newobj $destfp $reqengine $sinkobject $weight $width $walk_to_top $arg_separator $double_encode $smooth_threshold $x_rounding $default_host $hue $current $gmt_offset $fuzz $response $infos $frequency $criteria $encodedfile $category $errorcode $autoload_function $with_cas $attrsonly $fill $file $ctorparams $priority $read_write $field_override $systemid $errstr $gmagick $val2 $serial $protected_resource_url $new $rise $shared_library $matte $lowerbound $image2 $outputgamma $image1 $pkey $enclosure $new_setting $mysqlnd_stmt $mindocfrequency $boost $envs $onoff $packet $munglist $classname $shadingid $quality $pngname $src $bulk $source $html_table_options $end $slabid $cases $week $namechoice $pseudostring $subscriptionid $retval $env_key $conn_identifier $start_index $column_number $job_handle $to_parse $second $include_dot $file_handle $rendering_intent $bg $bz $pair $xml_string $encoded_header $io_interval $pipes $errdat $in_params $profile $definition $table-type $process_identifier $tpl_vars $new_file2 $new_file1 $event_base $linebreak $azimuth $start_revision $ang2 $layer_method $ignore_max_retries $imap_stream $bitreverse $importednode $clip_mask $connection_proxy $cipher $use_include_path $name $open_data $chunks $databases $targetpath $session_name $filtertype $script_name $array1_sort_order $tile $pfmfile $readpreference $verifier_token $inotify_instance $raaindex $upperbound $underlying $metric $return $repeat $tuple $autounlock $fieldindex $max_recs $join $shtype $message $log_file $iptcblock $documentboostvalue $first_num $var_name $pointsize $trans_time $code $color $executable $internal_date $msglist $base_directory $recipcert $vert_bar $proper_address $action $faultcode $collector $extracerts $machine $invert $attr_name $envrion $return_var $resolver_function $rigidity $perms $summaryonly $role $nth $mintermfrequency $checkpoint $known_string $dbname $cursor_def $space $error_description $sizelimit $ctor $log_function $divisor $longopts $other_options $suffix $list $mangled $thickness $quickprop_decay $proj $word_size $raw $ch $all $stmtname $cb $cc $ca $consumer_key $comp_method $cx $cy $local_file $bootstrap $xshear $node_namespace $varname $maxsize $serialized $intattr $cache $fieldcode $c4 $expungedeletes $matcheddn $c3 $c1 $silent $crypted $entries $is_null $note_name $msecs $retry_count $assoc_array $writeoptions $path1 $path2 $opened $yshear $mailbox $sarprop_step_error_threshold_factor $haystack $tightness $real0 $real1 $constant $null_as $md $is_hex $mh $ticks_per_second $ms $jpeg_file_name $starttime $char_space $data_length $realm $categorize $dst_image $wordwrap $case_insensitivity $fetchtype $variable_name $row_b $signum $utf32string $in_fd $get_as_float $deref $function $hits $julianday $deleteoptions $start_element_handler $ns_prefixes $width_height_type $draw_settings $batch $target $sequence $vars $clob_handle $uniqueid $writable $prevcount $referrals $is_dst $num_cursors $maxrows $clob_data $hash $number_frames $fragment $armor $decimals $additional_parameter $dev $var1 $y_rounding $preg_flags $output_callback $dst_im $color_matrix $font_size $errorsorwarnings $fit $tcp_server $process_sections $warning $comment $accessible $timeallowed $basenode $controller_name $parent $command $functionid $cast_as $sarprop_weight_decay_shift $bit_fail_limit $scene $locking $reserved $get_hash $matte_color $recipkey $ending $lob_identifier $field_offset $dividend $options $composite $cascade_max_cand_epochs $service $server $fk_table $eventcb $elevation $faultstring $dst_w $image $percent $old_password $cutoff $colorspace $protocol $kernel $num_points $ry $rx $objdesc $function_name $open $query_data $colorcount $encryption_key $contextengineid $path_from $nseconds $clause $func $charattrlength $inside $num $task $dictionary_link $array1_sort_flags $cascade_weight_multiplier $x_server $tint $backup_original_files $rprop_delta_min $description_dir $seq_element $right_operand $create_mode $decode_binary $quick_print $sslcheck $class_name $beta $xmlresponse $location $password $page $context $execute $glue $socket $octal_string $extends $xmldeclatation $add_empty $fetch_style $amount $algo $face $relation_name $worker $left_operand $pub_key_id $metadata $off_min $sem_identifier $dfn $errorcb $map $dfd $timetype $pri $brush $phonemes $save_path $pkcs12 $startterm $minimal $cas_token $cpoint $localname $nonce $dialect $old_value $sql_statement $delete_old_session $mxhosts $bits $recurse $gmagickdraw $iterator $stub $tag_rules $syntax_only $title $input_parameters $one_way $new_cache_expire $qtime $elementid $padding $strict $privkeyfile $smiley $signcert $pgid $charattrs $msg $param_val $ns $filepath $coordinate_array $chunklen $mimetypes $atomic $channeltype $keys $final_callback $align $timezone $coordinates $result_array $statement_proxy $ox $lob_mode $output_headers $zip $shortnames $get_stats $op $filehandle $initialval $whichflags $similarity $strong $collapsefunction $construct $constraints $all_frames $size $binary $xray $create_sid $charset $dict $character_set $env $debug $sorting_order $connectioninfo $out_fd $fields $cascade_candidate_change_fraction $rmin $break $parameter-type $qmanagername $timeout $blackpoint $cb_func $imagetype $recipcerts $profilename $escape $cas_tokens $grp $opt $commitwithin $short_format $property_value $secret $addr $pub_key_ids $llx $lly $filename $input_vector $ray $spkac $file_extensions $rewrites $first $log_message $repos_url $degrees $skip_results $sx $sy $retry_interval $closure $lifetime $want_peer $fill_opacity $pattern $sh $normalize_array $sd $text_leading $readcb $document $recpos $extensions $arguments $user_compare_function $allowable_tags $genre $ok $on $suggestions $thumbnail_geometry $path $mode_directory $obj $module_name $oy $callback $notification_code $throw $black $groupsort $simplepost $sql_text $howmuch $end_element_handler $lob_from $info $user_function $entire_row $outfilename $zend_extensions $stream_filter $white $http_method $zenith $operator
magic_methods	set get clone autoload destruct call construct wakeup isset unset
connection	samconnection fannconnection eventhttpconnection mysqlnduhconnection
exception	Exception SolrClientException MongoDB\Driver\Exception\WriteException SolrServerException Yar_Server_Exception MongoResultException ErrorException SolrIllegalOperationException MongoWriteConcernException SolrException SolrIllegalArgumentException Error StompException MongoCursorException RarException
types	simplexmlelement iterator mysqlnd_statement imagickpixel boolean|string splobjectstorage dateinterval imagickkernel gmagickpixel generator yaf_view_interface gmp yaf_request_abstract domdocument string gmagickdraw domattr collable solrdocument domdocumenttype integer yaf_bootstrap_abstract closure zmqcontext resource oci-collection hw_api_attribute datetimezone traversable mongodb\driver\writeconcern int|mongoint64 eventconfig gmagick yaf_route_abstract mysqlnd_prepared_statement void sdo_xmldocument memcached domnode mongocollection eventbuffer mysqli sammessage splobserver mysqli_result yaf_config_abstract soapheader mixed float number solrparams imagick boolean tokyotyranttable array solrinputdocument splsubject mysqlnduhstatement long zmqsocket array|object call recursiveiterator|iteratoraggregate eventsslcontext magickwand exception mysqli_stmt double mysqlnduhconnection mongodb\driver\query judy mongodb\driver\readpreference eventdnsbase reflector int tidy mongodb\driver\bulkwrite ktaglib_id3v2_frame yaf_plugin_abstract eventbase string|array cairosurface callable|int eventhttprequest bool eventbufferevent mongodb\driver\command oci-lob mongodb object variant mysqlnd_connection callable solrcollapsefunction recursiveiterator threaded imagickdraw yaf_response_abstract callback mongoclient
