une fonction de rappel callable utilisée par l'option de contexte log_write_batch lors de l'exécution d'un lot d'opérations
retourne la valeur du délai en millisecondes à attendre une activité du socket i/o
déplace la position du texte au début de la prochaine ligne
déchiffre les données data
retourne un tableau associatif avec tous les en-têtes http de la requête courante
retourne la chromacité du point rouge sous la forme d'un tableau avec les clés x et y
fait attendre le contexte appelant du thread référencé la fin de l'exécution
annule toutes les modifications faites sur l'archive
retourne le message de l'exception
liste les tables et leurs privilèges
vérifie si un tableau contient d'autres entrées
récupère l'image de rendu
ajoute une courbe de bézier au chemin courant
encadre une image avec un bord de couleur défini par l'attribut bordercolor d'un objet imagickpixel
définit l'espacement des caractères pour la page
invoque une méthode reflétée
retourne un identifiant d'image qui représente l'image obtenue à partir du fichier dont le nom est donné par filename
aligne les images d'une séquence pour former une seule image cohérente
récupère la largeur totale du texte le nombre de caractères le nombre de mots et le nombre d'espaces
la fonction cubrid_lob2_tell() est utilisée pour récupérer la position du curseur dans l'objet lob
analyse les couleurs d'une séquence d'images et choisit un nombre fixe de couleurs pour représenter l'image
la fonction cubrid_lob2_size() est utilisée pour récupérer la taille d'un objet lob
définit un attribut d'information
définit la graduation horizontale du texte affiché sur la page
vous permet d'empêcher les données entrantes ou sortantes ou les deux par défaut) d'être émises via le socket socket
retourne si les résultats morelikethis doivent être activés
extrait la clé publique du certificat certificate et la prépare pour être utilisée par les autres fonctions
envoi une réponse html au client
récupère le mode courant de la page
retourne l'identifiant du groupe de processus du processus courant
définit les attributs étendus d'une entrée définie par son nom
déchiffre une image
cette fonction ferme une connexion db2 créée avec db2_pconnect() et libère les ressources correspondances du serveur
construit un nouveau pool de workers
cette fonction est un alias de : soapheader::soapheader()
définit la valeur courante de la limite de l'anglet de la page
définit les attributs étendus d'une entrée définie par son index
définit le gestionnaire de rappel tokenhandler qui sera appelé plus tard avec la méthode oauthprovider::calltokenhandler()
copie un lob ou une partie d'un lob vers un autre lob
trie un tableau par les valeurs
fixe le domaine text_domain à utiliser lors des recherches avec gettext()
récupère le champ mis en évidence à utiliser comme champ par défaut ou comme champ de sauvegarde
importe un flux qui encapsule un socket dans une ressource
récupère les en-têtes mime pour une section particulière du corps d'un message spécifique
cette méthode ne doit pas être appelée
retourne un tableau avec les versions du client du protocole et du serveur
assemble une url
avertissement cette fonction n'est pas documentée et seule la liste des arguments est disponible
ajoute des guillemets simples autour des chaînes de caractères et ajoute des guillemets puis échappe les guillemets simples de la chaîne
commence une interface pour les sous-séquences d'affichages sur la page
dessine la ligne le long du chemin courant avec la couleur courante et la largeur de ligne courante puis clôt le chemin
divise l'opérande left_operand par l'opérande right_operand
récupère la classe déclarante
définit la matrice de transformation de texte de la page
returns all the group.query parameter values
ferme le lien établi avec la base de données et libère toutes les ressources de handle
la première syntaxe utilise libmcrypt 2.2.x et la seconde libmcrypt 2.4.x
déplace l'itérateur interne à la position position
convertit le nombre de jours du calendrier julien en date du calendrier français républicain
trouve la position du dernier graphème en effectuant une recherche sensible à la casse
interroge l'autre extrémité de la communication
termine la transaction courante en annulant toutes les requêtes émises depuis la dernière validation
ferme le document fdf
génère un identifiant unique dans la base de données
compose une image dans une autre à la position donné
retourne un numéro d'erreur pour le serveur dernière requête) identifié par id
retourne la date de publication de imagemagick sous forme de chaîne
cette méthode copie le noeud courant et retourne l'objet approprié dom
attache un attribut binaire à la requête radius courante
sépare le tableau array en plusieurs tableaux comptant size éléments
défini la fonction d'arrêt à utiliser durant l'entrainement
retourne un entier solrquery::facet_sort_index ou solrquery::facet_sort_count)
archive un objet ou une hiérarchie d'objets
définit une fonction de rappel pour un signal et une action
la fonction geoip_database_info() retourne la version de la base de données geoip tel que définie dans le fichier binaire
convertit la chaîne xml data et retourne un objet de la classe simplexmlelement
ajoute une ligne à la relation
ferme le fichier représenté par le pointeur handle
lit le fichier et renvoie le résultat dans un tableau
lit les résolutions en x et y d'une image
la fonction eio_busy() augmente artificiellement la charge en prenant un nombre delay de secondes à s'exécuter
retourne soit l'ensemble optimal de colonnes qui identifie de façon unique une ligne d'une table ou les colonnes qui sont automatiquement mises à jour lorsqu'un des valeurs de la ligne est mise à jour par une transaction
crée et retourne une nouvelle ressource d'évènement
retourne un tableau indexé par la position des colonnes représentant une ligne du jeu de résultats
affiche le texte text à la position courante avec la police et la taille courante
récupère le nom du module courant de session
fixe des options pour l'analyseur
retourne une erreur ainsi que certaines informations sur la dernière opération sqlsrv effectuée
est utilisé pour déclencher une erreur utilisateur
retourne la version compressée de la chaîne data compatible avec la sortie du programme gzip
transpose une entrée d'un dossier formaté gopher dans un tableau associatif
récupère la couleur de remplissage courante
la fonction cubrid_lob2_write() lit les données depuis le paramètre buf et les stocke dans l'objet lob
active le collecteur de références circulaires en définissant zend.enable_gc à 1
retourne le commentaire d'une entrée en utilisant l'index de l'entrée
retourne le jeu de résultats représentant une notification asynchrone en attente
cette fonction peut être utilisée pour voir quels sont les modules du driver actuellement surveillés
retourne un message d'erreur sous forme de chaîne de caractères pour le code d'erreur fourni
cette fonction compile le fichier filename en bytecodes et écrit le résultat dans le fichier filename
désalloue les ressources allouées par libevent pour l'objet eventbase
vous permet d'implémenter votre propre filtre de flux à utiliser avec les fonctions d'accès aux données externes comme fopen() fread() etc.)
retourne l'identifiant de la dernière ligne insérée ou la dernière valeur d'une séquence d'objets dépendamment du driver utilisé
définit la largeur de la ligne pour la page
vérifie si un itérateur est valide
retourne un tableau contenant les noms des boîtes aux lettres qui contiennent la chaîne content dans leur nom
retourne le nom de la session courante
crée un tableau à partir de variables et de leur valeur
retourne le code en deux lettre du continent correspondant à un nom d'hôte ou une adresse ip
charge un fichier chdb en le mappant en mémoire
retourne l'arc cosinus de arg
affiche une image ou une séquence sur un serveur x
insère la chaîne data à la position offset
testera la validité d'un fichier rpm pour que cela soit un fichier rpm
cette fonction vérifie si une classe donnée a été définie
méthode abstraite de fusion à implémenter par héritage de la classe
lit au plus len octets d'un objet de grande taille et retourne les données sous la forme d'une chaîne
retourne une description sur le système d'exploitation sur lequel tourne php
la fonction cubrid_lob2_new() est utilisée pour créer un nouvel objet lob (à la fois blob et clob)
efface un fichier virtuel nommé et libère ses structures de données mais pas le contenu
authentification ssh en utilisant l'agent ssh
ajoute une nouvelle page au document et spécifie diverses options
restaure une fonction sauvegardée
convertit value en un variant et l'assigne à l'objet variant
retourne la propriété de la taille du point de l'objet
teste le chiffrement par blocs d'un algorithme
récupère l'heure de la dernière modification du fichier courant de l'objet directoryiterator
récupère une variable cookie
demande un sous-système sftp depuis un serveur déjà connecté ssh2
crée un diff unifié contenant les différences entre la chaîne old_data et la chaîne new_data et retourne le diff ainsi créé
termine la définition d'un glyphe pour les polices de type 3
rééchantillonne l'image à la résolution désirée
effectue la même chose que la fonction c readlink
coupe une partie de la chaîne str à partir de la position offset et d'une longueur de length
retourne l'identité du thread actuellement en cours d'exécution
construit un template et retourne le résultat
retourne une chaîne formatée avec le format format en utilisant les arguments args
lie une variable php variable au marqueur oracle bv_name
envoie une alerte pour ce travail pendant son exécution
déchiffre et vérifie un texte donné et retourne les informations à propos de la signature
appelle une fonction ou une méthode utilisateur nommée function avec les arguments rassemblés dans un tableau
trouve la longueur du segment initial de subject qui contient uniquement les caractères depuis le masque mask
déplace l'itérateur à la position index dans la séquence d'images
construit un nouvel objet tokyotyrantiterator
lit la ressource de résultat d'une requête asynchrone exécutée par pg_send_query() pg_send_query_params() ou pg_send_execute()
exporte un nombre gmp vers une chaîne binaire
définit le gestionnaire de stockage utilisé par le cache de requêtes
vérifie et convertit le tableau associatif assoc_array en une requête sql valide
retourne le dernier code erreur du dernier appel à maxdb_connect()
recherche le prochain index absent qui est supérieur à l'index passé exclusif
recherche dans a en commençant à la position start vers les bits de poids lourd jusqu'à ce qu'elle rencontre un bit à 1
recherche toutes les fonctions déclarées dans le fichier nommé filename et écrit leurs bytecodes correspondants dans le fichier désigné par la ressource filehandle
cette fonction ajoute un fils à une liste de fils existante ou crée une nouvelle liste de fils
retourne la valeur d'un paramètre
retourne le statut courant du serveur mysql
transforme une représentation de clé dba par chaîne en une représentation par tableau
se déplace sur l'élément suivant
affiche un objet imagickdraw dans l'image courante
retourne un objet solrinputdocument équivalent à l'objet
contrairement à la méthode yaf_view_simple::assign() cette méthode assigne la référence à une valeur du moteur
dessine un rectangle aux coordonnées spécifiées
analyse l'image et identifie les unités similaires
peut être utilisé pour augmenter ou diminuer le degré de déboguage dans un endroit particulier de votre application
retourne une chaîne linéarisée contenant les variables de la session courante encodées stockées dans la variable superglobale $_session
bouge le côté d'une image le long des axes x ou y créant un parallélogramme
retourne la taille du tableau à taille fixe
modifie les paramètres de l'agent mnogosearch
lit le token de requête le secret et toute information supplémentaire du fournisseur de services
demande au tampon d'événement de commencer une renégociation ssl
stocke un certificat x509 dans un fichier nommé filename dans un format pkcs#12
utilisez db2_result() pour retourner une valeur d'une colonne spécifique dans la ligne courante d'un jeu de résultats
crée un nouveau fichier pdf avec diverses options
efface tous les messages marqués pour l'effacement par imap_delete() imap_mail_move() ou imap_setflag_full()
retourne l'id pour un genre
retourne le jour julien du timestamp unix timestamp nombre de secondes depuis le 1/1/1970), ou pour le jour courant si timestamp est omis
retourne le nom du socket local ou distant pour la connexion handle
définit quelle page est affichée lors de l'ouverture du document
convertit le nombre de jours du calendrier julien en une chaîne contenant une date du calendrier grégorien au format mois/jour/année
récupère la version de l'api graphicsmagick
définit l'information concernant le client pour un traçage oracle
le premier prototype sert lorsque php est compilé avec la bibliothèque libmcrypt 2.2.x le second lorsqu'il est compilé avec libmcrypt 2.4.x ou 2.5.x
ouvre un fichier de base de données paradox existant
définit les permissions pour le document
envoie la requête au serveur courant associé à l'identifiant link_identifier ou la base par défaut s'il est omis)
retourne la version du serveur mysql sous la forme d'un entier
est identique à la fonction htmlspecialchars() sauf que tous les caractères qui ont des équivalents en entités html sont effectivement traduits
ferme l'accès au fichier représenté par la ressource fd
définit le délimiteur ainsi que le caractère utilisé pour entourer les champs csv analysés
lit le nom de la table depuis le jeu de résultats result
retourne l'histogramme de l'image sous forme d'un tableau d'objets imagickpixel
identique à datetime::settime() mais fonctionne avec datetimeimmutable
la fonction geoip_country_name_by_name() retourne le nom complet du pays correspondant au nom de l'hôte ou à l'adresse ip
retourne un tableau indexé numériquement qui correspond à la prochaine ligne du résultat mysql result et déplace le pointeur interne de données d'un cran
cette fonction permet de définir la valeur courante de la ressource de boîte à cocher
retourne un tableau qui contient la ligne demandée dans le résultat result et déplace le pointeur de données interne d'un cran
compte le nombre d'entrées du map
retourne le nom de la classe de l'objet obj
remplit le chemin courant avec la couleur courante
élève base à la puissance exp
la fonction cubrid_fetch_array() est utilisée pour récupérer une seule ligne depuis le résultat de la requête et retourne un tableau
extrait l'archive complète ou les fichiers fournis vers un chemin spécifié
établie une connexion avec le serveur de cache memcache
termine le suivi d'une connexion postgresql initiée avec pg_trace()
applique le filtre filtertype à l'image en utilisant les paramètres args1 args2 et args3 lorsque cela est nécessaire
lit la date à laquelle le fichier a été modifié pour la dernière fois
retourne les méta-données d'un fichier au sein d'une archive phar
retourne le dernier message d'erreur
réinitialise le jeu de résultats et exécute la requête si elle n'a pas encore été exécutée
récupère la déclaration de la classe de la méthode réléchie
ferme la session courante
suspend l'exécution du processus courant jusqu'à ce qu'un processus fils spécifié par le paramètre pid ait terminé, qu'un signal ait mis fin à ce processus ou qu'un signal ait appelé un gestionnaire de signaux
associe des données ou informations à l'objet courant de l'itérateur
ajoute une émoticône à l'analyseur
compare la valeur priority1 avec la valeur priority2
dessine une polyligne en utilisant les paramètres courants du contour et la couleur ou la texture de remplissage et en utilisant le tableau de coordonnées spécifié
établit une connexion à un serveur sybase
est la seule méthode qu'une route personnalisée doit implémentée
retourne le nombre d'éléments de la table de hachage
efface l'objet de grande taille dont l'identifiant est oid pour la connexion connection
lit les données de la dernière ligne de la relation relation et place la ligne courante à ces données
se comporte presque comme msql_connect() mais à deux différences majeures près
lit l'attribut suivant d'une entrée
construit un objet eventbase
crée un nouveau tampon ssl dont ces données seront envoyées via un socket ssl
modifie les droits d'accès au fichier filename sur le serveur ftp ftp_stream en lui attribuant les droits de mode spécifié sous la forme d'un entier en base octale
la fonction snmpgetnext() est utilisée pour lire la valeur d'un objet snmp qui suit immédiatement l'objet dont l'identifiant est spécifié par le paramètre object_id
retourne la dernière erreur rencontrée sous forme de chaîne de caractères
retourne le nombre de champs du résultat associé à l'identifiant query_identifier
change la valeur de chaque pixel en fonction de l'intensité de la couleur et la compare à un seuil
retourne le type du champ d'index field_number
place un hyperlien à la position donnée à une page web
liste les processus mysql
fixe la longueur des portions de noir et blanc d'une ligne pointillée
lit les événements dans une instance inotify
reprise d'un service en pause
les champs sont triés suivant les critères spécifiés et la direction de tri les champs peuvent être triés par leurs valeurs leurs noms ou leurs nombres de valeurs
méthode magique pour accéder au champ comme à une propriété
construit un arbre de sdo_dataobjects à partir de l'adresse donnée d'une instance de document xml
fonction utile pour améliorer les requêtes durant le développement du code ainsi que lors des tests
vérifie si le paramètre est optionnel
détruit l'objet gmagick et libère toutes les ressources associées
dessine un cercle
définit la fréquence minimale d'un document
ping la connexion à la base de données et essaie de se reconnecter si la connexion est perdue
cette méthode peut être appelée avant ou après le début de l'itération du curseur
tente de renommer oldname en newname en le déplaçant de répertoire si nécessaire
la fonction cubrid_col_size() est utilisée pour récupérer le nombre d'éléments dans un attribut de type de collection set multiset sequence
retourne la consommation de mémoire de la ressource en megaoctets
la fonction cubrid_num_cols() est utilisée pour récupérer le nombre de colonnes depuis le résultat de la requête
la liste des gestionnaires de stockage disponibles dépend de la configuration lors de la compilation du plugin du cache de requêtes
construit un nouvel objet soapvar
retourne l'itérateur interne pour l'entrée courante
définit la clause de sélection liste les attributs à récupérer ainsi que les expressions pour exécution et récupération
définit les drapeaux de l'expression rationnelle
fixe comment les lignes sont jointes
instantie un objet sqlite3 et ouvre une connexion à la base de données sqlite 3
définit le type de l'image
assigne une valeur à l'élément dont l'index est index
retourne true si l'objet a encore des images pour passer en revue la liste
est similaire à hebrev() à la différence qu'elle convertit les nouvelles lignes (\n) en < br > \n
constructeur de la classe yaf_config_ini
cette méthode n'affecte pas l'état du curseur: si vous n'avez pas effectué de requête à ce stade vous pouvez encore appliquer des limites des skips etc
la fonction cubrid_lob2_export() est utilisée pour sauvegarder les données blob/clob dans un fichier
cette fonction retourne le jeu de caractères utilisé sur la connexion cubrid courante et est similaire à la fonction cubrid_get_charset()
supprime les éléments désignés par offset et length du tableau input et les remplace par les éléments du tableau replacement si ce dernier est présent
il s'agit d'un type de watcher avancé qui permet d'intégrer une boucle d'événement dans une autre actuellement seuls les événements io sont supportés dans la boucle interne les autres types de watcher ne doit pas être utilisés)
remplace les couleurs dans l'image à l'aide d'une table de correspondance
lit les attributs et les valeurs pour une entrée d'un résultat de recherche
aiguise une image
cette méthode compte le nombre de fils d'un élément
efface la ressource d'une base de données paradox et libère toute la mémoire
retourne plus de détails après une erreur pour la dernière requête sur le serveur
récupère la planéité de la page
vérifie si les en-têtes http ont déjà été envoyés
crée une nouvelle ressource de socket de type af_inet mis en attente sur toutes les interfaces locales pour le port port
retourne la position et la longueur du segment de chaîne qui vérifie le masque de l'expression rationnelle
convertit un timestamp tel que stocké dans un fichier paradox en un format humainement lisible
définit la couleur décrite par l'objet imagickpixel en utilisant les valeurs normalisées pour la densité, la saturation et la luminosité
ajoute un domaine à la liste des domaines utilisés pour la recherche
envoi une commande de fermeture à mysql
transforme le noeud source en une uri en y appliquant la feuille de style donnée par la méthode xsltprocessor::importstylesheet()
retourne un mot clé identifiant le type de la requête statement oci8
ajoute un fichier un dossier ou un lien symbolique en utilisant le chemin path dans le dossier de travail
supprime les num derniers éléments d'une collection
ouvre le fichier filename et l'analyse ligne par ligne à la recherche de balises meta
récupère l'adresse courant pour laquelle le socket d'écoute est lié
envoie le signal sig au processus pid
la fonction pcntl_sigtimedwait() opère exactement comme pcntl_sigwaitinfo() hormis le fait qu'elle prend deux paramètres supplémentaires : seconds et nanoseconds qui établissent une durée maximale d'attente
retourne le nom de la fonction associée à cette tâche i.e., la fonction appelée par l'agent gearman
dessine le premier caractère de la chaîne c verticalement dans l'image image fournie
crée une destination nommée sur une page arbitraire du document courant
retourne le chemin du dossier du fichier courant dans l'objet directoryiterator sans le nom du fichier lui-même
crée une instance gearmanclient représentant un client qui se connecte à un serveur de tâches et y enregistre des tâches à effectuer
retourne des informations sur le calendrier calendar spécifié
demande un verrou en écriture sur la relation relation
retourne le produit des valeurs du tableau array
retourne string après avoir inséré < br / > ou ' < br > ' devant toutes les nouvelles lignes\r\n \n\r \n et \r
la fonction mysqli_fetch_lengths() retourne un tableau contenant la longueur de chaque colonne de la ligne courante du jeu de résultats représenté par le paramètre result
stocke un enregistrement dans une base de données paradox
génère une signature sha-256 de l'image
est similaire à strcasecmp() à la différence près qu'elle permet de limiter le nombre de caractères utilisés pour comparer str1 et str2 avec le paramètre len
définit l'apparence de la page afin qu'elle tienne en largeur dans la page et définit la position haute de la page à la valeur top
cette fonction est un raccourci pour hwapi_insert()
génère une clé publique signée en utilisant un algorithme de hashage spécifié
recherche dans le tampon une occurrence de la chaîne what
crée un nouveau blob sur la connexion ibase link_identifier y copie le fichier file_handle en entier le referme et en retourne l'identifiant assigné
cette méthode est appelée en réponse à fstat()
supprime un ou plusieurs enregistrements
scinde la chaîne string dans un tableau grâce à une expression rationnelle
change l'utilisateur de la connexion à la base de données mysqlnd spécifié
remplace l'ordre de détection des encodages courant par encoding_list
ajoute un bruit blanc à une image
spécifie le caractère de substitution des caractères invalides ou des encodages invalides
charge l'image jpeg spécifiée
retourne une série d'éléments du tableau array commençant à l'offset offset et représentant length éléments
définit le jeu de caractères par défaut pour la connexion courante
lit le type de compression de l'image
définit les préférences de sécurité
un délai d'attente peut être défini à n'importe quel moment et va impacter la récupération des prochaines données associée avec ce curseur incluant la récupération de plus de résultats depuis la base de données
recherche par expression rationnelle multi-octets
effectue un test avec un jeu d'entrées et un jeu de sorties désirées
retourne l'état courant de la validation automatique
remplit le chemin courant avec la couleur courante en appliquant le pinceau courant stroke
efface la boîte aux lettres spécifiée
cette fonction retourne une grille contenant les boutons créés
demande si l'objet référencé s'est terminé durant l'exécution ; si il a subi une erreur fatale ou émis des exceptions qui n'ont pu être attrapées
retourne toutes les ressources régulières courantes dans un tableau
met à l'échelle les entrées et les sorties dans les données d'entrainement à l'intervalle spécifié
retournera la version courante de l'extension rpmreader
efface un champ du document
cette fonction retourne les informations de configuration de l'instance de cache
configure le mode de pointillé à la taille de b pour les traits noirs et w pour les traits blancs
démarre le profilage xhprof
vérifie si un itérateur est attaché
ajuste la durée qu'une frame d'une image animée sera affichée
lit la valeur de la colonne nommée index_or_name si c'est une chaîne), ou d'index index_or_name si c'est un entier), dans le résultat sqlite result
détermine si le pointeur de fichier fd se réfère à un type de terminal de périphérique valide
crée un nouveau fichier pdf dans le fichier filename
affiche ou sauvegarde une version webp de l' image fournie
retourne l'index de la couleur spécifiée dans la palette de l'image image
ouvre une nouvelle archive zip pour lecture
libère toutes les ressources allouées par un événement de tampon
charge un document xml depuis une chaîne de caractères
efface toutes les fonctions de rappel des tâches précédemment définies
retourne un tableau qui contient les éléments de input qui satisfont le masque pattern
désactive les vérifications de certificats et d'hôtes sll activé par défaut)
modifie la taille d'une image dans les dimensions données et supprime tous les profiles associés
la transaction commençant avec la fonction sqlsrv_begin_transaction() inclut toutes les requêtes qui sont exécutées après l'appel à la fonction sqlsrv_begin_transaction() et avant l'appel à la fonction sqlsrv_rollback() ou la fonction sqlsrv_commit()
récupère des noms similaires pour le nom et le pays fourni
fournit deux modes de dessin des images en vraies couleurs truecolors
applique l'algorithme radius salt à la valeur fournie
construit l'objet serveur http
configure la taille et la position de l'objet imagick
est fonctionnellement équivalent à la fonction memcached::replace() hormis le fait que la variable libre server_key peut être utilisée pour diriger la clé key vers un serveur spécifique
retourne le numéro de version fdf pour le document fdf_document pour l'api si aucun paramètre n'est donné
parcours une partie de la table des propriétés des objets en préservant optionnellement les clés
est utilisé pour effacer les informations stockées dans un tag id3
définit la forme à utiliser à la fin des lignes
tente de verrouiller le mutex de l'appelant sans le bloquer si le mutex appartient est verrouillé) à un autre thread
retourne un tableau contenant tous les genres possibles pouvant être stockés dans un tag id3
ouvre un socket vers un hôte/port arbitraire via un serveur connecté ssh
modifie l'entrée identifiée par dn avec les valeurs fournies dans entry
récupère un fils de l'élément courant
retourne l'erreur sqlstate depuis la dernière opération mysql
démarre un nouveau thread et exécute la méthode run implémentée
récupère une reflectionproperty pour une propriété d'une classe
fixe le mode pour error_reporting
ajoute une miniature sur la page courante
définit le délai de l'image
ajoute le processus pid au groupe d'id pgid
annule une requête http en attente
lit la version de l'api mnogosearch
retourne si l'on doit activer la mise en évidence pour les intervalles/jokers/flous/préfixes des requêtes
lit le nom de la colonne dont l'index est field_number
configure le nombre de tentatives et le délai d'expiration
ouvre une fenêtre de la taille et la position spécifiée
ajoute plusieurs composants à la forme form
vérifie si les connexions aux serveurs memcache sont des connexions persistentes
récupère un tableau contenant toutes les boîtes aux lettres auxquelles vous avez souscrit
le destructeur
essaie de supprimer un service de la base de données scm
vérifie si les fonctions rar émettent des alertes et retournent des valeurs d'erreur ou bien si elles émettent des exceptions n'incluent pas des erreurs de syntaxe telles que le fait de passer des types incorrects comme arguments)
c'est une implémentation de l'interface arrayaccess permettant la manipulation directe du contenu d'une archive tar/zip en utilisant les crochets opérateurs d'accès au tableau
trie le tableau array de telle manière que la corrélation entre les index et les valeurs soit conservée
compresse la chaîne donnée en utilisant le format de données deflate
définit la couleur de remplissage de la page
définit le délai d'attente d'une activité du socket i/o
envoie une requête au serveur actif courant associé à l'identifiant de connexion link_identifier
ferme la base de données associée à dbase_identifier
patche la chaîne str avec un patch unifié et retourne le résultat
défini le facteur d'augmentation utilisé pendant l'entrainement rprop
ajoute un lien web url sur la page courante
récupère une liste de toutes les collections de la base de données et les retourne sous la forme d'un tableau d'objets mongocollection
retourne une liste de dsn disponibles après l'avoir appelée plusieurs fois)
définit la propriété de gravité pour l'image courante
efface tous les documents correspondant à des requêtes
ferme et supprime la relation relation
lit le numéro de ligne d'un noeud
ouvre un flux pour accéder aux données référencées par oid
indique au gestionnaire de queue que l'application a atteint un point de synchronisation et que tous les messages qui ont été lus et écrits depuis le dernier point de synchronisation ont été rendus permanants
pour un jeu de tâches précédemment ajoutées avec les méthodes gearmanclient::addtask() gearmanclient::addtaskhigh() gearmanclient::addtasklow() gearmanclient::addtaskbackground() gearmanclient::addtaskhighbackground() ou gearmanclient::addtasklowbackground() cette méthode démarre l'exécution des tâches en parallèle
lit des données depuis le fichier fd et les place à la fin du tampon
retourne les formats supportés par gmagick
retourne le nom de la prochaine entrée du dossier identifié par dir_handle
retourne une sous-chaîne de haystack allant de la première occurrence de needle incluse jusqu'à la fin de la chaîne
liste les tables de la base de données spécifiée par database
envoie l'exception fournie lorsque ce travail est en cours d'exécution
la fonction date_default_timezone_set() définit le décalage horaire par défaut utilisé par toutes les fonctions date/heure
met à l'échelle une image aux dimensions fournies
crée une annotation rectangulaire sur la page courante
cette fonction vérifie si un noeud possède des enfants
place le pointeur de lecture du fichier zp à la position offset comptée en octets depuis le début du fichier
soustrait right de left
récupère le gestionnaire de travaux pour la tâche en cours
attache un attribut spécifique au vendreau à la requête courante radius
récupère la partie entière d'un variant
décode les en-têtes mime multiples
cette fonction accepte une tableau de données et tente de prédire la classe comme avec les fonctions de prédiction
exporte une extension reflétée
désactive les redirections permettant ainsi aux requêtes d'être redirigées manuellement
cette méthode est utilisée pour créer des collections limitées ainsi que des collections nécessitant des options spéciales
écrit les données de session vers le support de stockage
déplace le pointeur interne sur le champ suivant
cette méthode importe une feuille de style dans le xsltprocessor pour transformations
vérifie si une classe est définie comme interne par une extension ou faisant partie du coeur par opposition à une classe définie par l'utilisateur
retourne une chaîne représentant l'expression rationnelle
modifie le mot de passe de l'utilisateur username
déchiffre data qui a été chiffrée précédemment avec openssl_public_encrypt() puis stocke le résultat dans la variable decrypted
assigne une priorité pour un tampon d'événements
les champs sont spécifiés via la syntaxe fieldname : bool
génère l'entropie via openssl rand_poll() voir le manuel système)
scinde la chaîne string dans un tableau par expression rationnelle
echappe la chaîne value pour l'utiliser dans le contexte impliqué par le paramètre flags
fusionne les données data1 et data2 dans un nouveau jeux de données d'entrainement
détache le thread référencé depuis le contexte appelant ; très dangereux !
si le premier et unique paramètre est un tableau max() retourne la valeur la plus haute du tableau
retourne le message de diagnostic depuis la dernière vérification
récupère le nom du paramètre
liste les procédures stockées
si solrquery::sethighlightalternatefield() passe la valeur true ce paramètre spécifie le nombre maximal de caractères du champ à retourner
replace l'itérateur au début
identifie une image et retourne ses attributs
envoie via le socket une demande d'information concernant le script exécuté
établit une connexion à un serveur msql
cette méthode stocke une table de hachage existante dans un fichier du disque dans un format lisible par la fonction loadfromfile()
se déplace sur la prochaine ligne du fichier
cette méthode active la possibilité d'utiliser des fonctions php dans des expressions xpath
lit le nom d'hôte standard pour la machine hôte
contrôle diverses options de connexion pour une connexion ftp
démarre la session de déboguage dans le dossier dump_directory / pprof_process_id
cette fonction ajoute une nouvelle paire nom/valeur au mécanisme de réécriture d'url
définit le paramètre de requête explainother
récupère le numéro de la dernière exception lancée ou la raison de l'échec lors de l'appel d'une fonction
récupère la distance depuis la ligne de base des lettres en majuscule
crée un blob à partir des données fournies
retourne un objet eventhttpconnection qui représente une connexion http associée à la requête
récupère la copie d'un tableau
retourne le nom du hash spécifié
spécifie le schéma relaxng à utiliser pour validation
définit le nombre maximal de termes de requête à inclure dans chaque requête générée
retourne le nombre de lignes affectées par la dernière requête insert update ou delete sur le serveur associé à l'identifiant de connexion link_identifier
retourne la liste des types soap décrits dans le fichier wsdl du service web courant
renomme le fichier ou le dossier oldname en newname sur le serveur ftp_stream
cette fonction retourne un tableau représentant les noms des traits que la classe donnée utilise
récupère le nom du fichier d'une image particulière dans une séquence
la fonction snmp2_set() est utilisée pour définir la valeur d'un objet snmp spécifié par son identifiant object_id
termine l'objet chemin courant sans effectuer d'opérations de remplissage et de peinture
adoucit les contours de l'image à la mode des vignettes
vérifie si l'objet directoryiterator contient encore des fichiers
crée une nouvelle boîte aux lettres nommée mailbox
retourne la version du serveur mysql
la taille minimale de l'étape est un petit nombre positif déterminant combien la taille de l'étape peut être au minimal
retourne le nombre de lignes affectées durant la dernière commande oracle
identique à la méthode evembed::__construct() mais ne démarre pas automatiquement le watcher
vérifie si l'élément courant de l'objet directoryiterator est un lien symbolique
compte le nombre de lignes déjà lues dans le résultat result_id après ifx_query() ou ifx_do()
se déplace sur une ligne spécifique du fichier
abandonne la boucle active immédiatement
est fonctionnellement équivalent à memcached::add() mais la variable libre server_key peut être utilisée pour diriger la clé key sur un serveur spécifique
déchiffre les données data et retourne les données déchiffrées
la fonction cubrid_commit() est utilisée pour valider une transaction pointée par le paramètre conn_identifier
retourne la liste de polices configurés pour imagick
renvoie la date de dernière modification de l'inode d'un fichier
définit une fonction à appeler lorsqu'une tâche se termine
envoie la commande ftp brute command au serveur ftp identifié par ftp_stream
retourne la fréquence du timer dans les ticks par seconde
définit la couleur de remplissage de la page
récupère la version d'apache
modifie la position du texte
renforce les bords dans l'image avec un filtre de convolution pour un rayon donné
vérifie si une variable existe en mémoire partagée
défini les connexions dans le réseau
est similaire à la création d'une nouvelle instance com d'un objet com excepté qu'elle ne retournera un objet à votre script uniquement si l'objet est actuellement instancié
définit la profondeur de l'image
définit l'apparence de la page afin qu'elle tienne en hauteur dans la boite et définit la position gauche de la page à la valeur de left
supprime la base de données actuellement utilisée
la fonction pcntl_sigprocmask() ajoute retire ou configure les signaux bloqués en fonction du paramètre how
récupère et trie les numéros de messages en fonction des paramètres donnés
retourne la valeur de l'option option depuis la connexion ftp spécifiée
récupère la version courante solr
retourne une chaîne contenant le code d'erreur sqlstate de la dernière erreur
retourne une chaîne qui décrit le code résultat de la dernière méthode memcached exécutée
dessine une chaîne aux coordonnées spécifiées
retourne la valeur à l'index index
lit des informations à propos de la dernière requête mysql
cette méthode sauvegarde une table de hachage existante dans un fichier du disque dans un format lisible par la fonction loadfromfile()
ferme l'entrée de l'objet xmlreader qui est en train d'être analysé
concatène une matrice avec le ctm
utilisée pour rechercher les sessions ayant des attributs communs
retourne la prochaine paire clé/valeur de la carte map après la clé key spécifiée
efface un paramètre s'il est défini
crée un objet splfileobject pour le fichier
donne à chaque connexion un poids aléatoire compris entre min_weight et max_weight
initie un flux ou une connexion datagramme avec la destination remote_socket
lit toutes les entrées du résultat fourni ainsi que les attributs et les valeurs multiples
chiffre les données data
invoque la méthode réfléchie et lui passe les arguments sous la forme d'un tableau
définit la couleur à utiliser pour tracer les contours de l'objet
supprime toutes les fonctions précédemment enregistrées s'assurant ainsi qu'aucun travail ne sera envoyé à l'agent avec ces fonctions
retourne la valeur courante de l'espacement des caractères
la fonction cubrid_seq_drop() est utilisée pour supprimer un élément de la base de données en se basant sur la séquence de types d'attributs fournie
retourne un tableau php à partir d'un tableau à taille fixe
force l'écriture de toutes les données bzip2 mises en tampon pour le fichier représenté par bz
retourne l'identifiant unique pour une tâche
retourne le paramétrage courant du gestionnaire ob_iconv_handler()
indique si un attribut nommé name existe en tant que membre de l'élément
retourne la scène de l'image
récupère la valeur de la levée du texte
récupère le sexe d'un prénom pour un pays spécifique
va programmer le processus apache pour qu'il se termine une fois la requête php courante terminée
récupère le temps écoulé pour le dernier intervalle
cherche la valeur d'un en-tête
récupère le nom de la route actuellement en utilisation dans le processus routeur
détermine si une extension est chargée ou non
retourne le contenu du tampon de sortie sans l'effacer
avec cette méthode vous pouvez exécuter des commandes d'agrégation et récupérer les résultats via un curseur au lieu de ne récupérer qu'un seul document comme c'est le cas avec la méthode mongocollection::aggregate()
retourne la dernière erreur survenue pour ce sponsor
remplit une archive tar/zip à partir du contenu d'un répertoire
charge une police postscript type 1 depuis le fichier filename
retourne le rectangle entourant un texte et dessiné avec une police postscript type1
décode la chaîne encodée str dans l'en-tête mime
configure l'option spécifiée par optname au niveau de protocole level à la valeur pointée par optval pour le socket spécifié par socket
exécute une requête sql en appelant une seule fonction retourne le jeu de résultats s'il y en a) retourné par la requête en tant qu'objet pdostatement
est une alternative de sybase_fetch_row() qui utilise les noms de colonnes plutôt que des entiers comme index dans le tableau de résultats
définit la fréquence maximale du document
déplace le curseur de l'élément parent de l'attribut courant
contrôle si une déclaration xml sera générée au début du document xml
une fois qu'un code erreur est défini la plupart des opérations incluant les fonctions i/o ne peuvent être exécutées
cette fonction est un alias de : soapvar::soapvar()
sauvegarde une partie du corps du message spécifié
signe le paramètre plaintext avec les clés qui ont été fixées avec gnupg_addsignkey auparavant et retourne le texte signé ou la signature dépendamment de ce qui a été fixé avec gnupg_setsignmode
retourne un tableau contenant l'état de toutes les machines en fonctionnement
récupère le sous-chemin et le nom du fichier
indique si le fichier est exécutable
trie le tableau array en conservant la correspondance entre les index et leurs valeurs
retourne la position du curseur de champ utilisé par le dernier appel à la fonction mysqli_fetch_field()
supprime la couleur color précédemment allouée avec la fonction imagecolorallocate() pour l'image image
vérifie si tous les caractères de la chaîne text sont des caractères de contrôles spéciaux
affiche des informations à propos d'une variable de manière à ce qu'elle soit lisible
annule une transaction sans la fermer
retourne l'index de l'image courante dans la séquence imagick
définit la propriété de la taille du point de l'objet
identification en utilisant une clé publique lue depuis un fichier
patche le fichier file avec le patch binaire et enregistre le résultat dans le fichier dest
la fonction geoip_asnum_by_name() va récupérer l'asn autonomous system numbers associé avec l'adresse ip
nettoie récursivement un dossier de travail workingdir en finissant les opérations incomplètes et en supprimant les verrous
retourne le code de l'exception
défini le mode lecture et écriture pour le délai d'attente maximal d'un tampon d'événement
recherche si d'autres lignes sont disponibles dans le jeu de résultats donné
enregistre une ou plusieurs paires clé-valeur dans la base de données
retourne la somme de contrôle crc32() du fichier au sein de l'archive phar
choisit le mode par défaut des objets blob pour toutes les requêtes select
modifie la valeur de l'option option en remplaçant la valeur courante par newval
désactive le collecteur de références circulaires en configuration zend.enable_gc à 0
fournit les attributs et les valeurs définies dans une balise xml
la fonction cubrid_put() est utilisée pour mettre à jour un attribut de l'instance pointée par l' oid fournie
excepté pour la direction du dessin cette fonction fonctionne exactement de la même façon que la fonction pdf_arc()
définit la version swf à utiliser dans la vidéo
génère une clé suivant le hash fourni en utilisant le mot de passe password fourni
retourne le nombre de lignes affectées par la dernière requête insert update ou delete effectuée avec la connexion représentée par link_identifier
exécute une requête sql qui a été préparée par db2_prepare()
génère une ressource de certificat x509 depuis la csr donnée
essaie de démarrer le service nommé
ferme l'objet slob représenté par son identifiant bid
ajoute un nouveau profile à un objet imagick
efface le cache utilisateur/système
ré-active les appels qui permettent de modifier un buffer d'événements
la fonction cubrid_get_query_timeout() est utilisée pour récupérer le délai d'attente maximal d'une requête
remplit les références passées avec la taille courante du terminal
enregistre une fonction de rappel qui sera appelée lorsque php tentera d'écrire sur la sortie
calcule le hachage d'un fichier spécifié par le paramètre file_name en utilisant le contexte déclenché par morceaux de hachages et retourne le hachage correspondant
retourne la clé courante
lie un facet.date.end
modifie la position de l'itérateur à l'index index
retourne la taille du point d'une police de caractères uitilisé lors d'une annotation
teste si un champ dans une ressource de résultat est null
sélectionne une base de données mysql sur le serveur associé avec le paramètre link_identifier
analyse la clé key et la prépare pour être utilisée par d'autres fonctions
retourne les informations sur un fichier de base de données rrd
égalise l'histogramme d'une image
ajoute tous les objets de l'ensemble storage
retourne la commande de la requête ; une des constantes eventhttprequest::cmd_*
stoppe la mesure du temps pour l'intervalle précédent
attend que les événements deviennent actifs puis exécute leurs fonctions de rappel
retourne le nombre de champs dans un résultat
interrompt la recherche et appel la fonction de rappel définie par l'utilisateur
chiffre les données data avec la clé publique key et stocke le résultat dans crypted
en cas de succès retourne le temps courant tel que retourné par la fonction gettimeofday(), en analysant la valeur mise en cache dans base si possible et en appelant la fonction gettimeofday() ou la fonction clock_gettime() si aucun temps n'est disponible en cache
efface le fichier path sur un serveur ftp
calcule la racine carrée d'un nombre gmp avec reste
sélectionne une base de données et exécute une requête
entre dans une fonctionalité de méthode d'événement demandée par l'application
la fonction cubrid_is_instance() est utilisée pour vérifier si l'instance pointée par l' oid fournie existe ou non
mélange les éléments du tableau array
retourne la méthode d'interpolation de l'image spécifiée
change la taille d'un tableau à la taille fixe size
la fonction socket_send() envoie len octets au socket socket depuis le buffer buf
retourne une liste de noms de fichiers de configuration analysés en plus de php.ini
retourne la valeur de l'option memcached option
n'a besoin d'être appelée que si vous vous inquiétez de l'utilisation excessive de la mémoire durant l'exécution de votre script
ajoute un commentaire à l'image
définit l'intervalle de temps à attendre une activité du socket i/o
libère les ressources associées à un résultat
permet de contrôler la luminosité, la saturation et la teinte d'une image
cette fonction retourne un objet avec des propriétés en lecture seule qui retournent des informations à propos le serveur de base de données ibm db2 cloudscape ou apache derby
vérifie si l'entrée courante est un tableau ou un objet pour lequel un itérateur peut être obtenu via la méthode recursivearrayiterator::getchildren()
déplace la carte des couleurs d'une image d'un nombre de positions donné
si une fonction radius échoue alors elle enregistre un message d'erreur
tronque un fichier d'au moins offset octets en taille
cette fonction retourne un tableau numérique avec les longueurs des valeurs de chaque champ depuis la ligne courante du jeu de résultats ou retourne false si une erreur survient
défini le bit sûr limite utilisé pendant l'entrainement
récupère la hauteur de la page
définit le point de départ pour la nouvelle ligne
modifie une ou plusieurs options sur la connexion donnée
libère toute la mémoire associée à l'image image
récupère une représentation textuelle
la fonction password_hash() crée un nouveau hachage en utilisant un algorithme de hachage fort et irréversible
remplace les valeurs du tableau array1 avec les valeurs ayant les mêmes clés dans les tableaux fournis
retourne des informations sur le chemin path sous forme de chaine ou de tableau associatif ceci dépend du paramètre options
associe une variable php à un paramètre dans la requête sql retournée par db2_prepare()
convertit le nom de domaine au format idna ascii-compatible en unicode encodé en utf-8
construit un nouvel objet soapheader
récupère le statut http de la réponse
essaie de remplacer le groupe du lien symbolique filename par le groupe group
termine la page courante
avertissement cette fonction n'est pas documentée et seule la liste des arguments est disponible
effectue un entrainement avec un jeu de données d'entrainement
compare chaque image avec la suivante dans la séquence et retourne la région maximale d'encadrement des pixels différents qu'il découvre
arrête et re-démarre le watcher périodique
incrémente la valeur numérique de offset unités
retourne le type de raison
fixe le délai d'expiration d'un script en secondes
utilisé pour une linéarisation personnalisée
récupère une ligne du résultat d'une requête dans un tableau associatif
identique à datetime::setisodate() mais fonctionne avec datetimeimmutable
calcule la différence entre deux tableaux en utilisant une fonction rappel
envoie un courrier électronique
vérifie si le système de fichiers contenant le fichier path supporte les attributs étendus
dessine une portion d'un cercle avec un point milieu àx y
libère principale la mémoire utilisée par le document
défini la valeur du délai maximal d'attente de la fonction de rappel du timer
ouvre une base de données sqlite 3
attend un signal sur une variable de condition en spécifiant optionnellement un délai d'attente maximal
cette fonction insère un nouveau noeud juste avec le noeud de référence
retourne le nom du paquet imagemagick
retourne la base d'événement associée avec la connexion
remplit le chemin courant en utilisant des nombres différents de zéro
ajout un flou adaptatif à l'image
génère une nouvelle paire de clés une privée et une publique
récupère des informations sur l'utilisation de la mémoire par le cache de chemin de fichier résolu
récupère les en-têtes brutes de la requête envoyée au serveur solr
fixe des options pour une ressource ou une connexion
supprime une fonction des serveurs de travaux faisant ainsi qu'aucun travail ne sera envoyé à l'agent pour cette fonction
stocke sur le disque une version sérialisée de l'ensemble dans un format lisible par loadfromfile()
troue la dernière occurrence de needle dans haystack
lit l'apparence du champ field i.e
génère la somme de contrôle cyclique crc32 calculée sur 32 bits et appliquée à la chaîne str
récupère la dernière erreur survenue
fixe le mode pour signer
trie les entrées par les valeurs en utilisant une fonction définie par l'utilisateur
écrit l'historique dans le fichier filename
crée un objet splfileinfo pour le dossier qui contient le fichier courant
enregistre l'espace de noms namespaceuri et le prefix avec l'objet domxpath
retourne le dernier message rapporté par le serveur
crée un nouvel objet sphinxclient
influence le facteur instable d'une période pour les fonctions qui en sont sensibles
retourne le nom des fonctions définies dans le module module_name
tente de créer un dossier
récupère la valeur du délai maximal d'attente de la fonction de rappel d'inactivité
récupère le délai de l'image
enregistre le fichier local_file sur le serveur ftp
modifie la valeur du pointeur de champ
solrquery::terms_sort_index indique que les termes sont retournés triés sur l'index
exporte le contenu soit d'une copie de travail soit d'un référentiel dans un dossier propre
écrit le fichier remote_file présent sur la machine locale sur le serveur ftp ftp_stream
calcule l'addition vectoriel de real0 en real1 et retourne le vecteur résultant
met à jour le map avec une nouvelle paire clé-valeur
remet l'itérateur au début du tableau à taille fixe
supprime un événement de la liste des événements surveillées par libevent et libère les ressources allouées à cet événement
spécifie le fichier de sortie contenant les informations de profilage lors de l'exécution d'une feuille de style
retourne le nom avec le chemin de l'entrée d'une archive
définit les drapeaux
vérifie si le termscomponent est activé ou non
valide la transaction ouverte ce qui rend permanentes toutes les modifications faites sur la base de données au cours de cette transaction
retourne la couleur associée à un index spécifié
teste si le mode retourne les données par blocs e.g
affecte la marque seuil pour les évènements en lecture et écriture
simule un dessin au fusain
vérifie si la fin du fichier est atteinte
retourne la valeur de la configuration spécifiée
la fonction cubrid_disconnect() ferme le gestionnaire de connexion et se déconnecte du serveur
une fonction de rappel callable utilisée par l'option de contexte log_cmd_insert lors de l'insertion d'un document
cette fonction vérifie si la propriété property existe dans la classe spécifiée
définit la propriété globale de gravité pour l'objet imagick
trie les entrées par les valeurs en utilisant l'algorithme d'ordre naturel
retourne toujours un index de couleur disponible dans la palette de l'image image : soit c'est la couleur exacte soit c'est la meilleure approximation
s'abonne se désabonne) à un service
cette fonction obtient des informations sur l'entrée définie par son nom
compacte la base de données dbase_identifier effacement définitif de tous les enregistrements marqués pour l'effacement à l'aide de la fonction dbase_delete_record()
envoie la commande ftp allo pour allouer un espace sur le serveur ftp de filesize octets
efface une entrée de l'archive en utilisant son index
cette fonction peut être utilisée pour définir la verbosité des logs ainsi que le type d'activités à surveiller
récupère une liste des classes d'une extension
compte le nombre de mots dans la chaîne string
calcule la similarité entre les deux chaînes first et second selon la méthode décrite dans programming classics: implementing the world's best algorithms by oliver isbn 0-131-00413-1
retourne le titre de l'attribut
cette méthode est destinée à la création de conteneurs spécifique au format de fichier phar et n'est pas conçue pour être utilisée avec des archives phar basées sur tar ou sur zip
retourne la taille de l'image en octets
modifie la taille et la direction de la page en un format prédéfini
fixe la couleur pour le dessin remplissage ou les deux
vérifie si la méthode est finale
enregistre une plugin voir yaf_plugin_abstract
retourne la géométrie de la page d'une image sous la forme d'un tableau avec les clés width height x et y
retourne un tableau dont les clés sont les valeurs du précédent tableau array et les valeurs sont les clés
retourne le facteur de seuil d'erreur lors de l'étape sarprop
livre un chemin path non-versionné dans le référentiel à l'url url
libevent propose une couche d'abstraction sur l'api basique
crée une configuration utilisée pour ouvrir un dictionnaire
construit un objet comptable nommé ou non
récupère la liste des champs qui seront retournés dans la réponse
lie une variable php à un marqueur nommé ou interrogatif correspondant dans une requête sql utilisée pour préparer la requête
exécute une seule tâche en priorité haute et retourne une représentation du résultat sous la forme d'une chaîne de caractères
configure la méthode de récupération
récupère la scène de l'image
configure l'option option avec la nouvelle valeur value
lit la structure d'une section spécifié du corps d'un mail
définit le commentaire d'une archive zip
retourne le contenu d'un objet blob
modifie la priorité du processus courant par le paramètre spécifié increment
attache un attibut binaire spécifique au vendeur pour la requête courante radius
modifie l'identifiant de groupe réel du processus courant
retire un objet du stockage
annote une image avec du texte
retourne la chaîne str après avoir échappé tous les caractères qui doivent l'être pour être utilisée
l'appel à la méthode connect sur un objet samconnection connecte le script php à un serveur de messagerie
est utilisé pour lire le sous-objet snmp dont la profondeur est spécifiée par le paramètre object_id
retourne toute exception javascript non attrapée restante depuis un ou plusieurs appel s à la méthode v8js::executestring() sous forme de v8jsexception
retourne le nom de l'album d'un tag id3
vérifie si la connexion au serveur est toujours active
retourne la valeur numérique entière entier de la variable var en convertissant la valeur dans la base spécifiée par défaut en base 10)
calcule l'arc cosinus de chaque valeur de real et retourne le tableau résultant
retourne une ressource qui peut être utilisée avec les files de messages system v et la clé key
prépare une phrase depuis une chaîne non protégée par lucene
vérifie si la requête au serveur a réussi
est la version insensible à la casse de natsort()
réinitialise l'objet courant
défini la fonction d'activation pour tous les neurones de la couche dont le numéro est layer en comptant la couche d'entrée comme étant la couche numéro 0
retourne un tableau de statistiques collectées par le cœur du plugin de mise en cache
retourne e à la puissance arg
définit les données de l'application qui pourront ensuite être récupérées via la méthode gearmanclient::data()
l'instanciation de la classe runkit_sandbox_parent à l'intérieure d'un environnement sandbox créé à partir de la classe runkit_sandbox fournit certains moyens contrôlés) pour un sandbox fils pour accéder à son parent
récupère le nom du controlleur du module
force le thread référencé à se terminer
particulièrement pratique pour les types chaînes de type base64 et datetime
récupère les informations du cache et les métadonnées depuis le magasin apc
insère ou modifie la variable variable avec la clé variable_key dans le segment de mémoire shm_identifier
définit ou redéfinit une paire de couleur pour l'affichage à l'écran : fond d'écran et affichage
effectue une reconnexion au serveur avec les mêmes paramètres que lors de la connexion précédente avec connection
appelé pour chaque élément pour tester s'il a un fils ou non
duplique le blob fourni
récupère la propriété yaf.environ qui a été défini dans yaf.environ qui a comme valeur par défaut product
retourne une déviation aléatoire de la distribution beta avec les paramètres a et b
la fonction geoip_region_by_name() retourne le pays et la région correspondant au nom de l'hôte ou à l'adresse ip
construit un objet eventconfig qui pourra être passé au constructeur eventbase::__construct()
définit le format de sortie afin qu'il soit complet ou numérique
identique à la méthode evio::__construct() mais ne démarre pas automatiquement l'observateur
libère l'objet et supprime les événements associés
crée un nouveau writeconcern
exporte un pem valide formaté comme une clé publique signée
obtient un verrou exclusif sur une clé donnée
vérifie si le flux d'octets est valide pour l'encodage spécifique
prépare une commande pour l'exécution
désactive les événements event::read event::write ou event::read | event::write sur un événement de tampon
cette méthode peut être utilisée pour lire le contenu d'une archive phar externe
assigne une fonction de rappel qui sera appelée lorsque l'utilisateur appuiera sur la touche de suspension normalement ^z)
modifie la valeur du paramètre de la pdflib key par la valeur de value
simule un crayon à main levée
enregistre une nouvelle ligne dans la base de données
indique si un noeud de texte ne contient que des espaces ou est vide
génère une chaîne de caractères d'octets pseudo-aléatoires
crée une vue 3d
dépile et retourne le dernier élément du tableau array le raccourcissant d'un élément
spécifie le nombre maximal de lignes à ignorer
récupère la descente verticale de la police
liste les clés étrangères utilisées dans la table pk_table
crée une nouvelle image avec la couleur d'arrière-plan spécifiée
retourne le nombre de lignes affectées par les requêtes de type insert update et delete
récupère le point chromatique blanc
re-configure un événement timer
établit une connexion à la base identifiée par path avec le mode mode et l'identifiant handler
exécute une requête qui a déjà été préparée ou crée un pointeur pour cela
établit une connexion avec un serveur frontbase
retourne le numéro de la piste d'un tag id3
récupère les données de l'image pour une surface donnée ou null si la surface n'est pas une surface de l'image valide ou si cairocontext::finish() a été appelé
déplace le pointeur dans un itérateur directoryiterator
définit le mode de compatibilité qui va affecter la façon dont les calculs vont être effectués par toutes les fonctions de l'extension
remplace ou insère une entrée pour la clé key et avec la valeur value dans la base identifiée par handle
retourne un document fdf sous forme d'une chaîne de caractères
traite un message com en attendant jusqu'à timeoutms millisecondes ou bien en attendant qu'un message arrive dans la file d'attente
la fonction cubrid_insert_id() récupère l'identifiant généré pour la colonne auto_increment mise à jour par la précédente requête insert
configure le type de rendu de l'image
retourne la position dans le jeu complet de résultats de la requête à partir de laquelle on doit commencer à retourner les documents
spécifie une limite de temps cumulative en millisecondes autorisé au serveur pour effectuer les opérations sur le curseur
la fonction cubrid_lob2_seek64() est utilisée pour déplacer la position du curseur dans un objet lob d'une valeur fournie par le paramètre offset dans la direction fournie par le paramètre origin
active l'option qui fait que lors de la déconnexion du client web le script poursuit son exécution
replace l'itérateur sur le premier élément
crée une copie d'une structure fann
la prochaine itération de boucle d'évènement après que le temps expire sera lancée puis une sortie sera commandée sans bloquer en attente d'autres évènements
construit un nouvel object harudoc
définit une fonction de rapppel pour une uri spécifique
avertissement cette fonction n'est pas documentée et seule la liste des arguments est disponible
remplace le mode du fichier filename par le mode mode
ouvre une connexion persistante sur un serveur frontbase
déplace la position du texte à la position donnée
cette méthode résout tous les liens symboliques les chemins relatifs et retourne le véritable chemin qui mène à un fichier
force le type de la variable var en type
retourne la valeur pointée par un certain objet
récupère le message d'erreur mnogosearch
se comporte comme current() avec une différence
se déplace au noeud suivant
récupère un tableau contenant les paramètres d’entraînement
renvoie l'espace disque disponible dans le répertoire ou la partition
effectue une conversion han-kaku - zen-kaku sur la chaîne str
recherche needle dans haystack en utilisant une comparaison souple à moins que strict ne soit utilisé
crée un objet recursivefilteriterator depuis un objet recursiveiterator
retourne un tableau d'informations sur un utilisateur référencé par son id
supprime le contenu existant
vérifie si une ou plusieurs clés apc existent
configure une ou plusieurs options de l'objet imagick
cette méthode doit être utilisée pour déterminer si la compression est possible avant de charger une archive phar qui contient des fichiers compressés
valide une transaction globale sur les serveurs mysql participants commencée via la fonction mysqlnd_ms_xa_begin()
indique si le fichier est un lien symbolique
crée un document html depuis une représentation dom
retourne un tableau des types de signature supportés
chiffre les données et retourne les données chiffrées
créé un compte à rebours qui enverra un signal sigalrm au processus après seconds secondes
efface une variable stockée dans le cache
si le premier et le seul paramètre est un tableau min() retournera la plus petite valeur contenue dans le tableau
exécute la session curl fournie
définit le nom du champ dans lequel les termes seront récupérés
envoie le message message de type msgtype qui doit être plus grand que 0) à la file de messages identifiée par queue
associe une valeur à un nom correspondant ou à un point d'interrogation comme paramètre fictif) dans la requête sql qui a été utilisé pour préparer la requête
définit la profondeur d'un canal particulier de l'image
retourne la valeur de variant arrondi à decimals décimales
cette fonction accepte un deux ou quatre paramètres et pas trois :
la fonction cubrid_col_get() est utilisé pour récupérer le contenu des attributs des éléments d'un type de collection set multiset sequence sous la forme d'un tableau
crée un index sur le ou les champs spécifiés s'il n'existe pas déjà
charge la sortie binaire spécifiée vers le cache fichiers ou utilisateur d'apc
crée un objet de flux de texte ou ajoute un texte ainsi que des options explicites à un flux de texte existant
supprime un en-tête http précédemment ajouté avec header()
retourne le nombre de lignes affectées par la dernière requête delete insert ou update exécutée par l'objet pdostatement correspondant
cette fonction retourne un objet avec les noms de la colonne du jeu de résultats comme propriétés
la première syntaxe utilise libmcrypt 2.2.x et la seconde libmcrypt 2.4.x ou plus récent
ouvre un fichier compressé avec gzip pour y écrire ou y lire des données
retourne une chaîne de caractères représentant le message d'erreur le plus récent appelé par une fonction de traitement qu'elle ait réussi ou échoué
vide la base de données distante
récupère la version du cipher utilisé pour la connexion ssl courante
réinitialise le message de la dernière erreur
retourne le nombre de colonnes contenu dans la dernière requête
calcule la largeur d'un glyphe en points s'il a été écrit dans la police et la taille donnée
dans certaines situations il peut être nécessaire de supprimer un curseur du serveur
retourne l'entrée courante de l'objet directoryiterator
change le nom de l'utilisateur sur la session courante
remplace les valeurs du tableau array1 avec les valeurs des mêmes clés issues des tableaux suivants
configure l'url cible à qui seront envoyés les requêtes soap
implémente le formatage direct en mémoire
supprime le sémaphore donné
lorsque exécuté via le gestionnaire de contrôle de service un processus de service est requis pour archiver avec lui pour établir un service de surveillance et de communication ergonomique
extrait des informations sur l'utilisation de la mémoire du cache de fichiers
démarre la temporisation de sortie
vérifie les permissions utilisateurs pour un fichier
retourne le type de moteur utilisé pour créer la police
cette fonction est un raccourci pour hwapi_insert()
lit une ligne depuis un fichier et l'analyse suivant le format format spécifié, décrit dans la documentation de la fonction sprintf()
supprime un artefact associé à l'image
conversion en chaîne de caractères
vérifie si une méthode spécifique est définie dans une classe
retourne le type de la colonne identifiée par le paramètre column_number
compte le nombre d'éléments dans le tableau judy
récupère le fils filtré de l'itérateur interne
la fonction cubrid_get_autocommit() est utilisée pour récupérer le statut du mode auto-commit de la connexion à la base de données cubrid
retourne une chaîne de caractères hexadécimale représentant le crc d'une entrée d'une archive
demande si l'objet référencé est en cours d'exécution
dessine un pixel aux coordonnées spécifiées
définit la longueur minimale d'un mot en dessous de laquelle il sera ignoré
applique une expression arithmétique relationnelle ou logique à une pseudo-image
cette fonction annule la règle de réécriture et efface toutes les variables de réécriture précédemment définies avec la fonction output_add_rewrite_var() ou par le mécanisme de session si session.use_trans_sid a été défini lors de l'utilisation de la fonction session_start()
récupère le noeud courant
initialise une connexion par socket à la ressource spécifiée par hostname
passe à l'image précédente dans une séquence d'images imagick
lit un en-tête de jeu de résultats
la fonction pg_socket() retourne une ressource en lecture seule correspondant au socket sous-jacent de la connexion postgresql fournie
vérifie si la position courante est valide
fixe le gid effectif du processus courant
vérifie si tous les caractères de la chaîne text ont une représentation graphique et créeront une impression sur l'écran les espaces n'en font pas partie)
écrit tous les changements appliqués à relation depuis la dernière écriture vers le disque
spécifie l'opacité pour les contours de l'objet
fait que les nouvelles notifications sont placées en file d'attente tant que la fonction gupnp_service_thaw_notify() n'est pas appelée
retourne le poids de la connexion
lit une image ou une séquence d'images depuis un descripteur de fichier ouvert
la fonction runkit_lint() effectue une vérification de syntaxe lint sur le code php spécifié en testant les erreurs de scripts
définit ou redéfinit une couleur
annule le gestionnaire d'événements enregistré, spécifié par event
récupère le mode de compatibilité qui affecte la façon dont les calculs sont effectués par toutes les fonctions de l'extension
