xml	sdo_das_xml sdo_das_xml_document simplexmlelement simplexmliterator xmlreader
thread	thread
variable_names	$error_handler $blob $output $identifier $height $dither $def_value $option $agent $array2 $savepath $isname $xml_file $replace_by $use_keys $item $exit_struct $parameters_array $path_to $ascii $table-name $sql_param $intattrcount $imgtype $untrustedfile $maxlifetime $appname $revision $country_code $charlist $bordercolor $errornum $full_status $jday $srcy $srcx $domlist $wantsink $disposetype $dest_width $channel $cache_limiter $div $sec_level $dir $msgtype $icon $cache $watch_descriptor $imageformat $buttons $use_errors $dirent $dbaddr $field_index $rows $object_name $ca_path $descriptor $matcheddn $left $row_number $method_name $report_uncaught_exceptions $old_mbox $array_or_countable $text_domain $config_file $timelimit $selectors $newvalue $opcode $bid $fieldcode $imageinfo $rights $buf $userdata $progid $linecap $metrictype $namespaceuri $rownumber $param $msgs_in_queue $stylesheet $afmfile $what $nbytes $year $iter $type $action_name $qualifiedname $batch_size $result_type $node_name $new_data1 $purpose $new_data2 $message_type $retphar $field_value $tile_geometry $compare $fieldboostvalue $key_len $priv_passphrase $error_types $hebrew $stroke_width $userid $crop $pixels $max_chars_per_line $except $table_name $jargon $with_comments $nowait $numeric_prefix $buffer $fd2 $syntax $age $event_handler $blob_handle $result_identifier $pass $pasv $variable-name $max_memory $textflow $broker $callback_function $attr $handler $unixtimestamp $driver_options $section $artifact $antialias_steps $linefeed $dump_directory $measureerror $mustunderstand $servicename $language $body $days $soap_request $words $files $radius $binary_string $priority2 $priority1 $user_agent $contents $position $certs $frombase $arg_num $sid $sig $mimemail $limited $sourcefp $limiter $rev1 $rev2 $store_result $separator $cursor_offset $cainfo $off_hour $keydata $opts $size_correction $writecb $bestfit $lenght $seconds $req $res $query_identifier $ord $cacert $small $session_mode $red $ref $colorize $new_width $from_encoding $pathto $pipeline $pathname $note_value $lib_dir $slant $param_name $driverdata $session_data $angle $signum $page_number $bytes_transferred $library $part_number $infile $schema_type $string $from_root $color_space $ndocs $shmid $preview $bmax $case $max_item_length $id $hconn $part $it $iv $x509 $calendar $sections $access_token_url $startpos $modifiers $major $database_name $unescaped_string $dir_handle $resolution $oldnode $attribute $val $feature $data_type $base_dir $var $font $fam_monitor $token_secret $brightness $auth_passphrase $frame $is_output $mysql_connection $lob_to $factors $reference $zip_entry $priv_key_id $sandbox $batchsize $form $client_info $white_point $descriptorspec $column_key $server_hash $component $regs $imageid $functions $request_token_url $top $inc_by $miter $cmp_function $pxdoc $source_string $end_element_handler $check_consistency $columns $is_associative $value_cb $relation $brigade $env_keys $text $original $to_encoding $how $max_table_length $method $close_path $encoding_list $max_acquire $cut $response_code $timestamp $parent_outline $timeoutms $argument_count $config $border $faultname $max_decoded_len $offset $accuracy $x3 $x2 $x1 $x0 $seed $cookie $exp $alias $word_wrap $utf8string $url $out $uri $ury $urx $module $msgdesc $full_info $key $connection_string $classes $low $oid $replace $resource $rpmr $log $xsd_file $connoptions $bbcode_initial_tags $quota_limit $defaulthost $headerfault $principal $extendedclass $local_username $consumer_secret $layer $auth_protocol $normalized $parameter_type $errcode $database_password $request_parameters $json $query_data $field_number $spelling $dec_by $max_repetitions $zurl $workfactor $jd $gid $is_prefix $iptcdata $painttype $locale $zip $pubkeyfile $appendrawoutput $namespace_uri $saturation $indent $newdir $conn $get_scripts $window_handle $gstate $tidy $noise $background $route $usec $user $reps $group $client_flags $new_value $with_error_message $cipherid $tablename $bottom $error_message $nanoseconds $interlace $host_name $texture_wand $blackls1 $values $max_depth $pad_type $column-name $callbacks $train_data $callback_url $signal $database $docs $frompath $prompt $draw $streams_context $statement $value2 $value1 $y0 $y1 $y2 $y3 $mask_image $compcode $signature $event $autoload $handle $query_string $tagnum $luminosity $signature_alg $pmo $caches $x_resolution $bson $longitude $baseclass $isolation $param_arr $sparse_method $input $newscope $entry $from $off $checkbox $functionname $new_include_path $plugin $interval $methods $default $table $trans_args $tv_usec $optlist $encoding_mode $checkboxtree $flags $tobase $disable $hobj $zoom $other $doctype $level $boxname $index $sigma $args $success $directory $certificate $matrix $col_name $ctor_args $resumepos $cache_cb $reason $return_float $nonrecursive $to_parse $arg4 $arg1 $arg3 $arg2 $ang1 $link_or_trans_identifier $character_mask $right $privkey $original_name $tpl_content $cit_flags $large_object $initial $checkpoint $registernodens $fieldvalue $fieldname $x509cert $remote_socket $server_socket $x509certdata $token $newthis $tag $xresolution $more_entropy $search_value $newparent $old_file $sec_name $field_name $bbcode_container $str $extract_type $step $record $auth_type $cwd $fetch_type $optval $archive $iterator_class $privkeyfile $script_code $lowerlefty $lowerleftx $data-type $dest_height $remote_file $pad_string $fielddesc $extended $stroke_opacity $revno $step_callback $fstype $error_msg $hmode $lock $preferences $zp $column_name $array1 $blocksize $verbosity $main_str $variables $cap $event_name2 $log_dir $event_name1 $value $noise_type $params $geometry $request $gmax $arrays $n_retries $maxlen $trimmarker $constants $timezone_identifier $patch $initial_value $delimiter $string_literal $http_response_code $utf16string $priv_protocol $new_name $dbase_identifier $expr $encoding $host $outer_bevel $multiline $exception_handler $pdfdoc $estimated $interlace_scheme $envelope $value_compare_func $memcache_connection $dn $mtime $db $first_page $deep $req_params $connect_type $operation $description $save_indexes $adjoin $new_size $increment $process $reverse $base_group_id $tpl $liveforever $units $cols $index_key $bottom_left_x $bottom_left_y $page_per_pages $only_names $gamma $key_compare_func $character $unserialize $xstep $wouldblock $view $flag $set_leading $sec $set $y_resolution $result $filesize $events $return_array $bytes $dest $mime_encoded_text $version1 $cache_type $version2 $encoded_headers $message_code $xmlversion $http_headers $numcolors $variant $char_list $link_identifier $isglobal $suffix_as_key $details $setting $write_timeout $isid $peername $elementset $fetch_flag $exception $is_final $bind_password $threshold $exclusive $yray $delay $ident $ignore_transparent $targets $local_socket $schema $ex $ey $optname $constantname $font_index $row $rot $oid_only $trace $compose $lob1 $lob2 $observer $ed $priv_key $encoded_string $configargs $count $field $subjectlength $mask $new_link $help $country $idna_info $session_id $retries $slot $which $deleteoldrdn $imagecontents $search_criteria $intattrs $limit $port $shm_identifier $working_copy $anything $edition $fam $gravity $siginfo $type_name $code_page $term_type $work $community $next $target_frame $connection $multiplier $pos $minor $enc_type $parameter $month $variable $data $parameter-number $date $doc $chunk_size $perm $embed $bias $dirname $faultactor $word_space $right_operand $debug_level $new_height $offset_wand $dash_on $subject $callbackfunc $publicid $ncolors $dispose $atime $blobidarray $blur $treedepth $quota_root $in_charset $sinkinterface $file_name $clut $blue $new_location $property_name $pad_length $jpegname $bcc $len $errormode $skip $input_headers $database_options $fontsize $to $contextname $stack $result_id $bit_on $td $ty $tx $bv_name $qualifier $recursive $soapheaders $force $signmode $prefix $sense $new_path $max_len $context $components $min $algoname $minimal $xpath $hour $bevent $still_running $stmt $oldname $end_revision $filter $rmin $error_code $stringparam $region_code $phi $dash_off $cmd $fp $fl $blob_data $foreground $fd $fg $owner $hostname $micro_seconds $restrict $types $filtername $result_entry_identifier $spotname $pty $object $colnum $length $selectorcount $addtl $content $regex $iterations $keyword $on_off $dbhandle $green $needle $opened_path $signo $high $storage $src_y $src_x $securityprefs $grp $levels $val1 $src_w $src_h $script $matches $webindex $okay $fontfile $function_args $bytes_max $modifier $minute $callable $trigger $send_headers $hebrew_text $tdo $name1 $name0 $pieces $admin_server $infilename $day $substrchar $arr $oldset $authns $errmsg $severity $key_identifier $arg $ftp_stream $session $auto_release $scanner_mode $var_array $beginoptions $start $frame_name $msg_number $notext $orientation $displace $enclosure $convmap $encodingfile $decoration $gc $line_pos $signature_method $fontid $pid $label $max $document $update $create $non_repeaters $pri $username $condition $permissions $out_charset $query $document_or_id $fontname $contextnode $user_vars $aliases $uv $softlimit $oauthexception $class $fdf_data $fromlength $base $field_name_or_number $connection_id $sync $number $with_attrib $type_namespace $tag_name $black_point $ip_address $microseconds $mutex $uid $params_array $old $uir $detail $inner_bevel $precision $str2 $timeout_type $str1 $fdf_document $bind_rdn $wbmpname $yresolution $sealed_data $gmpnumber $rectangle $new_file $old_data $signed_text $scaling $extension_name $bbcode_arg_parser $tv_sec $dbpath $rename $effect $up_right_x $up_right_y $version $point_size $channel_type $desiredmsgtype $layout $cursor_type $bind_arg $sort_flags $n $m $extended_data $k $i $f $g $d $e $b $c $a $sp_name $rpath $x $y $w $r $image_type $p $q $status $csr $newflags $composite_object $gray $preserve_keys $imagedata $opacity $server_key $collection $blob_id $pub_key_ids $newname $points $dsn $comobject $hardlimit $threshold_map $plaintext $maxlength $buffers $vendor $shading $blendmode $xdoc $queue $assoc $pgid $frameid $cursor_orientation $wsdl $watermark_wand $extension $max_tries $stream $as_array $who $client_area $alpha $lob_type $interface_name $pagesize $flatness $word $permission $min_savings $property $properties $bucket $criteria $lowmark $new_password $extraattribs $depth $scope $object_id $destroy $direction $bgd_color $socket_type $auto_enable $saveflag $msgid2 $msgid1 $open_mode $overwrite $procedure $servers $sortfilter $modify $cert $reflector $encoder $interface $owner_password $npriorities $function_code $blockname $dispinterface $amplitude $draw_settings $author $ca_info $error $algorithm_directory $httponly $newrdn $round $x_shear $parameters $ttl $actor $thumbnail $mod $search $whence $passphrase $enabled $servername $finfo $topath $paramters $verbose $src_image $persistent $before_needle $foundonly $newfile $oldfile $read_timeout $dst_h $spool $argument $dst_w $salt $dst_x $dst_y $close $algorithm $format $y_shear $oid_format $refnode $bpc $psdoc $sftp $delimiters $url_encode $prepend $repos $raise $param_key $tail $hex_string $filters $template_dir $unique $supervar_name $expiry $whitepoint $memsize $dependencies $extrainfo $mimetype $expire $highmark $user_password $newval $address $extend $lookup_table $bmin $dbmode $deferred $expiration $iscritical $failure_callback $ann $destination $parser_mode $transfer_encoding $crypto_strong $parser $inputgamma $workingdir $wx $restart_syscalls $disp_time $real_usage $write $props $time $intparam $genre_id $operand $expression $addresses $base_dn $variable_key $upperrightx $upperrighty $write_ver $additional_headers $case_insensitive $peg $facility $data_size $radius_handle $mode $ind $inc $ini $outfile $extra_headers $preserve_cloned_files $backlog $varname1 $document_element_name $domain $attributes $bufferlength $node $revision_no $ordering $error_type $phase $namespace $param_number $latitude $charattr $link $split_length $pagenumber $record_number $line $additional_parameters $delta_x $callable_name $packet_id $cluster_threshold $replacement $items $scale $decrypted $f404 $style $stream_or_url $sharpen $glyphname $juliandaycount $errno $extra_parameters $repository_url $searchresult $compression_type $secure $ystep $headers $new_mbox $destfp $reqengine $sinkobject $weight $create_sid $walk_to_top $arg_separator $double_encode $smooth_threshold $x_rounding $dict $hue $current $gmt_offset $fuzz $response $infos $category $errorcode $autoload_function $with_cas $attrsonly $fill $file $priority $read_write $systemid $errstr $gmagick $val2 $serial $protected_resource_url $new $rise $shared_library $matte $image2 $outputgamma $image1 $pkey $streamid $new_setting $envs $onoff $packet $munglist $classname $shadingid $quality $pngname $src $source $html_table_options $debug $slabid $week $pseudostring $retval $env_key $element $start_index $column_number $second $include_dot $rendering_intent $bg $bz $pair $xml_string $encoded_header $pipes $queries $profile $definition $table-type $process_identifier $tpl_vars $new_file2 $new_file1 $event_base $linebreak $azimuth $start_revision $ang2 $layer_method $imap_stream $newnode $importednode $clip_mask $cipher $use_include_path $name $open_data $chunks $databases $targetpath $session_name $filtertype $script_name $array1_sort_order $pfmfile $verifier_token $inotify_instance $metric $return $tuple $max_recs $imagetype $shtype $message $iptcblock $documentboostvalue $first_num $var_name $pointsize $trans_time $code $color $executable $internal_date $msglist $base_directory $vert_bar $proper_address $action $faultcode $extracerts $machine $invert $envrion $return_var $resolver_function $rigidity $perms $summaryonly $role $read $off_min $dbname $cursor_def $space $sizelimit $log_function $longopts $other_options $suffix $list $mangled $thickness $raw $ch $stmtname $ray $cc $consumer_key $cx $cy $local_file $bootstrap $xshear $node_namespace $varname $maxsize $serialized $intattr $new_data $c4 $c2 $c3 $c1 $crypted $entries $is_null $note_name $msecs $retry_count $assoc_array $path1 $path2 $opened $yshear $mailbox $haystack $tightness $constant $null_as $md $is_hex $mh $ticks_per_second $ms $jpeg_file_name $char_space $data_length $realm $categorize $dst_image $wordwrap $case_insensitivity $fetchtype $row_b $row_a $utf32string $in_fd $get_as_float $deref $function $hits $julianday $start_element_handler $ns_prefixes $width_height_type $fingerprint $target $sequence $vars $clob_handle $referrals $is_dst $maxrows $clob_data $hash $number_frames $fragment $armor $decimals $additional_parameter $dev $var1 $y_rounding $preg_flags $output_callback $font_size $fit $tcp_server $process_sections $warning $comment $accessible $basenode $controller_name $parent $command $cast_as $scene $locking $reserved $matte_color $ending $field_offset $options $composite $service $server $elevation $faultstring $image $old_password $colorspace $protocol $kernel $num_points $ry $rx $objdesc $function_name $open $gmin $encryption_key $contextengineid $path_from $nseconds $charattrlength $inside $num $dictionary_link $array1_sort_flags $x_server $tint $backup_original_files $compression $case_sensitive $create_mode $quick_print $sslcheck $class_name $beta $location $password $page $column $execute $glue $socket $octal_string $extends $xmldeclatation $add_empty $fetch_style $amount $algo $face $relation_name $left_operand $pub_key_id $metadata $sem_identifier $errorcb $map $wait $modulus $phonemes $save_path $pkcs12 $startterm $cas_token $nonce $dialect $old_value $sql_statement $delete_old_session $mxhosts $recurse $gmagickdraw $iterator $stub $tag_rules $syntax_only $title $input_parameters $one_way $new_cache_expire $elementid $padding $strict $array $smiley $signcert $rmax $charattrs $msg $param_val $ns $filepath $coordinate_array $mimetypes $atomic $channeltype $final_callback $align $timezone $coordinates $result_array $module_name $lob_mode $output_headers $shortnames $op $filehandle $whichflags $strong $constraints $all_frames $size $binary $xray $width $charset $default_host $character_set $env $end $sorting_order $out_fd $fields $bitreverse $gmo $break $parameter-type $qmanagername $timeout $blackpoint $join $recipcerts $profilename $escape $cas_tokens $keys $opt $short_format $property_value $secret $addr $stream_or_context $llx $lly $filename $file_extensions $rewrites $log_message $repos_url $degrees $skip_results $sx $sy $retry_interval $closure $lifetime $want_peer $fill_opacity $pattern $sh $normalize_array $sd $text_leading $readcb $localname $recpos $extensions $arguments $user_compare_function $allowable_tags $genre $ok $on $suggestions $thumbnail_geometry $path $mode_directory $obj $ox $oy $callback $notification_code $throw $black $sql_text $lob_from $lcid $info $entire_row $outfilename $zend_extensions $stream_filter $white $http_method $zenith $operator
magic_methods	set get clone autoload destruct call construct wakeup isset unset
connection	samconnection
exception	Exception SolrClientException MongoResultException ErrorException SolrIllegalOperationException SolrIllegalArgumentException MongoCursorException SolrException
types	simplexmlelement reflector iterator int float tidy number gmagick yaf_route_abstract imagick boolean splobjectstorage tokyotyranttable mysqli ktaglib_id3v2_frame yaf_plugin_abstract array dateinterval splsubject gmagickpixel yaf_response_abstract callback string|array callable|int yaf_view_interface long yaf_request_abstract bool sdo_xmldocument call array|object gmp memcached gmagickdraw recursiveiterator|iteratoraggregate domnode imagickpixel domdocument string oci-lob mongodb void object variant boolean|string callable domattr threaded domdocumenttype integer recursiveiterator imagickdraw yaf_bootstrap_abstract closure magickwand exception mysqli_stmt resource splobserver mysqli_result double oci-collection hw_api_attribute datetimezone traversable soapheader yaf_config_abstract mixed int|mongoint64 mongoclient
