xml	xmlreader
variable_names	$values $priority $hostname $reserved $micro_seconds $error_handler $errstr $serial $output $object $signal $database $dither $length $prompt $pad_type $option $extraattribs $addtl $statement $enclosure $content $buf $isname $value1 $replace_by $y0 $y1 $y2 $y3 $item $keyword $onoff $dbhandle $needle $quality $signature $autoload $handle $ascii $source $bpc $node_namespace $signature_alg $src_y $src_x $caches $bson $personal $src_w $src_h $input $script $matches $env_key $element $entry $part_number $rights $errornum $nanoseconds $identifier $tv_sec $dest_width $sorting_order $modifier $height $untrustedfile $bz $cache_limiter $table $trigger $tv_usec $pipes $hebrew_text $icon $optlist $definition $id $flags $pieces $tobase $infilename $auto_release $up_right_x $seed $gc $arr $field_index $rows $version $severity $key_identifier $imap_stream $bitreverse $ftp_stream $level $schalter $cipher $use_include_path $name $open_data $boxname $row_number $scanner_mode $index $var_array $version2 $method_name $report_uncaught_exceptions $break $args $old_mbox $array_or_countable $text_domain $script_name $start $directory $privkey $certificate $left $msg_number $notext $ph_name $newvalue $resumepos $restrict $return_float $fieldcode $x509 $imageinfo $to_parse $metric $return $character_mask $userdata $linecap $array1_sort_order $namespaceuri $pid $how $addr $new_location $msgs_in_queue $large_object $pad_length $shtype $what $message $read $year $pri $username $dictionary $code $color $type $out_charset $query $internal_date $msglist $token $utf8string $tag $fontname $recipcert $n_retries $result_type $node_name $stylesheet $proper_address $action $purpose $more_entropy $message_type $search_value $extracerts $uv $write $bbcode_container $class $str $return_var $fromlength $base $field_name_or_number $error_types $connection_id $step $perms $sync $record $number $max_chars_per_line $except $table_name $type_namespace $k $tag_name $role $jargon $ip_address $uid $numeric_prefix $buffers $cut $script_code $buffer $lowerlefty $lowerleftx $fd2 $dest_height $age $precision $other_options $remote_file $suffix $pad_string $str2 $timeout_type $extended $str1 $fdf_document $pass $bind_rdn $pasv $proj $wbmpname $input_headers $raw $textflow $ch $stmtname $haystack $cc $sealed_data $fstype $cx $cy $typelib_name $local_file $error_msg $opt $signed_text $x509cert $database_options $zp $array2 $array1 $green $blocksize $image $varname $fontsize $serialized $main_str $variables $argument_count $day $up_right_y $authns $c4 $c2 $c3 $c1 $value $crypted $entries $blue $is_null $params $request $x509certdata $arrays $note_name $bind_arg $batch_size $maxlen $body $sort_flags $n $days $assoc_array $timezone_identifier $soap_request $i $f $g $d $e $b $c $a $mailbox $rpath $x $y $from $r $delimiter $p $q $user_agent $http_response_code $null_as $contents $status $mh $one_way $certs $csr $start_index $newflags $frombase $preserve_keys $dbase_identifier $arg_num $host $dst_image $case_insensitivity $second $exception_handler $row_b $row_a $pdfdoc $get_as_float $cainfo $newname $function $char_list $envelope $value_compare_func $dn $sig $plaintext $target $sequence $seconds $connect_type $shading $query_identifier $operation $description $verschlüsselungs-phrase $cacert $small $red $ref $assoc $is_dst $address $maxrows $wsdl $hash $process $reverse $armor $decimals $defaulthost $pathname $interface_name $output_callback $bottom_left_x $note_value $process_sections $comment $param_name $key_compare_func $basenode $details $property $is_xhtml $page_number $utf32string $charlist $infile $criteria $xstep $wouldblock $field_name $decrypted $flag $string $depth $recipkey $object_id $destroy $direction $field_offset $channel $options $shmid $result $case $max_item_length $filesize $target_frame $msgid2 $msgid1 $types $iv $version1 $dest $calendar $sections $mime_encoded_text $startpos $initial $fieldname $service $cert $server $major $x0 $httponly $database_name $unescaped_string $dir_handle $keydata $percent $allow_string $blockname $encoding $w $colorspace $protocol $backlog $fingerprint $link_identifier $final_callback $author $hebrew $verbosity $val $feature $utf16string $var $link $function_name $open $fetch_flag $font $bind_password $query_data $threshold $encryption_key $beta $thumbnail $field $search $whence $num $reference $zip_entry $minute $ident $servername $finfo $array1_sort_flags $maxlength $src_image $persistent $before_needle $right_operand $cwd $decode_binary $descriptorspec $optname $column_key $securityprefs $row $dst_h $julianday $dst_w $component $extensions $regs $salt $res $dst_x $dst_y $location $top $miter $password $page $is_output $column $format $glue $count $socket $octal_string $dir $subjectlength $mask $new_link $add_empty $arguments $is_associative $algo $face $delimiters $left_operand $slot $which $pub_key_id $queue $text $sem_identifier $client_flags $map $rownumber $flatness $max_table_length $search_criteria $modulus $method $hex_string $phonemes $filters $shared_library $pkcs12 $return_array $secure $limit $max_acquire $port $dialect $result_identifier $timestamp $shm_identifier $mxhosts $optval $tag_rules $embed $syntax_only $memsize $env_keys $title $position $mimetype $expire $new_cache_expire $dictionary_link $type_name $max_decoded_len $offset $padding $strict $array $smiley $priv_key_id $signcert $pgid $ns $alpha $chunklen $auto_enable $atomic $x3 $x2 $x1 $connection $index_or_name $delete_old_session $multiplier $result_array $condition $pos $extension $increment $minor $enc_type $parameter $month $dateiname $stmt $variable $lob_mode $source_string $destination $data $output_headers $jday $doc $url $out $uri $chunk_size $perm $shortnames $dependencies $dirname $ury $urx $module $exp $object_name $wx $full_info $key $connection_string $whichflags $base_group_id $oid $replace $clear_realpath_cache $passphrase $time $intparam $size $operand $subject $raw_output $expression $bbcode_initial_tags $spotname $width $charset $default_host $ncolors $variable_key $upperrightx $upperrighty $arg $additional_headers $env $case_insensitive $facility $extension_name $atime $layer $var1 $components $end $mode $agent $quota_root $in_charset $ini $json $binary_string $fields $field_number $spelling $outfile $extra_headers $class_name $functions $timeout $varname1 $workfactor $domain $imagetype $jpegname $jd $gid $recipcerts $profilename $bcc $node $bottom_left_y $escape $len $error_type $befehl $namespace $keys $is_prefix $errormode $skip $split_length $phi $pagenumber $record_number $codeset $line $additional_parameters $short_format $painttype $to $result_id $zip $callable_name $ty $tx $pub_key_ids $llx $lly $step_callback $filename $replacement $ordering $file_extensions $first $recursive $scale $get_scripts $typemap $soapheaders $style $gstate $skip_results $sx $sy $glyphname $lifetime $juliandaycount $pattern $priv_key $errno $binary $prefix $localname $language $oid_only $ystep $context $user $handler $headers $group $user_compare_function $new_mbox $allowable_tags $command $actor $hour $close $path $still_running $module_name $weight $callback $oldname $walk_to_top $arg_separator $tablename $double_encode $black $section $error_code $stringparam $error_message $cipherid $howmuch $cmd $host_name $category $signmode $outfilename $configargs $zend_extensions $fl $white $blackls1 $fd $file $operator
magic_methods	clone call construct destruct
exception	Exception ErrorException
types	mysqli object resource string domdocument int void float number long callable callback imagick simplexmlelement bool boolean mixed integer array domnode
