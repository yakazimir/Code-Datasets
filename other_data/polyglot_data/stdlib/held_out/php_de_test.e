die funktion dio_read() liest len bytes aus der datei fd
potenziert left_operand mit right_operand
liefert eine id über die auf ein system v shared memory segment mit dem gegebenen key zugegriffen werden kann
stellt eine persistente datenbankinstanz für path mit mode her und benutzt dabei den handler handler
beendet die ablaufverfolgung die mit pg_trace() aktiviert wurde
gibt sofern vorhanden den letzten fehler zurück der beim letzten kodieren/dekodieren von json aufgetreten ist
selektiert ein schema und führt in ihm eine anfrage aus
ermittelt den unterschied zwischen arrays mittels einer callbackfunktion für den datenvergleich
nimmt alle zu einem eintrag gehörenden veränderungen zurück
setzt den zeiger auf das erste element des iterators zurück
gibt die soap-header der letzten anwort zurück
verknüpft den namen der in address angegeben ist mit dem socket socket
löscht die per path angegebene datei auf dem ftp-server
stellt eine datenbankinstanz für path mit mode her und benutzt dabei handler
füllt und zeichnet den pfad mit der aktuellen füll- und zeichenfarbe
führt eine stored procedure in einer ms sql-datenbank aus
dekodiert die binärkodierung der parameter die entweder mit sqlite_udf_encode_binary() oder sqlite_escape_string() durchgeführt wurde
schreibt daten in einen gemeinsamen speicherbereich
liefert die anzahl der nachrichten im aktuellen postfach
mir dieser funktion wird der rückgabewert einer zuvor ausgeführten datenbank-prozedur stroed procedure geholt
schließt die nicht-persistente verbindung die durch die kennung connection bezeichnet wird
ermittelt die gruppenzugehörigkeit einer datei
liefert eine lesbare beschreibung des error_code zurückgegeben von sqlite_last_error()
berechnet die länge der hypotenuse eines rechtwinkligen dreiecks aus den längen der beiden katheten bzw
ermittelt die schnittmenge von arrays vergleicht daten mittels einer callbackfunktion
löscht das mit mailbox angegebene postfach
beendet eine verbindung mit dem frontbase-server welche die angegebene verbindungskennung hat
prüft ob eine variable existiert und nicht null ist
öffnet eine verbindung zu einer datenbank
gibt informationen über die gruppe mit der angegebenen id zurück
schließt den angegebenen verzeichniseintrag
mit socket_shutdown() können sie die übertragungung hereinkommender ausgehender oder aller daten die standardeinstellung über diesen socket abbrechen
gibt ein array zurück das die versionen von client protokoll und server enthält
die funktion implementiert einen vergleichsalgorithmus der alphanumerische strings sortiert wie es ein mensch täte
ausgehend vom eingabe-array array wird ein neues array mit umgekehrter reihenfolge der elemente zurückgegeben
wird verwendet um im ergebnis einer anfrage informationen über felder zu erhalten
setzt die reale benutzer-id uid des aktuellen prozesses
es wird ein string mit dem namen des aktiven cursors zurückgegeben oder null falls kein cursor geöffnet ist
akzeptiert eine variable anzahl von argumenten die jeweils entweder eine zeichenkette sein können die den namen einer variablen trägt oder ein array das aus solchen variablennamen oder anderen arrays besteht
weist dem angegebenen feld eine javascript aktion zu
diese methode wird nach iterator::rewind() und iterator::next() aufgerufen um zu prüfen ob die aktuelle position zulässig ist
liefert ein objekt mit diversen kopfelementen ähnlich imap_header()
gibt die länge eines feldes zurück
berechnet die anzahl der tage zwischen dem 21
gibt den inhalt eines eintrags unter verwendung seines namens zurück
liefert den fehlercode der letzten pcre regex-auswertung.
identisch zur funktion date() ausgenommen dass die zurückgegebene zeitangabe greenwich mean time gmt entspricht
liefert postfachinformationen
setzt einen eintragskommentar der via eintragsindex bestimmt wird
gibt einen string zurück aus dem die backslashes (\) entfernt wurden
schließt ein image das mittels pdf_open_image() aufgerufen wurde
gibt die statusfehlermeldung sowie die system- oder zip-meldung zurück
prüft ob der angegebene dateiname ein symbolischer link ist
abbobiert ein neues postfach
diese funktion sucht in haystack nach needle
versucht die zugriffsrechte der angegebenen datei zu denen in mode definierten zu ändern
creates a request group
führt die query auf der angegebenen datenbank- connection aus
gibt eine notify-nachricht zurück die vom sql-kommando notify gesendet wurde
setzt einen kommentar zu einem zip-archiv
liefert den namen des feldes der dem angegeben feldindex entspricht
transformiert die quell-node unter verwendung des mittels xsltprocessor::importstylesheet() importierten stylesheets in einen string
verarbeitet alle handles diese methode kann unabhängig davon aufgerufen werden ob eines der handles daten lesen oder schreiben muß
liefert die fehlernummer der letzten frontbase-operation
schließt den pfad füllt und umrandet ihn
gibt den wert des abgefragten felds zurück
wandelt das erste zeichen von str in einen großbuchstaben um wenn es ein zeichen des alphabets ist und gibt den veränderten string zurück
legt die datei fest die die persönliche wortliste enthält
setzt die benutzerdefinierten session-speicherfunktionen die zur speicherung und wiederherstellung von zur session gehörigen daten verwendet werden
liest oder setzt den benutzernamen der für die verbindung verwendet wird
errechnet eine signatur für die im parameter data angegebenen daten
zeigt die werte eines arrays als formatierten string entsprechend der format anweisungen siehe beschreibung der formatierungsmöglichkeiten der funktion sprintf() an
erzeugt einen dateizeiger zu dem per name bestimmten eintrag
die datei auf die handle zeigt wird geschlossen
wird verwendet um auf ein bestimmtes element der locale-angaben zuzugreifen
importiert den schlüssel keydata und gibt ein array mit informationen über den importprozess zurück
setzt die momentan aktive datenbank für die angegebene verbindungskennung
prüft ob alle zeichen des strings text sonderzeichem d.h
gibt den namen des durch den index bezeichneten feldes zurück
prüft ob für ein feld null-werte zugelassen sind
bis auf die zeichenrichtung verhält sich die funktion exakt wie pdf_arc()
gibt den für ergebnisse reservierten speicher frei
liefert die versionsnummer der aktuell laufenden zend engine als string
prüft ob eine extension geladen ist
legt die aktuelle füll- und zeichenfarbe als grauwert zwischen 0 und 1 inklusive fest
öffnet einen verzeichniseintrag für den lesezugriff
wird verwendet um feldinformationen aus einem bestimmten abfrageergebnis zu erhalten
wird aufgerufen um festzustellen ob die aktuelle verbindung im autocommit-modus arbeitet oder nicht
transformiert die quellnode unter verwendung des mittels xsltprocessor::importstylesheet() importierten stylesheets in ein neues domdocument
erlaubt ihnen ein einfaches leser/schreiber modell umzusetzen was auf nahezu jeder platform inklusive der meisten unix abkömmlinge und sogar windows) genutzt werden kann
versucht eine komplette datenbank vom server der mit der übergebenen link_identifier verbings-kennung assoziiert wird zu löschen
gibt einen verarbeitete version von str zurück in der das erste zeichen in einen kleinbuchstaben umgewandelt wurde sofern es sich um einen buchstaben handelte
fügt das handle ch dem multi-handle mh zu
gibt die prozess-id pid des datenbankservers zurück
gibt die aktuelle länge anzahl der zeichen eines bestimmten wertes in einem postgresql result zurück
diese funktion gibt eine neue bbcode ressource zurück die benötigt wird um bbcode strings zu parsen
ist der erste und einzige parameter ist ein array gibt max() den höchsten wert dieses arrays zurück
öffnet eine bzip2-datei .bz2 zum lesen oder schreiben
groß- und kleinschreibungsunabhängiger vergleich zweier zeichenketten der binary safe ausgeführt wird
liefert die parameter des session-cookie
liefert alle werte des arrays array mit einem numerischen index
dateiname oder uri des relaxng-schemas setzen das zur validierung verwendet werden soll
setzt den internen arrayzeiger ein element zurück
gibt das aktuelle element zurück
bereitet den string item so auf dass er korrekt innerhalb eines sql-befehls als parameter genutzt werden kann
liefert eine liste der geladenen apachemodule
findet dateipfade die mit dem angegebenen muster pattern übereinstimmen
prüft ob ein offset-punkt existiert oder nicht
setzt die untere schwelle für meldungen
gibt ein feld der ausführlichen fehlerbeschreibung zurück die mit der ergebniskennung result verbunden ist
versucht eine neue datenbank auf dem server anzulegen der mit der angegebenen verbindungs-kennung assoziiert wird
liest die inhalte eines large objects aus einer postgresql datenbank aus und speichert sie in einer datei im lokalen dateisystem
gibt die anzahl der felder spalten der angegebenen datenbank zurück
entschlüselt data die zuvor mit openssl_public_encrypt() verschlüsselt wurden und speichert das ergebnis in decrypted
der konstruktor erstellt soapclient -objekte im wsdl - oder non-wsdl -modus
liefert die vorgabeeigenschaften der angegebenen klasse
gibt die soap-header der letzten anfrage zurück
diese funktion sortiert ein array
liefert das aktuelle schlüssel-wert-paare des arrays array und erhöht den arrayzeiger
holt eine zeile des abfrageergebnisses das mit der ergebniskennung verknüpft ist
schreibt einen gemäß format erstellten string in den stream handle
setzt eine benutzerdefinierte funktionerror_handler, um fehler in einem skript zu behandeln
berechnet den tangens des winkels arg
diese funktion bestimmt den wochentag für den in julianday übergebenen tag im julianischen datum
liefert den integer -wert von var unter benutzung der angegebenen basis
gibt einen hash-string zurück der unter verwendung des des -basierenden unix-standard-hashingalgorithmus oder einem anderen auf ihrem system verfügbaren algorithmus erstellt wurde
schließt den pfad und umrandet ihn
gibt die anzahl der datenbankreihen zurück die durch die letzte sql-anfrage verändert gelöscht oder hinzugefügt wurden
rückgabewert: liefert den fehlertext der letzten mysql funktion
setzt die aktuelle position für grafikausgabe
konvertiert einen string vom format utf-8 nach utf-16
gibt die exceptionbeschreibung zurück
gibt den namen der aktuellen session zurück
vergleicht zwei den php-versionen angeglichenen versionsnummern
schließt die nicht persistente verbindung zum mysql-server die mit der angegebenen verbindungs-kennung verknüpft ist
gibt ein array der im wsdl beschriebenen funktionen für den web service zurück
setzt den zeiger auf das nächste element und überspringt dabei kindelemente
wandelt einen im zahlensystem zur basis frombase codierten numerischen string number in die entsprechende repräsentation zur basis tobase um
gibt den typ eines feldes in einer ergebnismenge zurück
gibt die reale numerische benutzer-id des aktuellen prozesses zurück
fügt eine neue seite in das dokument ein
erzeugt eine anonyme funktion anhand der übergebenen parameter und gibt einen eindeutigen namen für diese zurück
nutzen sie w3c xsd schema um das dokument während der verarbeitung zu validieren
versucht die gruppenzugehörigkeit der datei filename zu group zu ändern
ist mssql_fetch_array() ähnlich aber es besteht ein unterschied: statt eines arrays wird ein objekt zurückgegeben
sendet dem client der aktuellen anfrage eine antwort die einen fehler anzeigt
fügt ein script zum fdf dokument welches acrobat zu den doc-level scripts eines dokuments hinzufügt sobald das fdf importiert wurde
entfernt das gelöscht-flag der angegebenen nachricht
erstellt einen regulären ausdruck für eine suche nach übereinstimmungen ohne berücksichtigung von groß- und kleinschreibung
speichert x509 in der in filename angegebenen datei im format pkcs#12
sammelt die statistiken der datei die durch den dateizeiger handle geöffnet wurde
der inhalt des ausgabepuffers sofern vorhanden wird abgeschickt und der ausgabepuffer aber nur dieser wird deaktiviert
gibt den inhaltstyp des feldes zurück das der angabe in field_number entspricht
wechselt eine verzeichnisebene nach oben
erzeugt einen harten link
gibt den string haystack ab dem ersten vorkommen inklusive von needle bis zum ende zurück
setzt oder liest timeoutwerte für das imap protokoll
holt den inhalt des pdf-ausgabepuffers
bewirkt das gleiche wie strftime() mit dem unterschied dass die zeit nach greenwich mean time gmt zurückgegeben wird
fragt das entfernte ende eines gegebenen sockets ab
deklariert mnogosearch sessionparameter
liefert den inhalt eines felds aus einer frontbase result menge
lädt das remote_file vom ftp-server und schreibt es in die per dateizeiger angegebene datei
entschlüsselt und verifiziert einen gegebenen text und gibt informationen über die signatur zurück
füllt ein array mit num einträgen des wertes des value parameters
legt optionen des angegebenen felds fest
gibt ein array mit informationen über die aktuellen weichen und harten limits der ressourcen zurück
gibt den pfad des aktuellen verzeichnisses zurück das zum speichern der session-daten verwendet wird
legt das aktuelle strichmuster auf b schwarze und w weiße einheiten fest
die funktion konvertiert die in zeichensatz internal_encoding kodierte zeichenkette in output_encoding
beendet eine template-definition
führt eine ergebnislose anfrage gegen den datenbank-server aus
liefert die anzahl der kürzlich eingetroffenen nachrichten im aktuellen postfach
öffnet eine datei und liefert einen dateideskriptor zurück
schließt ein large object
diese funktion verwendet den knoten node der klasse simplexml und macht ihn zu einem domelement -knoten
beginnt eine glyphendefinition für eine typ-3-schriftart
ermittelt die nummer der zuletzt ausgelösten exception oder die ursache für einen gescheiterten funktionsaufruf
löscht die angegebene variable
lädt eine datei vom ftp-server und speichert sie in eine lokale datei
setzt den modus für das auslesen von lob-daten aus der datenbank
liest die liste aller pöstfächer
liefert die quota-beschränkungen für den aktuellen benutzer
füllt einen image-block mit variablen daten unter berücksichtigung seiner eigenschaften
gibt den systemtyp zurück des entfernten ftp-servers zurück
berechnet den kosinus hyperbolikus des parameters arg welcher definiert ist als exp arg + exp -arg/2
prüft ob eine datei oder ein verzeichnis existiert
erzwingt das schreiben aller gepufferten bzip2-daten für den dateizeiger bz
gibt die umwandlungs-tabelle zurück die intern in den funktionen htmlspecialchars() und htmlentities() verwendet wird
legt den value des angegebenen feldes fest
durchsucht haystack nach needle
gibt ein array zurück das alle werte einer bestimmten spalte eines abfrageergebnisses enthält
verschlüsselt data mit dem privaten key und speichert das ergebnis in crypted
die funktion socket_sendto() sendet len bytes aus dem puffer buf über den port port an den socket socket mit der adresse addr
erzeugt eine benannte schreibgeschützte datei aus daten im speicher
erzeugt eine session oder nimmt die aktuelle wieder auf die auf der session-kennung basiert die mit einer get- oder post-anfrage oder mit einem cookie übermittelt wurde
gibt den namen eines eintrags unter verwendung seines index zurück
bildet eine gemäß rfc2822 formatierte mail-adresse aus den übergebenen informationen
gibt einen englischen text zurück der die letzten fehlgeschlagenen sqlite-anfrage beschreibt
sendet die sql anfrage query an mysql ohne dass die datensätze des ergebnisses automatisch geholt und gepuffert werden wie es bei der verwendung von mysql_query() der fall ist
sendet eine null-terminierte zeichenkette an den postgresql-server
diese funktion kann genutzt werdem um die installation eines exceptionhandlers mit set_exception_handler() rückgängig zu machen und den vorherigen handler wieder in kraft zu setzen entweder den eingebauten oder eine andere benutzerdefinierte funktion
fügt einen kreis hinzu
baut über den gültigen socket-deskriptor socket der von socket_create() zurückgegeben wurde eine verbindung zu address auf
dekodiert eine uukodierte zeichenkette
schließt den descriptor der benötigt wird um in den system-logger zu schreiben
gibt eine ergebnismenge frei
gibt eine liste aller funktionen zurück die durch soapserver::addfunction() oder soapserver::setclass() hinzugefügt wurden
definiert während der laufzeit eine benannte konstante
zeichnet einen kreisbogen um den angegebenen mittelpunkt
gibt ein objekt zurück dessen eigenschaften mit den feldern des geholten datensatzes übereinstimmen
diese funktion gibt den string str zurück von dessen ende alle whitespaces entfernt wurden
gibt die schwere der exception zurück
gibt die aktuelle einstellung von session.cache_expire zurück
fügt eine zelle zu einer neuen oder bereits vorhandenen tabelle hinzu
setzt bestimmte flags des angegebenen feldes
liefert den zuletzt gefundenen fehler
platziert ein bild und skaliert es
wenn mehr als eine sql-anweisung an den server gesendet wird oder eine gespeicherte prozedur mit mehreren ergebnissen ausgeführt wird liefert der server mehrere ergebnismengen zurück
gibt ein array zurück in dem die werte des arrays array als schlüssel und die häufigkeit ihres auftretens als werte angegeben sind
diese funktion maskiert unescaped_string zur sicheren benutzung in mysql_query()
wählt ein anderes pdf-dokument aus in dem fdf-formulardaten angezeigt werden sollen als das formular dem sie entstammen
vergleicht array1 mit einem oder mehr anderen arrays und gibt die werte aus array1 zurück die in keinem der anderen arrays enthalten sind
gibt true zurück wenn die datei filename mittels http-post hochgeladen wurde
ändert den eigentümer der datei filename in benutzer user
diese funktion setzt die domain in der bei einem gettext() -aufruf gesucht werden soll gewöhnlich wird die domain nach einer applikation benannt
gibt die anzahl der tupels instanzen/datensätze/zeilen) zurück die von einer ausgeführten insert -, update - oder delete -abfrage betroffen sind
gibt den absoluten pfadnamen des aktuellen arbeitsverzeichnisses des skripts zurück
diese funktion erzwingt das schreiben des gesamten ausgabepuffers in die von dem dateizeiger handle angegebene datei
ermittelt den aktuellen wert der konfigurationsoption option
teilt das array in arrays mit size elementen auf
diese funktion wird von der pdflib gmbh nicht unterstützt
